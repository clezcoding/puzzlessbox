---
phase: 00-branding-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brand/tokens.css
  - brand/tailwind.preset.ts
  - brand/assets/apollo-icon-favicon.png
  - brand/assets/apollo-icon-app.png
  - brand/assets/apollo-icon-dark.png
  - brand/assets/apollo-wordmark.png
  - brand/assets/apollo-avatar.png
  - brand/assets/apollo-empty-inbox.png
  - brand/assets/apollo-empty-board.png
  - brand/assets/apollo-empty-caught.png
  - brand/assets/apollo-empty-notes.png
  - brand/assets/apollo-empty-links.png
  - brand/assets/apollo-empty-tasks.png
  - brand/assets/apollo-empty-cal.png
  - brand/assets/apollo-og.png
  - brand/assets/apollo-splash.png
  - brand/assets/apollo-loading.png
  - brand/assets/apollo-error.png
  - brand/assets/apollo-404.png
  - brand/assets/apollo-offline.png
  - brand/assets/apollo-capture.png
  - brand/assets/apollo-onboard.png
  - brand/assets/apollo-pose-think.png
  - brand/assets/apollo-pattern.png
  - brand/assets/apollo-email-header.png
  - brand/assets/apollo-stickers.png
  - brand/assets/apollo-notify.png
  - brand/tests/assets.test.js
  - brand/tests/tokens.test.js
  - brand/README.md
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
must_haves:
  truths:
    - "All 25 canonical Apollo PNG assets exist under brand/assets/ with non-zero file size (per D-03 keep-all, D-05 PNG kit)"
    - "brand/tokens.css defines light + dark theme tokens via @theme blocks covering bg, surface, border, text, brand, primary, info, and all 5 category pastel pairs (inbox/notes/links/tasks/termine bg+accent) per D-02, D-07, D-08"
    - "brand/tailwind.preset.ts exports a Tailwind v4-compatible Config referencing brand CSS custom properties (colors + fontFamily) consumable by Next.js 16 per D-06"
    - "Dark theme token parity exists via @theme dark covering all semantic surfaces, text, borders, brand, and category tokens; apollo-icon-dark.png included in asset set per D-08"
    - "node --test brand/tests/*.test.js passes with zero failures (validates 25 assets + required CSS variables)"
  artifacts:
    - brand/tokens.css
    - brand/tailwind.preset.ts
    - brand/assets/ (25 PNG files)
    - brand/tests/assets.test.js
    - brand/tests/tokens.test.js
    - brand/README.md
  key_links:
    - "brand/tailwind.preset.ts colors reference var(--color-*) defined in brand/tokens.css"
    - "brand/tests/tokens.test.js asserts tokens.css contains every locked D-07 category pastel variable"
    - "brand/tests/assets.test.js asserts every D-03 canonical PNG exists and is non-empty"
---

<objective>
Ship the Hallmark brand kit package at repo root `brand/` — Apollo PNG asset library, light + dark CSS tokens, Tailwind v4 preset, and node:test validation. Production-quality, downstream-consumable by Next.js 16 / `/gsd-ui-phase 4`. No WebApp UI built here. PNG kit only (SVG vectorization deferred per D-05).

Purpose: Establish the canonical `brand/` package surface so future Next.js consumes tokens/assets from `brand/` (not `.planning/sketches/`). Satisfies BRAND-01 (Hallmark brandkit: logo variants, palette, typography) and BRAND-02 (design tokens + asset library before WebApp UI).

Output: `brand/tokens.css`, `brand/tailwind.preset.ts`, `brand/assets/` (25 PNGs), `brand/tests/{assets,tokens}.test.js`, `brand/README.md`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-branding-design-system/00-CONTEXT.md
@.planning/phases/00-branding-design-system/00-RESEARCH.md
@.planning/phases/00-branding-design-system/00-PATTERNS.md
@.planning/phases/00-branding-design-system/00-UI-SPEC.md
@.planning/sketches/BRAND.md
@.planning/sketches/themes/default.css
@.planning/sketches/003-apollo-asset-pack/KIT.md
@.planning/sketches/003-apollo-asset-pack/README.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — brand/ skeleton with one asset, minimal tokens, minimal preset, smoke test</name>
  <files>brand/tokens.css, brand/tailwind.preset.ts, brand/assets/apollo-icon-favicon.png, brand/tests/assets.test.js, brand/tests/tokens.test.js, brand/README.md</files>
  <read_first>
    - .planning/phases/00-branding-design-system/00-PATTERNS.md (Tailwind v4 @theme pattern, test patterns)
    - .planning/sketches/themes/default.css (token source — light + dark values)
    - .planning/sketches/003-apollo-asset-pack/KIT.md (canonical asset list)
    - .planning/phases/00-branding-design-system/00-CONTEXT.md (D-02, D-06, D-07 locked hex)
  </read_first>
  <action>
    Wire ONE end-to-end path: a downstream Next.js `globals.css` can `@import "../brand/tokens.css"` and get a working `bg-brand` utility, with one promoted asset and a passing smoke test.

    1. Create `brand/` directory at repo root.
    2. Copy `.planning/sketches/003-apollo-asset-pack/assets/apollo-icon-favicon.png` to `brand/assets/apollo-icon-favicon.png` (binary copy, preserve transparency). Use `cp` via Shell since these are binary PNGs.
    3. Write `brand/tokens.css` with MINIMAL light-mode `@theme` block containing only: `--color-bg: #f7f6f3`, `--color-surface: #ffffff`, `--color-text: #2f3437`, `--color-brand: #c45c3e`, `--color-primary: #1a1a1a`, `--font-display: 'Instrument Serif', Georgia, serif`, `--font-sans: 'DM Sans', system-ui, sans-serif`. No dark block yet — expansion task adds it.
    4. Write `brand/tailwind.preset.ts` exporting a default object `satisfies Config` with `theme.extend.colors: { bg: 'var(--color-bg)', surface: 'var(--color-surface)', brand: 'var(--color-brand)' }` and `fontFamily: { display: ['var(--font-display)', 'serif'], sans: ['var(--font-sans)', 'sans-serif'] }`. Import `type { Config } from 'tailwindcss'`.
    5. Write `brand/tests/tokens.test.js` (node:test) asserting `brand/tokens.css` exists and contains `--color-bg`, `--color-brand`, `--font-display`. Use `node:fs`/`node:path`/`node:assert`.
    6. Write `brand/tests/assets.test.js` (node:test) asserting `brand/assets/apollo-icon-favicon.png` exists and `stats.size > 0`.
    7. Write `brand/README.md` (1-page) describing the `brand/` package purpose, directory layout, and how Next.js consumes it (`@import "../brand/tokens.css"` + preset import). Note SVG vectorization deferred per D-05.

    No other assets, no dark theme, no category pastels, no full preset — expansion task fills those. Real error handling on the single path: tests must fail loudly on missing file/token.
  </action>
  <verify>
    <automated>node --test brand/tests/assets.test.js brand/tests/tokens.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `brand/tokens.css` exists with a `@theme` block defining `--color-bg`, `--color-brand`, `--font-display`
    - `brand/tailwind.preset.ts` exports a default object satisfying `Config` with `brand` color and `display`/`sans` font families referencing CSS vars
    - `brand/assets/apollo-icon-favicon.png` exists with non-zero size
    - `node --test brand/tests/assets.test.js brand/tests/tokens.test.js` exits 0
    - `brand/README.md` documents the consumption pattern
  </acceptance_criteria>
  <done>Tracer slice committed: one asset promoted, minimal tokens + preset + tests pass end-to-end. Downstream Next.js can import tokens.css and use `bg-brand`.</done>
</task>

<task type="auto">
  <name>Task 2: Expand to full 25-PNG asset library, full light + dark tokens, full Tailwind preset, full test suite</name>
  <files>brand/tokens.css, brand/tailwind.preset.ts, brand/assets/ (24 more PNGs), brand/tests/assets.test.js, brand/tests/tokens.test.js</files>
  <read_first>
    - .planning/sketches/003-apollo-asset-pack/KIT.md (full 25-asset inventory)
    - .planning/sketches/003-apollo-asset-pack/assets/ (source PNGs to promote)
    - .planning/sketches/themes/default.css (full light + dark token values)
    - .planning/phases/00-branding-design-system/00-CONTEXT.md (D-07 category pastels, D-08 dark parity)
    - .planning/phases/00-branding-design-system/00-PATTERNS.md (full tokens.css + preset + test patterns)
  </read_first>
  <action>
    Expand the tracer skeleton to the full production brand kit.

    1. Promote all remaining 24 PNG assets from `.planning/sketches/003-apollo-asset-pack/assets/` to `brand/assets/` via Shell `cp` (binary). Full list per D-03 / KIT.md: apollo-icon-app, apollo-icon-dark, apollo-wordmark, apollo-avatar, apollo-empty-inbox, apollo-empty-board, apollo-empty-caught, apollo-empty-notes, apollo-empty-links, apollo-empty-tasks, apollo-empty-cal, apollo-og, apollo-splash, apollo-loading, apollo-error, apollo-404, apollo-offline, apollo-capture, apollo-onboard, apollo-pose-think, apollo-pattern, apollo-email-header, apollo-stickers, apollo-notify (24 files; favicon already promoted in tracer). Verify each copy with `ls -la brand/assets/ | wc -l` equals 25.

    2. Rewrite `brand/tokens.css` to full light + dark token set per D-02, D-07, D-08:
       - Light `@theme` block: `--color-bg: #f7f6f3`, `--color-bg-wash: #fbfbfa`, `--color-surface: #ffffff`, `--color-surface-soft: #f9f9f8`, `--color-border: #eaeaea`, `--color-border-strong: #d6d6d4`, `--color-text: #2f3437`, `--color-text-muted: #787774`, `--color-primary: #1a1a1a`, `--color-primary-hover: #333333`, `--color-brand: #c45c3e`, `--color-brand-soft: #fce8e0`, `--color-accent: #c45c3e`, `--color-accent-soft: #fce8e0`, `--color-cardboard: #c9a07a`, `--color-info: #1f6c9f`, `--color-info-soft: #e1f3fe`, `--color-destructive: #d9383a`, `--color-ink-bar: #1a1a1a`.
       - Category pastels (D-07 locked hex): `--color-inbox-bg: #f0eeea` + `--color-inbox-accent: #787774`; `--color-notes-bg: #f5f0e6` + `--color-notes-accent: #b8956a`; `--color-links-bg: #e8f0f5` + `--color-links-accent: #5a7a8f`; `--color-tasks-bg: #eaf3ec` + `--color-tasks-accent: #5f7d64`; `--color-termine-bg: #f5ebe8` + `--color-termine-accent: #c45c3e`.
       - Typography: `--font-display`, `--font-serif` (both `'Instrument Serif', Georgia, serif`), `--font-sans: 'DM Sans', system-ui, sans-serif`, `--font-mono: 'JetBrains Mono', ui-monospace, monospace`.
       - Spacing scale per UI-SPEC: `--spacing-xs: 4px`, `--spacing-sm: 8px`, `--spacing-md: 16px`, `--spacing-lg: 24px`, `--spacing-xl: 32px`, `--spacing-2xl: 48px`, `--spacing-3xl: 64px`.
       - Dark `@theme dark` block (D-08): invert bg/surface/border/text/primary; shift category bg to dark variants (`--color-inbox-bg: #2d2a26`, `--color-notes-bg: #302a20`, `--color-links-bg: #202b33`, `--color-tasks-bg: #222e25`, `--color-termine-bg: #332421`); keep brand terracotta `#c45c3e` and category accents unchanged; keep destructive `#d9383a`.

    3. Rewrite `brand/tailwind.preset.ts` to full preset: all semantic colors (bg, bg-wash, surface, surface-soft, border, border-strong, text, text-muted, primary, primary-hover, brand, brand-soft, accent, accent-soft, cardboard, info, info-soft, destructive, ink-bar), nested category color objects (inbox/notes/links/tasks/termine each with bg + accent), full fontFamily (display, serif, sans, mono), spacing scale. All values reference `var(--color-*)` / `var(--font-*)` / `var(--spacing-*)` from tokens.css. Export `satisfies Config`.

    4. Expand `brand/tests/assets.test.js` to assert all 25 canonical PNGs exist and are non-empty (use the full asset list from KIT.md).

    5. Expand `brand/tests/tokens.test.js` to assert tokens.css contains every required light + dark variable: all semantic colors, all 10 category pastel vars (5 bg + 5 accent), all 4 font vars, all 7 spacing vars, plus a `@theme dark` block present.

    Do NOT add SVGs (D-05 deferred). Do NOT regenerate Apollo scene illustrations for dark (D-08 — place existing PNGs on dark surfaces). Do NOT add Tailwind as a runtime dependency — `brand/` is a pure token/asset package; consumers install tailwindcss themselves.
  </action>
  <verify>
    <automated>node --test brand/tests/assets.test.js brand/tests/tokens.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `ls brand/assets/*.png | wc -l` equals 25
    - `brand/tokens.css` contains `@theme`, `@theme dark`, all 5 category pastel pairs, all 4 font vars, all 7 spacing vars
    - `brand/tailwind.preset.ts` exports nested category color objects (inbox/notes/links/tasks/termine) + full fontFamily + spacing, all referencing CSS vars
    - `node --test brand/tests/*.test.js` exits 0 with all asset + token assertions passing
    - No SVG files added; no Tailwind dependency added to brand/
  </acceptance_criteria>
  <done>Full 25-PNG asset library promoted; full light + dark tokens.css with category pastels + spacing; full Tailwind v4 preset; both test suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brand/ → Next.js build | Tokens + assets cross from static package into Next.js compile; no runtime user input |
| PNG asset provenance | Assets sourced from `.planning/sketches/003-apollo-asset-pack/` (Higgsfield-generated, keep-all per D-03); no external/untrusted upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-01 | Tampering | PNG asset integrity during copy | low | accept | Assets copied from trusted internal sketch folder; no untrusted upload path in this phase |
| T-00-02 | Information Disclosure | SVG script injection (deferred) | low | accept | D-05 ships PNG kit only; no SVGs in Phase 0, so no SVG-script-execution surface. Re-evaluate when SVG vectorization lands post-credit-topup |
| T-00-SC | Tampering | npm installs (tailwindcss/postcss) | low | accept | No npm installs in this phase — `brand/` is pure token/asset package with zero runtime deps; consumers install Tailwind themselves |

</threat_model>

<verification>
- `node --test brand/tests/*.test.js` passes with zero failures
- `ls brand/assets/*.png | wc -l` equals 25
- `grep -c '@theme dark' brand/tokens.css` equals 1
- `grep -c 'var(--color-inbox-bg)' brand/tailwind.preset.ts` equals 1
- No SVG files under `brand/assets/`
- No `package.json` / `node_modules` under `brand/` (pure static package)
</verification>

<success_criteria>
- All 25 canonical Apollo PNG assets versioned under `brand/assets/`
- `brand/tokens.css` defines full light + dark token set with all D-07 category pastels
- `brand/tailwind.preset.ts` exports Tailwind v4-compatible Config referencing CSS vars
- `node --test brand/tests/*.test.js` green
- Downstream Next.js 16 can `@import "../brand/tokens.css"` and consume preset (BRAND-02)
- SVG vectorization deferred per D-05 (no SVGs shipped)
</success_criteria>

<output>
Create `.planning/phases/00-branding-design-system/00-01-SUMMARY.md` when done.
</output>
