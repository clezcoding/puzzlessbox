---
phase: 00-branding-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - brand/BRAND.md
  - brand/VOICE.md
autonomous: true
requirements:
  - BRAND-01
must_haves:
  truths:
    - "brand/BRAND.md documents Apollo identity (D-01), Util Bone scaffold DNA (D-02), palette with terracotta accent #c45c3e, typography pairing (Instrument Serif + DM Sans + JetBrains Mono), and rejected mark directions (lettermark/seal/grid/box-buddy/cool-P) so future asset generations avoid look-drift"
    - "brand/VOICE.md contains 8 German microcopy examples covering empty, error, confirm, capture, offline, destructive, onboarding, and placeholder moments per D-09 — tone clever, dry, resourceful; capture verbs light (caught/stashed/sorted); no baby talk, meme spam, or AI-slop"
    - "brand/BRAND.md and brand/VOICE.md are decoupled from .planning/sketches/ (production copies, not references) per D-04"
  artifacts:
    - brand/BRAND.md
    - brand/VOICE.md
  key_links:
    - "brand/BRAND.md palette section references the same locked hex values declared in brand/tokens.css (D-07 category pastels, #c45c3e brand)"
    - "brand/VOICE.md microcopy aligns with Copywriting Contract rows in 00-UI-SPEC.md (Primary CTA, empty state, error state, destructive confirmation)"
---

<objective>
Ship the brand voice + identity documentation at `brand/BRAND.md` and `brand/VOICE.md` — production copies decoupled from `.planning/sketches/` (D-04), documenting Apollo identity, palette, typography, tone, and 8 German microcopy examples. Satisfies BRAND-01 (Tonalität dokumentiert, reproduzierbar ohne Look-Drift).

Purpose: Future asset generations (Phase 4 icons, later illustrations) and WebApp UI copy must reproduce the Hallmark look + voice without drift. These docs are the canonical reference `/gsd-ui-phase 4` reads.

Output: `brand/BRAND.md` (identity bible), `brand/VOICE.md` (8 microcopy examples).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-branding-design-system/00-CONTEXT.md
@.planning/phases/00-branding-design-system/00-UI-SPEC.md
@.planning/sketches/BRAND.md
@.planning/sketches/MANIFEST.md
@.planning/sketches/001-brand-mood-board/README.md
@.planning/sketches/002-logo-mark-context/README.md
@.planning/sketches/003-apollo-asset-pack/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write brand/BRAND.md — Apollo identity bible (promoted from sketches, decoupled per D-04)</name>
  <files>brand/BRAND.md</files>
  <read_first>
    - .planning/sketches/BRAND.md (source identity bible to promote)
    - .planning/sketches/MANIFEST.md (sketch winners 001-C / 002-B / 003 keep-all)
    - .planning/sketches/001-brand-mood-board/README.md (Util winner lock rationale)
    - .planning/sketches/002-logo-mark-context/README.md (Apollo/Critter winner rationale)
    - .planning/phases/00-branding-design-system/00-CONTEXT.md (D-01 Apollo, D-02 Util Bone + terracotta, D-03 keep-all, D-04 decouple, D-05 PNG-only)
    - .planning/phases/00-branding-design-system/00-UI-SPEC.md (locked palette, typography, spacing)
  </read_first>
  <action>
    Write `brand/BRAND.md` as the production brand identity bible. Promote and adapt content from `.planning/sketches/BRAND.md` — this is a production copy, not a reference link (D-04). Structure:

    1. **Header**: Title "Puzzlessbox — Brand Bible", version, date 2026-07-29, status "locked".

    2. **Identity (D-01)**: Apollo is the brand hero — raccoon mascot, myth companion of Hermes. Entire brand-facing identity derives from Apollo. No parallel abstract primary logo. State reversibility: costly (all assets + voice assume Apollo).

    3. **UI Scaffold DNA (D-02)**: Sketch 001-C Utilitarian Bone — warm bone board, charcoal CTA, Instrument Serif + DM Sans, hairline columns, quiet motion. Brand accent = terracotta `#c45c3e` (bandana). Util teal demoted to info-only.

    4. **Asset Policy (D-03, D-05)**: Apollo asset pack is keep-all (~25 PNGs under `brand/assets/`). Canonical character ref: `brand/assets/apollo-avatar.png`. Phase 0 ships PNG kit; SVG vectorization deferred until Higgsfield Recraft credits restored. Pragmatic A1 path after top-up: vectorize brand marks first (favicon, app icon, wordmark, avatar, dark icon).

    5. **Palette**: Lock the exact hex from 00-UI-SPEC.md Color section — Dominant `#f7f6f3` (bone), Secondary `#ffffff` (surfaces), Accent `#c45c3e` (terracotta), Destructive `#d9383a`. Plus the 5 category pastel pairs (D-07): Inbox `#f0eeea`/`#787774`, Notizen `#f5f0e6`/`#b8956a`, Links `#e8f0f5`/`#5a7a8f`, Tasks `#eaf3ec`/`#5f7d64`, Termine `#f5ebe8`/`#c45c3e`. Note: Termine accent deliberately reuses brand terracotta as capture-signal. State: category pastels must not compete with terracotta brand signal.

    6. **Typography**: Instrument Serif (display, masthead), DM Sans (UI, body), JetBrains Mono (rare timestamps, IDs, compact system metadata). Type pairing rationale: serif warmth + sans clarity + mono precision.

    7. **Spacing**: 4/8px base rhythm — 4, 8, 12, 16, 24, 32, 48, 64px steps. Small radii, hairline borders, minimal shadows. Structure from rules, columns, spacing — not from heavy chrome.

    8. **Shape + Motion**: Small radii, hairline borders, minimal shadows. Motion quiet and brief — restrained entry, status, capture feedback; always support `prefers-reduced-motion`.

    9. **Brand relationship**: Apollo leads brand-facing moments (face, terracotta accent, voice, illustration continuity). Utilitarian Bone stays neutral scaffold. Terracotta = brand signal; teal = informational only.

    10. **Rejected marks (do not regenerate)**: lettermark, seal, grid, box-buddy, cool-P. List each with one-line rejection reason.

    11. **Icon / Illustration style**: Transparent PNG only in Phase 0. Apollo poses and scene illustrations (`apollo-empty-*`, `apollo-error`, `apollo-splash`, etc.) use consistent character DNA — bandana terracotta `#c45c3e`, warm bone surfaces, no flat vector icon set. Dark mode: reuse existing PNGs on dark surfaces; do NOT regenerate scene illustrations for dark (D-08). Future icons in Phase 4 must match this style without look-drift.

    12. **Voice / tone (D-09)**: Summarize voice card here — clever, dry, resourceful; capture verbs light (gefangen/gestasht/sortiert); no baby talk, meme spam, or AI-slop. Link to `brand/VOICE.md` for the full 8 German microcopy examples (empty, error, confirm, capture, offline, destructive, onboarding, placeholder).

    13. **Dark mode (D-08)**: Full dark-theme token parity via `[data-theme=dark]` / `@theme dark`. Existing `brand/assets/apollo-icon-dark.png` included. Apollo scene illustrations are NOT regenerated for dark — place existing light PNGs on dark surfaces with clean bounding boxes/borders via CSS.

    14. **Downstream consumption**: Next.js 16 imports `brand/tokens.css` and `brand/tailwind.preset.ts`. `/gsd-ui-phase 4` reads this bible before generating any new UI asset.

    Write in German where it concerns product voice; technical sections (palette, typography, spacing) in English hex/code identifiers. Keep prose tight — no AI-slop filler.
  </action>
  <verify>
    <automated>test -f brand/BRAND.md &amp;&amp; grep -c '#c45c3e' brand/BRAND.md | grep -qv '^0$' &amp;&amp; grep -c 'Apollo' brand/BRAND.md | grep -qv '^0$' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `brand/BRAND.md` exists, > 100 lines
    - Contains terracotta hex `#c45c3e` and all 5 category pastel pairs
    - Documents Apollo as sole identity (D-01) and Util Bone scaffold (D-02)
    - Lists all 5 rejected mark directions (lettermark, seal, grid, box-buddy, cool-P)
    - Documents PNG-only policy (D-05) and SVG vectorization deferral
    - Documents dark mode parity (D-08) without scene regeneration
    - Contains **Voice / tone** section summarizing D-09 voice card with link to `brand/VOICE.md`
    - Contains **Icon / Illustration style** subsection (transparent PNG, Apollo poses, no dark scene regeneration)
    - No references to `.planning/sketches/` paths in body prose (decoupled per D-04)
  </acceptance_criteria>
  <done>brand/BRAND.md committed as production identity bible; decoupled from sketches; future asset generations have a canonical reference to prevent look-drift.</done>
</task>

<task type="auto">
  <name>Task 2: Write brand/VOICE.md — 8 German microcopy examples per D-09</name>
  <files>brand/VOICE.md</files>
  <read_first>
    - .planning/sketches/BRAND.md (voice card section, lines 53-59)
    - .planning/phases/00-branding-design-system/00-UI-SPEC.md (Copywriting Contract rows — Primary CTA, empty state, error state, destructive confirmation)
    - .planning/phases/00-branding-design-system/00-PATTERNS.md (8 microcopy examples already drafted)
    - .planning/phases/00-branding-design-system/00-CONTEXT.md (D-09 voice spec)
  </read_first>
  <action>
    Write `brand/VOICE.md` with German voice + microcopy guide per D-09. Tone: clever, dry, resourceful. Capture verbs light ("caught", "stashed", "sorted"). No baby talk, no meme spam, no AI-slop. Product UI is German (per project brief).

    Structure:

    1. **Header**: Title "Puzzlessbox — Voice & Microcopy", version, date 2026-07-29, status "locked".

    2. **Voice principles (D-09)**: 4-6 bullets — clever, dry, resourceful; capture verbs light; no baby talk; no meme spam; no AI-slop. Apollo speaks as the raccoon who keeps your chaos sorted.

    3. **8 microcopy examples** — use the exact 8 from 00-PATTERNS.md (lines 75-82), each with a one-line context note:
       1. **Empty State (Inbox)**: "Hier ist gähnende Leere. Apollo hat noch nichts gefangen."
       2. **Empty State (Notizen)**: "Keine Notizen stasht sich von selbst. Lass Apollo etwas aufschreiben."
       3. **Capture Success**: "Eintrag gesichert. Apollo hat es stibitzt und sortiert."
       4. **Error State**: "Da ist wohl ein Zahnrad blockiert. Versuche es gleich noch einmal."
       5. **Offline State**: "Keine Verbindung. Apollo sucht nach dem Signal..."
       6. **Destructive Confirmation**: "Löschen: Eintrag unwiderruflich löschen? Apollo kann ihn nicht wiederbeschaffen."
       7. **Onboarding Welcome**: "Hallo, ich bin Apollo. Lass uns das Chaos ordnen."
       8. **Capture Input Placeholder**: "Sende eine Nachricht, um den ersten Eintrag zu stashen..."

    4. **Capture verb glossary**: Define the light capture verbs — "gefangen" (caught), "gestasht" (stashed), "sortiert" (sorted), "stibitzt" (swiped/nabbed). Note: keep verbs physical and small — no enterprise nouns.

    5. **Anti-patterns (do not use)**: 3-4 examples of voice that would be AI-slop or off-brand (e.g., "Ihr Eintrag wurde erfolgreich persistiert", "Oops! Da ist wohl etwas schiefgelaufen 🤖", "Hey fam, lass uns das chaos ordnen lol").

    6. **Downstream usage**: WebApp UI copy, notifications, and LLM capture agent responses read this file before writing user-facing strings. `/gsd-ui-phase 4` consumes this.

    Write entirely in German (product UI is German per project brief). Keep prose tight.
  </action>
  <verify>
    <automated>test -f brand/VOICE.md &amp;&amp; [ $(grep -c '^##\|^###\|^[0-9]\.' brand/VOICE.md) -ge 8 ] &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `brand/VOICE.md` exists, > 50 lines
    - Contains exactly 8 numbered microcopy examples covering: empty, capture success, error, offline, destructive, onboarding, placeholder (and a second empty variant)
    - Voice principles section lists clever/dry/resourceful + the no-baby-talk/no-meme/no-AI-slop constraints (D-09)
    - Capture verb glossary defines gefangen/gestasht/sortiert/stibitzt
    - Anti-patterns section has 3+ off-brand examples marked as do-not-use
    - All copy in German
  </acceptance_criteria>
  <done>brand/VOICE.md committed with 8 locked German microcopy examples + voice principles + anti-patterns; downstream UI and LLM capture agents have a canonical voice reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brand/*.md → downstream agents | Documentation read by `/gsd-ui-phase 4` and LLM capture agents; no runtime user input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-03 | Information Disclosure | Voice docs leak internal codenames or unreleased features | low | accept | Brand docs are internal-canonical; repo is private (AGPL-3.0 + Commercial); no secrets in voice examples |

</threat_model>

<verification>
- `brand/BRAND.md` exists and contains `#c45c3e`, `Apollo`, all 5 rejected marks
- `brand/VOICE.md` exists and contains 8 numbered microcopy examples
- Both files have no prose references to `.planning/sketches/` paths (decoupled per D-04)
- Both files committed to git
</verification>

<success_criteria>
- `brand/BRAND.md` documents Apollo identity, palette, typography, rejected marks, dark mode policy (D-01, D-02, D-03, D-05, D-08)
- `brand/VOICE.md` contains 8 German microcopy examples + voice principles + anti-patterns (D-09)
- Both files decoupled from `.planning/sketches/` (D-04)
- ROADMAP success criterion 3 satisfied: "Tonalität und Icon-/Illustrationsstil sind kurz dokumentiert und machen spätere Asset-Generierungen ohne Look-Drift reproduzierbar"
</success_criteria>

<output>
Create `.planning/phases/00-branding-design-system/00-02-SUMMARY.md` when done.
</output>
