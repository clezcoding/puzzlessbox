---
phase: 05.4
slug: address-residual-v1-0-audit-polish-cal-title-only-sync-stale
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - hermes-plugin/tests/conftest.py
  - hermes-plugin/tests/test_orchestration.py
  - hermes-plugin/tests/test_channels.py
autonomous: true
requirements:
  - TEST-WARN
user_setup: []
estimate:
  tokens: 15000
  raw_tokens: 7500
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "hermes-plugin pytest suites that patch dialog.asyncio.create_task no longer emit RuntimeWarning: coroutine 'schedule_autosave_poll' was never awaited (D-09, D-11)"
    - "Production dialog.py fire-and-forget asyncio.create_task(schedule_autosave_poll(...)) pattern is unchanged (D-09) — fix is test-only"
    - "Scope limited to suites that trigger the warning: test_orchestration.py (4 call sites) + test_channels.py (1 call site) (D-10) — no full Hermes test audit"
    - "Shared mock_create_task fixture added to conftest.py because ≥3 identical call sites exist (5 total) — D-10 allows shared helper when cheap"
    - "No global filterwarnings = error added for the hermes-plugin package (D-11) — fix is at the mock source, not a warning suppression"
  artifacts:
    - "hermes-plugin/tests/conftest.py — shared mock_create_task fixture (side_effect closes the passed coroutine)"
    - "hermes-plugin/tests/test_orchestration.py — 4 patch(\"dialog.asyncio.create_task\") call sites updated to use side_effect=mock_create_task"
    - "hermes-plugin/tests/test_channels.py — 1 patch(\"dialog.asyncio.create_task\") call site updated to use side_effect=mock_create_task"
  key_links:
    - "dialog.asyncio.create_task patch → mock_create_task side_effect → coro.close() → no orphaned coroutine → no RuntimeWarning"
---

<objective>
Close the TEST-WARN audit gap: hermes-plugin pytest suites emit `RuntimeWarning: coroutine 'schedule_autosave_poll' was never awaited` because `patch("dialog.asyncio.create_task")` replaces create_task with a MagicMock that never schedules the passed coroutine. Add a shared `mock_create_task` fixture to `conftest.py` whose side_effect closes the passed coroutine, and update the 5 call sites (4 in test_orchestration.py + 1 in test_channels.py) to use it. Keep production `dialog.py` fire-and-forget `asyncio.create_task(schedule_autosave_poll(...))` unchanged (D-09). No global `filterwarnings = error` (D-11).

Purpose: The warning is a test-mock hygiene gap, not a production bug — production correctly fires-and-forgets the autosave poll coroutine on the ASGI event loop. The fix is to make the mock close the coroutine so Python does not emit the RuntimeWarning, keeping test output clean without suppressing warnings globally.
Output: Shared `mock_create_task` fixture in `hermes-plugin/tests/conftest.py`; 5 call sites updated in `test_orchestration.py` + `test_channels.py`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-CONTEXT.md
@.planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-RESEARCH.md
@.planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-PATTERNS.md
@hermes-plugin/dialog.py
@hermes-plugin/tests/conftest.py
@hermes-plugin/tests/test_orchestration.py
@hermes-plugin/tests/test_channels.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared mock_create_task fixture to hermes-plugin/tests/conftest.py (D-10 shared helper, ≥3 call sites)</name>
  <files>hermes-plugin/tests/conftest.py</files>
  <read_first>
    - hermes-plugin/tests/conftest.py (existing fixtures: MockSession, mock_create_item_result, mock_categories, active_draft_state, TEST_* constants — match the fixture style)
    - hermes-plugin/dialog.py (schedule_autosave_poll signature lines 92-104; asyncio.create_task call line 144 — confirm production pattern stays)
    - .planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-RESEARCH.md (Common Pitfalls §1: mock_create_task side-effect pattern)
    - .planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-PATTERNS.md (Coroutine Cleanup in Mocks pattern)
  </read_first>
  <action>
Add a module-level function `mock_create_task(coro, *args, **kwargs)` to hermes-plugin/tests/conftest.py that closes the passed coroutine and returns a MagicMock — per RESEARCH Common Pitfalls §1: `if hasattr(coro, "close"): coro.close()` then `return MagicMock()`. Also expose it as a pytest fixture `mock_create_task` (function-scoped) returning the function itself so tests can pass `side_effect=mock_create_task` to `patch("dialog.asyncio.create_task", side_effect=mock_create_task)`. Do NOT modify the existing MockSession, mock_create_item_result, mock_categories, or active_draft_state fixtures. Do NOT add `filterwarnings = error` to pytest.ini or conftest (D-11). Do NOT touch production dialog.py (D-09). The fixture is justified as a shared helper because 5 identical call sites exist (4 in test_orchestration.py + 1 in test_channels.py) — D-10 allows a shared helper when ≥3 call sites are cheap.
  </action>
  <verify>
    <automated>cd hermes-plugin && uv run python -c "from tests.conftest import mock_create_task; import asyncio; async def f(): pass; coro=f(); m=mock_create_task(coro); print('ok', m)"</automated>
  </verify>
  <acceptance_criteria>
    - mock_create_task function exists in hermes-plugin/tests/conftest.py and closes a passed coroutine (coro.close() called)
    - mock_create_task returns a MagicMock instance
    - pytest fixture mock_create_task registered (function-scoped) returning the function
    - No changes to existing fixtures (MockSession, mock_create_item_result, mock_categories, active_draft_state)
    - No filterwarnings = error added (D-11)
    - No changes to production dialog.py (D-09)
  </acceptance_criteria>
  <done>
    - mock_create_task function + fixture in conftest.py; coro.close() called; no filterwarnings = error; dialog.py untouched
  </done>
</task>

<task type="auto">
  <name>Task 2: Update 5 patch("dialog.asyncio.create_task") call sites to use side_effect=mock_create_task (D-09, D-11)</name>
  <files>hermes-plugin/tests/test_orchestration.py, hermes-plugin/tests/test_channels.py</files>
  <read_first>
    - hermes-plugin/tests/test_orchestration.py (lines 31, 202, 238, 266 — four `patch("dialog.asyncio.create_task")` call sites inside `with (...)` blocks)
    - hermes-plugin/tests/test_channels.py (line 47 — one `patch("dialog.asyncio.create_task")` call site)
    - hermes-plugin/tests/conftest.py (mock_create_task fixture from Task 1)
    - .planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-RESEARCH.md (Correct create_task Mocking in Tests code example)
  </read_first>
  <action>
In hermes-plugin/tests/test_orchestration.py and hermes-plugin/tests/test_channels.py, for each `patch("dialog.asyncio.create_task")` call site (5 total), change to `patch("dialog.asyncio.create_task", side_effect=mock_create_task)` and add `mock_create_task` to the test function signature (pytest fixture injection). For tests that already use `with (...)` multi-context blocks, keep the block structure and only modify the create_task patch line + add the fixture param. For test functions that do not currently take the `mock_create_task` fixture, add it as a parameter. Do NOT change any test assertions or test logic — only the create_task patch mechanism. Do NOT add `filterwarnings = error` to pytest.ini (D-11). Do NOT touch production dialog.py (D-09). After the change, all 5 call sites close the passed coroutine via the shared fixture, eliminating the RuntimeWarning at the source.
  </action>
  <verify>
    <automated>cd hermes-plugin && uv run pytest tests/test_orchestration.py tests/test_channels.py -q 2>&1 | rg -qi 'RuntimeWarning.*never awaited' && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - All 5 `patch("dialog.asyncio.create_task")` call sites (4 in test_orchestration.py lines 31/202/238/266 + 1 in test_channels.py line 47) updated to use `side_effect=mock_create_task`
    - `mock_create_task` fixture injected into each affected test function signature
    - No test assertions or test logic changed (only the create_task patch mechanism)
    - `cd hermes-plugin && uv run pytest tests/test_orchestration.py tests/test_channels.py -q` passes (all green)
    - `cd hermes-plugin && uv run pytest tests/test_orchestration.py tests/test_channels.py -q 2>&1 | rg -i 'RuntimeWarning.*never awaited'` returns 0 matches (D-11 done condition)
    - No `filterwarnings = error` added to pytest.ini or conftest (D-11)
    - No changes to production dialog.py (D-09)
  </acceptance_criteria>
  <done>
    - 5 patch("dialog.asyncio.create_task") call sites use side_effect=mock_create_task; affected suites green with 0 RuntimeWarning matches; production dialog.py unchanged; no filterwarnings = error
  </done>
</task>

</tasks>

<threat_model>
N/A — test-only plan. No auth, data, or production code paths touched. The fix closes coroutines in test mocks; production fire-and-forget pattern unchanged (D-09). No new trust boundary opened.
</threat_model>

<verification>
- `cd hermes-plugin && uv run pytest tests/test_orchestration.py tests/test_channels.py -q` — all green
- `cd hermes-plugin && uv run pytest tests/test_orchestration.py tests/test_channels.py -q 2>&1 | rg -i 'RuntimeWarning.*never awaited'` returns 0 matches (D-11)
- `rg -n 'filterwarnings' hermes-plugin/pytest.ini hermes-plugin/tests/conftest.py` returns 0 matches (no global suppression per D-11)
- `rg -n 'asyncio.create_task' hermes-plugin/dialog.py` returns the unchanged production call (D-09 unchanged)
- `rg -n 'side_effect=mock_create_task' hermes-plugin/tests/test_orchestration.py hermes-plugin/tests/test_channels.py` returns 5 matches
</verification>

<success_criteria>
- 5 create_task patch call sites use side_effect=mock_create_task
- No RuntimeWarning: coroutine ... was never awaited in affected suites
- Production dialog.py unchanged; no global filterwarnings = error
- TEST-WARN audit row closed (D-09, D-10, D-11 honored)
</success_criteria>

<output>
Create .planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-03-SUMMARY.md when done
</output>
