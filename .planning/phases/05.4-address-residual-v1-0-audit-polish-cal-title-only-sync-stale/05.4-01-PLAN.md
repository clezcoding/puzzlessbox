---
phase: 05.4
slug: address-residual-v1-0-audit-polish-cal-title-only-sync-stale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/services/calendar.py
  - api/tests/integration/test_calendar_title_only_sync.py
autonomous: false
requirements:
  - CAL-EDGE
user_setup: []
estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Title-only event (starts_at=null AND ends_at=null) synced to Google no longer returns 400 Missing end time — sync_local_event_to_google derives start=now(UTC), end=start+1h and includes dateTime start/end in insert body (D-01)"
    - "Derived times are persisted on the local event row (event.starts_at + event.ends_at) before/with the Google insert so the board reflects the same window the calendar shows (D-01)"
    - "Only-starts_at set (ends_at=null) derives end=start+1h; only-ends_at set (starts_at=null) derives start=end-1h; both provided passes through unchanged (D-01 discretion, recommended)"
    - "Idempotent after first normalize: a second sync_local_event_to_google call on an event whose times are already set (both non-null) does NOT re-derive a new now-based window — _normalize_times returns the existing times unchanged (specless_probe CAL-EDGE idempotency)"
    - "create_event path also normalizes (D-01 covers create/sync); provided times unchanged; null/null still produces a Google insert with dateTime (not all-day date schema)"
    - "No card sync badge, no toast on every title-only capture (D-03) — no UI surface added in this plan; UX discreet modal line is owned by Phase 05.2 D-23 and is not re-implemented here"
    - "Regression coverage is unit/integration only — no live Google E2E required this phase (D-04)"
  artifacts:
    - "api/app/services/calendar.py — _normalize_times helper + applied in create_event and sync_local_event_to_google"
    - "api/tests/integration/test_calendar_title_only_sync.py — regression tests for null/null, null-start, null-end, both-provided, idempotency"
  key_links:
    - "sync_local_event_to_google → _normalize_times → event.starts_at/ends_at persist → Google events().insert body.start/end dateTime"
    - "create_event → _normalize_times → Google insert + local Event row with normalized times"
---

<objective>
Close the CAL-EDGE audit gap: title-only Google Calendar sync (starts_at/ends_at null) must not 400 "Missing end time". Derive a default 1h timed window (start=now UTC, end=start+1h), persist on the local event row, and include Google `dateTime` start/end in the insert body. Keep create-on-edit PATCH path (D-02). No card badge / no toast (D-03). Regression coverage unit/integration only (D-04).

Purpose: Title-only capture currently 400s on Google insert when the user has Calendar connected + selected — the event never lands on Google and the board shows null times. D-01 (user-locked, against skip-sync) makes title-only capture still land on Google with a 1h default window.
Output: `_normalize_times` helper in `api/app/services/calendar.py` applied at both Google insert call sites, plus a focused regression test file.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-CONTEXT.md
@.planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-RESEARCH.md
@.planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-PATTERNS.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-CONTEXT.md
@api/app/services/calendar.py
@api/tests/integration/test_calendar_sync_wiring.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm costly-reversibility decision D-01 before implementing title-only sync normalize</name>
  <files>(no files — decision gate only)</files>
  <read_first>
    - .planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-CONTEXT.md (D-01 + reversibility note)
    - api/app/services/calendar.py (sync_local_event_to_google + create_event current bodies)
  </read_first>
  <decision>Confirm proceeding with D-01 as locked: when both starts_at and ends_at are null on Google create/sync, derive start=now(UTC), end=start+1h, persist derived times on the local event row, and include Google `dateTime` start/end in the insert body. Do NOT skip sync. Do NOT use all-day `date` schema.</decision>
  <context>D-01 is user-locked in CONTEXT.md but flagged `Reversibility: costly` — derived times become real calendar data users may edit; changing the default duration later does not rewrite past events. The orchestrator explicitly requested a checkpoint:decision before the implementing task despite the costly (not one-way) rating. Skipping sync (rejected by user in discuss) and all-day schema (rejected by user in discuss) are presented as non-default options for traceability only.</context>
  <options>
    <option id="option-a">
      <name>Proceed with D-01: derive + persist 1h window, sync to Google</name>
      <pros>Honors user-locked D-01; title-only capture lands on Google with a sane default; board shows the same window as the calendar; no UX dead-end for title-only events when Calendar is connected.</pros>
      <cons>Costly reversibility — past events keep their derived 1h window even if the default duration changes later; users may edit derived times on Google and expect them to round-trip (they will, via D-02 PATCH path).</cons>
    </option>
    <option id="option-b">
      <name>Skip sync for title-only events (rejected in /gsd-discuss-phase)</name>
      <pros>No derived-time data written; zero reversibility cost.</pros>
      <cons>Violates user-locked D-01; title-only events never land on Google when Calendar is connected — the audit gap (400 Missing end time) is replaced by a silent no-op, which is worse UX than a 1h default.</cons>
    </option>
    <option id="option-c">
      <name>Use all-day `date` schema for title-only (rejected in /gsd-discuss-phase)</name>
      <pros>No timed window invented; Google accepts all-day events.</pros>
      <cons>Violates user-locked D-01; all-day events clutter the calendar layout and diverge from the local event row schema (starts_at/ends_at are timed datetime columns); user explicitly rejected this in discuss.</cons>
    </option>
  </options>
  <resume-signal>Select: option-a (proceed with D-01), option-b, or option-c</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: TRACER — _normalize_times helper + apply at both Google insert paths + persist derived times + regression tests</name>
  <files>api/app/services/calendar.py, api/tests/integration/test_calendar_title_only_sync.py</files>
  <read_first>
    - api/app/services/calendar.py (sync_local_event_to_google lines 352-388; create_event lines 224-263; imports lines 1-26)
    - api/tests/integration/test_calendar_sync_wiring.py (_connect_calendar + _create_event_draft helpers; autosave + confirm sync tests for fixture reuse)
    - api/tests/integration/test_calendar.py (_mock_google_service, _mock_credentials, _auth_headers, MOCK_* constants)
    - api/app/models/event.py (Event.starts_at/ends_at nullable datetime columns)
    - .planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-RESEARCH.md (Pattern 1: Event Time Normalization)
    - .planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-PATTERNS.md (Time Normalization pattern)
  </read_first>
  <behavior>
    - null/null: _normalize_times(None, None) returns (now(UTC), now+1h) — both non-null, end > start
    - null-start: _normalize_times(None, end) returns (end-1h, end) — start < end
    - null-end: _normalize_times(start, None) returns (start, start+1h) — end > start
    - both-provided: _normalize_times(start, end) returns (start, end) unchanged
    - sync_local_event_to_google with null/null event: Google insert body has start.dateTime AND end.dateTime (not date schema); event.starts_at + event.ends_at persisted non-null; google_event_id set; no 400
    - sync_local_event_to_google with already-set times (second call after first normalize): does NOT re-derive now-based window — _normalize_times returns existing times unchanged (idempotency per specless_probe CAL-EDGE)
    - create_event with null/null: Google insert body has start.dateTime + end.dateTime; local Event row persisted with normalized starts_at/ends_at
    - create_event with both provided: insert body + local row use provided times unchanged
  </behavior>
  <action>
Add a module-level helper `_normalize_times(starts_at: datetime | None, ends_at: datetime | None) -> tuple[datetime, datetime]` in `api/app/services/calendar.py` per RESEARCH Pattern 1: both null → (now(UTC), now+1h); only-starts_at → (starts_at, starts_at+1h); only-ends_at → (ends_at-1h, ends_at); both → passthrough. Use `datetime.now(timezone.utc)` and `timedelta(hours=1)` (import `timedelta` — currently only `datetime, timezone` imported on line 12). Apply in `create_event`: replace the `if starts_at and ends_at:` block (lines 242-244) with `normalized_start, normalized_end = _normalize_times(starts_at, ends_at)` then set `body["start"] = {"dateTime": normalized_start.isoformat()}` and `body["end"] = {"dateTime": normalized_end.isoformat()}`; pass `normalized_start, normalized_end` into the `Event(...)` constructor instead of raw `starts_at, ends_at`. Apply in `sync_local_event_to_google`: before building `body` (before line 362), compute `normalized_start, normalized_end = _normalize_times(event.starts_at, event.ends_at)` and assign `event.starts_at = normalized_start; event.ends_at = normalized_end` so the local row is updated; then use `event.starts_at`/`event.ends_at` in the body (which are now normalized) — keep the `if event.starts_at and event.ends_at:` guard but it is now always true after normalize (still gate the body keys on truthiness for safety). Keep the existing `if event.google_event_id: return event` early-return at the top of sync_local_event_to_google (idempotency after first successful sync). Keep the existing try/except soft-fail (05.2 D-11). Do NOT add a card badge, toast, or any UI surface (D-03). Do NOT touch the PATCH update-on-edit path (D-02 unchanged). Do NOT add live Google E2E (D-04). Create `api/tests/integration/test_calendar_title_only_sync.py` with regression tests using the existing `_connect_calendar`, `_create_event_draft`, `_mock_google_service`, `_auth_headers`, `MOCK_*` helpers from `test_calendar_sync_wiring.py` / `test_calendar.py` (import them). Cover: (1) null/null sync derives 1h window + persists + Google insert body has dateTime start/end; (2) null-start derives end=start+1h; (3) null-end derives start=end-1h; (4) both-provided passthrough; (5) idempotency — second sync_local_event_to_google call on event with already-set times does not re-derive (assert event.starts_at unchanged across second call, and google_event_id already set short-circuits). Use `DATABASE_URL=postgresql+psycopg2://puzzless:puzzless@localhost:5433/puzzlessbox` per VALIDATION. Mock `app.services.calendar.build` to avoid live Google calls.</action>
  <verify>
    <automated>cd api && DATABASE_URL=postgresql+psycopg2://puzzless:puzzless@localhost:5433/puzzlessbox uv run pytest tests/integration/test_calendar_title_only_sync.py tests/integration/test_calendar_sync_wiring.py tests/integration/test_calendar.py -q</automated>
  </verify>
  <acceptance_criteria>
    - _normalize_times exists in api/app/services/calendar.py and handles all 4 input combinations per behavior spec
    - create_event normalizes before Google insert and persists normalized times on the local Event row
    - sync_local_event_to_google normalizes + persists derived times on the event row before Google insert; google_event_id early-return preserved
    - Google insert body always has start.dateTime + end.dateTime (never date schema) when sync proceeds
    - New test file passes; existing test_calendar_sync_wiring.py + test_calendar.py remain green (no regression)
    - No UI surface added (no card badge, no toast) — grep gate: `rg -n "toast|badge" api/app/services/calendar.py` returns 0 matches
    - Idempotency test asserts second sync on already-set times does not re-derive now-based window
  </acceptance_criteria>
  <done>
    - _normalize_times applied at create_event + sync_local_event_to_google; null/null derives 1h window; both-provided passthrough; idempotent after first normalize
    - New + existing calendar integration tests green; no UI surface added; no all-day schema; no live Google E2E
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Google Calendar API | Outbound write with user OAuth creds; body must satisfy Google start+end requirement |
| API → Postgres (events row) | Local persistence of derived times; owner_id RLS already enforced by existing model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.4-01 | Tampering | _normalize_times / sync_local_event_to_google | low | accept | Derived times are computed server-side from `datetime.now(timezone.utc)` — no user-controlled input reaches the derive path beyond the existing nullable starts_at/ends_at columns (already Pydantic-validated at the router). No new trust boundary opened. |
| T-05.4-02 | Information Disclosure | sync_local_event_to_google | low | accept | owner_id scoping unchanged — existing RLS + explicit owner_id filters (api/app/routers/items.py:54) cover the sync call site. Normalize does not change ownership semantics. |
| T-05.4-03 | Repudiation | derived-time persistence | low | accept | event.updated_at bumped on persist (existing pattern); derived times are auditable via the existing events row. No new audit gap. |
| T-05.4-04 | DoS | Google insert path | low | accept | Existing try/except soft-fail (05.2 D-11) bounds failure surface; normalize does not add retries or a queue (D-02). No new amplification. |
</threat_model>

<verification>
- `cd api && DATABASE_URL=postgresql+psycopg2://puzzless:puzzless@localhost:5433/puzzlessbox uv run pytest tests/integration/test_calendar_title_only_sync.py tests/integration/test_calendar_sync_wiring.py tests/integration/test_calendar.py -q` — all green
- `rg -n "Missing end time" api/` returns 0 matches (no test asserts the 400 path remains)
- `rg -n "date.*schema|all-day" api/app/services/calendar.py` returns 0 matches (D-01: no all-day schema)
- `rg -n "toast|badge" api/app/services/calendar.py` returns 0 matches (D-03: no UI surface)
- Manual-only (per VALIDATION): after deploy, open an event with null times synced; confirm modal shows discreet sync/derived-time state (owned by 05.2 D-23, not this plan)
</verification>

<success_criteria>
- Title-only event sync no longer 400s; derived 1h window lands on Google + local row
- _normalize_times applied at both create_event and sync_local_event_to_google
- Idempotency holds: second sync on already-set times does not re-derive
- Regression tests green; no UI surface added; no all-day schema; no live Google E2E
- D-01, D-02, D-03, D-04 honored; CAL-EDGE closed
</success_criteria>

<output>
Create `.planning/phases/05.4-address-residual-v1-0-audit-polish-cal-title-only-sync-stale/05.4-01-SUMMARY.md` when done
</output>
