---
phase: 05-coolify-deployment-ci-cd-h-rtung
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/Dockerfile
  - webapp/app/api/health/route.ts
  - webapp/app/api/health/route.test.ts
  - webapp/next.config.ts
  - .github/workflows/deploy-web.yml
autonomous: true
requirements: [OPS-02, OPS-04]
user_setup: []

must_haves:
  truths:
    - "webapp/Dockerfile builds a standalone Next.js production image (node:24-alpine multi-stage, pnpm frozen-lockfile, non-root nextjs user, EXPOSE 3000, CMD node server.js)"
    - "GET /api/health returns 200 JSON {status:'ok'} without auth (D-13)"
    - "webapp/next.config.ts has output:'standalone' so .next/standalone is produced for the runner stage"
    - ".github/workflows/deploy-web.yml is path-filtered on webapp/** + the workflow file, declares workflow_dispatch (D-03 first-push path), builds to ghcr.io/clezcoding/puzzlessbox-web with :latest + :sha-<sha> tags, and triggers COOLIFY_WEB_WEBHOOK via GET with Bearer COOLIFY_TOKEN (D-05, D-06, D-07, D-08)"
    - "All GitHub Actions in deploy-web.yml are pinned by full SHA + version comment (D-08)"
  artifacts:
    - webapp/Dockerfile
    - webapp/app/api/health/route.ts
    - webapp/app/api/health/route.test.ts
    - webapp/next.config.ts (with output: standalone)
    - .github/workflows/deploy-web.yml
  key_links:
    - "deploy-web.yml → GHCR puzzlessbox-web → COOLIFY_WEB_WEBHOOK"
    - "webapp/Dockerfile runner stage → /api/health route → Coolify health probe"
  prohibitions:
    - "No hardcoded webhook URLs or tokens in YAML (T-05-03)"
    - "No /ready as Traefik gate (D-12 — WebApp has no /ready; /api/health is liveness only)"
    - "No build_pack switch attempt (D-02 — WebApp is a fresh dockerimage app)"
---

<objective>
Author the WebApp deploy artifacts (Dockerfile, unauthenticated `/api/health` route + test, `next.config.ts` standalone output, `deploy-web.yml` GHCR workflow) so Plan 05-04 can create the Coolify WebApp app and trigger the first deploy.

Purpose: D-03 requires `webapp/Dockerfile` + `deploy-web.yml` + a first successful GHCR push to exist BEFORE the Coolify WebApp app is created (avoids empty-image first deploy). This plan authors those artifacts and the workflow; the first GHCR push happens when the workflow runs on `main`. OPS-02 (CI/CD to GHCR + webhook) and OPS-04 (health endpoint) are satisfied for the WebApp slice here; the Coolify app + domain + webhook wiring follows in Plan 05-04.
Output: 5 files committed; `pnpm test` green for the health route; `actionlint` clean on deploy-web.yml.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-CONTEXT.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-RESEARCH.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-PATTERNS.md
@.planning/phases/02-mcp-server/02-DEPLOY-SPEC.md
@.github/workflows/deploy-mcp.yml
@.github/workflows/ci.yml
@webapp/next.config.ts
@webapp/package.json
@api/app/routers/health.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WebApp /api/health route + standalone next.config + Dockerfile (D-13, D-07)</name>
  <files>webapp/app/api/health/route.ts, webapp/app/api/health/route.test.ts, webapp/next.config.ts, webapp/Dockerfile</files>
  <read_first>
    - webapp/next.config.ts (current — no output: standalone yet)
    - webapp/package.json (pnpm 11.15.1, next 16.2.12, vitest)
    - api/app/routers/health.py (analog: /health returns {status:ok})
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-PATTERNS.md (webapp/Dockerfile + route patterns)
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-RESEARCH.md (Pitfall 1: standalone missing assets; Pitfall 3: cold start 15s)
  </read_first>
  <behavior>
    - Test 1: GET /api/health returns 200 with JSON body {status:'ok'}
    - Test 2: GET /api/health does not require auth (no cookie / bearer)
    - Test 3: response is lightweight (no DB / fetch calls) — assert body has no db field
  </behavior>
  <action>
Write `webapp/app/api/health/route.ts` exporting `GET()` returning `NextResponse.json({ status: 'ok' })` — unauthenticated, no DB, no heavy work (D-13, T-05-04 DoS). Write the vitest test at `webapp/app/api/health/route.test.ts` asserting the three behaviors above. Update `webapp/next.config.ts` to add `output: 'standalone'` (keep existing images.remotePatterns). Write `webapp/Dockerfile` as a multi-stage standalone build per RESEARCH Pattern 1: `node:24-alpine` base, deps stage with `corepack enable pnpm && pnpm i --frozen-lockfile`, builder stage copying node_modules + source + `pnpm run build`, runner stage as non-root `nextjs:nodejs` (uid/gid 1001) copying `public`, `.next/standalone`, `.next/static` (Pitfall 1), `ENV PORT=3000 HOSTNAME=0.0.0.0`, `EXPOSE 3000`, `CMD ["node","server.js"]`. Do NOT copy alembic or python — this is the Next.js app only.
  </action>
  <verify>
    <automated>cd webapp && pnpm test -- app/api/health/route.test.ts</automated>
    <human-check>Run `docker build -t puzzlessbox-web:test ./webapp` and confirm the image builds and starts `node server.js` (slow — outside the 120s feedback budget).</human-check>
  </verify>
  <acceptance_criteria>
    - vitest green for /api/health route (3 behaviors)
    - `docker build` from webapp/ succeeds and produces an image that runs `node server.js` (verified via human-check above)
    - next.config.ts has `output: 'standalone'`
    - Dockerfile runner stage runs as non-root nextjs user
  </acceptance_criteria>
  <done>WebApp ships a standalone Docker image with an unauthenticated /api/health liveness route returning 200 {status:ok}.</done>
</task>

<task type="auto">
  <name>Task 2: Author deploy-web.yml GHCR workflow (D-05, D-06, D-07, D-08)</name>
  <files>.github/workflows/deploy-web.yml</files>
  <read_first>
    - .github/workflows/deploy-mcp.yml (canonical template — copy structure, swap context/image/secret/webhook)
    - .github/workflows/ci.yml (SHA-pinning convention)
    - .planning/phases/02-mcp-server/02-DEPLOY-SPEC.md (pinned Action SHAs)
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-PATTERNS.md (deploy-web.yml pattern with GET + status assert)
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-CONTEXT.md (D-05, D-06, D-07, D-08, D-16)
  </read_first>
  <action>
Create `.github/workflows/deploy-web.yml` mirroring `deploy-mcp.yml` with these swaps: name `Deploy WebApp`, path filter `webapp/**` + `.github/workflows/deploy-web.yml`, image `ghcr.io/${{ github.repository_owner }}/puzzlessbox-web` (D-07), context `./webapp`, webhook secret `COOLIFY_WEB_WEBHOOK` (D-16). Add `workflow_dispatch` to the `on:` block so Plan 05-04 can trigger the first GHCR push manually before the Coolify WebApp app is wired (D-03 ordering — image must exist before app pull; mirrors 05-03 tracer pattern). Per D-06 use **GET** (not MCP's POST) with `Authorization: Bearer ${{ secrets.COOLIFY_TOKEN }}` and assert HTTP 200/202 (audnexus-style): capture `curl -s -o /dev/null -w "%{http_code}"` into STATUS, exit 1 if not 200/202. Pin all Actions by full SHA + version comment exactly as in deploy-mcp.yml / 02-DEPLOY-SPEC.md (D-08): actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7, docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0, docker/metadata-action@dc808040100637a589fabce1cb79ff13a1411302 # v6.2.0, docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0. Keep `concurrency: deploy-web-${{ github.ref }}` + `cancel-in-progress: true` and `permissions: contents: read, packages: write`. Do NOT hardcode webhook URLs or tokens (T-05-03).
  </action>
  <verify>
    <automated>grep -v '^#' .github/workflows/deploy-web.yml | grep -c "secrets.COOLIFY_WEB_WEBHOOK\|secrets.GITHUB_TOKEN\|secrets.COOLIFY_TOKEN" | grep -qE "[2-3]" && grep -c "uses: .*@v[0-9]" .github/workflows/deploy-web.yml | { read n; [ "$n" = "0" ]; } && grep -q "puzzlessbox-web" .github/workflows/deploy-web.yml && grep -q '"%{http_code}"' .github/workflows/deploy-web.yml && grep -q "workflow_dispatch" .github/workflows/deploy-web.yml</automated>
  </verify>
  <acceptance_criteria>
    - deploy-web.yml path-filtered on webapp/** + the workflow file
    - `workflow_dispatch` trigger present (D-03 — first GHCR push before Coolify app wiring; mirrors 05-03 tracer)
    - Image is ghcr.io/clezcoding/puzzlessbox-web with :latest + :sha-<sha> tags (D-07)
    - Webhook trigger uses GET + Bearer COOLIFY_TOKEN + asserts 200/202 (D-06)
    - All Actions pinned by full SHA + version comment (D-08); zero `@vN` refs
    - No hardcoded webhook URLs / tokens (only secrets.* references)
    - actionlint clean (if installed; otherwise grep checks above pass)
  </acceptance_criteria>
  <done>deploy-web.yml mirrors deploy-mcp.yml structure, swaps to web image + COOLIFY_WEB_WEBHOOK, uses GET + status assert, all Actions SHA-pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → GHCR | build-push uses GITHUB_TOKEN to push public image |
| GHCR → Coolify pull | Coolify pulls public ghcr.io/clezcoding/puzzlessbox-web:latest without registry auth (D-17) |
| Workflow → Coolify webhook | curl GET to COOLIFY_WEB_WEBHOOK with Bearer COOLIFY_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information Disclosure | deploy-web.yml webhook URLs/tokens | high | mitigate | Webhook URL + token only ever referenced via secrets.COOLIFY_WEB_WEBHOOK / secrets.COOLIFY_TOKEN; never hardcoded. grep gate asserts no literal URLs. |
| T-05-04 | Denial of Service | /api/health route | low | mitigate | Route returns static {status:ok} with no DB / fetch / scraper call (D-13, RESEARCH Known Threat Patterns). Vitest asserts no db field. |
| T-05-SC | Tampering | npm installs (next etc.) | high | mitigate | next flagged [SUS] in RESEARCH Package Legitimacy Audit (recent release). Use pnpm frozen-lockfile (already pinned in package.json); no new install task here — image build reuses lockfile. |
</threat_model>

<verification>
- `pnpm --filter puzzlessbox-webapp test` green (health route)
- `docker build` from webapp/ succeeds
- `deploy-web.yml` grep checks pass (no @vN, has puzzlessbox-web, has status code assert, secrets refs ≥2)
- actionlint clean if installed
</verification>

<success_criteria>
- D-03 prep satisfied: webapp/Dockerfile + deploy-web.yml exist (first GHCR push happens when workflow runs on main)
- D-05, D-06, D-07, D-08, D-13 honored
- OPS-02 (web slice) + OPS-04 (web health endpoint) authored
</success_criteria>

<output>
Create `.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-02-SUMMARY.md` when done.
</output>
