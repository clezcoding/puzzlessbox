---
phase: 05-coolify-deployment-ci-cd-h-rtung
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/COVERAGE.md
autonomous: false
requirements: [OPS-01, OPS-02, OPS-04]
user_setup:
  - service: coolify
    why: "Copy WebApp Deploy Webhook URL into GitHub Secret COOLIFY_WEB_WEBHOOK (D-19); set WebApp health timings 10s/5s/5/15s in Coolify UI if CLI/MCP cannot (D-14)"
    dashboard_config:
      - task: "Create WebApp Coolify dockerimage app + copy webhook URL"
        location: "Coolify UI → Puzzlessbox project → New Resource → Docker Image (ghcr.io/clezcoding/puzzlessbox-web:latest)"
      - task: "Set GitHub Secret COOLIFY_WEB_WEBHOOK"
        location: "gh secret set COOLIFY_WEB_WEBHOOK or GitHub repo Settings → Secrets and variables → Actions"
      - task: "Set WebApp health timings 10s/5s/5/15s (D-14) if CLI/MCP cannot"
        location: "Coolify UI → WebApp app → Health Checks"
      - task: "Set GHCR puzzlessbox-web package Public after first push (D-17)"
        location: "GitHub Packages → puzzlessbox-web → Package settings → Public"

must_haves:
  truths:
    - "A new Coolify dockerimage app exists pulling ghcr.io/clezcoding/puzzlessbox-web:latest under pbox.puzzlesstool.online with Traefik/Let's-Encrypt HTTPS"
    - "GET https://pbox.puzzlesstool.online/api/health returns 200 JSON {status:'ok'} without auth (D-13)"
    - "WebApp Coolify health probe is /api/health with timings 10s/5s/5/15s (D-14)"
    - "A push to main (or workflow_dispatch) triggers deploy-web.yml, pushes :latest + :sha-<sha> to GHCR, and the Coolify webhook returns 200/202"
    - "MCP app (n5frtiupale5c2zjm9fyk1qc) health timings are retuned to 10s/5s/5/15s for consistency with D-14 (discretion: retune)"
    - "COVERAGE.md documents Coolify + GHCR API capabilities used with INTEGRATE/OPT-OUT + reasons"
    - "All three apps (API, MCP, WebApp) are live under *.puzzlesstool.online with /health returning 200"
  artifacts:
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/COVERAGE.md
    - "New WebApp Coolify app UUID (recorded in SUMMARY)"
  key_links:
    - "deploy-web.yml → GHCR puzzlessbox-web → COOLIFY_WEB_WEBHOOK → WebApp Coolify app → pbox.puzzlesstool.online/api/health"
    - "WebApp → API (BETTER_AUTH_URL + API base URL env) → Postgres"
  prohibitions:
    - "No WebApp Coolify app before first successful GHCR push (D-03 — empty-image risk)"
    - "No /ready as Traefik gate (D-12)"
    - "No GlitchTip / S3 / scraper harden in this plan (deferred — D-04, OPS-05/06)"
    - "No invented scripts for webhook URL retrieval (D-19 — flag MANUAL)"
---

<objective>
Create the WebApp Coolify dockerimage app under `pbox.puzzlesstool.online`, wire the webhook, trigger the first deploy, and verify `/api/health` over HTTPS. Then retune the MCP app health timings to D-14 for consistency, write the COVERAGE.md API matrix, and run the final phase verification (all three apps healthy, CI green, backups active).

Purpose: D-15 cutover order's final steps (WebApp → verify). Reuses the GHCR → Coolify → Traefik → health pattern proven by Plan 05-03's API tracer. Satisfies OPS-01 (WebApp slice), OPS-02 (web webhook), OPS-04 (WebApp + MCP health). D-03 forbids creating the Coolify WebApp app before the first GHCR push succeeds, so this plan triggers the workflow_dispatch build first, then creates the app.
Output: WebApp live at `https://pbox.puzzlesstool.online/api/health`; MCP health retuned; COVERAGE.md committed; phase verification green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-CONTEXT.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-RESEARCH.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-PATTERNS.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-02-SUMMARY.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-03-SUMMARY.md
@.github/workflows/deploy-web.yml
@webapp/Dockerfile
</context>

<tasks>

<task type="tracer">
  <name>End-to-end WebApp dockerimage deploy — one path only (D-01, D-03, D-13, D-15)</name>
  <files>(none — Coolify CLI/MCP creates the app server-side; deploy-web.yml already authored in 05-02)</files>
  <read_first>
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-CONTEXT.md (D-01, D-03, D-13, D-14, D-15, D-16, D-17, D-19; Coolify inventory UUIDs)
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-RESEARCH.md (WebApp env: Better Auth URL, API base URL, Google OAuth redirect host pbox; Pitfall 3 cold start 15s)
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-02-SUMMARY.md (deploy-web.yml + Dockerfile + /api/health ready)
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-03-SUMMARY.md (API live — env dependency)
  </read_first>
  <precondition>Plan 05-02 artifacts committed (webapp/Dockerfile, deploy-web.yml, /api/health route). Plan 05-03 API live at api.puzzlesstool.online (WebApp env depends on it).</precondition>
  <action>
First trigger the WebApp GHCR build so an image exists before the Coolify app pulls (D-03): run `gh workflow run deploy-web.yml` (workflow_dispatch) — if deploy-web.yml does not already declare `workflow_dispatch`, surface a checkpoint for the user to add it (do NOT edit the workflow here without re-reading 05-02; instead flag MANUAL if needed). Wait for the GHCR push to succeed (`gh run watch`). Then create the WebApp Coolify dockerimage app via Coolify CLI (`--context hostunlimited`) or Coolify MCP `create_dockerimage_application` in project `nlm9h0u5lh2rnf2fg10vuf16`: image `ghcr.io/clezcoding/puzzlessbox-web:latest`, attach domain `pbox.puzzlesstool.online` (D-01 — not app./web./apex) with Traefik/Let's-Encrypt TLS. Set WebApp env vars: `BETTER_AUTH_URL=https://pbox.puzzlesstool.online`, `NEXT_PUBLIC_API_BASE_URL=https://api.puzzlesstool.online`, Google OAuth redirect host `pbox.puzzlesstool.online` (RESEARCH Runtime State). Set Coolify health probe path `/api/health` (D-13) and timings 10s/5s/5/15s (D-14, Pitfall 3 cold start) — if CLI/MCP cannot set these, surface a checkpoint:human-verify for the user to set them in Coolify UI (D-19). Trigger the Coolify deploy (webhook) and verify `https://pbox.puzzlesstool.online/api/health` returns 200 `{"status":"ok"}`. Record the new WebApp app UUID in SUMMARY.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w "%{http_code}" https://pbox.puzzlesstool.online/api/health | grep -q "^200" && gh secret list | grep -q COOLIFY_WEB_WEBHOOK</automated>
  </verify>
  <acceptance_criteria>
    - First deploy-web.yml workflow_dispatch run succeeded; GHCR puzzlessbox-web package has :latest + :sha-<sha> tags
    - New WebApp Coolify dockerimage app UUID recorded in SUMMARY; app pulls ghcr.io/clezcoding/puzzlessbox-web:latest
    - `curl -sS https://pbox.puzzlesstool.online/api/health` returns 200 {"status":"ok"} (D-13)
    - WebApp env: BETTER_AUTH_URL + NEXT_PUBLIC_API_BASE_URL + Google OAuth redirect host pbox
    - Coolify health probe = /api/health, timings 10s/5s/5/15s (D-14)
    - GHCR puzzlessbox-web package Public (D-17)
    - GitHub Secret COOLIFY_WEB_WEBHOOK set
  </acceptance_criteria>
  <done>WebApp live at https://pbox.puzzlesstool.online/api/health returning 200; deploy-web.yml CI green; Coolify app healthy.</done>
</task>

<task type="auto">
  <name>Task 2: Retune MCP health timings to D-14 + write COVERAGE.md (D-14, API Coverage Decision Checkpoint)</name>
  <files>.planning/phases/05-coolify-deployment-ci-cd-h-rtung/COVERAGE.md</files>
  <read_first>
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-CONTEXT.md (D-14, D-19; discretion: retune MCP)
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-RESEARCH.md (Architectural Responsibility Map)
    - .planning/phases/02-mcp-server/02-DEPLOY-SPEC.md (MCP app uuid n5frtiupale5c2zjm9fyk1qc, /health)
  </read_first>
  <precondition>WebApp app from Task 1 is live at pbox.puzzlesstool.online/api/health.</precondition>
  <action>
Retune the MCP app (`n5frtiupale5c2zjm9fyk1qc`) Coolify health timings to interval 10s / timeout 5s / retries 5 / start_period 15s for consistency with D-14 (discretion: retune per RESEARCH recommendation). Use Coolify CLI (`--context hostunlimited app update …` or whatever the CLI exposes for health timings) or Coolify MCP `update_application`. If neither can set interval/timeout/retries/start_period, surface a checkpoint:human-verify for the user to set them in Coolify UI (D-19) — do NOT invent a script. Then write `COVERAGE.md` in the phase dir documenting the Coolify + GHCR + GitHub Actions API capabilities used across the phase, as an INTEGRATE/OPT-OUT matrix with reasons: e.g. `coolify app create (dockerimage) → INTEGRATE (D-02)`, `coolify app update build_pack → OPT-OUT (not supported, D-02)`, `coolify database backup create → INTEGRATE (D-09)`, `coolify health timings → INTEGRATE via CLI/MCP, MANUAL fallback UI (D-14, D-19)`, `gh secret set → INTEGRATE (D-16)`, `gh workflow run → INTEGRATE (first deploy)`, `GHCR public package → INTEGRATE (D-17)`, `Coolify webhook URL retrieval → MANUAL (D-19, API may not expose)`. Note any capability the CLI/MCP could NOT do and was surfaced as MANUAL.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-coolify-deployment-ci-cd-h-rtung/COVERAGE.md && grep -q "INTEGRATE" .planning/phases/05-coolify-deployment-ci-cd-h-rtung/COVERAGE.md && grep -q "OPT-OUT" .planning/phases/05-coolify-deployment-ci-cd-h-rtung/COVERAGE.md && grep -q "MANUAL" .planning/phases/05-coolify-deployment-ci-cd-h-rtung/COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - MCP app n5frtiupale5c2zjm9fyk1qc health timings = 10s/5s/5/15s (D-14) — or MANUAL checkpoint surfaced if CLI/MCP cannot
    - COVERAGE.md exists with INTEGRATE, OPT-OUT, and MANUAL entries
    - Every Coolify/GHCR/Actions capability used in the phase is classified with a reason
  </acceptance_criteria>
  <done>MCP health retuned to D-14; COVERAGE.md documents all Coolify/GHCR/Actions capabilities with INTEGRATE/OPT-OUT/MANUAL + reasons.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Final phase verification — all three apps healthy, CI green, backups active</name>
  <what-built>All three apps (API, MCP, WebApp) live under *.puzzlesstool.online with /health + /api/health returning 200; deploy-api.yml + deploy-web.yml CI green with webhook 200/202; backup schedule active with baseline backup; MCP health retuned to 10s/5s/5/15s; COVERAGE.md written.</what-built>
  <how-to-verify>
    1. `curl -sS https://api.puzzlesstool.online/health` → 200 {"status":"ok"}
    2. `curl -sS https://mcp.puzzlesstool.online/health` → 200 {"status":"ok","service":"mcp-server"} (or similar)
    3. `curl -sS https://pbox.puzzlesstool.online/api/health` → 200 {"status":"ok"}
    4. `curl -sS -o /dev/null -w "%{http_code}" https://mcp.puzzlesstool.online/mcp` (no auth) → 401 (MCP-02 still holds)
    5. `coolify --context hostunlimited database backup list pfqgb5pcvgi9oh64bpe3shtn` → enabled schedule + baseline backup entry
    6. Coolify UI → all three apps → Health Checks: path + 10s/5s/5/15s (D-14)
    7. GitHub Actions → Deploy API + Deploy WebApp + Deploy MCP → latest runs green
    8. GHCR → puzzlessbox-api + puzzlessbox-web + puzzlessbox-mcp packages → Public (D-17)
    9. `gh secret list` → COOLIFY_TOKEN, COOLIFY_API_WEBHOOK, COOLIFY_WEB_WEBHOOK, COOLIFY_MCP_WEBHOOK (D-16)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. an app unhealthy, a secret missing, GHCR package private)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → GHCR | build-push uses GITHUB_TOKEN to push public WebApp image |
| GHCR → Coolify pull | Coolify pulls public ghcr.io/clezcoding/puzzlessbox-web:latest without registry auth (D-17) |
| Workflow → Coolify webhook | curl GET to COOLIFY_WEB_WEBHOOK with Bearer COOLIFY_TOKEN |
| Public internet → pbox.puzzlesstool.online/api/health | unauthenticated liveness route (T-05-09) |
| WebApp → API | WebApp calls api.puzzlesstool.online with user session + service bearer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Information Disclosure | deploy-web.yml webhook URL/token | high | mitigate | Only secrets.COOLIFY_WEB_WEBHOOK / secrets.COOLIFY_TOKEN referenced (authored in 05-02); grep gate asserts no literal URLs. |
| T-05-10 | Denial of Service | /api/health (public) | low | mitigate | Route is liveness-only, no DB / auth / heavy work (D-13, 05-02 vitest asserts). /ready NOT used as Traefik gate (D-12). |
| T-05-11 | Spoofing | WebApp → API calls | medium | mitigate | WebApp env BETTER_AUTH_URL + NEXT_PUBLIC_API_BASE_URL pinned to pbox + api.puzzlesstool.online (HTTPS only); Better Auth session cookies scoped to pbox. |
| T-05-12 | Tampering | MCP health retune | low | mitigate | Coolify CLI/MCP authenticated via hostunlimited context; if UI-only, MANUAL checkpoint surfaces the change for user audit (D-19). |
| T-05-SC | Tampering | npm installs (next etc.) | high | mitigate | webapp pnpm frozen-lockfile (pinned in package.json); no new install task — image build reuses lockfile. next flagged [SUS] in RESEARCH audit. |
</threat_model>

<verification>
- `curl -sS https://pbox.puzzlesstool.online/api/health` → 200 {"status":"ok"}
- `curl -sS https://api.puzzlesstool.online/health` → 200 {"status":"ok"}
- `curl -sS https://mcp.puzzlesstool.online/health` → 200
- `gh secret list` → COOLIFY_TOKEN, COOLIFY_API_WEBHOOK, COOLIFY_WEB_WEBHOOK, COOLIFY_MCP_WEBHOOK
- `coolify --context hostunlimited database backup list pfqgb5pcvgi9oh64bpe3shtn` → enabled schedule + baseline backup
- GitHub Actions Deploy API + Deploy WebApp + Deploy MCP latest runs green
- GHCR puzzlessbox-api + puzzlessbox-web + puzzlessbox-mcp packages Public
- COVERAGE.md exists with INTEGRATE + OPT-OUT + MANUAL entries
</verification>

<success_criteria>
- OPS-01 (WebApp slice): WebApp runs as separate Coolify dockerimage app under pbox.puzzlesstool.online
- OPS-02 (web slice): deploy-web.yml builds to GHCR + triggers Coolify webhook (200/202)
- OPS-04 (WebApp + MCP): /api/health + /health endpoints + Coolify health checks 10s/5s/5/15s on all three apps
- D-01, D-03, D-13, D-14, D-15, D-16, D-17, D-19 honored
- COVERAGE.md API coverage matrix written
- All three apps healthy end-to-end (final phase verification)
</success_criteria>

<output>
Create `.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-04-SUMMARY.md` when done. Record WebApp app UUID, MCP retune result, COVERAGE.md path, final smoke curl results.
</output>
