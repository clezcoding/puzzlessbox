---
phase: 05-coolify-deployment-ci-cd-h-rtung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [OPS-03]
user_setup: []

must_haves:
  truths:
    - "puzzlessbox-db (pfqgb5pcvgi9oh64bpe3shtn) has an enabled local backup schedule with cron 0 3 * * * and retention 14/14"
    - "At least one baseline backup exists in the local backup list before any API cutover begins"
    - "Backup schedule is local-only (no S3) and does not block later --save-s3 / --s3-storage-uuid (OPS-06)"
  artifacts:
    - "Coolify backup schedule UUID on puzzlessbox-db (visible via `coolify --context hostunlimited database backup list pfqgb5pcvgi9oh64bpe3shtn`)"
  key_links:
    - "Coolify CLI context hostunlimited → puzzlessbox-db → backup schedule"
  prohibitions:
    - "No S3/offsite config in Phase 5 (D-11, OPS-06 deferred)"
    - "No custom pg_dump scripts (RESEARCH Don't Hand-Roll)"
---

<objective>
Create the local PostgreSQL backup schedule on `puzzlessbox-db` and trigger one immediate baseline backup before the API cutover (D-15 step 1).

Purpose: OPS-03 requires an active local backup schedule on the Coolify server. D-10 mandates a baseline backup before API cutover so the operator can roll back if the new dockerimage app misconfigures the DB connection. This plan is the precondition for Plan 05-03 (API cutover tracer) per D-15.
Output: Enabled backup schedule + baseline backup entry visible in Coolify CLI.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-CONTEXT.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enabled local backup schedule on puzzlessbox-db (D-09, D-11)</name>
  <files>(none — Coolify CLI creates the schedule server-side)</files>
  <read_first>
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-CONTEXT.md (D-09, D-11)
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-RESEARCH.md (Runtime State Inventory, Don't Hand-Roll)
  </read_first>
  <action>
Create the local backup schedule on database `puzzlessbox-db` (uuid `pfqgb5pcvgi9oh64bpe3shtn`) via Coolify CLI with context `hostunlimited`. Per D-09 use exactly: cron `0 3 * * *` (03:00 UTC), `--enabled`, `--retention-amount-locally 14`, `--retention-days-locally 14`. Do NOT pass `--save-s3` or `--s3-storage-uuid` (D-11 — local only; design must not block later OPS-06). If the CLI flag names differ from D-09's documented flags, surface the exact `coolify database backup create --help` output to the user and stop — do not invent a substitute script (D-19). Confirm the schedule UUID returned by the CLI and persist it in the plan SUMMARY for traceability.
  </action>
  <verify>
    <automated>coolify --context hostunlimited database backup list pfqgb5pcvgi9oh64bpe3shtn | grep -E "enabled|0 3 \* \* \*|14"</automated>
  </verify>
  <acceptance_criteria>
    - `coolify --context hostunlimited database backup list pfqgb5pcvgi9oh64bpe3shtn` shows an enabled schedule
    - Schedule cron is `0 3 * * *` and retention is 14 backups / 14 days local
    - No S3 storage attached (D-11)
    - Schedule UUID recorded in 05-01-SUMMARY.md
  </acceptance_criteria>
  <done>Enabled local backup schedule exists on puzzlessbox-db with cron 0 3 * * * and retention 14/14, no S3.</done>
</task>

<task type="auto">
  <name>Task 2: Trigger baseline backup and confirm entry (D-10)</name>
  <files>(none)</files>
  <read_first>
    - 05-01-SUMMARY.md (schedule UUID from Task 1)
  </read_first>
  <precondition>Task 1 schedule UUID is live on puzzlessbox-db.</precondition>
  <action>
Trigger one immediate baseline backup via `coolify --context hostunlimited database backup trigger pfqgb5pcvgi9oh64bpe3shtn` (or the schedule UUID returned by Task 1 — use whichever the CLI accepts; surface `--help` if ambiguous, do not guess). Wait for the backup to finish (poll `coolify database backup list` until a new entry appears with status `success`/`completed`). This baseline is the rollback point for the API cutover (Plan 05-03) per D-15. Record the baseline backup UUID + timestamp in the SUMMARY.
  </action>
  <verify>
    <automated>coolify --context hostunlimited database backup list pfqgb5pcvgi9oh64bpe3shtn | tail -5 | grep -Ei "success|completed"</automated>
  </verify>
  <acceptance_criteria>
    - At least one backup entry with status success/completed visible in the backup list
    - Baseline backup UUID + timestamp recorded in 05-01-SUMMARY.md
    - Backup created BEFORE Plan 05-03 API cutover begins (D-15 ordering)
  </acceptance_criteria>
  <done>One baseline backup exists on puzzlessbox-db and is recorded as the pre-cutover rollback point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coolify CLI → Coolify API | CLI uses hostunlimited context token; backup config crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | backup retention | low | accept | Local-only backups on Coolify host; VPS access already gated by Coolify auth. No offsite exposure in v1 (D-11). |
| T-05-02 | Tampering | backup schedule config | medium | mitigate | Use Coolify CLI with hostunlimited context (authenticated); record schedule UUID in SUMMARY for audit. No custom pg_dump script (D-19, RESEARCH Don't Hand-Roll). |
</threat_model>

<verification>
- `coolify --context hostunlimited database backup list pfqgb5pcvgi9oh64bpe3shtn` shows enabled schedule + ≥1 successful backup
- Schedule matches D-09 (cron 0 3 * * *, retention 14/14, local only)
- Baseline backup UUID recorded for Plan 05-03 rollback reference
</verification>

<success_criteria>
- OPS-03 satisfied: PostgreSQL has active local backup schedule on Coolify server
- D-09, D-10, D-11 honored
- Baseline backup exists before API cutover (D-15 step 1)
</success_criteria>

<output>
Create `.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-01-SUMMARY.md` when done. Record schedule UUID + baseline backup UUID + timestamp.
</output>
