---
phase: 02-mcp-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/models/mcp_client.py
  - api/app/models/__init__.py
  - api/alembic/versions/0004_mcp_clients.py
  - api/app/core/bootstrap.py
  - api/app/core/config.py
  - api/app/routers/internal.py
  - api/app/auth/jwt.py
  - api/app/main.py
  - mcp-server/requirements.txt
  - mcp-server/pytest.ini
  - mcp-server/app/__init__.py
  - mcp-server/app/config.py
  - mcp-server/app/auth.py
  - mcp-server/app/api_client.py
  - mcp-server/app/health.py
  - mcp-server/app/server.py
  - mcp-server/app/tools/__init__.py
  - mcp-server/app/tools/items.py
  - mcp-server/tests/conftest.py
  - mcp-server/tests/test_auth.py
  - mcp-server/tests/test_api_contract.py
  - mcp-server/tests/test_health.py
autonomous: true
requirements: [MCP-01, MCP-02]
user_setup:
  - service: mcp-bootstrap
    why: "Solo-Bootstrap eines mcp_clients-Row aus Hermes-Bearer (D-07)"
    env_vars:
      - name: MCP_BOOTSTRAP_TOKEN
        source: "Selbst generierter Hermes-Bearer (z.B. openssl rand -hex 32); wird gehasht, nie Plaintext gespeichert"
      - name: SERVICE_OWNER_ID
        source: "UUID einer existierenden Better-Auth user-Row (D-08 Guard verlangt sie)"

must_haves:
  truths:
    - "POST /internal/mcp-auth mit gültigem X-Service-Bearer + bekanntem bearer_hash (status active|grace, expires_at NULL oder > now) liefert 200 {owner_id}"
    - "POST /internal/mcp-auth mit unbekanntem/expired/grace-abgelaufenem bearer_hash liefert 401"
    - "POST /internal/mcp-auth ohne gültigen X-Service-Bearer liefert 401 (Gateway-Auth, D-03)"
    - "get_current_owner akzeptiert X-Owner-Id nur bei gültigem X-Service-Bearer und wenn owner_id valides UUID ist UND eine Better-Auth user-Row existiert; sonst 403 (D-08)"
    - "MCP-Request ohne Authorization-Header wird mit 401 + WWW-Authenticate: Bearer abgewiesen"
    - "MCP-Request mit unbekanntem Bearer wird mit 401 invalid_token abgewiesen"
    - "MCP create_item wired end-to-end: Bearer -> OwnerResolvingVerifier -> POST /internal/mcp-auth -> owner_id-claim -> POST /drafts mit Accept vnd.v1+json + X-Service-Bearer + X-Owner-Id + Idempotency-Key"
    - "owner_id im create_item-Tool stammt aus get_access_token().claims, niemals aus Tool-Argument"
    - "MCP /health liefert 200 unauthenticated"
  prohibitions:
    - "MCP darf Bearer/Token niemals im Klartext loggen (D-07)"
    - "MCP darf die mcp_clients-Tabelle nicht selbst öffnen — API besitzt sie, MCP ruft POST /internal/mcp-auth (D-06)"
    - "owner_id darf nie aus Tool-Input akzeptiert werden (Tenant-Leak)"
  backstop:
    - statement: "create_item mit vorhandenem Idempotency-Key wird von der API dedupliziert; MCP generiert genau einmal eine UUID wenn keiner geliefert wird (MCP-01 concurrency edge, D-19)"
      verification: backstop
  artifacts:
    - api/app/routers/internal.py
    - api/app/models/mcp_client.py
    - api/alembic/versions/0004_mcp_clients.py
    - mcp-server/app/server.py
    - mcp-server/app/auth.py
    - mcp-server/app/api_client.py
    - mcp-server/app/tools/items.py
  key_links:
    - "MCP OwnerResolvingVerifier.verify_token -> httpx POST /internal/mcp-auth (bearer_hash) -> AccessToken(claims={owner_id})"
    - "API get_current_owner service-path -> X-Owner-Id Guard (UUID + user-row) -> RLS owner-Filter"
    - "MCP create_item -> api_client.call_api POST /drafts (Header-Contract D-16)"

flagged_assumptions:
  - "MCP-01 concurrency edge (COVERAGE unresolved): Verhalten bei parallel/interrupted create_item ist NICHT durch Success-Criteria spezifiziert. Angenommen: API-Idempotency-Store (idempotency_keys) dedupliziert; MCP-Retry nur 1x auf 502/503. Falls Hermes keinen stabilen Key liefert und MCP je Versuch neu generiert, ist Dedupe NICHT garantiert — bewusst akzeptiert für v1."
  - "MCP-02 unclassified edge (COVERAGE unresolved): als 401/403-Auth-Enforcement-Prädikat aufgelöst (siehe truths). Kein weiterer verdeckter MCP-02-Fall angenommen."
  - "A1/A4/A5 (RESEARCH): In-Memory Client(mcp)-Signatur, TokenVerifier-Import-Pfad und AccessToken-Konstruktor-Felder in fastmcp 3.4.4 bei Impl verifizieren; Fallback ASGITransport dokumentiert."

assumption_delta_decision:
  primary_noun: "owner identity resolution"
  decision: add-alongside
  rationale: "Phase-1 SERVICE_OWNER_ID/service_principals (single-owner env, MCP->API Gateway) bleibt bestehen; D-02 mcp_clients (bearer_hash->owner_id, SaaS-ready) kommt für Hermes->MCP daneben. Keine Promotion/Ersetzung — zwei getrennte Auth-Hops (D-01)."
---

<objective>
Liefert den **Phase-Tracer**: den dünnsten produktionsreifen Pfad Hermes-Bearer → MCP → `OwnerResolvingVerifier` → `POST /internal/mcp-auth` → `create_item` → `POST /drafts`, plus 401 bei fehlendem/ungültigem Bearer. Baut dazu die API-seitige Owner-Auflösung (`mcp_clients` + `/internal/mcp-auth` + `X-Owner-Id`-Guard) und das MCP-Grundgerüst (`mcp-server/` FastMCP-App + Auth + httpx-Client + ein Tool + Health + Wave-0-Test-Infra).

Purpose: Beweist die Cross-Service-Architektur (zwei Auth-Hops D-01/D-03, DB-freier MCP D-06) end-to-end auf frühem Kontext, bevor die restlichen 5 Tools und der Deploy-Pfad expandieren.
Output: Lauffähige `mcp-server/`-App mit grüner Wave-0-Suite (401/403 + create_item-Contract + health) und die neuen API-Endpunkte `/internal/mcp-auth` + `X-Owner-Id`-Guard.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mcp-server/02-CONTEXT.md
@.planning/phases/02-mcp-server/02-RESEARCH.md
@.planning/phases/02-mcp-server/02-PATTERNS.md
@.planning/phases/02-mcp-server/COVERAGE.md
</context>

<artifacts_this_phase_produces>
**Neu in diesem Plan (02-01):**
- `api/app/models/mcp_client.py` — `MCPClient` SQLModel (`id` PK, `owner_id`, `bearer_hash` indexed, `status`, `expires_at`, `created_at`)
- `api/alembic/versions/0004_mcp_clients.py` — Migration `mcp_clients` (down_revision `0003_calendar_tokens`) + GRANT
- `api/app/routers/internal.py` — `POST /internal/mcp-auth`
- `api/app/core/bootstrap.py` — neue Funktion `ensure_mcp_client(settings)`
- `api/app/auth/jwt.py` — `X-Owner-Id`-Guard (D-03/D-08) im service-Pfad
- `api/app/core/config.py` — neu `MCP_BOOTSTRAP_TOKEN`
- `mcp-server/` — `app/{__init__,config,auth,api_client,health,server}.py`, `app/tools/{__init__,items}.py`, `requirements.txt`, `pytest.ini`, `tests/{conftest,test_auth,test_api_contract,test_health}.py`
- Symbole: `OwnerResolvingVerifier(TokenVerifier)`, `make_client()`, `call_api()`, `resolve_owner()`, `_to_tool_error()`, `register_tools(mcp)`, Tool `create_item`
- Env-Vars: `MCP_BOOTSTRAP_TOKEN` (API), `SERVICE_BEARER_TOKEN`/`MCP_API_BASE_URL`/`ENV` (MCP)

**Später (Referenz, nicht dieser Plan):** 5 weitere Tools (02-03), `/categories` + `/items/{id}` move (02-02), Dockerfile + `deploy-mcp.yml` + Coolify (02-04).
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: API Owner-Auflösung — mcp_clients + POST /internal/mcp-auth + X-Owner-Id-Guard</name>
  <files>api/app/models/mcp_client.py, api/app/models/__init__.py, api/alembic/versions/0004_mcp_clients.py, api/app/core/bootstrap.py, api/app/core/config.py, api/app/routers/internal.py, api/app/auth/jwt.py, api/app/main.py</files>
  <read_first>
    - api/app/models/service_principal.py (Model-Analog, exact)
    - api/app/models/category.py (SQLModel-Feld-Muster)
    - api/app/models/__init__.py (Registrierung)
    - api/alembic/versions/0002_idempotency.py (Migration-Vorlage: create_table + GRANT + BERLIN_NOW)
    - api/app/auth/jwt.py (get_current_owner service-Pfad Zeilen 90-108, _resolve_service_owner 67-87)
    - api/app/routers/auth.py (Router-Struktur + /verify Response-Shape {owner_id})
    - api/app/core/bootstrap.py (ensure_service_principal — Analog für ensure_mcp_client)
    - api/app/main.py (include_router + _lifespan Zeilen 41-64)
    - api/app/core/config.py (Settings-Felder)
  </read_first>
  <action>
    **1. Model `api/app/models/mcp_client.py`** — spiegle `ServicePrincipal`, aber mehrere Rows pro owner (dual-token grace, D-04): Klasse `MCPClient(SQLModel, table=True)`, `__tablename__ = "mcp_clients"`, Felder `id: uuid.UUID = Field(default_factory=uuid.uuid4, primary_key=True)`, `owner_id: uuid.UUID = Field(index=True)`, `bearer_hash: str = Field(index=True)`, `status: str = Field(default="active")` (Werte `active|grace`), `expires_at: datetime | None = Field(default=None)`, `created_at: datetime | None = None`. In `api/app/models/__init__.py` importieren und zu `__all__` hinzufügen.

    **2. Migration `api/alembic/versions/0004_mcp_clients.py`** — `revision = "0004_mcp_clients"`, `down_revision = "0003_calendar_tokens"`. `op.create_table("mcp_clients", ...)` mit Spalten `id` (`postgresql.UUID(as_uuid=True)`, PK), `owner_id` (UUID, not null), `bearer_hash` (Text, not null), `status` (Text, not null, server_default `'active'`), `expires_at` (DateTime(timezone=True), nullable), `created_at` (DateTime(timezone=True), server_default `BERLIN_NOW`, not null). Index auf `bearer_hash` (`op.create_index("ix_mcp_clients_bearer_hash", "mcp_clients", ["bearer_hash"])`) und auf `owner_id`. `op.execute("GRANT SELECT, INSERT, UPDATE, DELETE ON mcp_clients TO puzzlessbox_app")`. `downgrade()` droppt Indizes + Tabelle. Kein RLS nötig (nur interner service-Pfad liest via `/internal/mcp-auth`).

    **3. Bootstrap `ensure_mcp_client(settings)` in `api/app/core/bootstrap.py`** — spiegle `ensure_service_principal`. Guard: return sofort wenn `settings.MCP_BOOTSTRAP_TOKEN.strip()` leer ODER `settings.SERVICE_OWNER_ID.strip()` leer. `bearer_hash = hashlib.sha256(settings.MCP_BOOTSTRAP_TOKEN.encode()).hexdigest()`. Insert NUR wenn Tabelle leer (D-07 "nur wenn leer", nicht ON CONFLICT UPDATE): `SELECT count(*) FROM mcp_clients`; wenn 0, `INSERT INTO mcp_clients (id, owner_id, bearer_hash, status, expires_at, created_at) VALUES (gen_random_uuid(), CAST(:owner_id AS uuid), :bearer_hash, 'active', NULL, timezone('Europe/Berlin', now()))`. `logger.info("mcp_client bootstrap ok for owner_id=%s", owner_id)` — **niemals** Token/hash loggen (D-07). In `api/app/main.py` `_lifespan` nach `ensure_service_principal(...)` zusätzlich `ensure_mcp_client(get_settings())` aufrufen; Import ergänzen.

    **4. Config `api/app/core/config.py`** — Feld `MCP_BOOTSTRAP_TOKEN: str = ""` ergänzen (neben bestehendem `SERVICE_OWNER_ID`).

    **5. Router `api/app/routers/internal.py` — `POST /internal/mcp-auth`** — `router = APIRouter(tags=["internal"])`. Pydantic-Body-Model `MCPAuthRequest` mit `bearer_hash: str`. Endpoint `async def resolve_mcp_auth(payload: MCPAuthRequest, request: Request)`: (a) Gateway-Auth D-03 — lies `X-Service-Bearer`-Header; wenn fehlt oder `not hmac.compare_digest(header, settings.SERVICE_BEARER_TOKEN)` → `HTTPException 401 {code UNAUTHORIZED, message "Invalid service bearer token."}`. (b) Lookup: `SELECT owner_id FROM mcp_clients WHERE bearer_hash = :hash AND status IN ('active','grace') AND (expires_at IS NULL OR expires_at > timezone('Europe/Berlin', now())) LIMIT 1` via `Session(get_engine())`. (c) `scalar_one_or_none()` None → `HTTPException 401 {code UNAUTHORIZED, message "Unknown or expired MCP client."}`. (d) sonst `return {"owner_id": str(owner_id)}`. In `api/app/main.py` `include_router(internal.router)` ergänzen (Import aus `app.routers`). Dieser Endpunkt ist bewusst nicht unter dem Accept-Version-Skip — MCP sendet den Accept-Header trotzdem (D-16); alternativ Pfad `/internal` prüfen. **Halte den Accept-Header-Contract**: MCP-Client sendet `Accept: application/vnd.puzzlessbox.v1+json`, also 415 wird NICHT ausgelöst.

    **6. X-Owner-Id-Guard in `api/app/auth/jwt.py` (D-03/D-08)** — im service-Pfad von `get_current_owner`: nachdem `_resolve_service_owner(service_bearer)` den Service-Bearer validiert hat, prüfe ob Header `X-Owner-Id` gesetzt ist. Wenn ja: neue Funktion `_validate_owner_header(owner_id_header: str) -> str`: (a) `uuid.UUID(owner_id_header)` in try/except → bei ValueError `HTTPException 403 {code FORBIDDEN, message "Invalid owner id format."}`. (b) `SELECT 1 FROM "user" WHERE id = CAST(:oid AS uuid) LIMIT 1` (Better-Auth `user`-Tabelle, kein FK — siehe migration 0001 header) via `Session(get_engine())`; keine Row → `HTTPException 403 {code FORBIDDEN, message "Owner not provisioned."}`. (c) return den validierten `owner_id_header`. Der zurückgegebene owner überschreibt den service-principal-owner (MCP steuert Tenant via `X-Owner-Id`). Wenn `X-Owner-Id` fehlt, bleibt bisheriges Verhalten (service-principal-owner). Danach `set_request_owner(owner_id)`. Prüfe Better-Auth Tabellenname (`"user"` gequotet) gegen migration 0001 — falls abweichend, an tatsächlichen Namen anpassen.
  </action>
  <verify>
    <automated>cd api && python -c "import app.models, app.routers.internal, app.core.bootstrap; print('imports ok')" && python -m pytest tests -x -q -k "auth or schema or tenancy"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from app.models import MCPClient"` erfolgreich; `MCPClient.__tablename__ == "mcp_clients"`.
    - `POST /internal/mcp-auth` mit gültigem `X-Service-Bearer` + bekanntem aktiven `bearer_hash` → HTTP 200 Body `{"owner_id": "<uuid>"}`.
    - `POST /internal/mcp-auth` mit unbekanntem `bearer_hash` → HTTP 401, Body `{"error":{"code":"UNAUTHORIZED", ...}}`.
    - `POST /internal/mcp-auth` ohne/mit falschem `X-Service-Bearer` → HTTP 401.
    - `get_current_owner` mit gültigem Service-Bearer + `X-Owner-Id` = Nicht-UUID → 403 `FORBIDDEN`; mit UUID ohne user-Row → 403; mit UUID + existierender user-Row → owner_id akzeptiert.
    - `grep -n "MCP_BOOTSTRAP_TOKEN" api/app/core/bootstrap.py` liefert Treffer; kein `logger`-Aufruf in `ensure_mcp_client` enthält `bearer_hash` oder Token-Variable (D-07).
    - Migration-Kette valide: `cd api && alembic heads` zeigt genau einen Head `0004_mcp_clients` (falls DB verfügbar; sonst `python -c "import app.routers.internal"` + Datei-Existenz).
  </acceptance_criteria>
  <precondition>Better-Auth `user`-Tabelle existiert (Phase-1 migration 0001) — der D-08-Guard SELECT-et gegen sie.</precondition>
  <reversibility rating="costly">Neuer Header-Contract (X-Owner-Id) + neue Tabelle + interner Auth-Endpunkt (D-02/D-03/D-06/D-08) — mehrere gekoppelte Call-Sites, Rückbau erfordert Koordination MCP↔API.</reversibility>
  <done>API kann Hermes-Bearer-Hash zu owner_id auflösen (`/internal/mcp-auth`) und akzeptiert `X-Owner-Id` nur bei gültigem Service-Bearer + provisioniertem user (D-08). Committet.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: MCP-Grundgerüst — create_item end-to-end + 401/403-Seams + Wave-0-Tests</name>
  <files>mcp-server/requirements.txt, mcp-server/pytest.ini, mcp-server/app/__init__.py, mcp-server/app/config.py, mcp-server/app/auth.py, mcp-server/app/api_client.py, mcp-server/app/health.py, mcp-server/app/server.py, mcp-server/app/tools/__init__.py, mcp-server/app/tools/items.py, mcp-server/tests/conftest.py, mcp-server/tests/test_auth.py, mcp-server/tests/test_api_contract.py, mcp-server/tests/test_health.py</files>
  <read_first>
    - .planning/phases/02-mcp-server/02-RESEARCH.md (Patterns 1-5, Code Examples create_item, Validation Architecture, Wave 0 Gaps)
    - .planning/phases/02-mcp-server/02-PATTERNS.md (server.py/auth.py/api_client.py/health.py/tools Analog-Zuordnung)
    - api/app/core/config.py (Settings + get_settings Analog)
    - api/app/services/scraper.py (httpx.AsyncClient/Timeout/HTTPError-Muster Zeilen 119-167)
    - api/app/routers/auth.py (_map_better_auth_error Zeilen 41-66 — Fehler-Mapping-Analog)
    - api/app/routers/health.py (/health + /ready Muster)
    - api/tests/conftest.py (httpx.MockTransport Zeilen 151-162, ASGITransport 214-230)
    - api/app/routers/capture.py (POST /drafts Contract + DraftCreate)
    - api/app/main.py (API_VERSION_ACCEPT Konstante Zeile 16)
  </read_first>
  <behavior>
    - Test test_missing_bearer_401: Request an MCP `/mcp` ohne `Authorization` → 401 + Header `WWW-Authenticate: Bearer`.
    - Test test_invalid_bearer_401: Request mit Bearer, dessen Hash `/internal/mcp-auth` (gemockt) mit 401 beantwortet → 401 mit `invalid_token`.
    - Test test_owner_reject: `resolve_owner` gibt None (API 401/403) → verify_token gibt None → 401.
    - Test test_create_item_headers: `create_item` löst httpx `POST /drafts` aus mit Headern `Accept == application/vnd.puzzlessbox.v1+json`, `X-Service-Bearer` gesetzt, `X-Owner-Id` == owner aus Claim.
    - Test test_create_item_idempotency: ohne gelieferten Key → generierte UUID im `Idempotency-Key`-Header; mit geliefertem Key → verbatim weitergereicht.
    - Test test_create_item_owner_from_claim: owner_id kommt aus Token-Claim, nicht aus Tool-Argument (kein `owner_id`-Parameter im Tool-Schema).
    - Test test_health_ok: `GET /health` → 200 ohne Auth.
  </behavior>
  <action>
    **Reihenfolge TDD:** zuerst `tests/` + `conftest` schreiben (RED), dann App-Module bis grün (GREEN).

    **1. `mcp-server/requirements.txt`** — vollständig (auch für 02-04-Dockerfile wiederverwendet): `fastmcp==3.4.4`, `uvicorn==0.34.0`, `httpx==0.28.1`, `pydantic-settings==2.7.1`, `pytest`, `pytest-asyncio`. Keine `alembic`/`sqlmodel`/`psycopg2` (D-06 — MCP hat keine DB).

    **2. `mcp-server/pytest.ini`** — `[pytest]` mit `asyncio_mode = auto`. `mcp-server/app/__init__.py` und `mcp-server/app/tools/__init__.py`-Package-Dateien.

    **3. `mcp-server/app/config.py`** — spiegle `api/app/core/config.py`: `Settings(BaseSettings)`, `model_config = SettingsConfigDict(env_file=".env", extra="ignore")`, Felder `SERVICE_BEARER_TOKEN: str = "change-me-service-bearer"`, `MCP_API_BASE_URL: str = "http://puzzlessbox-api:8000"` (D-15), `ENV: str = "dev"`, `@property is_prod`. `@lru_cache get_settings()`. **Kein** `DATABASE_URL` (D-06).

    **4. `mcp-server/app/api_client.py`** — Konstante `API_VERSION_ACCEPT = "application/vnd.puzzlessbox.v1+json"` (identisch api/main.py). `make_client(settings) -> httpx.AsyncClient` mit `base_url=settings.MCP_API_BASE_URL`, `timeout=httpx.Timeout(15.0)` (D-17), Default-Headers `{"Accept": API_VERSION_ACCEPT, "X-Service-Bearer": settings.SERVICE_BEARER_TOKEN}`. `async def call_api(client, method, path, owner_id, *, json=None, idempotency_key=None)`: Header `{"X-Owner-Id": owner_id}` (D-03/D-16), plus `Idempotency-Key` wenn gesetzt; Retry-Schleife `for attempt in range(2)`: request; wenn `resp.status_code in (502, 503) and attempt == 0: continue` sonst break (D-18 — genau 1 Retry, nur 502/503). Bei `resp.status_code >= 400` → `raise _to_tool_error(resp)`; sonst `return resp.json()`. `async def resolve_owner(client, bearer_hash) -> str | None`: `POST /internal/mcp-auth` json `{"bearer_hash": bearer_hash}`; 200 → `resp.json()["owner_id"]`, sonst None. `_to_tool_error(resp) -> ToolError` (aus `fastmcp.exceptions`): lies `resp.json().get("error", {})` in try/except; `code = err.get("code","API_ERROR")`, `message = err.get("message","Request failed")`; `return ToolError(f"{code}: {message}")` — kein Roh-Dump (D-13).

    **5. `mcp-server/app/auth.py`** — `OwnerResolvingVerifier(TokenVerifier)` (Import `from fastmcp.server.auth import ...` verifizieren, sonst `fastmcp.server.auth.auth`; A4). `__init__(self, settings)` speichert settings + erstellt/hält httpx-Client-Factory. `async def verify_token(self, token: str) -> AccessToken | None`: `bearer_hash = hashlib.sha256(token.encode()).hexdigest()`; `owner_id = await resolve_owner(client, bearer_hash)`; None → `return None` (→ RFC-6750 401 via RequireAuthMiddleware); sonst `return AccessToken(token=token, client_id=owner_id, scopes=[], expires_at=None, claims={"owner_id": owner_id, "sub": owner_id})`. **Niemals** `token` loggen (D-07). AccessToken-Konstruktor-Felder bei Impl gegen 3.4.4 verifizieren (A5).

    **6. `mcp-server/app/tools/items.py` + `tools/__init__.py`** — `create_item` (aus RESEARCH Code Examples): `@mcp.tool async def create_item(title: Annotated[str, Field(description="Item title", min_length=1)], type: Annotated[Literal["note","link","task","event"], Field(description="Item type")], category_id: Annotated[str, Field(description="Target category UUID")], summary: Annotated[str, Field(description="Short summary")] = "", idempotency_key: Annotated[str | None, Field(description="Optional client idempotency key")] = None) -> dict`. Body: `owner_id = get_access_token().claims["owner_id"]` (**nie** Tool-Input), `body = {"title","type","category_id","summary"}`, `return await call_api(client, "POST", "/drafts", owner_id, json=body, idempotency_key=idempotency_key or str(uuid.uuid4()))` (D-09/D-16/D-19). `tools/__init__.py`: `register_tools(mcp, client)` registriert create_item (weitere Tools folgen 02-03). Owner-Claim-Zugriff via `from fastmcp.server.dependencies import get_access_token`.

    **7. `mcp-server/app/health.py`** — `register_health(mcp, api_client_factory)`: `@mcp.custom_route("/health", methods=["GET"]) async def health(request): return JSONResponse({"status":"ok","service":"mcp-server"})`. `@mcp.custom_route("/ready", methods=["GET"])`: pingt API `GET /health`; API-Fehler/Timeout → `JSONResponse(status_code=503, {"error":{"code":"SERVICE_UNAVAILABLE","message":"upstream API not ready"}})` sonst 200 `{"status":"ready"}` (D-22). custom_route umgeht RequireAuthMiddleware (unauth, verifiziert RESEARCH Pattern 1).

    **8. `mcp-server/app/server.py`** — `settings = get_settings()`; httpx-Client via `make_client(settings)`; `verifier = OwnerResolvingVerifier(settings)`; `mcp = FastMCP(name="Puzzlessbox MCP", auth=verifier)`; `register_health(mcp, ...)`; `register_tools(mcp, client)`; `allowed = ["mcp.puzzlesstool.online"]` (in dev ggf. zusätzlich localhost); `app = mcp.http_app(path="/mcp", stateless_http=True, allowed_hosts=allowed)` (D-21 Streamable HTTP, Pitfall 2/3). Modul-Level `app` für uvicorn (`app.server:app`).

    **9. Tests** (`tests/conftest.py`, `test_auth.py`, `test_api_contract.py`, `test_health.py`) — conftest: `httpx.MockTransport`-Fixture die `/internal/mcp-auth` + `/drafts` mockt (Analog api/tests/conftest 151-162); In-Memory `from fastmcp import Client` gegen `mcp` ODER Fallback `httpx.ASGITransport(app=mcp.http_app())` (A1). Tests decken die 7 `<behavior>`-Fälle. Sampling: `pytest mcp-server/tests -x -q`.
  </action>
  <verify>
    <automated>cd mcp-server && python -m pytest tests -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest mcp-server/tests/test_auth.py::test_missing_bearer_401 -x` grün: 401 + `WWW-Authenticate: Bearer`.
    - `pytest mcp-server/tests/test_auth.py::test_invalid_bearer_401 -x` grün: 401 `invalid_token`.
    - `pytest mcp-server/tests/test_auth.py::test_owner_reject -x` grün.
    - `pytest mcp-server/tests/test_api_contract.py -x` grün: create_item sendet `Accept: application/vnd.puzzlessbox.v1+json`, `X-Service-Bearer`, `X-Owner-Id` (=Claim), `Idempotency-Key` (provided verbatim / generiert).
    - `pytest mcp-server/tests/test_health.py -x` grün: `/health` 200 unauth.
    - `grep -rn "owner_id" mcp-server/app/tools/items.py` zeigt Bezug via `get_access_token().claims` und **kein** `owner_id`-Parameter im create_item-Signatur.
    - `grep -c "sqlmodel\|psycopg2\|alembic\|DATABASE_URL" mcp-server/requirements.txt mcp-server/app/config.py` == 0 (D-06 — MCP DB-frei).
    - Voller Tracer-Beweis: `pytest mcp-server/tests -q` grün (create_item happy path via MockTransport + 401-Seams).
  </acceptance_criteria>
  <reversibility rating="costly">Auth-Architektur (TokenVerifier-Subklasse, Header-Contract, stateless_http) prägt alle folgenden Tools; Umbau nach Expansion teuer.</reversibility>
  <done>`mcp-server/` lauffähig; create_item end-to-end (Bearer → /internal/mcp-auth → POST /drafts) grün getestet; 401 bei fehlendem/ungültigem Bearer; /health 200. Committet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes VPS → MCP (public HTTPS) | Untrusted Bearer kreuzt hier; nur TLS via Traefik, Auth am MCP (D-05) |
| MCP → API (internes Docker-Netz) | Service-Bearer + X-Owner-Id; API vertraut X-Owner-Id nur bei gültigem Service-Bearer (D-03) |
| API → Postgres (`mcp_clients`, `user`) | Owner-Auflösung + D-08-Guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Hermes-Bearer in Logs/Exceptions | high | mitigate | Nie Plaintext-Token/hash loggen (D-07); `verify_token`/`ensure_mcp_client` loggen nur owner_id; `_to_tool_error` gibt code+message, keinen Roh-Dump (D-13) |
| T-02-02 | Spoofing | Grace-Token nach Rotation weiter gültig | high | mitigate | `/internal/mcp-auth` filtert `status IN (active,grace) AND (expires_at IS NULL OR expires_at > now)`; abgelaufene grace-Rows → 401 (D-04) |
| T-02-03 | Elevation of Privilege | X-Owner-Id-Spoof falls Service-Bearer leakt | high | mitigate | X-Owner-Id nur akzeptiert bei gültigem Service-Bearer UND UUID-Format UND existierender user-Row → sonst 403 (D-08) |
| T-02-04 | Spoofing | Auth-Bypass 401/403 | high | mitigate | FastMCP `RequireAuthMiddleware` + `OwnerResolvingVerifier`; None → RFC-6750 401; Tests test_missing/invalid/owner_reject |
| T-02-05 | Information Disclosure | Plaintext bearer_hash-Storage | medium | mitigate | Nur sha256-hash in `mcp_clients` (D-02); Bootstrap-Token env-löschbar nach Insert (D-07) |
| T-02-06 | Tampering | mcp_clients ownership-Leak | medium | accept | Nur interner service-Pfad liest die Tabelle; kein Public-Endpoint; RLS entfällt (Rationale: API-owned, MCP DB-frei D-06) |
| T-02-SC | Tampering | pip installs (fastmcp/httpx/…) | high | mitigate | RESEARCH Package Legitimacy Audit: alle 4 OK/Approved, keine [ASSUMED]/[SUS]/[SLOP]; Versionen gepinnt |

**SSRF-Hinweis (T-02 Communication):** `MCP_API_BASE_URL` ist eine feste, aus vertrauenswürdiger Env stammende interne URL (D-15) — keine user-kontrollierte URL-Konstruktion, daher kein SSRF-Vektor über Tool-Input. Owner/IDs sind reine Pfad-/Body-Parameter, keine Hostnamen.
</threat_model>

<verification>
- `cd api && python -m pytest tests -x -q` grün (bestehende + neue Auth/Tenancy-Tests).
- `cd mcp-server && python -m pytest tests -x -q` grün (401/403 + create_item-Contract + health).
- `grep -rn "sha256\|compare_digest" api/app/routers/internal.py mcp-server/app/auth.py` belegt Hash-Auth; kein Plaintext-Log (D-07).
</verification>

<success_criteria>
Tracer beweist SC1 (ein validiertes Tool `create_item` spricht API als interner Client an) + SC2 (401/403 ohne/mit falschem Bearer) end-to-end. Rest der 6 Tools + Deploy expandieren in 02-02/03/04.
</success_criteria>

<output>
Create `.planning/phases/02-mcp-server/02-01-SUMMARY.md` when done
</output>
