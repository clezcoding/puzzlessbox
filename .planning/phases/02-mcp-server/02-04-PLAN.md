---
phase: 02-mcp-server
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - mcp-server/Dockerfile
  - mcp-server/.dockerignore
  - .github/workflows/deploy-mcp.yml
autonomous: false
requirements: [MCP-02]
user_setup:
  - service: coolify
    why: "MCP als separate Docker-Image-App unter mcp.puzzlesstool.online (D-20/D-23, OPS-01/04-Slice)"
    dashboard_config:
      - task: "Docker-Image-App anlegen (ghcr.io/<owner>/puzzlessbox-mcp:latest), Domain mcp.puzzlesstool.online, Traefik/Let's-Encrypt TLS"
        location: "Coolify -> Projekt -> New Resource -> Docker Image"
      - task: "Health-Check-Pfad /health konfigurieren (D-22)"
        location: "Coolify -> App -> Health Checks"
      - task: "Deploy-Webhook-URL + Token generieren, als GitHub Secret COOLIFY_MCP_WEBHOOK hinterlegen"
        location: "Coolify -> App -> Webhooks"
    env_vars:
      - name: SERVICE_BEARER_TOKEN
        source: "Gleicher Wert wie API (X-Service-Bearer Gateway-Auth, D-01) — Coolify App Env"
      - name: MCP_API_BASE_URL
        source: "Interne Docker-Netz-URL der API, z.B. http://puzzlessbox-api:8000 (D-15)"
      - name: ENV
        source: "prod"

must_haves:
  truths:
    - "mcp-server/Dockerfile baut ein lauffähiges Image (python:3.14-slim) das uvicorn app.server:app auf Port 8000 startet — OHNE alembic (MCP hat keine DB, D-06)"
    - ".github/workflows/deploy-mcp.yml baut bei Push auf main mit paths mcp-server/** ein Image und pusht ghcr.io/<owner>/puzzlessbox-mcp:latest + :sha-<sha> (D-20)"
    - "Der Workflow triggert nach erfolgreichem Push den Coolify-Deploy-Webhook"
    - "MCP ist in prod nur via HTTPS (Traefik TLS) unter mcp.puzzlesstool.online erreichbar; allowed_hosts akzeptiert diesen Host (D-05, Pitfall 3)"
  prohibitions:
    - "mcp-server/Dockerfile darf kein alembic/Migrationskommando enthalten (D-06)"
    - "GHCR/Workflow-Secrets (Bearer, Webhook-Token) dürfen nicht im Repo/Log im Klartext erscheinen"
  backstop:
    - statement: "Kein bestehender GHCR-Build-Workflow im Repo — deploy-mcp.yml ist greenfield, nicht 'mirror ci.yml' (RESEARCH A3, MEDIUM confidence). Action-SHAs + Coolify-Webhook-URL bei Impl verifizieren."
      verification: backstop
  artifacts:
    - mcp-server/Dockerfile
    - .github/workflows/deploy-mcp.yml
  key_links:
    - "GitHub Actions -> docker build/push -> GHCR -> Coolify-Webhook -> Deploy"
    - "Traefik TLS -> mcp.puzzlesstool.online -> mcp.http_app allowed_hosts"
---

<objective>
Liefert den vollen MCP-Deploy-Pfad (D-20/D-23): eigenes `mcp-server/`-Image, GitHub-Actions→GHCR-Build (`:latest` + `:sha-<sha>`), Coolify-Docker-Image-App unter `mcp.puzzlesstool.online` mit Traefik-TLS + Health-Check. Zieht den MCP-Slice von OPS-01/02/04 in Phase 2 vor.

Purpose: Erfüllt SC2 (separate Coolify-App, nur HTTPS+Bearer erreichbar) infrastrukturseitig.
Output: Dockerfile + GHCR-Workflow (greenfield) + dokumentierte Coolify-Konfiguration.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mcp-server/02-RESEARCH.md
@.planning/phases/02-mcp-server/02-PATTERNS.md
@.planning/phases/02-mcp-server/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
**Neu in diesem Plan (02-04):**
- `mcp-server/Dockerfile` — python:3.14-slim, uvicorn, KEIN alembic
- `mcp-server/.dockerignore` — tests/, __pycache__, .env
- `.github/workflows/deploy-mcp.yml` — GHCR build+push + Coolify-Webhook (greenfield)
- Coolify-App-Konfiguration (dokumentiert via user_setup)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Deploy-Topologie: separates mcp-server/ Docker-Image als eigene Coolify-App (D-23) bestätigen</decision>
  <context>D-23 ist als **one-way** markiert (Deploy-Topologie schwer rückholbar: eigenes Image, eigene Domain, eigener CI-Workflow, GHCR-Package). Diese Bestätigung dient der bewussten Freigabe vor dem irreversiblen Commit — nicht der Neuverhandlung der gelockten Entscheidung.</context>
  <options>
    <option id="separate-image">
      <name>Separate mcp-server/ Image + eigene Coolify-App (D-23, empfohlen)</name>
      <pros>Entkoppelt von API-Deploy; matcht MCP-02 "separate Coolify-App"; unabhängig skalierbar/rollbar</pros>
      <cons>Zusätzliches Image/Workflow/GHCR-Package + Domain zu pflegen</cons>
    </option>
    <option id="mounted">
      <name>FastMCP in api/ mounten (verworfen)</name>
      <pros>Ein Image weniger</pros>
      <cons>Widerspricht D-23 + MCP-02; koppelt MCP an API-Deploy; RESEARCH markiert STACK.md-Notiz als überholt</cons>
    </option>
  </options>
  <resume-signal>Bestätige "separate-image" (D-23) oder benenne Abweichung</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Dockerfile + GHCR-Deploy-Workflow + Coolify-Wiring</name>
  <files>mcp-server/Dockerfile, mcp-server/.dockerignore, .github/workflows/deploy-mcp.yml</files>
  <!-- planner-discipline-allow: alembic -->
  <read_first>
    - api/Dockerfile (Analog: python:3.14-slim, WORKDIR, pip install, CMD — ABER api hat alembic-Step, MCP nicht)
    - .github/workflows/ci.yml (Skelett: concurrency, permissions, SHA-pinned actions — KEIN Docker-Build vorhanden)
    - .planning/phases/02-mcp-server/02-RESEARCH.md (Deploy-Sektion, Environment Availability: greenfield-Workflow)
    - .planning/phases/02-mcp-server/02-PATTERNS.md (deploy-mcp.yml No-Analog-Sektion; nur Skelett übernehmbar)
    - mcp-server/requirements.txt (aus 02-01 — vollständig, Basis für pip install)
    - mcp-server/app/server.py (aus 02-01 — Modul `app.server:app` als uvicorn-Target + allowed_hosts)
  </read_first>
  <action>
    **1. `mcp-server/Dockerfile`** — spiegle `api/Dockerfile` OHNE alembic-Zeilen: `FROM python:3.14-slim`; `WORKDIR /app`; `apt-get install curl` (für Coolify-Health-Check via curl, wie api); `COPY requirements.txt .`; `RUN pip install --no-cache-dir -r requirements.txt`; `COPY app ./app`; `CMD ["uvicorn", "app.server:app", "--host", "0.0.0.0", "--port", "8000"]` (kein `alembic upgrade head` — D-06). `mcp-server/.dockerignore`: `tests/`, `__pycache__/`, `*.pyc`, `.env`, `.pytest_cache/`.

    **2. `.github/workflows/deploy-mcp.yml`** — greenfield (RESEARCH A3): 
    - `on: push: branches: [main], paths: ["mcp-server/**", ".github/workflows/deploy-mcp.yml"]`.
    - `concurrency: {group: deploy-mcp-${{ github.ref }}, cancel-in-progress: true}` (Skelett aus ci.yml).
    - `permissions: {contents: read, packages: write}`.
    - Job `build-push`: `runs-on: ubuntu-latest`. Steps mit **SHA-gepinnten** Actions (wie ci.yml, V5 Supply-Chain — bei Impl aktuelle SHAs verifizieren): `actions/checkout`, `docker/login-action` (registry `ghcr.io`, username `${{ github.actor }}`, password `${{ secrets.GITHUB_TOKEN }}`), `docker/metadata-action` (images `ghcr.io/${{ github.repository_owner }}/puzzlessbox-mcp`, tags `type=raw,value=latest` + `type=sha,prefix=sha-`), `docker/build-push-action` (context `./mcp-server`, push true, tags aus metadata). 
    - Finaler Step (nach erfolgreichem Push): Coolify-Webhook auslösen — `curl -fsSL -X POST "${{ secrets.COOLIFY_MCP_WEBHOOK }}"` (Secret, nie inline). Nur `if: success()`.

    **3. allowed_hosts prod-Check** — falls `mcp-server/app/server.py` (02-01) `allowed_hosts` noch nicht prod-final hat: sicherstellen `allowed_hosts=["mcp.puzzlesstool.online"]` (Pitfall 3). Falls schon gesetzt: keine Änderung (nicht doppelt editieren; dies ist eine Verify-Notiz, keine erzwungene Modifikation — 02-01 besitzt server.py).

    **4. Coolify-Konfiguration** — via `user_setup` dokumentiert (App, Domain, Health-Check /health, Webhook-Secret, Env-Vars). Executor kann Coolify-API/MCP nutzen falls verfügbar; sonst human-check am Phase-Ende.
  </action>
  <verify>
    <automated>docker build -t puzzlessbox-mcp:test mcp-server && python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/deploy-mcp.yml')); assert 'mcp-server/**' in str(d); assert 'packages' in str(d); print('workflow ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docker build mcp-server` erfolgreich; Image startet `uvicorn app.server:app` (kein alembic-Step: `grep -c alembic mcp-server/Dockerfile` == 0).
    - `deploy-mcp.yml` valides YAML; `on.push.paths` enthält `mcp-server/**`; `permissions.packages == write`; metadata-tags enthalten `latest` + `sha-`.
    - `grep -c "secrets.COOLIFY_MCP_WEBHOOK\|secrets.GITHUB_TOKEN" .github/workflows/deploy-mcp.yml` >= 2; kein Klartext-Token/Bearer im Workflow (`grep -c "Bearer [A-Za-z0-9]" .github/workflows/deploy-mcp.yml` == 0).
    - Alle `uses:`-Actions sind SHA-gepinnt (kein `@v` Float-Tag): `grep -E "uses: .*@v[0-9]" .github/workflows/deploy-mcp.yml` liefert keine Treffer.
  </acceptance_criteria>
  <precondition>GHCR erreichbar via `GITHUB_TOKEN`; Coolify-App + Webhook-Secret `COOLIFY_MCP_WEBHOOK` vorab angelegt (user_setup) — der Webhook-Step schlägt sonst fehl.</precondition>
  <reversibility rating="one-way">D-23 Deploy-Topologie: eigenes Image, eigene Domain, GHCR-Package, CI-Workflow — Rückbau erfordert Löschen von App/Package/Domain. Checkpoint oben deckt die bewusste Freigabe.</reversibility>
  <done>Dockerfile baut MCP-Image (kein alembic); deploy-mcp.yml pusht GHCR :latest+:sha und triggert Coolify-Webhook; SHA-gepinnte Actions. Committet.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy-Verifikation (Health + TLS + Auth) — end-of-phase human-check</name>
  <files>.planning/phases/02-mcp-server/02-04-SUMMARY.md (curl-Evidenz; keine Code-Änderung — reine Live-Verifikation)</files>
  <read_first>
    - mcp-server/app/health.py (aus 02-01 — /health + /ready)
    - .planning/phases/02-mcp-server/02-RESEARCH.md (Pitfall 2/3 stateless_http/allowed_hosts)
  </read_first>
  <action>
    Nach Coolify-Deploy die produktive Erreichbarkeit + Auth-Enforcement gegen den Live-Endpunkt prüfen. Da `workflow.human_verify_mode = end-of-phase`, kein `checkpoint:human-verify` mid-plan — stattdessen `<human-check>` als Phase-Gate. Der Executor stellt die konkreten curl-Kommandos bereit und der Betreiber bestätigt.
  </action>
  <verify>
    <human-check>
      Gegen die Live-URL prüfen (curl-Beispiele im SUMMARY dokumentieren):
      1. `curl -sS https://mcp.puzzlesstool.online/health` → 200 `{"status":"ok","service":"mcp-server"}` (TLS-Zertifikat gültig, Traefik/Let's-Encrypt).
      2. `curl -sS -o /dev/null -w "%{http_code}" -X POST https://mcp.puzzlesstool.online/mcp` (ohne Authorization) → 401.
      3. `curl -sS -X POST https://mcp.puzzlesstool.online/mcp -H "Authorization: Bearer WRONG"` → 401 invalid_token.
      4. Coolify Health-Check zeigt App als healthy; abgestürzter Container wird nicht geroutet (OPS-04-Slice).
      Bestätige "approved" oder benenne Abweichungen.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `/health` liefert live 200 über HTTPS mit gültigem TLS-Zertifikat.
    - `POST /mcp` ohne/mit falschem Bearer → 401 (SC2 live bewiesen).
    - Coolify meldet App healthy; Health-Check-Pfad `/health` konfiguriert.
  </acceptance_criteria>
  <precondition>02-04 Task 1 deployed; Coolify-App läuft unter mcp.puzzlesstool.online (user_setup abgeschlossen).</precondition>
  <done>Live-MCP über HTTPS erreichbar, Health grün, 401 ohne gültigen Bearer bestätigt. Committet (SUMMARY mit curl-Evidenz).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Traefik (TLS) → MCP | Öffentlicher HTTPS-Endpunkt; TLS-Terminierung Coolify Traefik (D-05) |
| GitHub Actions → GHCR → Coolify | CI-Secrets (GITHUB_TOKEN, Webhook) kreuzen hier |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | Information Disclosure | Klartext-Bearer/Webhook im Workflow/Log | high | mitigate | Nur `${{ secrets.* }}`; kein inline-Token; grep-Gate gegen `Bearer <literal>` |
| T-02-15 | Tampering | Supply-Chain: gefloatete Action-Tags | high | mitigate | Alle `uses:` SHA-gepinnt (V5); grep-Gate gegen `@v<n>` |
| T-02-16 | Spoofing | HTTP statt HTTPS erreichbar | high | mitigate | Traefik erzwingt TLS/Let's-Encrypt; allowed_hosts nur mcp.puzzlesstool.online (Pitfall 3); human-check TLS-Zert |
| T-02-17 | Denial of Service | Session-Loss bei Redeploy | low | accept | `stateless_http=True` (02-01) — keine Session-Affinität; Redeploy unkritisch |
| T-02-SC | Tampering | Docker base-image / pip supply chain | high | mitigate | Gepinnte requirements (02-01 Audit); python:3.14-slim wie api/ (bekannt) |
</threat_model>

<verification>
- `docker build mcp-server` grün; `grep -c alembic mcp-server/Dockerfile` == 0.
- `deploy-mcp.yml` valides YAML, Secrets referenziert, Actions SHA-gepinnt.
- End-of-phase human-check: Live /health 200 + 401 ohne Bearer.
</verification>

<success_criteria>
SC2 infrastrukturell erfüllt: MCP läuft als separate Coolify-Docker-Image-App unter mcp.puzzlesstool.online, nur HTTPS+Bearer, Health-Check aktiv.
</success_criteria>

<output>
Create `.planning/phases/02-mcp-server/02-04-SUMMARY.md` when done
</output>
