---
phase: 02-mcp-server
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - mcp-server/app/api_client.py
  - mcp-server/app/tools/items.py
  - mcp-server/app/tools/categories.py
  - mcp-server/app/tools/__init__.py
  - mcp-server/tests/test_tools_schema.py
  - mcp-server/tests/test_api_contract.py
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "Alle 6 Tools sind auf dem FastMCP-Server registriert: create_item, list_categories, create_category, move_item, confirm_item, update_item"
    - "confirm_item patcht optional Felder (PATCH /drafts/{id}) und ruft dann POST /drafts/{id}/confirm (D-10)"
    - "update_item mappt auf PATCH /drafts/{id} für title/summary/category (D-12)"
    - "move_item mappt auf PATCH /items/{id} mit {category_id} (D-11/D-12)"
    - "list_categories mappt auf GET /categories; create_category auf POST /categories"
    - "Schema-invalide Tool-Argumente (fehlender required-Wert, falscher Enum, Nicht-UUID) werden vor dem Netzwerk-Hop abgewiesen (kein Crash)"
    - "API-Fehler {error:{code,message}} wird als ToolError 'code: message' zurückgegeben, kein Roh-Dump (D-13)"
    - "Retry: 502->200 = genau 1 Retry; 500 = 0 Retry; 503,503 = fail (D-18)"
  prohibitions:
    - "MCP exponiert ausschliesslich diese 6 Tools — kein calendar/link/board/events-Endpunkt als Tool (Scope-Fence)"
    - "owner_id kommt in jedem Tool aus get_access_token().claims, nie aus Tool-Input"
  artifacts:
    - mcp-server/app/tools/categories.py
    - mcp-server/tests/test_tools_schema.py
  key_links:
    - "confirm_item -> PATCH /drafts/{id} (optional) -> POST /drafts/{id}/confirm"
    - "move_item -> PATCH /items/{id} (02-02 Endpunkt)"
    - "list/create_category -> GET/POST /categories (02-02 Endpunkte)"
---

<objective>
Expandiert das Tool-Set von 1 (Tracer create_item) auf die vollen 6 MCP-01-Tools. Fügt die item-Tools `confirm_item`, `update_item`, `move_item` und die category-Tools `list_categories`, `create_category` hinzu, mit `Annotated[..., Field()]`-Schemas und Contract-/Schema-Tests.

Purpose: Vervollständigt SC1 (alle 6 Tools mit validierten Schemas, API als interner Client).
Output: 6 registrierte Tools + grüne Schema-/Contract-/Retry-/Error-Tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mcp-server/02-RESEARCH.md
@.planning/phases/02-mcp-server/02-PATTERNS.md
@.planning/phases/02-mcp-server/02-01-SUMMARY.md
@.planning/phases/02-mcp-server/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
**Neu in diesem Plan (02-03):**
- `mcp-server/app/tools/items.py` — Tools `confirm_item`, `update_item`, `move_item` (ergänzt create_item)
- `mcp-server/app/tools/categories.py` — Tools `list_categories`, `create_category`
- `mcp-server/app/api_client.py` — Helper-Methoden falls nötig (nutzt bestehende `call_api`)
- `mcp-server/tests/test_tools_schema.py` — Pydantic-Reject
- `mcp-server/tests/test_api_contract.py` — Mapping/Retry/Error für alle Tools
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Item-Tools — confirm_item, update_item, move_item</name>
  <files>mcp-server/app/tools/items.py, mcp-server/app/api_client.py, mcp-server/tests/test_api_contract.py</files>
  <!-- planner-discipline-allow: get_access_token().claims -->
  <read_first>
    - mcp-server/app/tools/items.py (bestehendes create_item aus 02-01)
    - mcp-server/app/api_client.py (call_api-Signatur aus 02-01)
    - .planning/phases/02-mcp-server/02-RESEARCH.md (Code Examples confirm_item; Tool->API Mapping-Tabelle)
    - .planning/phases/02-mcp-server/02-PATTERNS.md (items.py Analog: PATCH /drafts, confirm, move)
    - api/app/routers/capture.py (PATCH /drafts/{id} + POST /drafts/{id}/confirm Contract)
    - api/app/routers/items.py (PATCH /items/{id} move-Contract aus 02-02)
  </read_first>
  <behavior>
    - confirm_item ohne Patch-Felder: ruft NUR POST /drafts/{id}/confirm.
    - confirm_item mit title/summary/category: ruft erst PATCH /drafts/{id} mit nur den gesetzten Feldern, dann confirm.
    - update_item: PATCH /drafts/{id} mit den gesetzten Feldern; owner aus Claim.
    - move_item: PATCH /items/{id} mit {category_id}.
    - Alle: owner_id aus get_access_token().claims, kein owner_id-Parameter.
    - Retry-Contract: call_api retryt 502/503 genau 1x, nicht 500.
  </behavior>
  <action>
    In `mcp-server/app/tools/items.py` drei Tools ergänzen (Muster aus RESEARCH Code Examples + create_item):

    **`confirm_item`** (D-10): Params `item_id: Annotated[str, Field(description="Draft UUID")]`, optional `title/summary/category_id: Annotated[str | None, Field(...)] = None`. Body: `owner_id = get_access_token().claims["owner_id"]`; `patch = {k:v for k,v in {"title":title,"summary":summary,"category_id":category_id}.items() if v is not None}`; wenn `patch`: `await call_api(client, "PATCH", f"/drafts/{item_id}", owner_id, json=patch)`; dann `return await call_api(client, "POST", f"/drafts/{item_id}/confirm", owner_id)`.

    **`update_item`** (D-12): Params `item_id`, optional `title/summary/category_id`. Body: owner aus Claim; `patch` wie oben; wenn leer → `ToolError("VALIDATION_ERROR: no fields to update")`; sonst `return await call_api(client, "PATCH", f"/drafts/{item_id}", owner_id, json=patch)`. (Hinweis: `PATCH /drafts` gilt für status draft|auto_saved; confirmed-Items nur Category-Move via move_item — dokumentiere das im Tool-Docstring.)

    **`move_item`** (D-11/D-12): Params `item_id: Annotated[str, Field(description="Item UUID")]`, `category_id: Annotated[str, Field(description="Target category UUID")]`. Body: owner aus Claim; `return await call_api(client, "PATCH", f"/items/{item_id}", owner_id, json={"category_id": category_id})` (kein Idempotency-Key — nur create_item, D-16).

    Alle drei via `register_tools` registrieren. Docstrings in English (D-14).
  </action>
  <verify>
    <automated>cd mcp-server && python -m pytest tests/test_api_contract.py tests/test_tools_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest mcp-server/tests/test_api_contract.py -x` grün inkl. Mapping: confirm_item(patch) → PATCH dann POST /confirm; update_item → PATCH /drafts; move_item → PATCH /items.
    - Retry-Test grün: MockTransport 502→200 = 1 Retry beobachtet; 500 = 0 Retry; 503,503 = ToolError.
    - `grep -c "get_access_token().claims" mcp-server/app/tools/items.py` >= 4 (create+confirm+update+move); `grep -c "owner_id:" mcp-server/app/tools/items.py` == 0 (kein owner-Parameter).
  </acceptance_criteria>
  <reversibility rating="reversible">Additive Tools über bestehendem call_api; einzeln entfernbar.</reversibility>
  <done>confirm_item/update_item/move_item registriert + Contract-getestet. Committet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Category-Tools + Schema-/Error-Tests</name>
  <files>mcp-server/app/tools/categories.py, mcp-server/app/tools/__init__.py, mcp-server/tests/test_tools_schema.py, mcp-server/tests/test_api_contract.py</files>
  <read_first>
    - mcp-server/app/tools/items.py (Tool-Muster + register)
    - mcp-server/app/tools/__init__.py (register_tools)
    - mcp-server/app/api_client.py (call_api, _to_tool_error)
    - .planning/phases/02-mcp-server/02-PATTERNS.md (categories.py Tool-Struktur)
    - api/app/routers/categories.py (GET/POST /categories Contract aus 02-02)
    - api/tests/conftest.py (MockTransport-Muster für Contract-Tests)
  </read_first>
  <behavior>
    - list_categories: GET /categories; gibt Liste zurück; owner aus Claim.
    - create_category: POST /categories mit {name}; 201 → {id,name}.
    - Schema-Reject: create_item ohne type → Reject; type nicht in Enum → Reject; category_id fehlt → Reject (vor Netzwerk).
    - Error-Map: API 404 {error:{code:NOT_FOUND,message}} → ToolError "NOT_FOUND: ...".
  </behavior>
  <action>
    Neue Datei `mcp-server/app/tools/categories.py`:

    **`list_categories`** (aus RESEARCH Pattern 3): keine Args; `owner_id = get_access_token().claims["owner_id"]`; `return await call_api(client, "GET", "/categories", owner_id)`.

    **`create_category`**: `name: Annotated[str, Field(description="Category name", min_length=1)]` (Discretion: optional `color`/`sort_order` NUR wenn 02-02 sie API-seitig anbietet — sonst weglassen). owner aus Claim; `return await call_api(client, "POST", "/categories", owner_id, json={"name": name})`.

    In `mcp-server/app/tools/__init__.py` `register_tools` erweitern, sodass alle 6 Tools registriert werden (items: create/confirm/update/move; categories: list/create).

    **Tests:** `mcp-server/tests/test_tools_schema.py` — via In-Memory-`Client(mcp)`/Schema-Introspektion prüfen, dass create_item ohne `type` bzw. mit ungültigem Enum bzw. ohne `category_id` einen Validierungsfehler wirft (kein Server-Crash). `mcp-server/tests/test_api_contract.py` — Error-Map-Fall: MockTransport liefert 404 `{"error":{"code":"NOT_FOUND","message":"Draft not found."}}` → Tool wirft `ToolError` dessen Text mit `NOT_FOUND:` beginnt; Mapping-Fall: alle 6 Tools treffen den erwarteten HTTP-method+path.
  </action>
  <verify>
    <automated>cd mcp-server && python -m pytest tests -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Volle Suite grün: `pytest mcp-server/tests -q`.
    - `python -c "from app.server import mcp; import asyncio; ..."` bzw. Test belegt genau 6 registrierte Tools (Namen: create_item, confirm_item, update_item, move_item, list_categories, create_category) — **keine** weiteren.
    - `test_tools_schema.py`: create_item ohne type / falscher Enum / fehlende category_id → Validierungsfehler (kein Crash).
    - `test_api_contract.py::test_error_map`: API 404 → `ToolError` Text startet mit `NOT_FOUND:`.
    - Scope-Fence: `grep -rn "@mcp.tool\|mcp.tool(" mcp-server/app/tools` listet exakt 6 Tools; keine calendar/link/board/events-Namen.
  </acceptance_criteria>
  <reversibility rating="reversible">Additive Tools + Tests.</reversibility>
  <done>Alle 6 Tools registriert, Schema-Reject + Error-Map + Mapping getestet. Committet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes → MCP Tool-Input | Untrusted Tool-Argumente; Pydantic-Validierung vor Netzwerk-Hop |
| MCP → API | owner aus Token-Claim, nie Tool-Input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Elevation of Privilege | owner_id aus Tool-Input (Tenant-Leak) | high | mitigate | Alle 6 Tools lesen owner via get_access_token().claims; kein owner-Parameter im Schema (grep-Gate) |
| T-02-11 | Tampering | Blanket-Retry dupliziert Writes | medium | mitigate | Retry nur 502/503 ×1 (D-18); Idempotency-Key nur create_item |
| T-02-12 | Information Disclosure | Roh-API-Fehler leakt interne Details | medium | mitigate | `_to_tool_error` → nur code+message (D-13) |
| T-02-13 | Spoofing | Tool ausserhalb der 6 exponiert Phase-1-Surface | medium | mitigate | Scope-Fence: exakt 6 Tools registriert (grep-Gate) |
</threat_model>

<verification>
- `cd mcp-server && python -m pytest tests` grün (volle Suite).
- Tool-Count-Assertion == 6; Scope-Fence-grep sauber.
</verification>

<success_criteria>
SC1 vollständig: 6 Tools mit validierten Schemas sprechen die API als interner Client an (create/confirm/update auf /drafts; move auf /items; list/create auf /categories).
</success_criteria>

<output>
Create `.planning/phases/02-mcp-server/02-03-SUMMARY.md` when done
</output>
