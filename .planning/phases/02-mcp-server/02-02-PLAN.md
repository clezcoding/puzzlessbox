---
phase: 02-mcp-server
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - api/app/routers/categories.py
  - api/app/routers/items.py
  - api/app/main.py
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "GET /categories liefert die Kategorien des owners (RLS: eigene owner_id ODER System-Default NULL owner_id), gefiltert per X-Owner-Id"
    - "POST /categories mit {name} legt eine Kategorie für den owner an und liefert 201 mit {id, name}"
    - "PATCH /items/{id} mit {category_id} verschiebt ein Item des owners in eine andere Kategorie (auch bei status confirmed, D-12)"
    - "PATCH /items/{id} auf ein fremdes/nicht existierendes Item liefert 404"
  prohibitions:
    - "Categories/Items-Endpunkte dürfen X-Owner-Id nicht umgehen — jede Query filtert per owner (AUTH-04)"
  artifacts:
    - api/app/routers/categories.py
    - api/app/routers/items.py
  key_links:
    - "categories/items Router -> get_db_for_owner -> RLS owner-Filter"
    - "move_item (MCP 02-03) -> PATCH /items/{id} {category_id}"
---

<objective>
Liefert die neuen API-Endpunkte, die die MCP-Tools `list_categories`, `create_category` und `move_item` konsumieren (D-11): `GET/POST /categories` und `PATCH /items/{id}` (Category-Move). Reine API-Seite (Board-Surface früh), von MCP in 02-03 aufgerufen; WebApp konsumiert später (Phase 4).

Purpose: Ohne diese Endpunkte können 3 der 6 MCP-01-Tools ihren API-Hop nicht ausführen.
Output: Drei neue Routen, RLS-owner-gefiltert, in `main.py` registriert.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mcp-server/02-CONTEXT.md
@.planning/phases/02-mcp-server/02-RESEARCH.md
@.planning/phases/02-mcp-server/02-PATTERNS.md
</context>

<artifacts_this_phase_produces>
**Neu in diesem Plan (02-02):**
- `api/app/routers/categories.py` — `GET /categories`, `POST /categories`
- `api/app/routers/items.py` — `PATCH /items/{id}` (Category-Move, D-12)
- `api/app/main.py` — `include_router(categories.router)`, `include_router(items.router)`
- Pydantic-Body-Model `CategoryCreate {name}` (+ optional color/sort Discretion, siehe unten)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Categories-Router — GET/POST /categories</name>
  <files>api/app/routers/categories.py, api/app/main.py</files>
  <read_first>
    - api/app/routers/capture.py (list_board_items 224-239 = RLS-Read-Analog; create_draft-Insert-Muster; get_db_for_owner + current_owner_id)
    - api/app/models/category.py (Category-Model: id/owner_id/name/created_at)
    - api/app/routers/__init__.py (leer — Router werden direkt in main.py importiert)
    - api/app/main.py (include_router-Muster 59-64)
    - api/alembic/versions/0001_initial_schema.py (categories RLS-Policy: NULL owner_id = System-Default sichtbar für alle)
  </read_first>
  <action>
    Neue Datei `api/app/routers/categories.py`: `router = APIRouter(tags=["categories"])`. Importiere `get_db_for_owner`, `current_owner_id`, `text`, `Session`, `Category`.

    **`GET /categories`** — `def list_categories(db: Session = Depends(get_db_for_owner)) -> list[Category]`: `owner_id = current_owner_id.get()`; `SELECT id, owner_id, name, created_at FROM categories WHERE owner_id = :owner_id OR owner_id IS NULL ORDER BY name` (NULL owner_id = System-Defaults sichtbar, spiegelt category-model-Kommentar + migration 0001 RLS-Policy). `return [Category.model_validate(dict(row)) for row in rows.mappings()]`.

    **`POST /categories`** — Pydantic-Body `CategoryCreate(SQLModel)` mit `name: str` (Discretion D-11: optional `color: str | None = None`, `sort_order: int | None = None` NUR wenn du sie hier gleich einführst; minimal reicht `name` — falls du sie weglässt, dokumentiere im SUMMARY, dass BOARD-Felder Phase 4 nachrüstet). `def create_category(payload: CategoryCreate, db=Depends(get_db_for_owner))`: `owner_id = current_owner_id.get()`; `INSERT INTO categories (id, owner_id, name, created_at) VALUES (gen_random_uuid(), CAST(:owner_id AS uuid), :name, timezone('Europe/Berlin', now())) RETURNING id`; `status_code=201`; `return {"id": str(new_id), "name": payload.name}`. Unique-Constraint auf `name` beachten (Category.name ist unique global — bei Konflikt fängt der bestehende `http_exception_handler`/DB-Error; ggf. IntegrityError → 409 mappen mit `{code CONFLICT, message "Category name already exists."}`).

    In `api/app/main.py`: Import `categories` aus `app.routers`, `application.include_router(categories.router)`.
  </action>
  <verify>
    <automated>cd api && python -c "import app.routers.categories" && python -m pytest tests -x -q -k "categor or capture or tenancy"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /categories` (mit gültigem Owner-Auth) → 200, Liste enthält System-Default-Kategorien (Inbox/Notizen/Links/Tasks/Termine, NULL owner_id) + eigene.
    - `POST /categories` `{"name":"Test"}` → 201 Body `{"id":"<uuid>","name":"Test"}`; danach erscheint "Test" in `GET /categories` desselben owners.
    - Fremder Owner sieht die angelegte "Test"-Kategorie NICHT (nur eigene + NULL-Default).
    - Doppelter name → 409 `CONFLICT` (kein 500).
  </acceptance_criteria>
  <reversibility rating="costly">Board-API-Surface früh eingeführt (D-11) — Feld-Entscheidungen (color/sort) beeinflussen Phase-4-WebApp-Contract.</reversibility>
  <done>Kategorien lesbar + anlegbar, owner-gefiltert, in main.py registriert. Committet.</done>
</task>

<task type="auto">
  <name>Task 2: Items-Move-Router — PATCH /items/{id}</name>
  <files>api/app/routers/items.py, api/app/main.py</files>
  <read_first>
    - api/app/routers/capture.py (_apply_draft_patch 45-88 category-move-Zweig; _lookup_draft_type 27-42; table_for_item_type-Import)
    - api/app/services/timeout.py (table_for_item_type — Typ→Tabelle)
    - api/app/models/note.py (DraftUpdate-Shape)
    - api/app/main.py (include_router)
  </read_first>
  <action>
    Neue Datei `api/app/routers/items.py`: `router = APIRouter(tags=["items"])`. Pydantic-Body `ItemMove(SQLModel)` mit `category_id: uuid.UUID`.

    **`PATCH /items/{item_id}`** — `def move_item(item_id: str, payload: ItemMove, db=Depends(get_db_for_owner))`: `owner_id = current_owner_id.get()`. Item-Typ auflösen über `board_items`-VIEW (spiegle `_lookup_draft_type`: `SELECT type FROM board_items WHERE id=:id AND owner_id=:owner`); None → 404 `NOT_FOUND`. `table = table_for_item_type(ItemType(row["type"]))`. UPDATE (spiegle `_apply_draft_patch` category-Zweig, ABER **ohne** `status IN ('draft','auto_saved')`-Filter — D-12 erlaubt Move auch bei `confirmed`): `UPDATE {table} SET category_id = :category_id, updated_at = NOW() WHERE id = :item_id AND owner_id = :owner_id RETURNING id`; `scalar_one_or_none()` None → 404 `NOT_FOUND`. `db.commit()`; `return {"id": item_id, "category_id": str(payload.category_id)}`.

    Move darf den 30s-Timer **nicht** neu starten (anders als `patch_draft`) — reiner Category-Move. In `api/app/main.py`: `include_router(items.router)`.
  </action>
  <verify>
    <automated>cd api && python -c "import app.routers.items" && python -m pytest tests -x -q -k "item or capture or tenancy"</automated>
  </verify>
  <acceptance_criteria>
    - `PATCH /items/{id}` `{"category_id":"<uuid>"}` auf eigenes Item → 200 `{"id","category_id"}`; `GET /board-items` zeigt neue category_id.
    - Move funktioniert auch für `status = confirmed` Items (D-12).
    - `PATCH /items/{id}` auf fremdes/nicht existierendes Item → 404 `NOT_FOUND`.
    - `grep -n "status IN" api/app/routers/items.py` liefert **keinen** Treffer im move-UPDATE (Move ist statusunabhängig, D-12).
  </acceptance_criteria>
  <reversibility rating="reversible">Neuer isolierter Endpunkt; leicht entfernbar falls Move-Shape sich ändert.</reversibility>
  <done>Items per PATCH zwischen Kategorien verschiebbar (statusunabhängig), owner-gefiltert, registriert. Committet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP → API | X-Owner-Id (nur bei gültigem Service-Bearer, aus 02-01-Guard); categories/items-Queries filtern per owner |
| API → Postgres | RLS + explizites owner-WHERE |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Elevation of Privilege | Cross-tenant Category/Item-Zugriff | high | mitigate | Jede Query `WHERE owner_id = :owner` (+ RLS); Move/Read nie ohne owner-Filter (AUTH-04); 404 statt Leak bei fremdem Item |
| T-02-08 | Tampering | Move auf fremdes Item | medium | mitigate | UPDATE `WHERE id AND owner_id` → 0 Rows → 404 |
| T-02-09 | Information Disclosure | 500 mit DB-Detail bei Unique-Konflikt | low | mitigate | IntegrityError → 409 `CONFLICT` (kein Roh-DB-Dump) |
</threat_model>

<verification>
- `cd api && python -m pytest tests -x -q` grün.
- Beide Router in `api/app/main.py` registriert (`grep -c "include_router(categories\|include_router(items" api/app/main.py` == 2).
</verification>

<success_criteria>
Trägt zu SC1 bei: `list_categories`/`create_category`/`move_item` haben ab jetzt reale API-Endpunkte. Konsumiert von MCP-Tools in 02-03.
</success_criteria>

<output>
Create `.planning/phases/02-mcp-server/02-02-SUMMARY.md` when done
</output>
