---
phase: 03-hermes-plugin-timeout-spike
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - hermes-plugin/__init__.py
  - hermes-plugin/plugin.yaml
  - hermes-plugin/config.py
  - hermes-plugin/schemas.py
  - hermes-plugin/tools.py
  - hermes-plugin/formatters.py
  - hermes-plugin/dialog.py
  - hermes-plugin/pyproject.toml
  - hermes-plugin/.gitignore
  - hermes-plugin/tests/__init__.py
  - hermes-plugin/tests/conftest.py
  - hermes-plugin/tests/test_formatter.py
  - hermes-plugin/tests/test_orchestration.py
autonomous: true
requirements: [CAP-02, MCP-03, MCP-04]
user_setup: []

must_haves:
  truths:
    - "User sendet Freitext an Hermes → Plugin ruft MCP create_item auf → gibt deutsche Stash-Check-Karte (Titel, Typ, Kategorie, Kurz-Zusammenfassung) mit Edit-Hinweis als Chat-Reply zurück (CAP-02 happy path end-to-end)"
    - "Plugin kommuniziert ausschließlich über MCP HTTPS+Bearer mit mcp.puzzlesstool.online — kein direkter DB-Zugriff, keine DB-Credentials im Plugin (MCP-03 prohibitions)"
    - "Plugin treibt nicht den 30s-Timer — API DraftTimeoutManager bleibt Autorität (MCP-04 Spike 001 VALIDATED landed)"
    - "MCP_BEARER wird ausschließlich aus Umgebungsvariablen geladen, niemals hardcodiert (D-12 prohibitions)"
    - "format_confirmation erzeugt deutsches Plain-Text-Template aus DraftPreview — kanalneutral, keine Markdown/HTML-Spezifika (CAP-04 Voraussetzung)"
  artifacts:
    - hermes-plugin/__init__.py
    - hermes-plugin/plugin.yaml
    - hermes-plugin/config.py
    - hermes-plugin/schemas.py
    - hermes-plugin/tools.py
    - hermes-plugin/formatters.py
    - hermes-plugin/dialog.py
    - hermes-plugin/pyproject.toml
    - hermes-plugin/tests/conftest.py
    - hermes-plugin/tests/test_formatter.py
    - hermes-plugin/tests/test_orchestration.py
  key_links:
    - "dialog.handle_user_message → tools.call_mcp_create_item → MCP /mcp create_item → API POST /drafts"
    - "dialog.handle_user_message → formatters.format_confirmation → chat reply"
    - "config.get_settings → env MCP_URL, MCP_BEARER"
  prohibitions:
    - "Kein direkter Datenbankzugriff vom Hermes-VPS (MCP-03 — Plugin nur MCP-Tools)"
    - "Hermes-Cron darf nicht als 30s-Timer verwendet werden (MCP-04 — Spike 001 INVALIDATED als Timer; API DraftTimeoutManager bleibt Autorität)"
    - "Kein Hardcodieren von MCP_BEARER im Quellcode (D-12 — nur über env)"

assumptions:
  - id: A-CAP-02-02
    requirement: CAP-02
    probe: "covered"
    claim: "Stash-Check-Karte ist deutscher Plain-Text mit Titel/Typ/Kategorie/Kurz + Edit-Hinweis; Edit passiert konversationell per Freitext (D-01)."
    resolution: "covered by tracer Task 1 — formatter + dialog happy path"
  - id: A-MCP-03-02
    requirement: MCP-03
    probe: "covered"
    claim: "Plugin ruft create_item nur über MCP HTTPS+Bearer auf; httpx.AsyncClient mit Bearer-Header; keine DB-Credentials."
    resolution: "covered by tracer Task 1 — tools.call_mcp_create_item via streamablehttp_client"
  - id: A-MCP-04-02
    requirement: MCP-04
    probe: "backstop"
    claim: "Spike 001–004 VALIDATED; Plugin implementiert keinen Timer, verlässt sich auf API DraftTimeoutManager."
    resolution: "landed — no timer code in plugin; poll folgt in Plan 03"
---

<objective>
TRACER: Thinnester End-to-End-Slice des Hermes-Plugins. User-Text → MCP create_item → deutsche Stash-Check-Karte als Chat-Reply. Beweist die Architektur (Plugin = reiner MCP-Client, kanalneutrale Plain-Text-Ausgabe, keine Timer-Logik im Plugin) in einem commit, bevor Plan 03/04 expandieren.

Purpose: Die kritische Annahme — dass ein leichtgewichtiges Plugin ausschließlich über MCP orchestriert und die Bestätigung kanalneutral plain-text ausgeben kann — ist bislang nur per Spike mock validiert. Dieser Tracer schlägt den Pfad production-quality in den Monorepo und macht die MCP-04-Spike-Ergebnisse im Code sichtbar (kein Timer im Plugin).
Output: `hermes-plugin/` Skeleton mit config, schemas, tools (MCP-Client), formatters (CAP-02), dialog (happy path), plugin.yaml, pyproject.toml, .gitignore, conftest + 2 Test-Dateien.
</objective>

## Artifacts this phase produces

**Plugin-Module (`hermes-plugin/`):**
- `config.py` — `Settings(BaseSettings)`, `get_settings()` (MCP_URL, MCP_BEARER, ENV)
- `schemas.py` — `DraftPreview(BaseModel)` (id, title, type, category, summary)
- `formatters.py` — `format_confirmation(draft) -> str` (portiert aus spike/004)
- `tools.py` — `call_mcp_create_item(title, type, category_id, summary) -> dict` (streamablehttp_client + Bearer)
- `dialog.py` — `handle_user_message(session, text) -> str` (happy path only)
- `__init__.py` — leer (Registrierung via plugin.yaml)
- `plugin.yaml` — Hermes-Plugin-Manifest
- `pyproject.toml` — Paket-Metadaten (httpx, pydantic, mcp, pydantic-settings; keine DB-Libs)
- `.gitignore` — `.env`, `__pycache__/`, `.pytest_cache/`, `*.egg-info/`

**Tests:**
- `tests/conftest.py` — MCP-Transport-Mock, TEST_OWNER, TEST_CATEGORY, TEST_DRAFT UUIDs
- `tests/test_formatter.py` — `format_confirmation` für alle 4 TYPE_LABELS
- `tests/test_orchestration.py` — `handle_user_message` happy path (mock MCP, MCP-only trace)

**Symbols (Downstream-Consumer):**
- Plan 03: erweitert `dialog.handle_user_message` (Edit/Concurrency/Poll) + `tools.call_mcp_*` (update/confirm/discard/get_item_status)
- Plan 04: `tests/test_channels.py` nutzt `dialog.handle_user_message` + `format_confirmation` für kanalneutrale Verifikation

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-CONTEXT.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-RESEARCH.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-PATTERNS.md
@.claude/skills/spike-findings-puzzlessbox/SKILL.md
@.planning/spikes/004-confirmation-payload/format_confirmation.py
@.planning/spikes/002-mcp-confirm-flow/confirm_flow.py
@mcp-server/app/tools/items.py
@mcp-server/app/api_client.py
@brand/VOICE.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "User-Text → create_item → Stash-Check-Karte" — ein Pfad only</name>
  <files>hermes-plugin/__init__.py, hermes-plugin/config.py, hermes-plugin/schemas.py, hermes-plugin/tools.py, hermes-plugin/formatters.py, hermes-plugin/dialog.py, hermes-plugin/tests/__init__.py, hermes-plugin/tests/conftest.py, hermes-plugin/tests/test_formatter.py, hermes-plugin/tests/test_orchestration.py</files>
  <read_first>
    - .planning/spikes/004-confirmation-payload/format_confirmation.py (format_confirmation + DraftPreview TypedDict — portieren)
    - .planning/spikes/002-mcp-confirm-flow/confirm_flow.py (run_live — streamablehttp_client + ClientSession Pattern)
    - .planning/phases/03-hermes-plugin-timeout-spike/03-PATTERNS.md (config.py, schemas.py, tools.py, formatters.py, dialog.py Patterns)
    - mcp-server/app/api_client.py (Bearer-Header, Accept-Versioning Pattern)
    - brand/VOICE.md (Capture-Verb-Glossar, Apollo-Stimme)
    - .claude/skills/spike-findings-puzzlessbox/SKILL.md (Spike-Ergebnisse — keine Timer-Logik im Plugin)
  </read_first>
  <action>
    Wire EINEN Pfad: `handle_user_message(session, text)` → `call_mcp_create_item(title, type, category_id, summary)` → `format_confirmation(draft)` → return chat reply. Keine anderen Call-Sites, kein Edit/Discard/Poll in diesem Task.

    - `hermes-plugin/config.py`: pydantic-settings `Settings(BaseSettings)` mit `MCP_URL` (default `https://mcp.puzzlesstool.online/mcp`), `MCP_BEARER` (default ""), `ENV` (default "dev"). `@lru_cache get_settings()`. Pattern aus 03-PATTERNS.md Zeilen 24–47.
    - `hermes-plugin/schemas.py`: pydantic `DraftPreview(BaseModel)` mit `id: uuid.UUID, title, type, category, summary`. Pattern aus 03-PATTERNS.md Zeilen 56–66.
    - `hermes-plugin/formatters.py`: portiere `format_confirmation(draft)` aus `.planning/spikes/004-confirmation-payload/format_confirmation.py` unverändert (TYPE_LABELS, Stash-Check-Template). Verwende `DraftPreview` aus schemas.py.
    - `hermes-plugin/tools.py`: `async def call_mcp_create_item(title, type, category_id, summary) -> dict` — verwendet `mcp.ClientSession` + `mcp.client.streamable_http.streamablehttp_client` mit `Authorization: Bearer {settings.MCP_BEARER}` Header. Pattern aus 03-PATTERNS.md Zeilen 76–90 und spike/002 run_live. Raises `RuntimeError` bei `result.isError`. Kein eigener Timer, kein Polling in diesem Task.
    - `hermes-plugin/dialog.py`: `async def handle_user_message(session, message_text: str) -> str` — happy path only: ruft `call_mcp_create_item` auf (type/category Heuristik D-09/D-10 darf stub sein: type='note', category='Inbox' für ersten Slice), formatiert `DraftPreview` aus Response, gibt `format_confirmation(draft)` zurück. Kein Edit/Discard/Poll/Concurrency in diesem Task.
    - `hermes-plugin/tests/conftest.py`: shared fixtures — MCP-Transport-Mock (httpx.MockTransport oder mcp-Session-Mock), TEST_OWNER, TEST_CATEGORY, TEST_DRAFT UUIDs (an spike/002 anlehnen).
    - `hermes-plugin/tests/test_formatter.py`: unit-Test für `format_confirmation` — verifiziert alle 4 TYPE_LABELS, enthält "Stash-Check", "Eintrag sichern", Titel, Kategorie, Kurz.
    - `hermes-plugin/tests/test_orchestration.py`: integration-Test für `handle_user_message` happy path — mockt MCP-Transport, assertiert dass create_item gerufen wird UND reply "Stash-Check" + Titel enthält. Verifiziert dass keine direkte DB-Verbindung existiert (nur MCP-Call im Trace).
    - `hermes-plugin/tests/__init__.py`: leer.
    - `hermes-plugin/__init__.py`: leer (Registrierung folgt in Task 2 via plugin.yaml).

    Kein Hermes-Cron, kein asyncio.sleep-Timer, kein direkter DB-Call im Plugin (MCP-04/MCP-03 prohibitions). Real Error Handling auf MCP-Fehler (RuntimeError weiterreichen).
  </action>
  <verify>
    <automated>cd hermes-plugin && pytest tests/test_formatter.py tests/test_orchestration.py -x</automated>
  </verify>
  <done>
    - handle_user_message happy path funktioniert end-to-end (mock MCP) und ist committed
    - format_confirmation erzeugt deutsches Stash-Check-Template für alle 4 Typen
    - Plugin ruft ausschließlich MCP create_item auf — keine DB-Credentials, kein Timer
    - 2 Test-Dateien grün
  </done>
  <acceptance_criteria>
    - hermes-plugin/ existiert mit config.py, schemas.py, tools.py, formatters.py, dialog.py
    - format_confirmation identisch zu spike/004 (Stash-Check, Eintrag sichern, 30s-Hinweis)
    - call_mcp_create_item verwendet streamablehttp_client + Bearer-Header aus settings.MCP_BEARER
    - handle_user_message gibt format_confirmation-String zurück
    - test_formatter.py + test_orchestration.py grün
    - Kein asyncio.sleep(30), kein psycopg2/SQLAlchemy in hermes-plugin/
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Plugin-Manifest plugin.yaml + pyproject.toml + .gitignore (D-11, D-12 prohibitions)</name>
  <files>hermes-plugin/plugin.yaml, hermes-plugin/pyproject.toml, hermes-plugin/.gitignore</files>
  <read_first>
    - .planning/phases/03-hermes-plugin-timeout-spike/03-CONTEXT.md (D-11 monorepo + rsync/pull, D-12 env-only secrets)
    - .planning/phases/03-hermes-plugin-timeout-spike/03-RESEARCH.md (empfohlene Projektstruktur)
    - mcp-server/app/config.py (pydantic-settings Pattern für pyproject deps)
    - mcp-server/pyproject.toml (falls vorhanden — dep-Pattern)
  </read_first>
  <action>
    - `hermes-plugin/plugin.yaml`: Hermes-Plugin-Manifest (Hermes v0.19.0 Plugin-Format — Name, Version, Entry, Description). Referenziert `__init__.py` als Einstiegspunkt. Keine Secrets im Manifest.
    - `hermes-plugin/pyproject.toml`: Python-Paket-Metadaten (name='hermes-plugin', version, python='>=3.14'). Dependencies: `httpx>=0.28.1`, `pydantic>=2.10`, `mcp>=1.0` (für ClientSession/streamablehttp_client), `pydantic-settings>=2.0`. Dev: `pytest>=8.0`, `pytest-asyncio>=0.23`. Keine DB-Abhängigkeiten (keine psycopg2, sqlalchemy — MCP-03 prohibitions sichtbar in deps).
    - `hermes-plugin/.gitignore`: ignoriert `.env`, `__pycache__/`, `.pytest_cache/`, `*.egg-info/`. Stellt sicher dass MCP_BEARER niemals committet wird (T-03-01).
  </action>
  <verify>
    <automated>test -f hermes-plugin/plugin.yaml && test -f hermes-plugin/pyproject.toml && test -f hermes-plugin/.gitignore && grep -q '\.env' hermes-plugin/.gitignore && ! grep -rqi 'MCP_BEARER.*=.*[a-zA-Z0-9]' hermes-plugin/plugin.yaml hermes-plugin/pyproject.toml</automated>
  </verify>
  <done>
    - plugin.yaml, pyproject.toml, .gitignore existieren
    - .env in .gitignore
    - Kein hardcodierter Bearer in Manifest/Paket
    - Keine DB-Abhängigkeiten in pyproject.toml
  </done>
  <acceptance_criteria>
    - plugin.yaml enthält Plugin-Name und Entry-Referenz
    - pyproject.toml listet httpx, pydantic, mcp, pydantic-settings — keine DB-Libs
    - .gitignore enthält .env
    - grep findet keinen hardcodierten Bearer
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes-VPS → MCP-Server | Untrusted network; Bearer über HTTPS, 401 bei fehlendem Token (Phase 2) — ASVS V2.1.1 (bearer authn), V6.3.1 (TLS) |
| Plugin-Prozess → Env | MCP_BEARER wird aus Hermes-Umgebung/Secrets geladen, nicht aus Repo-Dateien — ASVS V2.5.1 (env-only secrets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | ASVS Ref |
|-----------|----------|-----------|----------|-------------|-----------------|----------|
| T-03-01 | Information Disclosure | MCP_BEARER in .git | high | mitigate | config.py lädt ausschließlich via pydantic-settings aus env; .gitignore enthält .env; kein Hardcode in tools.py/dialog.py | ASVS V2.5.1, V6.1.1 |
| T-03-02 | Elevation of Privilege | direkter DB-Zugriff vom VPS | high | mitigate | pyproject.toml enthält keine DB-Libs; tools.py nur MCP-Client; Code-Review-Gate in test_orchestration.py assertiert MCP-only | ASVS V4.1.1, V4.1.3 |
| T-03-03 | Tampering | Hermes-Cron als Timer | high | mitigate | kein asyncio.sleep(30) in tracer; MCP-04 Spike 001 landed; Timer bleibt in API DraftTimeoutManager | ASVS V5.4.1 (integrity of state transitions) |
| T-03-SC | Tampering | pip installs (httpx [SUS], mcp, pydantic) | high | mitigate | Package-Legitimacy-Audit in RESEARCH.md; httpx offiziell verifiziert; pydantic [OK]; mcp ist offizielles SDK | ASVS V10.4.1 (dependency integrity) |
</threat_model>

<verification>
- `pytest hermes-plugin/tests/ -x` — tracer + formatter grün
- `grep -r 'asyncio.sleep(30)' hermes-plugin/` → keine Treffer (MCP-04 prohibitions sichtbar)
- `grep -rE 'psycopg2|sqlalchemy|SQLModel' hermes-plugin/` → keine Treffer (MCP-03 prohibitions sichtbar)
- `grep -rE 'MCP_BEARER.*=.*[A-Za-z0-9]{20}' hermes-plugin/` → keine hardcodierten Secrets
</verification>

<success_criteria>
- Tracer: User-Text → MCP create_item → deutsche Stash-Check-Karte end-to-end grün
- Plugin ist reiner MCP-Client (keine DB-Abhängigkeiten, keine Timer-Logik)
- format_confirmation identisch zu spike/004
- MCP-04 Spike-Ergebnisse im Code sichtbar (kein Timer im Plugin)
- pyproject.toml/plugin.yaml/.gitignore decken D-11/D-12 prohibitions
</success_criteria>

<output>
Create `.planning/phases/03-hermes-plugin-timeout-spike/03-02-SUMMARY.md` when done
</output>
