---
phase: 03-hermes-plugin-timeout-spike
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - hermes-plugin/dialog.py
  - hermes-plugin/tools.py
  - hermes-plugin/tests/test_orchestration.py
autonomous: true
requirements: [CAP-02, MCP-03]
user_setup: []

must_haves:
  truths:
    - "Nach der Bestätigungskarte wird nächste Freitext-Nachricht als Edit interpretiert (D-01); Plugin mapt per LLM-Heuristik geänderte Felder und ruft update_item nur mit geänderten Keys auf (D-02)"
    - "Nach erfolgreichem Edit sendet Plugin nur stillen ACK (D-03) — keine neue Stash-Check-Karte; Timer-Reset passiert API-seitig via PATCH /drafts/{id}"
    - "Höchstens ein aktiver Draft pro Chat/Session (D-07) — neue Capture während Draft offen fragt user: alt sichern / alt verwerfen / warten"
    - "Plugin pollt nach ~30–35s den Item-Status über MCP (D-06); bei status='auto_saved' sendet es Chat-Ping (D-05); kein API→Hermes-Webhook, kein Hermes-Cron als Treiber"
    - "Plugin ruft ausschließlich MCP-Tools auf (create_item, update_item, confirm_item, discard_item) — keine direkten API-Calls, keine DB-Credentials (MCP-03 prohibitions)"
  artifacts:
    - hermes-plugin/dialog.py
    - hermes-plugin/tools.py
    - hermes-plugin/tests/test_orchestration.py
  key_links:
    - "dialog.handle_user_message → tools.call_mcp_update_item (nur geänderte Keys) → silent ACK"
    - "dialog.schedule_autosave_poll → asyncio.sleep(32) → tools.call_mcp_get_item_status → chat ping"
    - "dialog.start_capture_flow → session.get_state('active_draft') → Konflikt-Reply bei D-07"
  prohibitions:
    - "Kein direkter Datenbankzugriff vom Hermes-VPS (MCP-03 — Plugin nur MCP-Tools)"
    - "Hermes-Cron darf nicht als 30s-Timer verwendet werden (MCP-04 — Poll ist Lesezugriff, Timer bleibt API-seitig)"
    - "Kein Hardcodieren von MCP_BEARER im Quellcode (D-12)"

assumptions:
  - id: A-CAP-02-03
    requirement: CAP-02
    probe: "unclassified — review manually"
    claim: "Edit-Flow ist konversationell (D-01): Freitext nach Karte = Edit; LLM-Heuristik mapt auf geänderte Felder; silent ACK (D-03)."
    resolution: "covered by Task 1 — edit flow + silent ACK tests"
  - id: A-MCP-03-03
    requirement: MCP-03
    probe: "unclassified — review manually"
    claim: "Plugin orchestriert create→update→confirm/discard ausschließlich über MCP; update_item nur mit geänderten Keys; Poll über MCP-Tool."
    resolution: "covered by Task 1 + Task 2 — alle Pfade via tools.call_mcp_*"
---

<objective>
Expansion des Tracer-Skeletons (Plan 02) um die drei konversationellen Verhaltensweisen aus den locked decisions: konversationelles Edit (D-01/D-02/D-03), Single-Active-Draft-Concurrency (D-07), Post-Autosave-Poll (D-05/D-06). Setzt das `discard_item`-Tool aus Plan 01 voraus.

Purpose: Der Tracer beweist nur den happy path. Die_locked decisions verlangen konkrete Dialog-Zustandsmaschinen für Edit, Concurrency und Poll — diese sind kritisch für CAP-02 (Edit-Option) und MCP-03 (orchestriert den gesamten Bestätigungs-Flow).
Output: Erweiterte `dialog.py` mit Edit/Concurrency/Poll, erweiterte `tools.py` mit update_item/discard_item/confirm_item/get_item_status Wrappern, neue Tests in `test_orchestration.py`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-CONTEXT.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-RESEARCH.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-PATTERNS.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-01-PLAN.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-02-PLAN.md
@.claude/skills/spike-findings-puzzlessbox/SKILL.md
@mcp-server/app/tools/items.py
@brand/VOICE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Konversationeller Edit-Flow + Single-Active-Draft Concurrency (D-01, D-02, D-03, D-07)</name>
  <files>hermes-plugin/dialog.py, hermes-plugin/tools.py, hermes-plugin/tests/test_orchestration.py</files>
  <read_first>
    - hermes-plugin/dialog.py (Tracer-Version aus Plan 02 — handle_user_message happy path)
    - hermes-plugin/tools.py (call_mcp_create_item aus Plan 02)
    - .planning/phases/03-hermes-plugin-timeout-spike/03-RESEARCH.md (Pattern 1: Conversational Edit Flow, Pattern 3: Single-Active-Draft)
    - .planning/phases/03-hermes-plugin-timeout-spike/03-PATTERNS.md (dialog.py Pattern Zeilen 134–182)
    - .planning/phases/03-hermes-plugin-timeout-spike/03-CONTEXT.md (D-01…D-03, D-07, D-09/D-10)
    - brand/VOICE.md (Apollo-Stimme, Capture-Verb-Glossar)
    - mcp-server/app/tools/items.py (update_item/confirm_item/discard_item Signaturen)
  </read_first>
  <behavior>
    - Test edit_free_text_calls_update_item_only_changed_keys: User-Nachricht "Titel: Neuer Titel" nach Karte → dialog ruft call_mcp_update_item(item_id, title="Neuer Titel") auf, KEIN summary/category_id
    - Test edit_silent_ack_no_new_card: nach erfolgreichem update_item gibt dialog kurzen ACK-String zurück (z.B. "✍️ Änderungen übernommen."), NICHT format_confirmation
    - Test explicit_confirm_calls_confirm_item: User "Eintrag sichern" → call_mcp_confirm_item(item_id), session.state('active_draft') cleared, Reply "✅ Eintrag erfolgreich gesichert!"
    - Test explicit_discard_calls_discard_item: user "Verwerfen" → call_mcp_discard_item(item_id), state cleared, Reply "🗑️ Eintrag verworfen."
    - Test single_active_draft_conflict: bestehender active_draft + neue Capture → dialog fragt "⚠️ Du hast noch einen offenen Entwurf. Sichern oder Verwerfen?" — KEIN create_item call
    - Test llm_heuristic_url_to_link: Heuristik erkennt URL im Text → type='link'; datetime → type='event'; sonst 'note' (D-09)
    - Test low_confidence_falls_back_to_inbox: bei niedriger Category-Confidence → category='Inbox' (D-10), kein Blockieren
  </behavior>
  <action>
    Erweitere hermes-plugin/dialog.py um Zustandslogik:
    - `handle_user_message(session, text)`:
      1. `active_draft = await session.get_state('active_draft')`. Wenn None → `start_capture_flow(session, text)`.
      2. Wenn text.lower() in {'eintrag sichern','sichern','confirm'} → `call_mcp_confirm_item(active_draft['id'])`, clear state, return "✅ Eintrag erfolgreich gesichert!" (Apollo voice).
      3. Wenn text.lower() in {'verwerfen','löschen','discard'} → `call_mcp_discard_item(active_draft['id'])`, clear state, return "🗑️ Eintrag verworfen." (Apollo voice).
      4. Sonst: konversationelles Edit (D-01/D-02). `updated_fields = _llm_extract_edits(text, active_draft)` — Heuristik: "Titel: X" → title, "Kurz: X" → summary, "Kategorie: X" → category_id (lookup). Wenn Felder: `call_mcp_update_item(active_draft['id'], **updated_fields)`, state updaten, silent ACK return (D-03). Wenn keine Felder erkannt: Fallback-Hinweis.
    - `start_capture_flow(session, text)`:
      1. `active_draft = await session.get_state('active_draft')`. Wenn vorhanden → D-07 Konflikt: setze `pending_capture_text`, return "⚠️ Du hast noch einen offenen Entwurf. Sichern oder Verwerfen, bevor wir fortfahren?" — KEIN create_item.
      2. Sonst: `_llm_choose_type_category(text)` (D-09/D-10) — Heuristik: URL → link, datetime → event, sonst note; category default Inbox bei niedriger Confidence. `result = await call_mcp_create_item(title, type, category_id, summary)`. Setze `active_draft` in session. Return `format_confirmation(draft)`.

    Erweitere hermes-plugin/tools.py um:
    - `async def call_mcp_update_item(item_id, **fields) -> dict` — nur geänderte Keys (D-02)
    - `async def call_mcp_confirm_item(item_id) -> dict`
    - `async def call_mcp_discard_item(item_id) -> dict` (setzt Plan 01 voraus)
    - `async def call_mcp_get_item_status(item_id) -> str` (für Poll in Task 2)

    Alle call_mcp_* verwenden denselben streamablehttp_client + Bearer Header Pattern wie call_mcp_create_item. Keine direkten API-Calls, keine DB-Credentials (MCP-03 prohibitions).
  </action>
  <verify>
    <automated>cd hermes-plugin && pytest tests/test_orchestration.py -k "edit or confirm or discard or single_active or llm_heuristic or low_confidence" -x</automated>
  </verify>
  <done>
    - Edit-Flow: Freitext nach Karte → update_item nur mit geänderten Keys → silent ACK
    - Explizite confirm/discard → entsprechende MCP-Tools, state cleared
    - Single-Active-Draft: Konflikt-Reply, kein zweites create_item
    - LLM-Heuristik: URL→link, datetime→event, sonst note; Inbox-Fallback
    - Alle 7 Tests grün
  </done>
  <acceptance_criteria>
    - dialog.py enthält handle_user_message, start_capture_flow, _llm_extract_edits, _llm_choose_type_category
    - tools.py enthält call_mcp_update_item, call_mcp_confirm_item, call_mcp_discard_item, call_mcp_get_item_status
    - Silent ACK nach Edit (kein format_confirmation)
    - Konflikt-Reply bei bestehendem active_draft
    - 7 neue Tests grün
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Post-Autosave Poll über MCP (D-05, D-06)</name>
  <files>hermes-plugin/dialog.py, hermes-plugin/tools.py, hermes-plugin/tests/test_orchestration.py</files>
  <read_first>
    - hermes-plugin/dialog.py (Stand nach Task 1)
    - hermes-plugin/tools.py (call_mcp_get_item_status aus Task 1)
    - .planning/phases/03-hermes-plugin-timeout-spike/03-RESEARCH.md (Pattern 2: Post-Autosave Polling)
    - .planning/phases/03-hermes-plugin-timeout-spike/03-CONTEXT.md (D-05, D-06)
    - .claude/skills/spike-findings-puzzlessbox/references/draft-timeout.md (API Timer VALIDATED — Plugin pollt nur)
  </read_first>
  <behavior>
    - Test schedule_autosave_poll_calls_get_item_status: nach create_item wird schedule_autosave_poll(draft_id, delay=0.05) aufgerufen → call_mcp_get_item_status(draft_id) gerufen
    - Test autosave_ping_sent_on_auto_saved: status='auto_saved' → session.send_message mit "📦 Automatisch gestasht" (Apollo voice per brand/VOICE.md)
    - Test autosave_ping_silent_on_confirmed: status='confirmed' → kein chat ping (user hat schon bestätigt)
    - Test autosave_ping_silent_on_discarded: status='discarded' → kein ping
    - Test poll_does_not_drive_timer: poll verwendet asyncio.sleep(delay) zum Warten — kein eigener Timer, kein Hermes-Cron (MCP-04 prohibitions)
  </behavior>
  <action>
    Füge `schedule_autosave_poll(session, draft_id, delay_seconds=32.0)` in hermes-plugin/dialog.py hinzu (Pattern 2 aus RESEARCH.md Zeilen 210–221):
    - `await asyncio.sleep(delay_seconds)` — delay default 32s (D-06: ~30–35s), testbar mit kleinem delay.
    - `status = await call_mcp_get_item_status(draft_id)` über MCP.
    - Wenn `status == 'auto_saved'`: `await session.send_message("📦 Automatisch gestasht (lands on board).")` (D-05, Apollo voice).
    - Sonst (confirmed/discarded/draft): kein ping.
    - Kein eigener Timer, kein Hermes-Cron (MCP-04 — Poll ist Lesezugriff, Timer bleibt API-seitig).
    - Hook in `start_capture_flow`: nach erfolgreichem create_item, `asyncio.create_task(schedule_autosave_poll(session, draft_id))` (fire-and-forget, nicht blockierend).

    `call_mcp_get_item_status(item_id)` in tools.py: ruft MCP-Tool ab, das Item-Status zurückgibt. Falls MCP keinen direkten get_item_status-Tool hat (Phase 2 MCP-01 listet nur create/list_categories/create_category/move/confirm/update), verwende `list_board_items` oder einen read-Aufruf und extrahiere status. Dokumentiere die Wahl im Code-Kommentar. (Discretion per CONTEXT.md: "Exact API route/shape ... if not already exposed".)
  </action>
  <verify>
    <automated>cd hermes-plugin && pytest tests/test_orchestration.py -k "autosave or poll" -x</automated>
  </verify>
  <done>
    - schedule_autosave_poll wartet via asyncio.sleep, ruft get_item_status über MCP
    - Bei auto_saved: chat ping "📦 Automatisch gestasht"
    - Bei confirmed/discarded: kein ping
    - Poll ist Lesezugriff — kein Timer, kein Hermes-Cron
    - 5 Tests grün
  </done>
  <acceptance_criteria>
    - schedule_autosave_poll in dialog.py
    - call_mcp_get_item_status in tools.py (via MCP)
    - Ping nur bei auto_saved
    - asyncio.sleep als Wartemechanismus, kein cron
    - 5 neue Tests grün
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → MCP-Server | Bearer über HTTPS; 401 bei fehlendem Token |
| Plugin-Session-State | active_draft pro Chat — kein Cross-Chat-Leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Information Disclosure | active_draft cross-chat leak | medium | mitigate | session-State ist per Chat/Session isoliert; kein globaler Cache |
| T-03-06 | Tampering | Poll als Timer-Missbrauch | low | accept | Poll liest nur status; Timer-Reset passiert API-seitig via PATCH; kein Schreibzugriff vom Poll |
| T-03-02 | Elevation of Privilege | direkter DB-Zugriff | high | mitigate | alle Tools via MCP; tools.py enthält nur call_mcp_* Wrapper; keine DB-Libs in pyproject.toml (Plan 02) |
| T-03-03 | Tampering | Hermes-Cron als Timer | high | mitigate | Poll verwendet asyncio.sleep, nicht cron; Timer bleibt API DraftTimeoutManager |
</threat_model>

<verification>
- `pytest hermes-plugin/tests/ -x` — alle Tests grün (tracer + edit + concurrency + poll)
- `grep -rE 'psycopg2|sqlalchemy|SQLModel' hermes-plugin/` → keine Treffer (MCP-03)
- `grep -r 'cron' hermes-plugin/` → keine Timer-Treiber (MCP-04)
</verification>

<success_criteria>
- Konversationeller Edit-Flow mit silent ACK (D-01/D-02/D-03)
- Single-Active-Draft Concurrency mit Konflikt-Reply (D-07)
- Post-Autosave Poll über MCP mit chat ping (D-05/D-06)
- LLM-Heuristik für type/category mit Inbox-Fallback (D-09/D-10)
- Plugin bleibt reiner MCP-Client — keine DB, kein Timer
</success_criteria>

<output>
Create `.planning/phases/03-hermes-plugin-timeout-spike/03-03-SUMMARY.md` when done
</output>
