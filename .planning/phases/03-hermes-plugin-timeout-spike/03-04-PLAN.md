---
phase: 03-hermes-plugin-timeout-spike
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - hermes-plugin/setup.sh
  - hermes-plugin/README.md
  - hermes-plugin/tests/test_channels.py
autonomous: true
requirements: [CAP-04, MCP-03]
user_setup:
  - service: hermes-vps
    why: "Plugin läuft auf externem Hermes-VPS; Setup-Skript schreibt MCP_URL/MCP_BEARER in Hermes env/secrets"
    env_vars:
      - name: MCP_URL
        source: "https://mcp.puzzlesstool.online/mcp (prod) — via setup.sh prompt"
      - name: MCP_BEARER
        source: "Coolify Secret / MCP service bearer token — via setup.sh prompt; niemals committen"

must_haves:
  truths:
    - "Setup-Skript `setup.sh` fragt interaktiv MCP_URL + MCP_BEARER ab, validiert sie (URL ist HTTPS, Bearer nicht leer), schreibt sie in hermes-plugin/.env und stellt sicher dass .env in .gitignore steht (D-12)"
    - "Capture-Flow ist kanalneutral: gleiche Plain-Text Stash-Check-Payload funktioniert auf Telegram, WhatsApp, Discord ohne kanalspezifische Anpassung in Puzzlessbox (CAP-04)"
    - "Plugin ruft ausschließlich MCP-Tools auf — Setup-Skript hinterlegt nur MCP_URL/MCP_BEARER, keine DB-Credentials (MCP-03 prohibitions)"
  artifacts:
    - hermes-plugin/setup.sh
    - hermes-plugin/README.md
    - hermes-plugin/tests/test_channels.py
  key_links:
    - "setup.sh → hermes-plugin/.env (MCP_URL, MCP_BEARER) → config.get_settings()"
    - "dialog.handle_user_message → format_confirmation → kanalneutrale Plain-Text-Reply (Telegram/WhatsApp/Discord)"
  prohibitions:
    - "Kein direkter Datenbankzugriff vom Hermes-VPS (MCP-03 — Setup hinterlegt keine DB-Credentials)"
    - "Kein Hardcodieren von MCP_BEARER im Quellcode oder setup.sh default (D-12 — nur env)"
    - "Keine kanalspezifische Anpassung in Puzzlessbox (CAP-04 — Hermes adapter übernehmen Buttons/Markdown)"

assumptions:
  - id: A-CAP-04-02
    requirement: CAP-04
    probe: "unclassified — review manually"
    claim: "Capture-Flow ist kanalneutral; gleiche Plain-Text-Payload an Telegram/WhatsApp/Discord; kanalspezifische Buttons nur in Hermes-Adaptern."
    resolution: "covered by Task 2 — test_channels.py verifiziert identische Payload über 3 Mock-Adapter"
---

<objective>
Setup-Skript (D-12) für First-Run-Konfiguration auf dem Hermes-VPS und kanalneutrale Verifikation (CAP-04) des Bestätigungs-Flows über Telegram, WhatsApp, Discord Mock-Adapter. Setzt den Tracer (Plan 02) und die Dialog-Expansion (Plan 03) voraus.

Purpose: D-12 verlangt ein interaktives Setup-Skript, das MCP_URL/MCP_BEARER sicher in env schreibt — ohne Hardcode, ohne Commit. CAP-04 muss beweisen, dass die Plain-Text-Payload kanalneutral ist, bevor Phase 4 die WebApp baut.
Output: `hermes-plugin/setup.sh`, `hermes-plugin/README.md` (Deploy-Anleitung), `hermes-plugin/tests/test_channels.py`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-CONTEXT.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-RESEARCH.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-PATTERNS.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-02-PLAN.md
@.planning/phases/03-hermes-plugin-timeout-spike/03-03-PLAN.md
@brand/VOICE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Interaktives setup.sh + README (D-12)</name>
  <files>hermes-plugin/setup.sh, hermes-plugin/README.md</files>
  <read_first>
    - .planning/phases/03-hermes-plugin-timeout-spike/03-CONTEXT.md (D-11 monorepo + rsync/pull, D-12 env-only secrets)
    - .planning/phases/03-hermes-plugin-timeout-spike/03-RESEARCH.md (Pitfall 3: Token-Leakage)
    - hermes-plugin/.gitignore (aus Plan 02 — .env muss bereits ignoriert sein)
    - hermes-plugin/config.py (aus Plan 02 — Settings lädt MCP_URL, MCP_BEARER aus env)
  </read_first>
  <action>
    - `hermes-plugin/setup.sh` (ausführbar, `#!/usr/bin/env bash`, `set -euo pipefail`):
      1. Prüfe ob `.env` bereits existiert → warnung vor Überschreiben, bestätigung abfragen.
      2. Prompt für `MCP_URL` (default `https://mcp.puzzlesstool.online/mcp`). Validiere: muss mit `https://` beginnen.
      3. Prompt für `MCP_BEARER` (kein echo — `read -rs`). Validiere: nicht leer, min 20 Zeichen.
      4. Schreibe `MCP_URL=<value>` und `MCP_BEARER=<value>` in `hermes-plugin/.env` (chmod 600).
      5. Prüfe dass `hermes-plugin/.gitignore` `.env` enthält — falls nicht, füge es hinzu (Defensive Guard gegen T-03-01).
      6. Prüfe dass `.env` nicht von git tracked ist (`git check-ignore .env`).
      7. Echo: "Konfiguration geschrieben nach hermes-plugin/.env. Bitte Hermes neu laden."
      Keine Hardcodierung von Tokens im Skript. Keine Defaults für Bearer. Verwende `read -rs` für Bearer-Eingabe.
    - `hermes-plugin/README.md`:
      1. Plugin-Zweck (Capture-Flow über MCP, kanalneutral).
      2. Deploy-Anleitung (D-11): `git pull` oder `rsync` auf Hermes-VPS, dann `bash setup.sh`, dann Hermes reload.
      3. Abhängigkeiten (`pip install -e .` oder pyproject.toml).
      4. Sicherheits-Hinweise: MCP_BEARER nur via setup.sh, niemals committen; .env in .gitignore.
      5. Verweis auf `.claude/skills/spike-findings-puzzlessbox/SKILL.md` für Spike-Ergebnisse (MCP-04 landed).
  </action>
  <verify>
    <automated>test -x hermes-plugin/setup.sh && bash -n hermes-plugin/setup.sh && grep -q 'read -rs' hermes-plugin/setup.sh && grep -q 'MCP_BEARER' hermes-plugin/setup.sh && ! grep -E 'MCP_BEARER=[A-Za-z0-9]{20}' hermes-plugin/setup.sh && grep -q '\.env' hermes-plugin/.gitignore</automated>
  </verify>
  <done>
    - setup.sh ist ausführbar und syntaktisch gültig
    - Fragt MCP_URL + MCP_BEARER interaktiv ab (read -rs für Bearer)
    - Validiert HTTPS + nicht-leer
    - Schreibt .env mit chmod 600
    - .gitignore enthält .env
    - Kein hardcodierter Bearer im Skript
    - README dokumentiert Deploy + Sicherheit
  </done>
  <acceptance_criteria>
    - setup.sh ausführbar (`test -x`)
    - `bash -n` syntax-check erfolgreich
    - `read -rs` für Bearer vorhanden
    - Kein hardcodierter Bearer (grep negativ)
    - .env in .gitignore
    - README.md enthält Deploy- + Sicherheits-Abschnitt
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Kanalneutrale CAP-04 Verifikation (Telegram, WhatsApp, Discord)</name>
  <files>hermes-plugin/tests/test_channels.py</files>
  <read_first>
    - hermes-plugin/dialog.py (Stand aus Plan 03 — handle_user_message, format_confirmation)
    - hermes-plugin/formatters.py (format_confirmation aus Plan 02)
    - hermes-plugin/tests/conftest.py (aus Plan 02 — MCP-Transport-Mock, session-Fixture)
    - .planning/phases/03-hermes-plugin-timeout-spike/03-CONTEXT.md (D-11: kanallose Plain-Text-Payloads)
    - .planning/spikes/004-confirmation-payload/README.md (Kanalneutralität VALIDATED)
    - brand/VOICE.md (Plain-Text-Stimme)
  </read_first>
  <behavior>
    - Test telegram_same_payload: handle_user_message über Telegram-Mock-Adapter → reply enthält "Stash-Check", "Eintrag sichern", Titel — identisch mit erwarteter Plain-Text-Payload
    - Test whatsapp_same_payload: gleiche Eingabe über WhatsApp-Mock-Adapter → identische reply wie Telegram
    - Test discord_same_payload: gleiche Eingabe über Discord-Mock-Adapter → identische reply
    - Test all_channels_no_markdown: reply enthält keine Markdown-Spezifika (kein `**`, kein `#`), kein HTML — reiner Plain-Text (CAP-04)
    - Test all_channels_same_edit_ack: edit-Flow produziert identischen silent ACK über alle 3 Adapter
    - Test channel_specific_buttons_only_in_adapter: plugin-Ebene enthält keine Telegram-Keyboard, WhatsApp-Button, Discord-Component — diese liegen in Hermes-Adaptern (Puzzlessbox-Plugin ist kanalneutral)
  </behavior>
  <action>
    Erstelle `hermes-plugin/tests/test_channels.py`:
    - Definiere 3 Mock-Adapter-Fixtures (`telegram_session`, `whatsapp_session`, `discord_session`) in conftest.py oder direkt in test_channels.py. Jeder Mock-Adapter implementiert `get_state`, `set_state`, `clear_state`, `send_message` — speichert messages in einer Liste. Keine kanalspezifische Render-Logik in den Mocks (sie repräsentieren die Hermes-Adapter-Schnittstelle, nicht Puzzlessbox-Logik).
    - Für jeden Adapter: rufe `handle_user_message(session, "Notiz: Q3 Roadmap")` auf, assert reply ist identisch (gleicher String) über alle 3 Adapter.
    - Assert reply enthält "Stash-Check", "Eintrag sichern", "Q3 Roadmap" (Titel), "Auto-Save in 30s".
    - Assert reply enthält keine Markdown-Tokens (`**`, `#`, `__`), kein HTML (`<`, `>`).
    - Für edit-Flow: sende "Titel: Neuer Titel" nach Karte, assert silent ACK identisch über alle Adapter.
    - Assert dass hermes-plugin/dialog.py und hermes-plugin/formatters.py keine kanalspezifischen Imports oder Verzweigungen enthalten (`grep -E 'telegram|whatsapp|discord' hermes-plugin/dialog.py hermes-plugin/formatters.py` → leer).
  </action>
  <verify>
    <automated>cd hermes-plugin && pytest tests/test_channels.py -x</automated>
  </verify>
  <done>
    - 3 Mock-Adapter (Telegram, WhatsApp, Discord) produzieren identische reply
    - reply ist reiner Plain-Text (kein Markdown, kein HTML)
    - silent ACK nach Edit identisch über alle Adapter
    - hermes-plugin/dialog.py und formatters.py enthalten keine kanalspezifischen Verzweigungen
    - 6 Tests grün
  </done>
  <acceptance_criteria>
    - test_channels.py existiert mit 6 Tests
    - Identische reply über Telegram/WhatsApp/Discord
    - reply enthält keine Markdown/HTML-Tokens
    - grep nach kanalspezifischen Tokens in dialog.py/formatters.py leer
    - Alle Tests grün
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VPS-Operator → setup.sh | Interaktive Eingabe von MCP_BEARER; muss vor Leckage geschützt werden |
| Hermes-VPS → MCP-Server | Bearer über HTTPS; setup schreibt env, nicht Quelle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | MCP_BEARER in .git via setup.sh | high | mitigate | setup.sh verwendet `read -rs`, schreibt .env mit chmod 600, prüft .gitignore, `git check-ignore .env`; kein Hardcode |
| T-03-07 | Spoofing | kanalspezifische Verzweigung im Plugin | medium | mitigate | test_channels.py assertiert keine kanalspezifischen Tokens in dialog/formatters; CAP-04 kanalneutral |
| T-03-02 | Elevation of Privilege | DB-Credentials via setup.sh | high | mitigate | setup.sh fragt nur MCP_URL/MCP_BEARER ab; keine DB-Credentials; pyproject.toml ohne DB-Libs (Plan 02) |
| T-03-SC | Tampering | bash setup.sh (kein pip) | low | accept | setup.sh ist bash-only, keine Paket-Installs; pyproject.toml-Installation ist separater manueller Schritt |
</threat_model>

<verification>
- `bash -n hermes-plugin/setup.sh` — Syntax gültig
- `pytest hermes-plugin/tests/test_channels.py -x` — kanalneutrale Tests grün
- `grep -rE 'telegram|whatsapp|discord' hermes-plugin/dialog.py hermes-plugin/formatters.py` → leer (CAP-04)
- `git check-ignore hermes-plugin/.env` → matched (T-03-01)
</verification>

<success_criteria>
- setup.sh schreibt MCP_URL/MCP_BEARER sicher in .env (D-12)
- .env ist git-ignored, chmod 600, kein Hardcode
- README dokumentiert Deploy (D-11) + Sicherheit
- Kanalneutrale Payload über Telegram/WhatsApp/Discord identisch (CAP-04)
- Keine kanalspezifische Verzweigung in Plugin-Ebene
</success_criteria>

<output>
Create `.planning/phases/03-hermes-plugin-timeout-spike/03-04-SUMMARY.md` when done
</output>
