# Phase 05.1: Address tech debt: G-05-7 + security harden advisories - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-05
**Phase:** 05.1-address-tech-debt-g-05-7-security-harden-advisories
**Areas discussed:** Lokaler MCP-Bootstrap (G-05-7), HSTS-Platzierung, Server-/X-Powered-By-Banner, MCP oauth-protected-resource well-known

---

## Lokaler MCP-Bootstrap (G-05-7)

| Option | Description | Selected |
|--------|-------------|----------|
| a | Nur Doku / manuell INSERT | |
| b | Compose + Doku (nach Signup OWNER setzen) ★ initial | |
| c | Auto nach erstem Signup (API/first-user Hook) | ✓ |
| d | Claude entscheidet | |

**User's choice:** 1c
**Follow-up:** 5a trigger on first user when `mcp_clients` empty; 6b env-or-generate; 7a also service principal / owner = first user
**Security revise (user OK):** 6b′ — prod requires env token; never log plaintext; dev may stdout/`0600` file only

---

## HSTS-Platzierung

| Option | Description | Selected |
|--------|-------------|----------|
| a | Coolify/Traefik für pbox/api/mcp ★ | ✓ |
| b | App-Middleware je Service | |
| c | Beides | |
| d | Claude entscheidet | |

**User's choice:** 2a; max-age 8a (`31536000` + `includeSubDomains`, no preload)
**Notes:** Research: apply per FQDN, not apex with includeSubDomains

---

## Server-/X-Powered-By-Banner

| Option | Description | Selected |
|--------|-------------|----------|
| a | Proxy strip + Next.js `poweredByHeader: false` ★ | ✓ |
| b | Nur App-seitig | |
| c | Nur Proxy | |
| d | Claude entscheidet | |

**User's choice:** 3a

---

## MCP `.well-known/oauth-protected-resource` 404

| Option | Description | Selected |
|--------|-------------|----------|
| a | `resource_metadata` aus WWW-Authenticate entfernen | |
| b | Minimaler RFC-9728-Stub ★ | ✓ |
| c | Warn akzeptieren / nur Doku | |
| d | Claude entscheidet | |

**User's choice:** 4b; path 9a = `/.well-known/oauth-protected-resource/mcp`
**Security revise (user OK):** 4b′ — stub with `resource` + `bearer_methods_supported` only; no fake `authorization_servers`; document static bearer ≠ MCP OAuth AS

---

## Claude's Discretion

- Coolify/Traefik exact middleware mechanism
- Stub `resource` string matching advertised identifier
- Optional local compose default bootstrap token for ENV=dev
- Test layout for guards + well-known 200

## Deferred Ideas

- dbhub prod DSN tooling
- Phase 3 schedule_autosave_poll RuntimeWarning
- Full MCP OAuth AS / HSTS preload / MCP-06 IP allowlist
