---
phase: 05.1
slug: address-tech-debt-g-05-7-security-harden-advisories
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-05
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (API / MCP), vitest (Web) |
| **Config file** | `api/pytest.ini`, `mcp-server/pytest.ini` |
| **Quick run command** | `api/.venv/bin/pytest api/tests/unit/test_health.py -q` |
| **Full suite command** | `api/.venv/bin/pytest` and `mcp-server/.venv/bin/pytest` |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `api/.venv/bin/pytest api/tests/unit/test_health.py -q`
- **After every plan wave:** Run full API + MCP pytest suites (and webapp tests for web tasks)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 05.1-01-01 | 01 | 1 | G-05-7 / D-01..D-05 | T-05.1-01 | No plaintext token in logs; empty-table atomic bootstrap | integration | `api/.venv/bin/pytest api/tests/integration/test_bootstrap.py -x` | ❌ W0 | ⬜ pending |
| 05.1-02-01 | 02 | 1 | D-10..D-12 | T-05.1-03 | Well-known stub 200; no fake authorization_servers | integration | `mcp-server/.venv/bin/pytest mcp-server/tests/test_auth.py -x` | ✅ extend | ⬜ pending |
| 05.1-03-01 | 03 | 2 | D-06..D-09 | T-05.1-02 | HSTS + banner strip; poweredByHeader false | unit + manual | `pnpm --filter webapp test` + curl prod headers | ✅ extend | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `api/tests/integration/test_bootstrap.py` — stubs for first-user MCP auto-bootstrap (empty-table guard, prod refuse generate, never log plaintext)
- [ ] Extend `mcp-server/tests/test_auth.py` — well-known RFC 9728 stub 200 + schema asserts
- [ ] Extend webapp config/tests — assert `poweredByHeader: false`

*Existing pytest/vitest infra covers frameworks; Wave 0 adds missing bootstrap test file.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| HSTS on pbox/api/mcp | D-06, D-07 | Coolify/Traefik live headers | `curl -sI https://{host}` → `Strict-Transport-Security: max-age=31536000; includeSubDomains` |
| Server / X-Powered-By stripped | D-08 | Proxy live headers | `curl -sI` shows no `Server`/`X-Powered-By` (or Traefik-stripped) |
| Apex HSTS not set with includeSubDomains | D-07 | Negative check on live apex | Confirm apex `puzzlesstool.online` not force-HSTS'd in this phase |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
