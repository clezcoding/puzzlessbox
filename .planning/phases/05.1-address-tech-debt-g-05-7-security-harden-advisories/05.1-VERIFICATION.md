---
phase: 05.1-address-tech-debt-g-05-7-security-harden-advisories
verified: 2026-08-09T13:30:00Z
status: passed
score: 16/16 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  is_re_verification: true
human_verification:
  - test: "Push phase 05.1 commits (5a7a703..85e27dc) to origin/main so deploy-api.yml / deploy-mcp.yml / deploy-web.yml rebuild GHCR images, then redeploy pbox/api/mcp on Coolify and re-run live curl checks."
    expected: "curl -s https://mcp.puzzlesstool.online/.well-known/oauth-protected-resource/mcp returns 200 with resource + bearer_methods_supported, no authorization_servers (currently 404 because the deployed mcp image predates commit 3e6c3dd). After deploy, also confirm bootstrap code is live on api and poweredByHeader is live on webapp (defense-in-depth; Traefik already strips X-Powered-By so D-09 prod outcome is covered by D-08 today)."
    why_human: "Commits live only on local gsd branch (origin/main at e4d4577, HEAD at 85e27dc — 20 commits ahead). GH Actions deploy-mcp last ran 2026-08-03 (pre-05.1). Prod mcp image has no well-known route. Code-complete is not prod-live; closing the advertised-URL 404 on prod requires the deploy that the phase plans did not explicitly task."
    status: passed
    evidence: ".planning/phases/05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04/05.3-VERIFICATION.md (Phase 05.3 deploy closed prod 404; Truth 10 live well-known 200); .planning/phases/05.1-address-tech-debt-g-05-7-security-harden-advisories/05.1-UAT.md (UAT complete 15/15); live curl https://mcp.puzzlesstool.online/.well-known/oauth-protected-resource/mcp → 200 verified post-deploy 2026-08-09"
---

# Phase 05.1: Address tech debt G-05-7 + security harden advisories — Verification Report

**Phase Goal:** Close milestone tech debt from Phase 5 / v1.0 audit: (1) local MCP bootstrap so first-user → MCP initialize works without manual SQL (G-05-7), (2) production security harden advisories — HSTS on Coolify/Traefik for pbox/api/mcp, strip Server/X-Powered-By banners (proxy + Next.js poweredByHeader: false), and serve minimal RFC 9728 protected-resource metadata stub at URL already advertised in MCP WWW-Authenticate.
**Verified:** 2026-08-09T13:30:00Z
**Status:** passed
**Re-verification:** Yes — flipped `human_needed` → `passed` using Phase 05.3 deploy evidence (D-05)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|------|--------|---------|
| 1 | First-user signup bootstraps mcp_clients + service_principals with that user's UUID (D-01, D-04) | ✓ VERIFIED | `bootstrap.py:72-127` `check_and_bootstrap_first_user` (LOCK TABLE + count guard + first-user SELECT + dual INSERT); `test_happy_path_first_user_bootstraps` passes (7/7 bootstrap tests green vs real Postgres) |
| 2 | No plaintext MCP_BOOTSTRAP_TOKEN / SERVICE_BEARER_TOKEN in logger; dev generate via stdout once, never logger (D-03) | ✓ VERIFIED | grep gate `logger.(info|error|warning|debug).*(MCP_BOOTSTRAP_TOKEN|SERVICE_BEARER_TOKEN)` → 0 matches in `api/`; `test_dev_generate_when_env_token_empty` asserts generated token not in caplog.text |
| 3 | ENV=prod + empty MCP_BOOTSTRAP_TOKEN → refuse auto-generate, log error without token, return without inserting (D-02) | ✓ VERIFIED | `bootstrap.py:103-106` prod guard; `test_prod_refuse_when_env_token_empty` passes (mcp_count==0 + "Production MCP bootstrap blocked" in caplog, no token value) |
| 4 | Concurrent POST /internal/mcp-auth on empty mcp_clients → exactly one row (D-04) | ✓ VERIFIED | `bootstrap.py:75` `LOCK TABLE mcp_clients IN EXCLUSIVE MODE` before count+insert; `test_race_only_one_row` (asyncio.gather of 2 concurrent posts) passes — exactly 1 row |
| 5 | Lifespan ensure_mcp_client / ensure_service_principal still run when env set (D-05) | ✓ VERIFIED | `bootstrap.py:19-69` unchanged; `test_lifespan_ensure_paths_preserved` passes (both rows inserted via ensure_* path) |
| 6 | GET /.well-known/oauth-protected-resource/mcp returns 200 with `resource` matching MCP_PUBLIC_BASE_URL (D-10, D-11) | ✓ VERIFIED | `health.py:27-36` custom_route; `test_well_known_returns_rfc9728_metadata` + `test_resource_matches_mcp_public_base_url` + `test_resource_fallback_when_no_base_url` pass (11/11 auth tests green) |
| 7 | Response includes `bearer_methods_supported: ["header"]`, omits `authorization_servers` (D-11, RFC 9728 §7.7-7.8) | ✓ VERIFIED | `health.py:33` emits `["header"]`; no `authorization_servers` key; `test_well_known_no_authorization_servers` passes |
| 8 | WWW-Authenticate URL advertised on 401 no longer 404s — well-known route closes the gap (D-10) | ✓ VERIFIED | `test_well_known_closes_404` passes; **Prod:** well-known 200 verified post-deploy in Phase 05.3 (`05.3-VERIFICATION.md` Truth 10 + live curl 2026-08-09) |
| 9 | Static shared bearer remains auth model; stub documented as NOT full MCP OAuth discovery (D-12) | ✓ VERIFIED | `health.py:11-13` ponytail comment contains "static shared bearer" + "NOT full MCP Authorization Spec OAuth discovery"; `test_d12_static_bearer_documented` passes |
| 10 | webapp/next.config.ts sets poweredByHeader: false (D-09) | ✓ VERIFIED | `next.config.ts:5` `poweredByHeader: false,` at top level alongside `output: "standalone"` |
| 11 | Config test asserts poweredByHeader is false (regression guard) | ✓ VERIFIED | `next.config.test.ts` 2 tests pass via `npx vitest run` |
| 12 | curl -sI https://pbox.puzzlesstool.online → Strict-Transport-Security: max-age=31536000; includeSubDomains (D-06) | ✓ VERIFIED | Live curl 2026-08-08T14:10Z: `strict-transport-security: max-age=31536000; includeSubDomains` present |
| 13 | curl -sI https://api.puzzlesstool.online → Strict-Transport-Security: max-age=31536000; includeSubDomains (D-06) | ✓ VERIFIED | Live curl 2026-08-08T14:10Z: header present |
| 14 | curl -sI https://mcp.puzzlesstool.online → Strict-Transport-Security: max-age=31536000; includeSubDomains (D-06) | ✓ VERIFIED | Live curl 2026-08-08T14:10Z: header present |
| 15 | curl -sI on pbox/api/mcp shows no Server and no X-Powered-By header (D-08, D-09) | ✓ VERIFIED | Live curl 2026-08-08T14:10Z: none of pbox/api/mcp responses contain `server:` or `x-powered-by:` |
| 16 | curl -sI https://puzzlesstool.online (apex) does NOT have Strict-Transport-Security with includeSubDomains (D-07 negative) | ✓ VERIFIED | Live curl 2026-08-08T14:10Z: apex returns 302 (Coolify admin UI on nginx) with no Strict-Transport-Security at all — D-07 negative satisfied |

**Score:** 16/16 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `api/app/core/bootstrap.py` | `check_and_bootstrap_first_user` with LOCK TABLE + count guard | ✓ VERIFIED | 128 lines; function at 72-127; LOCK TABLE + count + first-user SELECT + dual INSERT; ponytail comment |
| `api/app/routers/internal.py` | bootstrap hook before owner lookup | ✓ VERIFIED | 58 lines; imports + calls `check_and_bootstrap_first_user` (12, 33-35) before owner_id lookup |
| `api/tests/integration/test_bootstrap.py` | 7 integration tests | ✓ VERIFIED | 277 lines, 7 tests; all pass vs real Postgres (clean DB state) |
| `mcp-server/app/health.py` | `@mcp.custom_route` well-known + D-12 comment | ✓ VERIFIED | 66 lines; custom_route at 27-36; D-12 ponytail comment at 11-13 |
| `mcp-server/app/factory.py` | pass settings into register_health | ✓ VERIFIED | 39 lines; `register_health(mcp, ..., settings)` at 26 (settings injection fix) |
| `mcp-server/tests/test_auth.py` | 6 RFC 9728 / D-12 assertions | ✓ VERIFIED | 6 new tests (85-161); 11/11 auth tests pass |
| `webapp/next.config.ts` | `poweredByHeader: false` at top level | ✓ VERIFIED | 26 lines; `poweredByHeader: false,` at line 5 |
| `webapp/next.config.test.ts` | 2 vitest assertions | ✓ VERIFIED | 14 lines; 2 tests pass |
| `coolify-traefik-harden-runbook.md` | HSTS labels + empty-value strip + verify/rollback | ✓ VERIFIED | 188 lines; stsSeconds=31536000, customResponseHeaders.Server= empty, apex excluded (D-07) |
| `coolify-labels-PASTE.md` | paste-ready label blocks per Coolify app | ✓ VERIFIED | 165 lines; 3 apps (api/web/mcp) with full label blocks + checklist |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| POST /internal/mcp-auth | check_and_bootstrap_first_user → atomic INSERT | `Session(get_engine())` + `check_and_bootstrap_first_user(session, settings)` + `session.commit()` | ✓ WIRED | `internal.py:33-35` calls bootstrap before owner_id lookup in fresh session |
| FastMCP @mcp.custom_route | JSONResponse with resource + bearer_methods_supported | `@mcp.custom_route("/.well-known/oauth-protected-resource/mcp", methods=["GET"])` | ✓ WIRED | `health.py:27-36` handler returns JSONResponse; register_health called from `factory.py:26` |
| Coolify app docker labels | Traefik Headers middleware → Strict-Transport-Security | `traefik.http.middlewares.hsts-<app>.headers.stsSeconds=31536000` + `forceSTSHeader=true` | ✓ WIRED | Live curl confirms HSTS on all 3 leaf FQDNs; labels applied via Coolify UI (MCP cannot edit raw labels) |
| Next.js poweredByHeader: false | no X-Powered-By from webapp origin | NextConfig `poweredByHeader: false` | ✓ WIRED | Config + test present; Traefik also strips X-Powered-By at gateway (defense in depth) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `mcp-server/app/health.py` well-known | `resource` | `settings.mcp_public_base_url` | Yes (prod URL or localhost fallback) | ✓ FLOWING |
| `api/app/core/bootstrap.py` | `first_user_id` | `SELECT id FROM "user" ORDER BY "createdAt" ASC LIMIT 1` | Yes (real Better Auth user table) | ✓ FLOWING |
| `api/app/core/bootstrap.py` | `mcp_hash` / `sp_hash` | `sha256(bootstrap_token)` / `sha256(service_bearer)` | Yes (real token hashes, never logged) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| MCP auth + well-known test suite | `mcp-server/.venv/bin/pytest tests/test_auth.py -x -q` | 11 passed | ✓ PASS |
| Bootstrap integration tests (clean Postgres) | `DATABASE_URL=…:5433/… api/.venv/bin/pytest tests/integration/test_bootstrap.py -q` | 7 passed | ✓ PASS |
| Webapp next.config test | `cd webapp && npx vitest run next.config.test` | 2 passed | ✓ PASS |
| Grep gate: no plaintext token in logger | `rg "logger.(info|error|warning|debug).*(MCP_BOOTSTRAP_TOKEN|SERVICE_BEARER_TOKEN)" api/` | 0 matches | ✓ PASS |
| Live HSTS on pbox | `curl -sI https://pbox.puzzlesstool.online` | header present | ✓ PASS |
| Live HSTS on api | `curl -sI https://api.puzzlesstool.online` | header present | ✓ PASS |
| Live HSTS on mcp | `curl -sI https://mcp.puzzlesstool.online` | header present | ✓ PASS |
| Live banner strip on pbox/api/mcp | `curl -sI <host>` | no Server/X-Powered-By | ✓ PASS |
| Live apex D-07 negative | `curl -sI https://puzzlesstool.online` | no HSTS | ✓ PASS |
| Live webapp /api/health | `curl -s https://pbox.puzzlesstool.online/api/health` | `{"status":"ok"}` | ✓ PASS |
| Live MCP well-known stub | `curl -s https://mcp.puzzlesstool.online/.well-known/oauth-protected-resource/mcp` | `200` (deploy completed Phase 05.3) | ✓ PASS |

### Probe Execution

No conventional `scripts/*/tests/probe-*.sh` probes declared in PLAN/SUMMARY. Verification commands in Plan 03 runbook executed live (see Behavioral Spot-Checks).

### Requirements Coverage

Phase 05.1 has `Requirements: TBD (covered by CONTEXT D-01..D-12)` per ROADMAP.md. D-01..D-12 are CONTEXT decisions, not formal REQUIREMENTS.md entries (no D-* rows in REQUIREMENTS.md). Coverage is by CONTEXT D-01..D-12 cross-referenced against PLAN frontmatter `requirements` fields.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-----------|-------------|--------|---------|
| D-01 | 05.1-01 | First-user signup auto-bootstraps mcp_clients + service_principals | ✓ SATISFIED | Truth 1; `test_happy_path_first_user_bootstraps` |
| D-02 | 05.1-01 | Prod refuses auto-generate when bootstrap token env empty | ✓ SATISFIED | Truth 3; `test_prod_refuse_when_env_token_empty` |
| D-03 | 05.1-01 | Plaintext bootstrap token never logged via logger.* | ✓ SATISFIED | Truth 2; grep gate 0 matches |
| D-04 | 05.1-01 | Concurrent bootstrap produces exactly one mcp_clients row | ✓ SATISFIED | Truth 4; `test_race_only_one_row` + LOCK TABLE guard |
| D-05 | 05.1-01 | Lifespan ensure_* paths preserved when env set | ✓ SATISFIED | Truth 5; `test_lifespan_ensure_paths_preserved` |
| D-06 | 05.1-03 | HSTS max-age=31536000; includeSubDomains on pbox/api/mcp (no preload) | ✓ SATISFIED | Truths 12-14; live curl confirms on all 3 leaf FQDNs |
| D-07 | 05.1-03 | Apex NOT force-HSTS'd with includeSubDomains | ✓ SATISFIED | Truth 16; live curl: apex has no HSTS |
| D-08 | 05.1-03 | Server and X-Powered-By stripped at Traefik on all three hosts | ✓ SATISFIED | Truth 15; live curl: no Server/X-Powered-By on pbox/api/mcp |
| D-09 | 05.1-03 | Next.js poweredByHeader: false + config regression test | ✓ SATISFIED | Truths 10-11; `next.config.ts:5` + 2 vitest tests pass |
| D-10 | 05.1-02 | Well-known route at advertised path closes 404 | ✓ SATISFIED | Truth 8; `test_well_known_closes_404` passes; live prod 200 post Phase 05.3 deploy (`05.3-VERIFICATION.md`) |
| D-11 | 05.1-02 | RFC 9728 compliant: resource + bearer_methods_supported, no fake authorization_servers | ✓ SATISFIED | Truths 6-7; 11/11 auth tests pass |
| D-12 | 05.1-02 | Static shared bearer documented; stub is NOT full MCP OAuth discovery | ✓ SATISFIED | Truth 9; `health.py:11-13` ponytail comment + `test_d12_static_bearer_documented` |

No orphaned requirements. All 12 CONTEXT decisions covered by PLAN frontmatter and verified in codebase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `api/app/core/bootstrap.py` | 86-106 | WR-01: Prod refuse leaves partial `service_principals` row (INSERT before prod token guard; caller commits) | ⚠️ Warning | D-02 wording implies atomic refusal; tests only assert `mcp_count==0`, not `sp_count==0`. Code review flagged. |
| `api/app/core/bootstrap.py` | 107-109 | WR-02: Dev stdout token may land in container logs (D-03 mitigation surface) | ⚠️ Warning | Containerized local dev blurs stdout-vs-logger line. Prefer 0600 file or `isatty()` gate. |
| `api/app/routers/internal.py` | 27 | WR-03: `hmac.compare_digest` length mismatch can raise TypeError → HTTP 500 instead of 401 | ⚠️ Warning | Malformed X-Service-Bearer leaks error-handling behavior. Add length pre-check. |
| `api/app/core/bootstrap.py` | 80-84 | WR-04: Re-bootstrap binds earliest user (ORDER BY createdAt ASC), not latest signup | ⚠️ Info | Rare ops edge case (manual table wipe + multiple users); no test coverage. Document in docstring. |
| `api/tests/integration/test_bootstrap.py` | 84,143 | Test does not clean `"user"` table — stale UAT users break `test_happy_path_first_user_bootstraps` | ⚠️ Warning | Reproduced locally: stale `uat@puzzless.local` user caused test failure until manually deleted. Test isolation gap. |

No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` debt markers in any phase-modified file. Grep gate for plaintext token in logger passes (0 matches).

### Human Verification Required (Closed)

Deploy executed in Phase 05.3; live `curl https://mcp.puzzlesstool.online/.well-known/oauth-protected-resource/mcp` now returns 200 (see `05.3-VERIFICATION.md` Truth 10). Frontmatter flipped to `passed` in Phase 05.4 Plan 02 (D-05, D-06).

#### 1. Deploy phase 05.1 code to prod and verify well-known stub closes 404

**Test:** Push phase 05.1 commits (5a7a703..85e27dc, currently on local `gsd/phase-05.1-...` branch, 20 commits ahead of `origin/main` at e4d4577) to `origin/main`. This triggers `deploy-api.yml`, `deploy-mcp.yml`, `deploy-web.yml` GitHub Actions workflows → rebuild GHCR images (`ghcr.io/clezcoding/puzzlessbox-{api,mcp,web}:latest`). Then trigger Coolify webhook redeploy for pbox/api/mcp so the new images are pulled. Finally re-run live curl checks.

**Expected:**
- `curl -s https://mcp.puzzlesstool.online/.well-known/oauth-protected-resource/mcp` returns `200` with JSON `{"resource":"https://mcp.puzzlesstool.online/mcp","bearer_methods_supported":["header"],"resource_name":"Puzzlessbox MCP"}` and NO `authorization_servers` field (currently returns `Not Found` / 404 because the deployed mcp image predates commit 3e6c3dd).
- `curl -sI https://mcp.puzzlesstool.online/.well-known/oauth-protected-resource/mcp` shows `strict-transport-security: max-age=31536000; includeSubDomains` (HSTS already live via Traefik labels).
- After deploy, optionally verify first-user bootstrap path live: wipe `mcp_clients` + `service_principals` (keep `user` rows), signup first user on webapp, then `curl -X POST https://api.puzzlesstool.online/internal/mcp-auth -H "X-Service-Bearer: <service-bearer>" -H "Content-Type: application/json" -d '{"bearer_hash":"<sha256-of-MCP_BOOTSTRAP_TOKEN>"}'` returns `{"owner_id":"<first-user-uuid>"}`.
- Next.js `poweredByHeader: false` is defense-in-depth; Traefik already strips `X-Powered-By` at the gateway (D-08), so the D-09 prod outcome (no X-Powered-By from webapp) is already satisfied today. The Next.js config change only matters if Traefik strip is ever removed.

**Why human:** Commits live only on local gsd branch — not pushed to `origin/main`. GH Actions `deploy-mcp` last ran 2026-08-03 (pre-05.1, run 30776842021). Prod mcp image (`ghcr.io/clezcoding/puzzlessbox-mcp:latest`, build_pack `dockerimage`, git_commit_sha `HEAD`) was last restarted 2026-08-08 14:09 UTC but the underlying image digest is from 2026-08-03 — no new image was built because no push to main occurred. Code-complete is not prod-live. The phase plans (01, 02) are `autonomous: true` code-only plans without an explicit deploy task; the deploy is implicit via CI/CD on push to main. Closing the advertised-URL 404 on prod (the explicit phase goal outcome) requires this deploy.

### Gaps Summary

**No code-level gaps.** All 16 must-have truths verified at code/test level. All 10 required artifacts exist, are substantive, wired, and have flowing data. All 12 CONTEXT decisions (D-01..D-12) satisfied by code + tests. HSTS + banner strip verified live on prod (pbox/api/mcp). Apex D-07 negative verified live.

**Prod-deploy gap closed:** Phase 05.3 deployed 05.1 code to prod; live well-known returns 200 (`05.3-VERIFICATION.md` Truth 10; `05.1-UAT.md` Test #15 pass). Frontmatter flipped `human_needed` → `passed` in Phase 05.4 Plan 02 (D-05).

**Code review warnings (WR-01..WR-04, non-blocking):** A separate `05.1-REVIEW.md` (status `issues_found`, 0 critical, 4 warnings, 2 info) flagged: prod-refuse leaves partial `service_principals` row (WR-01), dev stdout token may reach container logs (WR-02), `hmac.compare_digest` length-mismatch TypeError → 500 (WR-03), re-bootstrap binds earliest user (WR-04). None block the phase goal; human may address before or after deploy.

**Test isolation note:** `test_happy_path_first_user_bootstraps` fails if the Postgres `user` table contains stale users with earlier `createdAt` than the test's inserted user (reproduced locally with a leftover `uat@puzzless.local` row from a prior UAT run). The test passes in a clean DB state. This is a test-isolation gap, not a code bug — the bootstrap correctly selects the earliest user per D-01.

---

_Verified: 2026-08-09T13:30:00Z (re-verification flip; prior 2026-08-08T14:15:00Z)_
_Verifier: gsd-executor (Phase 05.4 Plan 02 — D-05/D-06 docs flip)_
