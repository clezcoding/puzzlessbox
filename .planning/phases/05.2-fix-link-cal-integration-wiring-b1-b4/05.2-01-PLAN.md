---
phase: 05.2-fix-link-cal-integration-wiring-b1-b4
plan: 01
type: execute
wave: 1
depends_on: ["05.2-00"]
files_modified:
  - api/app/services/link_scrape.py
  - api/app/services/scraper.py
  - api/app/services/categories.py
  - api/app/routers/links.py
  - api/app/routers/capture.py
  - api/app/models/board.py
  - api/alembic/versions/0007_board_items_link_fields.py
  - webapp/lib/api-client.ts
  - webapp/components/board/board-card.tsx
  - api/tests/integration/test_capture_async_scrape.py
autonomous: false
requirements: [LINK-01, LINK-02]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low  # sample_count=0; leave low until calibration samples exist

must_haves:
  truths:
    - "POST /drafts for type=link returns immediately with {id, type} and does not await scrape (D-01, D-03)."
    - "After commit, scrape_manager.schedule_scrape runs via asyncio.create_task and writes metadata_ + scrape_status on the existing Link row (D-01, D-17)."
    - "On scrape_status failed|timed_out, Link keeps hostname fallback title and category_id points at seeded Links category (D-02 → Phase 1 D-11)."
    - "board_items VIEW link branch exposes url AS summary, metadata->>'image' AS image, and scrape_status; non-link branches expose NULL image and NULL scrape_status (D-05, D-08, D-24)."
    - "GET /board-items BoardItem JSON includes optional image and scrape_status fields (D-05, D-21)."
    - "BoardCard thumbnail uses item.image (not summary.startsWith http); meta line shows truncated URL/hostname from summary (D-06, D-07)."
    - "OG thumb uses native img with referrerPolicy no-referrer; next.config remotePatterns unchanged (D-29, D-30, D-32)."
    - "validate_url_ssrf remains on every scrape entry (SSRF boundary)."
  artifacts:
    - api/app/services/link_scrape.py
    - api/app/services/categories.py
    - api/alembic/versions/0007_board_items_link_fields.py
    - api/app/models/board.py
    - webapp/components/board/board-card.tsx
  key_links:
    - "create_draft link commit -> scrape_manager.schedule_scrape -> scrape_service.scrape(validate_url_ssrf) -> Link.metadata_/scrape_status"
    - "failed|timed_out scrape path -> links_category_id(db) -> Link.category_id = Links"
    - "0007 DROP/CREATE board_items -> list_board_items SELECT image, scrape_status -> BoardCard item.image"
  prohibitions:
    - "Do not edit alembic 0006 in place; only new 0007 revision (D-08)"
    - "Do not introduce external job brokers; in-process asyncio only (D-17)"
    - "Do not open next/image remotePatterns to all hosts (D-32)"
    - "Do not remove or deprecate POST /links (D-04 — share path later in plan 02)"
---

<objective>
Tracer bullet: one production-quality link path — Hermes `POST /drafts` → async scrape → board_items VIEW URL/image → BoardCard thumb — proving B1+B2 architecture end-to-end before expansion.

Purpose: Close audit B1/B2 happy path for LINK-01/02 without waiting for full retry/UI polish or calendar work.
Output: `LinkScrapeManager` + Alembic 0007 VIEW + BoardItem/BoardCard wired; Wave 0 scrape happy-path tests turn green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-CONTEXT.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-RESEARCH.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-PATTERNS.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-00-SUMMARY.md

@api/app/services/timeout.py
@api/app/services/scraper.py
@api/app/routers/capture.py
@api/app/routers/links.py
@api/app/models/board.py
@api/alembic/versions/0006_board_color_sortorder.py
@webapp/lib/api-client.ts
@webapp/components/board/board-card.tsx
@webapp/next.config.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking" status="resolved">
  <name>Task 1: Approve one-way board_items VIEW migration (D-08)</name>
  <decision>Apply new Alembic revision 0007_board_items_link_fields that DROP/CREATE board_items VIEW adding link url AS summary, image, scrape_status columns (D-08).</decision>
  <context>
    D-08 is one-way: once applied in prod, undo requires another Alembic revision restoring the prior VIEW shape. Editing 0006 in place is forbidden. Downgrade will recreate the 0006 VIEW SQL (empty summary for links, no image/scrape_status columns). Confirm before executor writes/applies 0007.
  </context>
  <options>
    <option id="apply-0007">
      <name>Apply 0007 VIEW recreation</name>
      <pros>Unlocks B2 board URL + OG image without N+1; matches locked D-05/D-08.</pros>
      <cons>Prod rollback needs a follow-up migration; VIEW DROP briefly during migrate.</cons>
    </option>
    <option id="abort-phase">
      <name>Abort / re-discuss schema approach</name>
      <pros>Avoids one-way door if product wants alternate read model.</pros>
      <cons>B2 stays broken; milestone audit blocker remains.</cons>
    </option>
  </options>
  <resolved>
    <choice>apply-0007</choice>
    <resolved_at>2026-08-08</resolved_at>
    <targets>local DATABASE_URL AND Coolify production Postgres (DATABASE_URL_COOLIFY / API app DB)</targets>
    <note>Operator (2026-08-08): apply-0007 on both local and Coolify DBs. Executor MUST run alembic upgrade head against local, then Coolify prod (via Coolify exec / temporary DATABASE_URL pointing at Coolify DSN — never commit secrets). Verify board_items columns on both before marking Task 2 done.</note>
  </resolved>
  <resume-signal>apply-0007 (RESOLVED 2026-08-08 — dual target: local + Coolify)</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: TRACER — link draft → async scrape → board-items image/summary E2E</name>
  <files>api/app/services/link_scrape.py, api/app/services/scraper.py, api/app/services/categories.py, api/app/routers/links.py, api/app/routers/capture.py, api/app/models/board.py, api/alembic/versions/0007_board_items_link_fields.py, api/tests/integration/test_capture_async_scrape.py</files>
  <precondition>Task 1 resolved apply-0007 (local + Coolify); Wave 0 stubs from 05.2-00 exist</precondition>
  <reversibility rating="one-way">D-08 Alembic VIEW DROP/CREATE — undo requires another revision; gated by Task 1 checkpoint (RESOLVED apply-0007 dual-DB).</reversibility>
  <read_first>
    - api/app/services/timeout.py (DraftTimeoutManager cancel-then-spawn + RLS Session pattern)
    - api/app/services/scraper.py (validate_url_ssrf, ScrapeResult, scrape())
    - api/app/routers/capture.py (_insert_draft link branch, create_draft commit + timeout_manager.schedule_timeout, list_board_items SELECT)
    - api/app/routers/links.py (`_links_category_id` SELECT name='Links' — extract pattern for Phase 1 D-11)
    - api/alembic/versions/0006_board_color_sortorder.py (BOARD_ITEMS_VIEW + GRANT pattern)
    - api/app/models/board.py (BoardItem fields)
    - 05.2-PATTERNS.md link_scrape.py + 0007 sections
    - 05.2-CONTEXT.md D-01, D-02, D-03, D-05, D-08, D-17, D-20, D-24
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md Phase 1 D-11 (hostname + failed + category Links)
  </read_first>
  <behavior>
    - test_draft_link_returns_immediately_without_awaiting_scrape: POST /drafts type=link returns 200/201 body keys id+type before scrape completes; response time does not wait on Firecrawl (mock scrape with asyncio.sleep delay; assert elapsed &lt; soft timeout).
    - test_async_scrape_writes_metadata_and_scrape_status: after awaiting scrape task / polling, Link row has metadata_ with title/description/image and scrape_status in {ok, partial, failed, timed_out}; GET /board-items for that id has summary == url and image matching metadata image (D-05).
    - test_async_scrape_fail_or_timeout_sets_hostname_and_links_category: mock scrape failure OR TimeoutError; after job settles, Link.title is hostname/URL fallback, scrape_status in {failed, timed_out}, and category_id equals seeded Links category id (D-02 → Phase 1 D-11).
  </behavior>
  <action>
    Checkpoint already resolved apply-0007 for BOTH local and Coolify: create api/alembic/versions/0007_board_items_link_fields.py with revision id `0007_board_items_link_fields`, down_revision `0006_board_color_sortorder`. upgrade(): DROP VIEW IF EXISTS board_items; CREATE VIEW with all four UNION branches including NEW columns image and scrape_status — link branch: `url AS summary`, `(metadata->>'image') AS image`, `scrape_status`; note/task/event branches: keep existing summary, `NULL::text AS image`, `NULL::text AS scrape_status`. GRANT SELECT ON board_items TO puzzlessbox_app. downgrade(): DROP VIEW; recreate exact 0006 BOARD_ITEMS_VIEW shape (no image/scrape_status columns) + GRANT (D-08). Apply with `alembic upgrade head` against local DATABASE_URL, then against Coolify prod DSN (DATABASE_URL_COOLIFY or Coolify `execute_command` in API container) — never commit connection strings. Confirm on both DBs: `\d+ board_items` / SQL shows summary/image/scrape_status.
    Extract `_links_category_id(db)` from links.py into shared `api/app/services/categories.py` as `links_category_id(db) -> uuid.UUID` (same SELECT name='Links'; RuntimeError if missing). Update links.py create_link to import that helper — no duplicated SQL. Services must not import from routers.
    Extend ScrapeStatus Literal in scraper.py to include pending|scraping|ok|partial|timed_out|failed|skipped (D-20). Keep validate_url_ssrf at start of scrape(). For tracer, map existing ok/failed results; treat title-without-image as partial (discretion). Soft-timeout default: 12s within 8–15s band (D-02 discretion).
    Create api/app/services/link_scrape.py: class LinkScrapeManager with `_active_tasks: Dict[str, asyncio.Task]`, methods schedule_scrape(link_id, owner_id, url), cancel_scrape(link_id), async _run_scrape_job — mirror timeout.py RLS set_config + SET LOCAL ROLE. Job: set scrape_status=scraping; asyncio.wait_for(scrape_service.scrape(url), timeout=12); write title/metadata_/scrape_status; on TimeoutError or terminal failed: set timed_out|failed + hostname fallback title AND set category_id = links_category_id(db) (D-02 → Phase 1 D-11). Export singleton scrape_manager. Tracer may implement single-attempt job; retries land in plan 02.
    Wire capture.py create_draft: after db.commit() for ItemType.link, call scrape_manager.schedule_scrape(str(row.id), owner_id, url) where url is the persisted link.url (D-01, D-17). Do not await the task. Response body remains `{id, type}` only (D-03).
    Extend BoardItem model with `image: str | None = None` and `scrape_status: str | None = None`. Extend list_board_items SELECT to include image, scrape_status.
    Remove xfail from the three tracer tests above; implement assertions including Links category_id on fail/timeout. Leave cancel/retry tests xfail for plan 02.
  </action>
  <verify>
    <automated>cd api && .venv/bin/pytest tests/integration/test_capture_async_scrape.py::test_draft_link_returns_immediately_without_awaiting_scrape tests/integration/test_capture_async_scrape.py::test_async_scrape_writes_metadata_and_scrape_status tests/integration/test_capture_async_scrape.py::test_async_scrape_fail_or_timeout_sets_hostname_and_links_category -x</automated>
  </verify>
  <acceptance_criteria>
    - File 0007_board_items_link_fields.py exists with down_revision == 0006_board_color_sortorder.
    - `rg -n "url AS summary" api/alembic/versions/0007_board_items_link_fields.py` matches ≥1.
    - `rg -n "schedule_scrape" api/app/routers/capture.py` matches ≥1.
    - `rg -n "links_category_id" api/app/services/categories.py api/app/services/link_scrape.py api/app/routers/links.py` matches ≥1 per file (shared helper wired).
    - BoardItem declares image and scrape_status.
    - Three tracer tests pass against Postgres integration fixtures; fail/timeout case asserts category_id == Links.
    - `alembic upgrade head` applied on local DATABASE_URL AND Coolify prod DB; both board_items VIEWs expose image + scrape_status.
    - `rg -n "remotePatterns" webapp/next.config.ts` unchanged from pre-task (no executor edits to that file this plan).
  </acceptance_criteria>
  <done>Link capture async scrape + VIEW + board API fields work end-to-end; fail/timeout applies Phase 1 D-11 Links category.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BoardCard uses item.image + hostname meta (B2 UI tracer)</name>
  <files>webapp/lib/api-client.ts, webapp/components/board/board-card.tsx, webapp/tests/board-link-scrape.test.tsx</files>
  <read_first>
    - webapp/components/board/board-card.tsx (current summary.startsWith http hack lines ~56–110)
    - webapp/lib/api-client.ts (BoardItem interface)
    - brand/tokens.css (muted colors)
    - 05.2-CONTEXT.md D-06, D-07, D-25, D-29, D-30, D-31, D-32
    - .agents/skills/hallmark/SKILL.md (if present — anti-slop)
  </read_first>
  <behavior>
    - BoardCard with type=link and item.image set renders img whose src equals item.image.
    - BoardCard with type=link and summary=https://example.com/x shows truncated hostname/URL text, not using summary as image src.
    - Missing image shows muted placeholder (Link2 or equivalent); no layout jump on img onError.
  </behavior>
  <action>
    Extend BoardItem in api-client.ts with `image?: string | null` and `scrape_status?: string | null` (D-05, D-21).
    In board-card.tsx: delete the thumbnailUrl = summary.startsWith("http") heuristic. Use `item.image` for thumb. Render native `<img>` (not next/image) with `referrerPolicy="no-referrer"` and onError → show muted placeholder state (D-06, D-29, D-30, D-31). Meta line: truncated hostname from `item.summary` via URL parse fallback to summary truncate (D-07). Keep Hallmark/Emil subtle craft — no loud badges (D-25). Spinner for pending/scraping can be stubbed visually if scrape_status present; full state matrix in plan 02.
    Convert corresponding it.todo cases in board-link-scrape.test.tsx to real tests for image src + no summary-as-image.
  </action>
  <verify>
    <automated>cd webapp && pnpm exec vitest run tests/board-link-scrape.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "summary\.startsWith\([\"']http" webapp/components/board/board-card.tsx` returns 0 matches.
    - `rg -n "referrerPolicy=\"no-referrer\"" webapp/components/board/board-card.tsx` matches ≥1.
    - BoardItem type includes image and scrape_status.
    - Vitest board-link-scrape image cases pass.
  </acceptance_criteria>
  <done>Board shows OG thumb from item.image and URL meta from summary (B2 UI tracer).</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / File | Plan |
|---------------|------|
| `LinkScrapeManager` / `scrape_manager` | 05.2-01 |
| `links_category_id` / `api/app/services/categories.py` | 05.2-01 |
| `0007_board_items_link_fields.py` | 05.2-01 |
| `BoardItem.image`, `BoardItem.scrape_status` | 05.2-01 |
| BoardCard native OG `<img>` | 05.2-01 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /drafts link URL | Untrusted URL enters scrape pipeline |
| Scrape worker → egress HTTP | SSRF risk to internal networks |
| API → board poll client | image URLs are third-party; must not expand Next optimizer allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.2-01 | Information Disclosure (SSRF) | scrape_service.scrape / LinkScrapeManager | high | mitigate | Call validate_url_ssrf(url) before any outbound fetch; reuse existing scraper guard; tests cover private IP rejection via existing scraper suite. |
| T-05.2-02 | Tampering | board_items VIEW migration | medium | mitigate | New Alembic revision only; GRANT SELECT to puzzlessbox_app; human checkpoint before apply (D-08). |
| T-05.2-03 | Information Disclosure | next/image optimizer | high | mitigate | Native img for OG; leave webapp/next.config.ts remotePatterns unchanged (D-32). |
| T-05.2-SC | Tampering | npm/pip installs | high | accept | No new packages; reuse installed httpx/scraper stack. |
</threat_model>

<verification>
- Happy-path async scrape integration tests green.
- Board-link Vitest image cases green.
- `bash scripts/test.sh` green.
- Optional human-check (end-of-phase): board shows URL meta + thumb after link capture.
</verification>

<success_criteria>
- B1/B2 tracer path works: draft→async scrape→board image/summary.
- D-08 migration applied only after apply-0007 decision.
- LINK-01/02 happy path observable without calendar work.
</success_criteria>

<output>
Create `.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-01-SUMMARY.md` when done.
</output>
