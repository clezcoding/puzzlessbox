---
phase: 05.2-fix-link-cal-integration-wiring-b1-b4
plan: 03
type: execute
wave: 3
depends_on: ["05.2-01", "05.2-02"]
files_modified:
  - api/app/services/calendar.py
  - api/app/services/timeout.py
  - api/app/routers/capture.py
  - api/app/routers/items.py
  - webapp/components/board/item-modal.tsx
  - webapp/lib/api/items.ts
  - webapp/lib/api-client.ts
  - webapp/tests/modal.test.tsx
  - api/tests/integration/test_calendar_sync_wiring.py
autonomous: true
requirements: [CAL-02, CAL-03]
user_setup:
  - service: google_calendar
    why: "Live soft-fail UX check needs Connected calendar; automated tests mock Google"
    env_vars: []
    dashboard_config:
      - task: "Optional end-of-phase: connect Google in Settings wizard for manual CAL-02 UAT"
        location: "WebApp /settings Calendar"

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low  # sample_count=0; leave low until calibration samples exist

must_haves:
  truths:
    - "On first transition to auto_saved or confirmed, event with null google_event_id calls sync_local_event_to_google which UPDATES the existing Event row (never inserts a second Event) (D-09)."
    - "When not Connected or no calendar_id, local event stays board-visible and Google is skipped (D-10)."
    - "Google create errors soft-fail: local status committed, google_event_id stays null, Hermes/confirm still succeeds (D-11)."
    - "POST /events remains available and can share sync helper (D-12)."
    - "PATCH /items for events pushes only title, summary/body, starts_at, ends_at; category_id/sort_order stay local (D-13)."
    - "PATCH uses stored etag via update_event_with_etag; 412 returns CONCURRENCY_CONFLICT with etag+remote_state; force via If-None-Match: * (D-14)."
    - "PATCH without google_event_id create-on-edit when Connected (D-15)."
    - "Soft-delete with google_event_id calls Google events.delete; restore is local-only (D-16)."
    - "Modal: discreet sync-miss line; CTA Mit Google synchronisieren when Connected else Settings pointer; Vorschau erneut laden for scrape fails; toast only on manual retry fail; silent success (D-23, D-25, D-26, D-27, D-28)."
    - "412 conflict panel continues to work (BOARD-04); no Google token leakage in API error bodies."
  artifacts:
    - api/app/services/calendar.py
    - api/app/routers/items.py
    - webapp/components/board/item-modal.tsx
  key_links:
    - "confirm_draft / timeout autosave -> sync_local_event_to_google(existing Event)"
    - "PATCH /items -> update_event_with_etag | create-on-edit -> 412 concurrency_conflict"
    - "DELETE /items -> events.delete when google_event_id set"
    - "item-modal -> rescrape API + sync CTA + existing conflict panel"
  prohibitions:
    - "Do not deprecate POST /events (D-12)"
    - "Do not auto-recreate Google event on restore (D-16)"
    - "Do not put Google OAuth tokens in 412/remote_state payloads"
    - "Do not show card-level Google sync badges (D-23)"
---

<objective>
Wire B3+B4: Google Calendar create on board-visible transition, PATCH/DELETE sync with ETag/412, and modal retry/sync CTAs (including scrape retry UI) so CAL-02/03 and BOARD-04 conflict UX work on the Hermes path.

Purpose: Close remaining milestone audit blockers after link tracer/expansion.
Output: Green test_calendar_sync_wiring.py + modal Vitest; capture/timeout/items/calendar services wired.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-CONTEXT.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-RESEARCH.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-PATTERNS.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-01-SUMMARY.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-02-SUMMARY.md
@.planning/v1.0-MILESTONE-AUDIT.md

@api/app/services/calendar.py
@api/app/services/timeout.py
@api/app/routers/capture.py
@api/app/routers/items.py
@api/app/routers/events.py
@webapp/components/board/item-modal.tsx
@webapp/lib/api/items.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sync_local_event_to_google on confirm + autosave (CAL-02 / B3)</name>
  <files>api/app/services/calendar.py, api/app/services/timeout.py, api/app/routers/capture.py, api/tests/integration/test_calendar_sync_wiring.py</files>
  <read_first>
    - api/app/services/calendar.py (create_event, connection_status, _calendar_service)
    - api/app/routers/events.py (create_event call site — keep)
    - api/app/routers/capture.py (confirm_draft)
    - api/app/services/timeout.py (_execute_autosave commit)
    - api/app/models/event.py (google_event_id, etag)
    - 05.2-CONTEXT.md D-09, D-10, D-11, D-12
    - 05.2-RESEARCH.md Code Example sync_local_event_to_google
  </read_first>
  <behavior>
    - test_confirm_draft_event_syncs_existing_row_google_event_id: confirm event draft while Connected → same Event.id gains google_event_id+etag; row count events unchanged.
    - test_autosave_event_syncs_when_google_connected: timeout autosave to auto_saved triggers sync when google_event_id null.
    - test_google_create_soft_fail_keeps_local_board_visible: Google insert raises → status still confirmed/auto_saved, google_event_id null, HTTP confirm still 200.
  </behavior>
  <action>
    Add `sync_local_event_to_google(db, owner_id, event: Event) -> Event` on calendar_service (or module function). Rules (D-09–D-11): if google_event_id already set → return (idempotent); if connection_status not connected or selected_calendar_id missing → return unchanged (D-10); else events().insert into selected calendar; write google_event_id+etag onto THE SAME event instance; on exception record sync failure (discretion: metadata or sync_error field) leave google_event_id null; never db.add(new Event(...)).
    Call sync after successful confirm_draft status flip for ItemType.event (after commit or in same session before response — ensure local status committed even if Google fails per D-11). Call sync from timeout.py after successful autosave UPDATE for events.
    Keep POST /events using create_event or refactor to shared insert helper without removing the route (D-12).
    Un-xfail the three Wave 0 tests above.
  </action>
  <verify>
    <automated>cd api && .venv/bin/pytest tests/integration/test_calendar_sync_wiring.py -k "confirm_draft or autosave or soft_fail" -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "sync_local_event_to_google" api/app/services/calendar.py api/app/routers/capture.py api/app/services/timeout.py` matches in all three areas (definition + both call sites).
    - `rg -n "@router.post\(\"/events\"" api/app/routers/events.py` still present.
    - Three named tests pass; assert no second Event row created on sync.
  </acceptance_criteria>
  <done>B3: board-visible event transitions push Google without breaking local capture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH/DELETE /items Google sync + 412 (CAL-03 / B4)</name>
  <files>api/app/routers/items.py, api/app/services/calendar.py, api/tests/integration/test_calendar_sync_wiring.py</files>
  <read_first>
    - api/app/routers/items.py (update_item, delete_item, restore_item, ItemUpdate, _update_item_fields)
    - api/app/routers/events.py (etag PATCH body mapping lines ~110–126)
    - api/app/services/calendar.py (update_event_with_etag, concurrency_conflict)
    - webapp/lib/api/items.ts (If-None-Match: * force header)
    - 05.2-CONTEXT.md D-13, D-14, D-15, D-16
    - 05.2-RESEARCH.md Pitfall 3
  </read_first>
  <behavior>
    - test_patch_items_event_etag_412_returns_concurrency_conflict: stale etag → 412 error.code CONCURRENCY_CONFLICT with details.etag + remote_state; response JSON must not contain refresh_token or access_token substrings.
    - test_patch_items_create_on_edit_when_no_google_event_id: Connected + null google_event_id → sync/create on existing row.
    - test_delete_item_with_google_event_id_calls_events_delete: soft-delete invokes Google delete; restore_item does not call insert.
  </behavior>
  <action>
    After local event field update in update_item: if item is event and calendar fields changed (title/summary/starts_at/ends_at only — D-13), then: if google_event_id set → update_event_with_etag using stored row.etag (or If-None-Match: * when request header is `*` for force overwrite per D-14 / existing client); if google_event_id null → sync_local_event_to_google create-on-edit when Connected (D-15). Let HTTPException 412 from concurrency_conflict propagate unchanged shape. category_id/sort_order updates skip Google.
    On delete_item: if event has google_event_id, call calendar delete helper (add delete_remote_event if missing) then soft-delete local (D-16). restore_item: local only — no Google insert.
    Un-xfail remaining calendar_sync_wiring tests.
  </action>
  <verify>
    <automated>cd api && .venv/bin/pytest tests/integration/test_calendar_sync_wiring.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All six tests in test_calendar_sync_wiring.py pass.
    - 412 body uses code CONCURRENCY_CONFLICT.
    - `rg -n "access_token|refresh_token" api/app/routers/items.py` returns 0 in response construction paths.
    - Force overwrite reads header If-None-Match equal to `*`.
  </acceptance_criteria>
  <done>B4 PATCH/DELETE calendar wiring + 412 contract green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Item modal — scrape retry + Google sync CTAs + OG img (UI)</name>
  <files>webapp/components/board/item-modal.tsx, webapp/lib/api/items.ts, webapp/lib/api-client.ts, webapp/tests/modal.test.tsx, webapp/tests/board-link-scrape.test.tsx</files>
  <read_first>
    - webapp/components/board/item-modal.tsx (conflict panel, og-preview Image usage)
    - webapp/lib/api/items.ts (updateItem force / 412)
    - brand/VOICE.md
    - 05.2-CONTEXT.md D-20, D-23, D-25, D-26, D-27, D-28, D-29, D-30, D-31
    - webapp/tests/modal.test.tsx
  </read_first>
  <behavior>
    - Link modal shows German scrape status copy (never raw enum); button label exactly „Vorschau erneut laden“ when status in failed|timed_out|partial; calls rescrape API; toast only on failure after manual retry; success silent.
    - Event modal without google_event_id shows discreet sync-miss line; button „Mit Google synchronisieren“ when Connected else Settings calendar pointer (D-27).
    - OG preview uses native img referrerPolicy no-referrer + onError placeholder (D-29–D-31).
    - Existing conflict-panel still renders for 412 (BOARD-04).
  </behavior>
  <action>
    Replace next/image in og-preview with native img (D-29). Add German label map for scrape_status (D-20). Wire „Vorschau erneut laden“ → POST rescrape from plan 02 (D-26). Wire „Mit Google synchronisieren“ → PATCH that triggers create-on-edit or dedicated sync call; if not Connected, link/copy pointing to /settings calendar wizard (D-27). Toast only on manual retry failure (D-28). No success toast. No card badge work here (D-23). Preserve conflict panel + saveWithForce If-None-Match behavior. Extend BoardItem client type with google_event_id if board poll exposes it — if VIEW lacks google_event_id, use detail fetch or extend board SELECT for events only (discretion: nullable field on BoardItem for events so modal can detect sync miss without N+1 per card — prefer adding google_event_id to VIEW event branch NULL for non-events, or pass from item detail; do not N+1 per card on board list).
    Add/extend modal.test.tsx cases for CTA labels and conflict panel regression.
  </action>
  <verify>
    <automated>cd webapp && pnpm exec vitest run tests/modal.test.tsx tests/board-link-scrape.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Vorschau erneut laden" webapp/components/board/item-modal.tsx` matches ≥1.
    - `rg -n "Mit Google synchronisieren" webapp/components/board/item-modal.tsx` matches ≥1.
    - `rg -n "from \"next/image\"" webapp/components/board/item-modal.tsx` returns 0 OR remaining Image imports unused for OG (OG path must be native img).
    - `rg -n "referrerPolicy=\"no-referrer\"" webapp/components/board/item-modal.tsx` matches ≥1.
    - data-testid=conflict-panel still present.
    - Modal tests pass.
  </acceptance_criteria>
  <done>Modal scrape + Google soft-fail UX and 412 panel complete (B2/B4 UI).</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / File | Plan |
|---------------|------|
| `sync_local_event_to_google` | 05.2-03 |
| `delete_remote_event` (if added) | 05.2-03 |
| items.py PATCH/DELETE Google wiring | 05.2-03 |
| ItemModal CTAs + native OG img | 05.2-03 |

### Phase-wide artifact rollup (01–03)

| Symbol | File |
|--------|------|
| `LinkScrapeManager` / `scrape_manager` | api/app/services/link_scrape.py |
| `apply_scrape_to_link` | api/app/services/scraper.py or link_scrape.py |
| `0007_board_items_link_fields` | api/alembic/versions/0007_board_items_link_fields.py |
| `POST /links/{id}/rescrape` | api/app/routers/links.py |
| `sync_local_event_to_google` | api/app/services/calendar.py |
| BoardItem `image`, `scrape_status` (+ optional `google_event_id`) | api/app/models/board.py + webapp/lib/api-client.ts |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Google Calendar API | Encrypted tokens decrypt only server-side; never echo to clients |
| WebApp → PATCH /items | Untrusted field updates; calendar sync must not leak secrets in 412 |
| Modal → rescrape / sync CTAs | Owner session only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.2-06 | Information Disclosure | calendar sync / 412 detail | high | mitigate | concurrency_conflict remote_state contains calendar event fields only (title/times/etag); never access_token/refresh_token; test asserts absence. |
| T-05.2-07 | Tampering | PATCH force overwrite | medium | mitigate | Force only when If-None-Match is `*`; default path uses stored etag via update_event_with_etag. |
| T-05.2-08 | Elevation of Privilege | Google token storage | high | mitigate | Reuse existing encrypt_token path; no new plaintext token logging; soft-fail errors store generic sync_error strings. |
| T-05.2-01 | SSRF | rescrape from modal | high | mitigate | Rescrape still goes through scrape_service.validate_url_ssrf. |
| T-05.2-SC | Tampering | package installs | high | accept | No new Google packages; already installed [SUS]/[OK] per RESEARCH legitimacy audit. |
</threat_model>

<verification>
- Full test_calendar_sync_wiring.py green.
- modal + board-link-scrape Vitest green.
- `bash scripts/test.sh` green.
- `cd api && .venv/bin/pytest` wave gate before verify-work.
- human-check (end-of-phase): Connected calendar create; 412 panel; scrape retry CTA.
</verification>

<success_criteria>
- CAL-02/03 wired on Hermes capture + board edit paths.
- BOARD-04 412 UX preserved/extended.
- B1–B4 audit blockers closable after UAT.
- All D-09..D-16 and D-23..D-28 UI decisions satisfied.
</success_criteria>

<output>
Create `.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-03-SUMMARY.md` when done.
</output>
