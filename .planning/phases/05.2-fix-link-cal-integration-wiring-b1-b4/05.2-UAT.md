---
status: complete
phase: 05.2-fix-link-cal-integration-wiring-b1-b4
source: [05.2-VERIFICATION.md]
started: 2026-08-08T20:08:01Z
updated: 2026-08-09T09:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Coolify deploy + WEBAPP_URL
expected: API+webapp redeployed with 05.2-04 commits; Coolify API env WEBAPP_URL=https://pbox.puzzlesstool.online (matches Settings host)
result: pass
evidence: |
  PR #64 merged (Dockerfile standalone path). Coolify deploy wigafet0136g5bqplp75zmws status=finished, New container is healthy.
  WEBAPP_URL=https://pbox.puzzlesstool.online on API. GET https://pbox.puzzlesstool.online/api/health → {"status":"ok"}

### 2. G-05.2-2 retest: Settings → Connect Google
expected: No 415/UNAUTHORIZED JSON page; browser reaches Google consent; after OAuth lands on /settings
result: pass
evidence: |
  User confirmed 2026-08-09: Connect Google reaches consent and returns to /settings after OAuth.

### 3. Connected calendar create + 412 conflict panel
expected: Confirm event with Google Connected → google_event_id set; remote conflict → conflict panel; scrape retry CTA still works
result: pass
step1: pass
evidence_step1: |
  Event `49604d18-1f4c-48bb-8adf-6420ae119fd8` (UAT-A2 Note EDITED, type=event).
  Title-only sync failed: Google 400 "Missing end time" (starts_at/ends_at null; API logs).
  PATCH with event_start+event_end → google_event_id=`7iulr3qi2mcemogk95edp59034`, etag set.
  Modal reopened: sync-miss panel gone.
  Note: UI "Mit Google synchronisieren" alone insufficient without times; type-change note→event had no schedule.
step2: pass
evidence_step2: |
  Staled DB etag to "stale-uat-etag" on synced event.
  Title edit → blur → conflict-panel with Übernehmen/Behalten/Abbrechen.
  items/... returned 412. Local shows "UAT conflict local title".
  Note: Google side title shows "—" (API remote_state uses Google `summary`, UI reads `title`).
step3: pass
evidence_step3: |
  Set links.scrape_status=failed on UAT-A2 Link. Modal shows "Vorschau erneut laden".
  Click → `/links/.../rescrape` 202. scrape_status → partial; UI "Vorschau unvollständig".
prior: |
  Was blocked by OAuth connect; unblocked after test 2 pass.

### Prior (already passed)

### P1. Board link thumb after Hermes capture
expected: Send link via Hermes; board shows hostname meta + OG thumb (or spinner→image)
result: pass
source: automated

### P2. Integration suite green
expected: Wave 0 integration files pass (12 tests)
result: pass
source: automated

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-05.2-2
  truth: "Settings → Connect Google without 415; callback → /settings; then Connected calendar + 412 panel"
  status: resolved
  reason: "Connect Google + google_event_id create + 412 conflict panel + scrape retry verified on prod 2026-08-09."
  severity: blocker
  test: 3
  artifacts:
    - path: "webapp/Dockerfile"
    - path: "webapp/components/settings/calendar-wizard.tsx"
    - path: "webapp/lib/api/calendar.ts"
    - path: "api/app/routers/calendar.py"
  debug_session: ".planning/debug/google-oauth-accept-header.md"
