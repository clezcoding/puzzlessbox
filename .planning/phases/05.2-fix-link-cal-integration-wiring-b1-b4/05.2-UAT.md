---
status: diagnosed
phase: 05.2-fix-link-cal-integration-wiring-b1-b4
source: [05.2-VERIFICATION.md, COVERAGE.md]
started: 2026-08-08T20:08:01Z
updated: 2026-08-09T05:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Board link thumb after Hermes capture
expected: Send link via Hermes; board shows hostname meta + OG thumb (or spinner→image)
result: pass
source: automated
evidence: |
  OrbStack stack healthy (:8000/:8001/:5433). Webapp :3000.
  Board Links column: "Example Domain" + meta "example.com"; native <img referrerPolicy=no-referrer> OG thumb.
  "example.org" scraping state showed spinner "Vorschau wird geladen".
  Failed link modal shows "Vorschau erneut laden" CTA.
  Note: Hermes send not available locally — board display of scrape_status/image verified via seeded post-scrape rows + integration suite for async scrape wiring.

### 2. Connected calendar create + 412 panel
expected: Confirm event with Google Connected; edit conflicting remote → conflict panel; scrape retry CTA
result: issue
reported: "Wenn ich mich auf der prod/coolify url anmelde und in den settings den google calender verbinden möchte werde ich auf diese error seite geleitet — https://api.puzzlestool.online/auth/google/connect → {\"error\":{\"code\":\"UNSUPPORTED_MEDIA_TYPE\",\"message\":\"Accept header must include application/vnd.puzzlessbox.v1+json\"}}"
severity: blocker

### 3. Integration suite green
expected: DATABASE_URL=postgresql://…; both Wave 0 integration files pass (12 tests)
result: pass
source: automated
evidence: |
  DATABASE_URL=postgresql+psycopg2://puzzless:puzzless@localhost:5433/puzzlessbox
  PATH includes api/.venv/bin (alembic)
  pytest tests/integration/test_capture_async_scrape.py tests/integration/test_calendar_sync_wiring.py -v
  → 12 passed in 18.05s

## Summary

total: 3
passed: 2
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-05.2-2
  truth: "Confirm event with Google Connected; edit conflicting remote → conflict panel; scrape retry CTA"
  status: failed
  reason: "User reported: Wenn ich mich auf der prod/coolify url anmelde und in den settings den google calender verbinden möchte werde ich auf diese error seite geleitet — https://api.puzzlestool.online/auth/google/connect → {\"error\":{\"code\":\"UNSUPPORTED_MEDIA_TYPE\",\"message\":\"Accept header must include application/vnd.puzzlessbox.v1+json\"}}"
  severity: blocker
  test: 2
  root_cause: "AcceptVersionMiddleware requires application/vnd.puzzlessbox.v1+json on all non-skip paths; Settings starts Google OAuth via window.location to /auth/google/connect (no Accept header), so API returns 415 before OAuth. Same gate blocks /auth/google/callback. AND-gate: middleware skip-list omits /auth/google/* + bare browser navigation bypasses apiFetch (Accept+Bearer)."
  artifacts:
    - path: "api/app/main.py"
      issue: "_VERSION_SKIP_PREFIXES omits /auth/google/connect and /auth/google/callback"
    - path: "webapp/components/settings/calendar-wizard.tsx"
      issue: "handleConnect uses window.location.href without Accept/Bearer"
    - path: "webapp/lib/api/calendar.ts"
      issue: "getCalendarConnectUrl builds bare connect URL"
    - path: "api/app/routers/calendar.py"
      issue: "connect 302 + callback also behind Accept gate"
  missing:
    - "Exempt /auth/google/connect and /auth/google/callback from AcceptVersionMiddleware"
    - "Prefer start connect via authenticated apiFetch (Bearer) then redirect — bare window.location may 401 next (JWT cookie not on API host)"
    - "Callback should redirect to webapp Settings, not return JSON to Google browser redirect"
  debug_session: ".planning/debug/google-oauth-accept-header.md"
