---
phase: 05.2-fix-link-cal-integration-wiring-b1-b4
plan: 02
type: execute
wave: 2
depends_on: ["05.2-01"]
files_modified:
  - api/app/services/link_scrape.py
  - api/app/services/scraper.py
  - api/app/services/categories.py
  - api/app/routers/links.py
  - api/app/routers/capture.py
  - webapp/components/board/board-card.tsx
  - webapp/tests/board-link-scrape.test.tsx
  - api/tests/integration/test_capture_async_scrape.py
autonomous: true
requirements: [LINK-01, LINK-02]
user_setup: []

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low  # sample_count=0; leave low until calibration samples exist

must_haves:
  truths:
    - "Exactly one in-flight scrape per link_id; schedule_scrape cancels prior task (D-19)."
    - "Scrape job performs exactly 2 auto-retries on transient failure then terminal scrape_status (D-26)."
    - "apply_scrape_to_link on failed|timed_out sets hostname fallback title and category_id via links_category_id(db) (D-02 → Phase 1 D-11)."
    - "POST /links still works and shares apply_scrape_to_link / scrape_service path with capture (D-04)."
    - "Manual rescrape endpoint schedules scrape_manager for failed|timed_out|partial links (D-26)."
    - "BoardCard: spinner for pending/scraping; muted placeholder for timed_out/failed; image for ok/partial when present; no loud badges; no raw scrape_status codes in DOM (D-20, D-22)."
    - "Process restart may leave pending — accepted; manual retry recovers (D-18)."
  artifacts:
    - api/app/services/link_scrape.py
    - api/app/routers/links.py
    - webapp/components/board/board-card.tsx
  key_links:
    - "POST /links and scrape_manager both call shared apply helper"
    - "apply_scrape_to_link failed|timed_out -> links_category_id -> Link.category_id Links"
    - "POST /links/{id}/rescrape (or equivalent) -> scrape_manager.schedule_scrape"
    - "Board poll scrape_status -> BoardCard thumb slot state machine"
  prohibitions:
    - "No durable job table / startup replay (D-18 deferred)"
    - "No external queue brokers (D-17)"
    - "Do not show raw scrape_status enum strings in UI (D-20)"
---

<objective>
Expand B1/B2 beyond the tracer: full scrape job mechanics (cancel, retries, shared POST /links), rescrape API, and BoardCard scrape_status visual states per D-19–D-22/D-26.

Purpose: Production-harden async scrape and board feedback without calendar scope.
Output: Green Wave 0 cancel/retry tests + BoardCard state Vitest; shared scrape apply used by POST /links.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-CONTEXT.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-RESEARCH.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-PATTERNS.md
@.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-01-SUMMARY.md

@api/app/services/link_scrape.py
@api/app/services/scraper.py
@api/app/routers/links.py
@api/app/routers/capture.py
@webapp/components/board/board-card.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scrape job — single-flight, 2 retries, shared apply helper</name>
  <files>api/app/services/link_scrape.py, api/app/services/scraper.py, api/app/routers/links.py, api/tests/integration/test_capture_async_scrape.py</files>
  <read_first>
    - api/app/services/link_scrape.py (tracer manager from plan 01)
    - api/app/services/scraper.py
    - api/app/services/categories.py (links_category_id from plan 01)
    - api/app/routers/links.py (full POST /links handler)
    - api/app/services/timeout.py (cancel-then-spawn)
    - 05.2-CONTEXT.md D-02, D-04, D-17, D-18, D-19, D-20, D-26
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md Phase 1 D-11
    - 05.2-RESEARCH.md Pitfall 1 + Open Questions (RESOLVED) 2.0s backoff
  </read_first>
  <behavior>
    - test_schedule_scrape_cancels_prior_inflight_for_same_link_id: second schedule_scrape for same link_id cancels first task; only one active entry in _active_tasks.
    - test_scrape_job_retries_transient_failure_twice: mock scrape raises transient then succeeds on 3rd attempt (1 initial + 2 retries) OR ends failed after 2 retries; assert attempt count == 3 max and final scrape_status terminal.
    - test_async_scrape_fail_or_timeout_sets_hostname_and_links_category still green after apply_scrape_to_link refactor (category_id Links + hostname title on failed|timed_out).
  </behavior>
  <action>
    Add shared helper `apply_scrape_to_link(db, link_id, owner_id, result: ScrapeResult | ...)` (or equivalent name) in scraper.py or link_scrape.py that writes title, metadata_ JSON (title/url/description/image), scrape_status — used by LinkScrapeManager and POST /links (D-04). When final scrape_status is failed or timed_out: set hostname/URL fallback title and set category_id = links_category_id(db) from api/app/services/categories.py (D-02 → Phase 1 D-11). Do not leave draft Inbox category on terminal fail/timeout.
    Expand LinkScrapeManager._run_scrape_job: set pending→scraping; on transient failure sleep backoff 2.0s (RESOLVED discretion) and retry until exactly 2 auto-retries consumed (D-26); map TimeoutError→timed_out; empty title→failed with hostname fallback; title without image→partial (discretion); route all terminal writes through apply_scrape_to_link so fail/timeout always gets Links category_id. Ensure schedule_scrape cancel-then-spawn keyed by link_id (D-19). Clear task from map in finally when done.
    Refactor links.py create_link to call shared apply helper after scrape_service.scrape (keep route; do not remove POST /links).
    Un-xfail the cancel/retry Wave 0 tests and keep fail-category test green after refactor.
  </action>
  <verify>
    <automated>cd api && .venv/bin/pytest tests/integration/test_capture_async_scrape.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All scrape tests in test_capture_async_scrape.py pass (no xfail remaining), including test_async_scrape_fail_or_timeout_sets_hostname_and_links_category.
    - `rg -n "apply_scrape_to_link|schedule_scrape" api/app/routers/links.py` shows shared path usage (≥1 match for apply or scrape_manager).
    - `rg -n "links_category_id" api/app/services/link_scrape.py api/app/services/scraper.py` shows ≥1 match in the apply helper module used on failed|timed_out.
    - POST /links route decorator `@router.post("/links"` still present.
    - Backoff/retry loop exists in link_scrape.py (_run_scrape_job) with 2.0s sleep.
  </acceptance_criteria>
  <done>Scrape manager production mechanics + shared POST /links path green; fail/timeout keeps Phase 1 D-11 Links category.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manual rescrape API for modal retry (backend)</name>
  <files>api/app/routers/links.py, api/app/routers/capture.py, api/tests/integration/test_capture_async_scrape.py</files>
  <read_first>
    - api/app/routers/links.py
    - api/app/services/link_scrape.py
    - 05.2-CONTEXT.md D-26
    - api/tests/conftest.py (auth headers)
  </read_first>
  <behavior>
    - test_rescrape_endpoint_schedules_scrape_for_owner_link: authenticated POST to rescrape path for own link returns 202 or 200 `{id, scrape_status: "pending"|"scraping"}` and schedules scrape_manager; foreign owner gets 404.
  </behavior>
  <action>
    Add owner-scoped endpoint e.g. `POST /links/{link_id}/rescrape` (prefer under links router) that loads Link by id+owner_id, sets scrape_status=pending, commits, calls scrape_manager.schedule_scrape(link_id, owner_id, link.url), returns quickly without awaiting scrape (D-26 backend half). Reject other owners with 404. Add integration test; keep capture.py unchanged unless a thin re-export is cleaner — prefer links.py only to avoid capture.py conflict with plan 03.
  </action>
  <verify>
    <automated>cd api && .venv/bin/pytest tests/integration/test_capture_async_scrape.py -k rescrape -x</automated>
  </verify>
  <acceptance_criteria>
    - Route exists: `rg -n "rescrape" api/app/routers/links.py` matches ≥1.
    - Test rescrape_endpoint passes.
    - Response does not include Firecrawl/Camoufox secrets or internal error stack traces.
  </acceptance_criteria>
  <done>Manual rescrape API ready for modal button in plan 03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BoardCard scrape_status thumb states (spinner / placeholder / image)</name>
  <files>webapp/components/board/board-card.tsx, webapp/tests/board-link-scrape.test.tsx</files>
  <read_first>
    - webapp/components/board/board-card.tsx (after plan 01)
    - brand/VOICE.md (tone — no raw codes)
    - 05.2-CONTEXT.md D-20, D-21, D-22, D-25
    - webapp/components/ui/ (existing Loader2 / muted patterns)
  </read_first>
  <behavior>
    - pending/scraping → spinner (Loader2 or equivalent) visible; prefers-reduced-motion respected (no infinite spin or CSS media query).
    - timed_out/failed → muted placeholder, no warning badge text.
    - ok/partial with image → img shown; without image → placeholder.
    - Document body / accessible text must not include raw tokens pending|scraping|timed_out|failed|partial|skipped as visible user copy.
  </behavior>
  <action>
    Implement D-22 state machine in BoardCard thumb slot. Use existing lucide icons + muted bg from tokens. Subtle transitions &lt;300ms ease-out; honor prefers-reduced-motion (D-25). Do not add card-level sync badges. Leave German modal copy and „Vorschau erneut laden“ button to plan 03 item-modal (card stays clean).
    Complete remaining board-link-scrape Vitest cases for spinner/placeholder/no-raw-code.
  </action>
  <verify>
    <automated>cd webapp && pnpm exec vitest run tests/board-link-scrape.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - All board-link-scrape.test.tsx cases pass (no it.todo left for card states).
    - Card render for scrape_status=failed does not display the substring timed_out or failed as user-visible text (queryByText null).
    - Spinner path exists for pending and scraping.
  </acceptance_criteria>
  <done>Board card scrape visual states match D-20/D-22.</done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol / File | Plan |
|---------------|------|
| `apply_scrape_to_link` (shared helper) | 05.2-02 |
| `POST /links/{id}/rescrape` | 05.2-02 |
| BoardCard scrape_status state machine | 05.2-02 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → rescrape endpoint | Must be owner-scoped; URL already stored — still runs validate_url_ssrf via scrape_service |
| Background scrape → egress | Same SSRF boundary as plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.2-04 | Spoofing | POST /links/{id}/rescrape | high | mitigate | get_db_for_owner + owner_id filter; 404 for non-owned ids; no cross-tenant scrape kickoff. |
| T-05.2-05 | Denial of Service | scrape retry loop | medium | mitigate | Cap at 2 auto-retries + single-flight per link_id (D-19/D-26); soft-timeout 8–15s. |
| T-05.2-01 | Information Disclosure (SSRF) | shared apply / scrape | high | mitigate | validate_url_ssrf on every scrape() call including rescrape path. |
| T-05.2-SC | Tampering | package installs | high | accept | No new packages. |
</threat_model>

<verification>
- Full test_capture_async_scrape.py green.
- board-link-scrape Vitest green.
- `bash scripts/test.sh` green.
</verification>

<success_criteria>
- B1 scrape mechanics complete (retries, single-flight, shared POST /links, rescrape API).
- B2 card states complete for scrape_status.
- LINK-01/02 expansion beyond tracer done.
</success_criteria>

<output>
Create `.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-02-SUMMARY.md` when done.
</output>
