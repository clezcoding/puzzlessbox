---
phase: 05.2
fixed_at: 2026-08-09T06:15:00Z
review_path: .planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-REVIEW.md
iteration: 1
findings_in_scope: 12
fixed: 12
skipped: 0
status: all_fixed
verification_env: main_checkout
use_worktrees: false
---

# Phase 05.2: Code Review Fix Report

**Fixed at:** 2026-08-09T06:15:00Z  
**Source review:** `.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-REVIEW.md`  
**Iteration:** 1  
**Verification env:** main checkout (`workflow.use_worktrees=false`)

**Summary:**
- Findings in scope: 12
- Fixed: 12
- Skipped: 0

## Fixed Issues

### CR-01: Restore after soft-delete leaves dangling `google_event_id`

**Files modified:** `api/app/routers/items.py`, `api/tests/integration/test_calendar_sync_wiring.py`  
**Commit:** `7ef46b5`  
**Status:** fixed: requires human verification  
**Applied fix:** Soft-delete for events now clears `google_event_id` and `etag` alongside `deleted_at`. Integration test asserts cleared linkage after delete/restore.

### WR-01: OAuth callback errors strand user on API host

**Files modified:** `api/app/routers/calendar.py`  
**Commit:** `f407159`  
**Status:** fixed  
**Applied fix:** Wrap `exchange_code` / `upsert_tokens` in try/except; on failure redirect to `{WEBAPP_URL}/settings?calendar=error`.

### WR-02: Client navigates to `authorization_url` without host allowlist

**Files modified:** `webapp/lib/api/calendar.ts`  
**Commit:** `7a34124`  
**Status:** fixed  
**Applied fix:** Parse URL and require hostname `accounts.google.com` before navigation; otherwise throw `INVALID_OAUTH_URL`.

### WR-03: Connect button allows double-submit

**Files modified:** `webapp/components/settings/calendar-wizard.tsx`  
**Commit:** `7515b92`  
**Status:** fixed  
**Applied fix:** Added `connecting` state, early-return guard, and `disabled={connecting}` on connect button.

### WR-04: `WEBAPP_URL` default survives prod without startup guard

**Files modified:** `api/app/core/config.py`  
**Commit:** `4943f20`  
**Status:** fixed  
**Applied fix:** `@model_validator` rejects localhost/`127.0.0.1` `WEBAPP_URL` when `ENV=prod`.

### WR-05: Modal Google sync CTA does not refresh `google_event_id`

**Files modified:** `webapp/components/board/item-modal.tsx`  
**Commit:** `45b8a91`  
**Status:** fixed: requires human verification  
**Applied fix:** After successful sync PATCH, refetch board items and pass updated `google_event_id` through `onUpdated`.

### WR-06: `sync_local_event_to_google` swallows all failures

**Files modified:** `api/app/services/calendar.py`  
**Commit:** `4fa7c46`  
**Status:** fixed  
**Applied fix:** Log exception via `logger.exception` before rollback/re-commit (local success + sync-miss UI unchanged).

### WR-07: SSRF check is TOCTOU vs scraper fetch

**Files modified:** `api/app/services/scraper.py`  
**Commit:** `614cae5`  
**Status:** fixed: requires human verification  
**Applied fix:** Re-run `validate_url_ssrf` immediately before Firecrawl/Camoufox handoff; reject Firecrawl metadata when final URL fails SSRF. Residual DNS-rebinding window remains while scrapers resolve hostnames themselves (`ponytail` comment documents upgrade path).

### WR-08: Draft link create leaves `scrape_status` NULL

**Files modified:** `api/app/routers/capture.py`  
**Commit:** `32dba3a`  
**Status:** fixed  
**Applied fix:** Set `scrape_status="pending"` on draft `Link` insert (matches `POST /links`).

### IN-01: Duplicate error branches in `handleConnect`

**Files modified:** `webapp/components/settings/calendar-wizard.tsx`  
**Commit:** `7515b92` (same commit as WR-03)  
**Status:** fixed  
**Applied fix:** Collapsed identical `ApiError`/generic catch branches into a single catch while adding the connect guard.

### IN-02: OAuth Accept-skip tests only assert `!= 415`

**Files modified:** `api/tests/unit/test_health.py`  
**Commit:** `82b4527`  
**Status:** fixed  
**Applied fix:** Assert connect → `401`, callback missing params → `422`. Unit tests passed.

### IN-03: Type-change to `event` does not trigger Google sync

**Files modified:** `api/app/routers/items.py`  
**Commit:** `78196db`  
**Status:** fixed: requires human verification  
**Applied fix:** Pass `ensure_remote=True` into `_sync_event_after_local_update` when type changes into `event`, so connected calendars get an insert without waiting for a later field edit.

---

_Fixed: 2026-08-09T06:15:00Z_  
_Fixer: Claude (gsd-code-fixer)_  
_Iteration: 1_
