---
phase: 05.2-fix-link-cal-integration-wiring-b1-b4
fixed_at: 2026-08-09T04:45:00Z
review_path: .planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-REVIEW.md
iteration: 1
findings_in_scope: 10
fixed: 10
skipped: 0
status: all_fixed
verification_env: main_checkout
---

# Phase 05.2: Code Review Fix Report

**Fixed at:** 2026-08-09T04:45:00Z
**Source review:** `.planning/phases/05.2-fix-link-cal-integration-wiring-b1-b4/05.2-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope: 10
- Fixed: 10
- Skipped: 0
- Verification: main checkout (`workflow.use_worktrees=false`); AST checks + inline SSRF asserts + `vitest run tests/modal.test.tsx`

## Fixed Issues

### CR-01: SSRF check misses IPv4-mapped IPv6 and `0.0.0.0`

**Files modified:** `api/app/services/scraper.py`
**Commit:** `6bf6d64`
**Status:** fixed
**Applied fix:** `_is_blocked_ip` unwraps `ipv4_mapped`, uses stdlib `is_private`/`is_loopback`/`is_link_local`/`is_reserved`/`is_multicast`/`is_unspecified`, plus explicit `0.0.0.0` guard.

### CR-02: Type-change away from event orphans Google Calendar event

**Files modified:** `api/app/routers/items.py`
**Commit:** `a175539`
**Status:** fixed: requires human verification
**Applied fix:** Before type change from `event`, call `delete_remote_event` when `google_event_id` set. Post-commit sync uses `effective_type == event` only.

### WR-01: Cancelled scrape leaves status `scraping`

**Files modified:** `api/app/services/link_scrape.py`
**Commit:** `cbbdd01`
**Status:** fixed: requires human verification
**Applied fix:** On `CancelledError`, if current task still owns slot, set status `pending`, then re-raise.

### WR-02: `discard_draft` does not cancel in-flight link scrape

**Files modified:** `api/app/routers/capture.py`
**Commit:** `8af94e1`
**Status:** fixed
**Applied fix:** After timeout cancel, call `scrape_manager.cancel_scrape(draft_id)` for link drafts.

### WR-03: `DraftTimeoutManager` unconditional `finally` pop races reschedule

**Files modified:** `api/app/services/timeout.py`
**Commit:** `4b3ca8d`
**Status:** fixed: requires human verification
**Applied fix:** `finally` pops `_active_tasks[draft_id]` only when `asyncio.current_task()` still owns the slot.

### WR-04: `POST /links` awaits scrape without soft timeout

**Files modified:** `api/app/routers/links.py`
**Commit:** `9113374`
**Status:** fixed
**Applied fix:** Wrap scrape in `asyncio.wait_for(..., SOFT_SCRAPE_TIMEOUT)`; on timeout set `timed_out` + hostname metadata.

### WR-05: BoardCard long-press timer recreated every render

**Files modified:** `webapp/components/board/board-card.tsx`
**Commit:** `9c5ec23`
**Status:** fixed
**Applied fix:** `longPressTimer` is `useRef<ReturnType<typeof setTimeout> | null>(null)`.

### WR-06: Event PATCH commits locally before Google sync; disconnect breaks updates

**Files modified:** `api/app/routers/items.py`
**Commit:** `11a51a3`
**Status:** fixed: requires human verification
**Applied fix:** In `_sync_event_after_local_update`, soft-skip remote update when not connected (mirrors `delete_remote_event`). Soft-skip logic first landed in `a175539`; `11a51a3` is the dedicated WR-06 commit.

### IN-01: Modal does not refresh OG image after rescrape

**Files modified:** `webapp/components/board/item-modal.tsx`, `webapp/tests/modal.test.tsx`
**Commit:** `c156e4d`
**Status:** fixed: requires human verification
**Applied fix:** After `rescrapeLink`, poll `getBoardItems` until terminal `scrape_status`, then `onUpdated` with title/image. Tests mock `getBoardItems`.

### IN-02: Link modal seeds Notizen from URL summary

**Files modified:** `webapp/components/board/item-modal.tsx`
**Commit:** `f51473c`
**Status:** fixed
**Applied fix:** For `type === "link"`, init `body` to `""` (board VIEW has no description field); URL stays in `url` from `summary`.

---

_Fixed: 2026-08-09T04:45:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
