---
phase: 05.2-fix-link-cal-integration-wiring-b1-b4
verified: 2026-08-08T20:07:07Z
status: human_needed
score: 7/12 must-haves verified
behavior_unverified: 5
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 6/12
  is_re_verification: true
  gaps_closed:
    - "GET /board-items BoardItem JSON includes optional image, scrape_status, and google_event_id fields (D-05, D-21)"
  gaps_remaining: []
  regressions: []
behavior_unverified_items:
  - truth: "POST /drafts type=link returns immediately; asyncio scrape writes metadata_ + scrape_status (LINK-01, D-01, D-03)"
    test: "Run pytest tests/integration/test_capture_async_scrape.py::test_draft_link_returns_immediately_without_awaiting_scrape and ::test_async_scrape_writes_metadata_and_scrape_status against postgresql:// Postgres"
    expected: "Immediate {id,type}; row gains title/description/image metadata + scrape_status ok|partial"
    why_human: "Code+wiring present; integration needs local Postgres — behavior not exercised this pass"
  - truth: "failed|timed_out scrape sets hostname title + Links category_id (LINK-02, D-02)"
    test: "pytest …::test_async_scrape_fail_or_timeout_sets_hostname_and_links_category"
    expected: "Terminal fail/timeout → hostname title + category Links"
    why_human: "apply_scrape_to_link + links_category_id wired; integration not run"
  - truth: "Single-flight cancel per link_id; exactly 2 auto-retries; rescrape schedules scrape_manager (D-19, D-26)"
    test: "pytest …::test_schedule_scrape_cancels_prior_inflight_for_same_link_id ::test_scrape_job_retries_transient_failure_twice ::test_rescrape_endpoint_schedules_scrape_for_owner_link"
    expected: "Cancel-then-spawn; max 3 attempts; POST /links/{id}/rescrape → 202 pending"
    why_human: "State transition / retry invariants need runtime test; Postgres required"
  - truth: "confirm/autosave calls sync_local_event_to_google on existing Event; soft-fail keeps local board-visible (CAL-02, D-09–D-11)"
    test: "pytest tests/integration/test_calendar_sync_wiring.py -k 'confirm_draft or autosave or soft_fail'"
    expected: "Same Event.id gains google_event_id; soft-fail → status confirmed, google_event_id null, HTTP 200"
    why_human: "Call sites in capture.py + timeout.py present; insert-in-place logic present; integration not run"
  - truth: "PATCH /items ETag 412 CONCURRENCY_CONFLICT + create-on-edit; DELETE calls events.delete (CAL-03, D-14–D-16)"
    test: "pytest tests/integration/test_calendar_sync_wiring.py -k 'etag_412 or create_on_edit or delete_item'"
    expected: "412 with etag+remote_state no tokens; create-on-edit when Connected; delete invokes Google delete"
    why_human: "items.py + update_event_with_etag wired; integration not run"
human_verification:
  - test: "Board link thumb after Hermes capture"
    expected: "Send link via Hermes; board shows hostname meta + OG thumb (or spinner→image)"
    why_human: "Live scrape + board poll against running API/Postgres"
  - test: "Connected calendar create + 412 panel"
    expected: "Confirm event with Google Connected; edit conflicting remote → conflict panel; scrape retry CTA"
    why_human: "Live Google OAuth + Connected calendar"
  - test: "Integration suite green"
    expected: "DATABASE_URL=postgresql://…; both Wave 0 integration files pass (12 tests)"
    why_human: "Needs local Postgres; unit gate intentionally skips integration"
---

# Phase 05.2: Fix LINK/CAL integration wiring B1-B4 — Verification Report

**Phase Goal:** Close audit blockers B1–B4 — wire existing scrape + Google Calendar into Hermes capture + board/edit UI so LINK-01/02 and CAL-02/03 work end-to-end (not only orphaned POST /links and POST /events).
**Verified:** 2026-08-08T20:07:07Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (commit `176d6e9`)

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| 1 | LINK-01: draft link returns immediately; async scrape writes metadata + scrape_status | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `capture.py:171-173` schedule after commit; `link_scrape.py` LinkScrapeManager + apply; integration needs Postgres |
| 2 | LINK-02: failed\|timed_out → hostname + Links category | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `apply_scrape_to_link` + `links_category_id` in `link_scrape.py`; integration not run |
| 3 | board_items VIEW: link url AS summary, metadata image, scrape_status; non-links NULL | ✓ VERIFIED | `0007_board_items_link_fields.py` BOARD_ITEMS_VIEW; regression OK |
| 4 | GET /board-items BoardItem JSON includes image, scrape_status, google_event_id | ✓ VERIFIED | `board.py:20` `str \| None`; `BoardItem.model_validate` OK; unit `test_board_item_optional_link_and_calendar_fields` pass; `capture.py:309-321` SELECT + validate |
| 5 | BoardCard: item.image thumb; scrape_status spinner/placeholder; no raw status in DOM; native img no-referrer | ✓ VERIFIED | `board-card.tsx` linkThumbState; prior vitest 4/4 |
| 6 | validate_url_ssrf on every scrape entry (SSRF) | ✓ VERIFIED | `scraper.py` validate inside `scrape()`; regression OK |
| 7 | Single-flight cancel, 2 retries, shared apply, rescrape API | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | schedule cancel-then-spawn; MAX_SCRAPE_RETRIES=2; POST rescrape; integration not run |
| 8 | CAL-02: sync_local_event_to_google on confirm/autosave; soft-fail; no second Event insert | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | call sites capture + timeout; integration not run |
| 9 | CAL-03: PATCH etag 412 + create-on-edit; DELETE → Google delete; restore local-only | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | items.py sync + delete_remote; integration not run |
| 10 | Modal: sync-miss CTA, Vorschau erneut laden, conflict panel | ✓ VERIFIED | `item-modal.tsx` + rescrapeLink; prior vitest CTA path |
| 11 | POST /links and POST /events remain available | ✓ VERIFIED | routes present (regression) |
| 12 | Wave 0 stubs collectable; VALIDATION.md Per-Task map lists Task IDs | ✓ VERIFIED | 12 integration tests `--collect-only` |

**Score:** 7/12 truths verified (5 present, behavior-unverified; 0 failed)

### Deferred Items

None — 05.2 is last milestone phase; no later phase absorbs remaining human checks.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `api/app/services/link_scrape.py` | LinkScrapeManager + apply_scrape_to_link | ✓ VERIFIED | regression: imports/schedule still wired |
| `api/app/services/categories.py` | links_category_id | ✓ VERIFIED | prior pass |
| `api/alembic/versions/0007_board_items_link_fields.py` | VIEW DROP/CREATE | ✓ VERIFIED | prior pass |
| `api/app/models/board.py` | BoardItem image/scrape_status/google_event_id | ✓ VERIFIED | gap closed: `google_event_id: str \| None`; model_validate works |
| `webapp/components/board/board-card.tsx` | thumb state machine | ✓ VERIFIED | uses item.image + scrape_status |
| `api/app/routers/links.py` | POST /links + rescrape | ✓ VERIFIED | apply + schedule_scrape |
| `api/app/services/calendar.py` | sync_local_event_to_google + update_event_with_etag | ✓ VERIFIED | helpers present |
| `api/app/routers/items.py` | PATCH/DELETE Google sync | ✓ VERIFIED | create-on-edit + 412 + delete_remote |
| `webapp/components/board/item-modal.tsx` | CTAs + conflict | ✓ VERIFIED | rescrapeLink wired |
| `api/tests/integration/test_capture_async_scrape.py` | Named scrape contracts | ✓ VERIFIED | 6 collectable |
| `api/tests/integration/test_calendar_sync_wiring.py` | Named calendar contracts | ✓ VERIFIED | 6 collectable |
| `api/tests/unit/test_models.py` | BoardItem regression | ✓ VERIFIED | `test_board_item_optional_link_and_calendar_fields` pass (176d6e9) |
| `webapp/tests/board-link-scrape.test.tsx` | BoardCard states | ✓ VERIFIED | prior 4/4 |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| create_draft link commit | scrape_manager → scrape_service → Link.metadata_/scrape_status | `schedule_scrape` after commit | ✓ WIRED | `capture.py:173` |
| failed\|timed_out | links_category_id → Link.category_id | apply_scrape_to_link | ✓ WIRED | shared by manager + POST /links |
| 0007 board_items | list_board_items → BoardCard item.image | SELECT image, scrape_status + model_validate | ✓ WIRED | gap closed — validate no longer crashes |
| POST /links/{id}/rescrape | scrape_manager.schedule_scrape | router 202 | ✓ WIRED | `links.py` |
| confirm_draft / timeout autosave | sync_local_event_to_google(existing Event) | after status flip | ✓ WIRED | capture + timeout |
| PATCH /items | update_event_with_etag \| create-on-edit → 412 | `_sync_event_after_local_update` | ✓ WIRED | items.py |
| DELETE /items | events.delete when google_event_id | delete_remote_event | ✓ WIRED | before soft-delete |
| item-modal | rescrape API + sync CTA + conflict panel | rescrapeLink / updateItem | ✓ WIRED | component + lib/api/links.ts |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| LinkScrapeManager | metadata_/scrape_status | scrape_service.scrape → apply_scrape_to_link UPDATE | Yes (when job runs) | ✓ FLOWING |
| board_items VIEW | image, scrape_status | links.metadata->>'image', links.scrape_status | Yes at SQL layer | ✓ FLOWING |
| GET /board-items | BoardItem | model_validate(row) | Yes — serialize path proven by unit | ✓ FLOWING |
| BoardCard | item.image | props from board fetch | Path open post-fix | ✓ FLOWING |
| sync_local_event_to_google | google_event_id/etag | Google events().insert → same Event row | Yes when Connected | ✓ FLOWING |
| item-modal sync-miss | item.google_event_id | board-items LEFT JOIN events | Path open post-fix | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Unit gate | `bash scripts/test.sh` | mcp 41 + hermes 30 + api 11 pass | ✓ PASS |
| BoardItem.model_validate | `.venv/bin/python` + validate dict with image/scrape_status/google_event_id | OK dump keys present | ✓ PASS |
| Named unit regression | `pytest …::test_board_item_optional_link_and_calendar_fields` | 1 passed | ✓ PASS |
| Integration collect | `pytest … --collect-only -q` | 12 tests listed | ✓ PASS |
| Integration named scrape | needs Postgres | not run this pass | ? SKIP |

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| — | — | No phase probe scripts declared | SKIP |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| LINK-01 | 00, 01, 02 | Link → metadata JSONB via Hermes capture | ? NEEDS HUMAN | Scrape + board serialize path wired; integration/live scrape not run |
| LINK-02 | 00, 01, 02 | Sensible category (Links on fail/timeout) | ? NEEDS HUMAN | apply_scrape_to_link + links_category_id; integration not run |
| CAL-02 | 00, 03 | Events sync read/write with Google on Hermes path | ? NEEDS HUMAN | sync_local_event_to_google on confirm/autosave; board google_event_id path open |
| CAL-03 | 00, 03 | ETag / If-Match; no silent overwrite | ? NEEDS HUMAN | update_event_with_etag + 412; integration not run |

No orphaned phase requirements.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | `str \| null` gone from board.py (176d6e9) | — | prior blocker closed |
| — | — | No TBD/FIXME/XXX in phase production files | — | — |

### Prohibitions (judgment-tier — non-authoritative)

| Prohibition | Status | Notes |
| ----------- | ------ | ----- |
| No external job brokers / durable scrape table | ✓ (judgment) | asyncio-only manager |
| Do not widen next.config remotePatterns to all hosts | ✓ (judgment) | BoardCard native img |
| Do not deprecate POST /links or POST /events | ✓ (judgment) | routes present |
| Do not put Google tokens in 412 payloads | ✓ (judgment) | concurrency_conflict shape |
| Do not show card-level Google sync badges / raw scrape_status | ✓ (judgment) | BoardCard vitest prior |
| unverified-prohibition — human review recommended | flagged | Live Connected-calendar soft-fail UX not observed |

### Human Verification Required

1. **Board link thumb after Hermes capture** — Send link via Hermes; board shows hostname meta + OG thumb (or spinner→image). Why human: live scrape + board poll.
2. **Connected calendar create + 412 panel** — Confirm event with Google Connected; edit conflicting remote → conflict panel; scrape retry CTA. Why human: live Google OAuth.
3. **Integration suite green** — `DATABASE_URL=postgresql://…`; run both Wave 0 integration files (12 tests).

### Gaps Summary

Prior blocker (`google_event_id: str | null` → PydanticUserError on GET /board-items) **closed** in `176d6e9`. Unit regression proves `BoardItem.model_validate` with image/scrape_status/google_event_id. Unit gate green (api 11). No remaining FAILED must-haves.

Five truths stay ⚠️ PRESENT_BEHAVIOR_UNVERIFIED — code+wiring present, integration needs local Postgres. Status → **human_needed** (not gaps_found).

---

_Verified: 2026-08-08T20:07:07Z_
_Verifier: Claude (gsd-verifier)_
