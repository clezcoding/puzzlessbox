---
phase: 01-datenmodell-backend-api
plan: 06
type: execute
wave: 3
depends_on: [01-05]
files_modified:
  - webapp/package.json
  - webapp/app/api/auth/[...all]/route.ts
  - webapp/lib/auth.ts
  - webapp/lib/auth.config.ts
  - api/app/main.py
  - api/app/auth/__init__.py
  - api/app/auth/jwt.py
  - api/app/routers/auth.py
  - api/app/routers/capture.py
  - api/alembic/versions/0002_idempotency.py
  - api/tests/unit/test_auth.py
  - api/tests/integration/test_auth.py
  - api/tests/integration/test_capture.py
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, CAP-01]
user_setup: []

must_haves:
  truths:
    - "Auth table contract (D-21): Better Auth owns the singular `user` table (its default). Alembic 0001 (Plan 05) does NOT create a competing `users` table. Core domain tables use `owner_id UUID` referencing Better Auth `user.id` (no FK, cross-owner). Signup lock hook queries `SELECT count(*) FROM \"user\"` (D-24)."
    - "POST /auth/signup with email+password creates a user when user_count==0 and rejects with 409 SIGNUP_LOCKED when user_count>0 via Better Auth databaseHooks user.create:before (AUTH-01, AUTH-03, D-24)"
    - "POST /auth/login returns a JWT; FastAPI verifies it via PyJWKClient against Next.js Better Auth JWKS endpoint and extracts owner_id from sub claim (AUTH-02, D-21)"
    - "FastAPI accepts JWT from Authorization Bearer OR puzzlessbox_session cookie (D-22); cookie domain .puzzlesstool.online, httponly, samesite=lax, secure in prod"
    - "X-Service-Bearer matching SERVICE_BEARER_TOKEN env resolves to a service principal owner_id from service_principals table without JWT (D-23, prep for Phase 2 MCP)"
    - "POST /drafts with {title, type in {note,link,task,event}, category_id, summary} routes insertion to the correct table by type (notes/links/tasks/events), persists a row with status=draft, and returns 201 with {id, type} (CAP-01, D-01, D-02, D-30)"
    - "GET /board-items returns rows from the board_items VIEW filtered to the caller's owner_id (D-01)"
    - "First signup succeeds, second signup returns 409 (AUTH-03 boundary)"
    - "Cross-tenant GET /board-items with a foreign owner_id JWT returns empty list (not 403) (AUTH-04 boundary)"
    - "Idempotency-Key replay on POST /drafts returns identical 201 without creating a second draft (D-34)"
  artifacts:
    - "webapp/lib/auth.ts + auth.config.ts — Better Auth with email/password + databaseHooks signup lock; databaseAdapter points at Postgres `user` table (Better Auth default, D-21, D-24)"
    - "webapp/app/api/auth/[...all]/route.ts — Better Auth handler exposing /api/auth/* including /api/auth/jwks (D-21)"
    - "api/app/auth/jwt.py — get_current_owner via PyJWKClient (D-21) + cookie/bearer extraction (D-22) + service bearer (D-23)"
    - "api/app/routers/auth.py — /auth/signup, /auth/login, /auth/verify (proxies to Better Auth; signup lock via databaseHooks)"
    - "api/app/routers/capture.py — POST /drafts (type-discriminated polymorphic insert into notes/links/tasks/events), GET /board-items (draft CRUD; PATCH/confirm timer wiring deferred to Plan 02)"
    - "api/app/main.py — extended from Plan 01 to register auth + capture routers (minimal include_router calls)"
    - "api/alembic/versions/0002_idempotency.py — idempotency_keys(owner_id, key, response, created_at) (D-34)"
  key_links:
    - "Next.js Better Auth /api/auth/jwks → FastAPI PyJWKClient cache (D-21)"
    - "owner_id JWT sub claim → RLS policy + app-level WHERE filter (D-25, from Plan 05)"
    - "Better Auth databaseHooks user.create:before → SELECT count(*) FROM \"user\" → 409 SIGNUP_LOCKED when >0 (D-24, D-21 auth table contract)"
    - "POST /drafts type discriminator → correct table INSERT (notes/links/tasks/events) → response {id, type} (D-01, D-30, CAP-01)"
  prohibitions:
    - "No full WebApp UI (Phase 4) — only minimal Better Auth auth API routes + lib"
    - "No timeout state machine in capture.py (Plan 02 owns timer wiring)"
    - "No scraper / calendar (Plans 03/04)"
    - "No competing `users` table in migrations — Better Auth owns `user` (D-21)"
---

<objective>
Auth + draft CRUD plan: minimal Next.js Better Auth bootstrap (JWKS endpoint, email/password, databaseHooks signup lock D-24) + FastAPI JWKS verify (D-21) + cookie/bearer auth (D-22) + service bearer (D-23) + draft CRUD tracer (POST /drafts, GET /board-items) + idempotency (D-34). Proves auth + draft path end-to-end on top of Plan 05 schema.

Purpose: D-21 puts auth in Next.js for JWKS; D-24 signup lock needs Better Auth databaseHooks. This plan wires the cross-service auth contract and the first CRUD path. Plans 02/03/04 build on this draft CRUD + auth dependency.
Output: Minimal webapp Better Auth shell, FastAPI auth router + JWKS verify + capture router, idempotency migration, auth + capture tests green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenmodell-backend-api/01-CONTEXT.md
@.planning/phases/01-datenmodell-backend-api/01-RESEARCH.md
@.planning/phases/01-datenmodell-backend-api/01-PATTERNS.md
@.planning/phases/01-datenmodell-backend-api/01-VALIDATION.md
@.planning/phases/01-datenmodell-backend-api/01-01-SUMMARY.md
@.planning/phases/01-datenmodell-backend-api/01-05-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — Better Auth JWKS + signup lock + FastAPI JWKS verify + draft POST + board-items GET end-to-end</name>
  <files>webapp/package.json, webapp/app/api/auth/[...all]/route.ts, webapp/lib/auth.ts, webapp/lib/auth.config.ts, api/app/main.py, api/app/auth/__init__.py, api/app/auth/jwt.py, api/app/routers/auth.py, api/app/routers/capture.py, api/tests/integration/test_capture.py</files>
  <read_first>
    - .planning/phases/01-datenmodell-backend-api/01-RESEARCH.md (lines 225-251 JWKS pattern, lines 279-283 signup lock pitfall)
    - .planning/phases/01-datenmodell-backend-api/01-PATTERNS.md (lines 42-75 jwt analog)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-01, D-02, D-21, D-22, D-24, D-30)
    - api/app/core/database.py, api/app/models/note.py, api/app/models/link.py, api/app/models/task.py, api/app/models/event.py (from Plan 05)
    - api/app/main.py (from Plan 01 — extend with include_router)
    - api/tests/conftest.py (from Plan 01 — mock JWKS, two-owner fixtures)
  </read_first>
  <action>
Webapp minimal Better Auth bootstrap (NOT full UI — only auth API routes + lib): webapp/package.json pins better-auth@1.6.25, next@16. webapp/lib/auth.config.ts — Better Auth config with emailAndPassword enabled, databaseAdapter pointing at DATABASE_URL (Postgres) using Better Auth's default `user` table (singular — D-21 auth table contract; do NOT configure a plural `users` table). databaseHooks.user.create:before hook queries `SELECT count(*) FROM \"user\"` (quoted — `user` is a reserved word in Postgres) and throws if >0 (D-24 signup lock). webapp/lib/auth.ts — export auth instance. webapp/app/api/auth/[...all]/route.ts — export GET/POST handlers from Better Auth (exposes /api/auth/* including /api/auth/jwks for JWKS, D-21). FastAPI side: api/app/auth/jwt.py — PyJWKClient against BETTER_AUTH_JWKS_URL env (default http://webapp:3000/api/auth/jwks), cache_jwk_set=True, lifespan=300; get_current_owner dependency extracts token from Authorization Bearer OR puzzlessbox_session cookie (D-22), returns payload sub as owner_id; reject missing sub with 401. api/app/routers/auth.py — /auth/signup + /auth/login proxy to Better Auth over internal httpx.AsyncClient, forward session cookie (cookie domain .puzzlesstool.online, httponly, samesite=lax, secure in prod, D-22); /auth/verify returns {owner_id} after get_current_owner. api/app/routers/capture.py — POST /drafts requires type in {note,link,task,event} (D-30), routes insertion to the correct table by type (notes/links/tasks/events per D-01 — polymorphic insert), persists row with owner_id from JWT + status=draft (D-02), returns 201 with {id, type} (CAP-01, D-30); GET /board-items queries the board_items VIEW filtered to caller owner_id (D-01). Pattern deviation note: PATTERNS.md lists per-type routers (notes.py/links.py/etc.) as the analog convention, but Phase 1 consolidates draft creation for all four types into capture.py (POST /drafts with type discriminator per D-30) because drafts share identical create-time validation + status lifecycle; per-type routers split out only when type-specific read/update fields diverge (links.py in Plan 03, events.py in Plan 04). Document this consolidation decision in capture.py module docstring. Extend main.py from Plan 01 to register auth + capture routers (minimal include_router calls — do not rewrite the rest of main.py). Integration test test_capture.py::test_draft_roundtrip: mock JWKS (from conftest), signup → login → POST /drafts (note) → 201 with {id, type:note} → GET /board-items → contains draft. Do NOT implement timeout (Plan 02), scraper (Plan 03), calendar (Plan 04). Do NOT implement service bearer or idempotency in this tracer (Task 2).
  </action>
  <verify>
    <automated>cd api && alembic upgrade head && pytest tests/integration/test_capture.py::test_draft_roundtrip -q</automated>
  </verify>
  <acceptance_criteria>
    - POST /auth/signup with email+password when user_count==0 returns 201 and creates one user in Better Auth `user` table (AUTH-01, D-21)
    - Second signup returns 409 SIGNUP_LOCKED (AUTH-03 boundary, D-24)
    - POST /auth/login returns 200 + JWT; FastAPI verifies via JWKS (AUTH-02, D-21)
    - POST /drafts with valid JWT + {title, type:note, category_id, summary} returns 201 with {id, type:note}; row inserted into notes table (CAP-01, D-01)
    - POST /drafts with type:link inserts into links table; type:task → tasks; type:event → events (polymorphic routing, D-30)
    - GET /board-items with valid JWT returns the caller's draft via the VIEW (D-01)
    - Cross-tenant GET /board-items with foreign owner_id JWT returns empty list (not 403) (AUTH-04 boundary)
  </acceptance_criteria>
  <done>One end-to-end path (signup → login → create draft (each type routes to correct table) → list board) works against migrated Postgres with RLS enforced + Better Auth JWKS; committed.</done>
  <reversibility rating="costly">Cross-service auth contract (JWKS + cookie domain + Better Auth `user` table ownership) is expensive to change post-launch.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service bearer (D-23) + idempotency (D-34) + auth tests</name>
  <files>api/app/auth/jwt.py, api/app/routers/auth.py, api/app/routers/capture.py, api/alembic/versions/0002_idempotency.py, api/tests/unit/test_auth.py, api/tests/integration/test_auth.py, api/tests/integration/test_capture.py</files>
  <read_first>
    - api/app/auth/jwt.py, api/app/routers/auth.py, api/app/routers/capture.py (from Task 1)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-23, D-34)
    - .planning/phases/01-datenmodell-backend-api/01-VALIDATION.md (test map: AUTH-01/02/03)
    - api/tests/conftest.py (from Plan 01)
  </read_first>
  <behavior>
    - test_registration: POST /auth/signup with email+password when user_count==0 returns 201 and creates exactly one user (AUTH-01)
    - test_signup_lock: after first user exists, second POST /auth/signup returns 409 with code SIGNUP_LOCKED (AUTH-03 boundary)
    - test_jwt_decode: get_current_owner extracts owner_id from sub claim; expired/invalid token returns 401 (AUTH-02)
    - test_login_persists_session: POST /auth/login returns 200 + sets puzzlessbox_session cookie on parent domain puzzlesstool.online (D-22)
    - test_service_bearer: requests with X-Service-Bearer matching SERVICE_BEARER_TOKEN env resolve to a service owner_id from service_principals without JWT (D-23)
    - test_idempotency: Idempotency-Key replay on POST /drafts returns identical 201 without creating a second draft (D-34)
  </behavior>
  <action>
Extend jwt.py: add service bearer path — when X-Service-Bearer header matches SERVICE_BEARER_TOKEN env, look up service_principals row by bearer_hash and resolve to its owner_id without JWT (D-23). New Alembic revision 0002_idempotency.py: idempotency_keys(owner_id uuid, key text, response jsonb, created_at timestamptz, pk(owner_id, key)). Wire capture.py POST /drafts: optional Idempotency-Key header — if present, check idempotency_keys; on hit return stored response; on miss store key+response after create (D-34). test_auth.py (unit): test_jwt_decode + test_service_bearer using mock JWKS from conftest. test_auth.py (integration): test_registration + test_signup_lock + test_login_persists_session. test_capture.py: add test_idempotency + test_cross_tenant_draft (two owners, each POST /drafts, assert neither sees the other's rows via /board-items — AUTH-04 boundary). [BLOCKING] `alembic upgrade head` before verify.
  </action>
  <verify>
    <automated>cd api && alembic upgrade head && pytest tests/unit/test_auth.py tests/integration/test_auth.py tests/integration/test_capture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - First signup succeeds (201); second signup returns 409 SIGNUP_LOCKED (AUTH-03 boundary exact)
    - Login returns 200 + cookie on parent domain; expired JWT returns 401
    - Service bearer header authenticates without JWT (D-23)
    - Idempotency-Key replay returns identical 201 without duplicate draft (D-34)
    - Cross-tenant /board-items returns empty (not 403) (AUTH-04 boundary)
  </acceptance_criteria>
  <done>Auth router complete with signup lock, login, verify, service bearer, idempotency; all auth + capture tests green; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Traefik | Public HTTPS; untrusted |
| Traefik→FastAPI | Internal; Bearer/JWT verified |
| FastAPI→Next.js JWKS | Internal; key material fetch (D-21) |
| FastAPI→Next.js Better Auth | Internal; signup/login proxy (D-24) |
| FastAPI→Postgres | Internal; RLS enforces tenant (Plan 05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-auth | Spoofing | auth/jwt.py JWKS verification | high | mitigate | PyJWKClient cache + RS256 verification + exp check; reject missing sub (D-21) |
| T-01-signup | Tampering | auth router signup | high | mitigate | Better Auth databaseHooks user.create:before SELECT count(*) FROM users → 409 SIGNUP_LOCKED when >0 (D-24, AUTH-03) |
| T-01-cookie | Spoofing | session cookie domain | medium | mitigate | Cookie domain .puzzlesstool.online, httponly, samesite=lax, secure in prod (D-22) |
| T-01-service-bearer | Spoofing | X-Service-Bearer service auth | high | mitigate | Constant-time compare against SERVICE_BEARER_TOKEN env; resolve owner_id from service_principals.bearer_hash (D-23) |
| T-01-idempotency | Tampering | Idempotency-Key replay | low | mitigate | Store key+response in idempotency_keys(owner_id, key); replay returns stored response (D-34) |
</threat_model>

<verification>
- `cd api && alembic upgrade head && pytest tests -q` all green
- `curl -H "Accept: application/vnd.puzzlessbox.v1+json" -H "Authorization: Bearer <jwt>" http://localhost:8000/board-items` returns caller's drafts
- Second signup returns 409 SIGNUP_LOCKED
- `curl -H "X-Service-Bearer: $SERVICE_BEARER_TOKEN" ...` authenticates without JWT
</verification>

<success_criteria>
- AUTH-01: signup creates user (tested)
- AUTH-02: login + JWT verify via JWKS (tested)
- AUTH-03: signup lock after first user (tested boundary)
- AUTH-04: RLS + cross-tenant isolation (tested boundary via /board-items)
- CAP-01: POST /drafts persists structured draft (tested)
- D-21: Better Auth JWKS in Next.js, FastAPI verifies via PyJWKClient
- D-22: cookie on parent domain + bearer accepted
- D-23: service bearer prep for Phase 2 MCP
- D-24: signup lock via databaseHooks
- D-34: idempotency on POST /drafts
</success_criteria>

<output>
Create `.planning/phases/01-datenmodell-backend-api/01-06-SUMMARY.md` when done
</output>
