---
phase: 01-datenmodell-backend-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/requirements.txt
  - api/pytest.ini
  - api/app/__init__.py
  - api/app/main.py
  - api/app/core/config.py
  - api/app/core/database.py
  - api/app/core/errors.py
  - api/app/routers/__init__.py
  - api/app/routers/health.py
  - api/tests/__init__.py
  - api/tests/conftest.py
  - api/tests/unit/__init__.py
  - api/tests/integration/__init__.py
  - api/tests/unit/test_health.py
  - api/Dockerfile
  - api/.env.example
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "FastAPI app runs with title 'Puzzlessbox API' and exposes /health (200 liveness) + /ready (DB ping, 503 on fail) (D-31)"
    - "Accept header versioning middleware rejects missing `Accept: application/vnd.puzzlessbox.v1+json` with 415 (D-27)"
    - "All error responses follow {error:{code,message,details?}} (D-33); /docs + /redoc disabled when ENV=prod (D-32)"
    - "Wave 0 test harness (pytest.ini, conftest.py with mock Postgres engine + mock JWKS) collects tests (RESEARCH Validation Architecture)"
  artifacts:
    - "api/app/main.py — FastAPI app with error handler, Accept-versioning middleware, /docs disabled in prod (D-27, D-32, D-33)"
    - "api/app/core/database.py — session dependency stub (SET app.owner_id + role wiring finalized in Plan 05 once tables exist)"
    - "api/app/routers/health.py — /health + /ready (D-31)"
    - "api/tests/conftest.py — mock Postgres engine, transactional sessions, mock JWKS (Wave 0)"
  key_links:
    - "Accept-versioning middleware → 415 on missing header (D-27)"
    - "Unified error handler → {error:{code,message,details?}} (D-33)"
  prohibitions:
    - "No models / migration / RLS in this plan (Plan 05)"
    - "No auth router / signup / login / draft CRUD (Plan 06)"
    - "No timeout / scraper / calendar (Plans 02/03/04)"
---

<objective>
FastAPI shell + Wave 0 test infra: runnable app with /health+/ready, Accept-versioning, unified errors, /docs-prod-off, and pytest harness (conftest with mock Postgres + mock JWKS) ready for Plans 05/06 to build schema + auth on top.

Purpose: Every later Phase 1 plan needs this shell + test harness. Splitting it out keeps each plan under 15 files and lets Plan 05 own schema/migration while Plan 06 owns auth + draft CRUD.
Output: Runnable FastAPI shell, Wave 0 pytest infra, Dockerfile, .env.example.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenmodell-backend-api/01-CONTEXT.md
@.planning/phases/01-datenmodell-backend-api/01-RESEARCH.md
@.planning/phases/01-datenmodell-backend-api/01-PATTERNS.md
@.planning/phases/01-datenmodell-backend-api/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FastAPI shell + health router + unified errors + Accept versioning</name>
  <files>api/requirements.txt, api/app/__init__.py, api/app/main.py, api/app/core/config.py, api/app/core/database.py, api/app/core/errors.py, api/app/routers/__init__.py, api/app/routers/health.py, api/tests/unit/test_health.py, api/Dockerfile, api/.env.example</files>
  <read_first>
    - .planning/phases/01-datenmodell-backend-api/01-PATTERNS.md (lines 159-196 shared patterns)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-27, D-31, D-32, D-33)
  </read_first>
  <behavior>
    - test_health: GET /health returns 200 {status:ok} unconditionally (D-31)
    - test_ready_db_up: GET /ready returns 200 when DB SELECT 1 succeeds
    - test_ready_db_down: GET /ready returns 503 when DB unreachable (D-31)
    - test_versioning_415: request missing `Accept: application/vnd.puzzlessbox.v1+json` returns 415 (D-27)
    - test_docs_disabled_prod: with ENV=prod, GET /docs returns 404 (D-32)
    - test_error_shape: HTTPException response matches {error:{code,message,details?}} (D-33)
  </behavior>
  <action>
requirements.txt pins: fastapi==0.138.0, sqlmodel==0.0.22, pyjwt[crypto]==2.10.1, cryptography==42.0.8, httpx==0.28.1, uvicorn==0.34.0, alembic==1.13.3, psycopg2-binary, pytest, pytest-asyncio. main.py: create FastAPI app titled "Puzzlessbox API", register unified error handler (D-33) for HTTPException/RequestValidationError/Exception → {error:{code,message,details?}}, never leak stack traces when ENV=prod. Add Accept-versioning middleware (D-27): reject requests missing `Accept: application/vnd.puzzlessbox.v1+json` with 415 (skip for /health, /ready, /docs). core/database.py: session dependency stub that will SET app.owner_id + role per request (RLS wiring finalized in Plan 05 once tables exist; here expose a dependency that returns a session). core/config.py: pydantic-settings BaseSettings with DATABASE_URL, BETTER_AUTH_JWKS_URL, SERVICE_BEARER_TOKEN, ENCRYPTION_KEY, ENV, SCRAPER_ENABLED, DOCS_BASIC_AUTH. routers/health.py: /health 200 unconditional; /ready checks DB SELECT 1, 503 on fail (D-31). /docs + /redoc disabled when ENV=prod (D-32). Dockerfile: python:3.14-slim, install requirements, CMD uvicorn app.main:app. .env.example lists all env vars. Write api/tests/unit/test_health.py implementing the six behavior tests above using FastAPI TestClient (DB mocked via conftest fixtures from Task 2 — for test_ready_db_down, patch the database dependency to raise). Do NOT create auth, capture, links, events routers (Plans 02-06). Do NOT create models or migration (Plan 05).
  </action>
  <verify>
    <automated>cd api && python -c "import app.main; assert app.main.app.title == 'Puzzlessbox API'" && pytest tests/unit/test_health.py -q</automated>
  </verify>
  <done>FastAPI shell runs; /health+/ready respond; Accept-versioning + unified errors + /docs-prod-off wired; six health tests green; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 pytest harness (pytest.ini + conftest with mock Postgres + mock JWKS)</name>
  <files>api/pytest.ini, api/tests/__init__.py, api/tests/conftest.py, api/tests/unit/__init__.py, api/tests/integration/__init__.py</files>
  <read_first>
    - .planning/phases/01-datenmodell-backend-api/01-RESEARCH.md (lines 392-422 Validation Architecture, Wave 0 gaps)
    - .planning/phases/01-datenmodell-backend-api/01-VALIDATION.md (Wave 0 requirements, test map)
  </read_first>
  <action>
pytest.ini: asyncio_mode=auto, testpaths=tests. conftest.py: mock Postgres engine via transactional fixture (rollback after each test), mock JWKS endpoint via httpx mock serving a generated RS256 keypair (consumed by Plan 06 auth tests), fixtures for two distinct owner_ids for tenancy tests (consumed by Plan 05 test_rls + Plan 06 cross-tenant tests). tests/__init__.py, tests/unit/__init__.py, tests/integration/__init__.py empty package markers. Do NOT create test files themselves (Plan 05 owns test_models.py + test_tenancy.py; Plan 06 owns test_auth.py + test_capture.py).
  </action>
  <verify>
    <automated>cd api && pytest --co -q tests/ && test -f pytest.ini && test -f tests/conftest.py</automated>
  </verify>
  <done>pytest.ini + conftest.py with mock Postgres + mock JWKS + two-owner fixtures exist; `pytest --co tests/` collects zero tests without error; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Traefik | Public HTTPS; untrusted |
| Traefik→FastAPI | Internal; Bearer/JWT verified (wired in Plan 06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-docs | Information Disclosure | /docs /redoc exposure | medium | mitigate | Disable in prod ENV=prod; Basic-Auth gate when DOCS_BASIC_AUTH=true (D-32) |
| T-01-versioning | Tampering | API version middleware | medium | mitigate | Reject missing Accept header with 415 (D-27) |
| T-01-SC | Tampering | pip installs (fastapi, sqlmodel, pyjwt, cryptography, httpx, uvicorn, alembic) | high | mitigate | All [OK] per RESEARCH Package Legitimacy Audit; pin versions in requirements.txt |
</threat_model>

<verification>
- `cd api && python -c "import app.main"` succeeds
- `cd api && pytest --co -q tests/` collects without error
- `curl http://localhost:8000/health` returns 200
- Request missing Accept header returns 415
</verification>

<success_criteria>
- FastAPI shell runnable with /health + /ready + unified errors + Accept versioning + /docs-prod-off
- Wave 0 test harness (pytest.ini, conftest.py, mock JWKS, two-owner fixtures) ready for Plans 05/06
</success_criteria>

<output>
Create `.planning/phases/01-datenmodell-backend-api/01-01-SUMMARY.md` when done
</output>
