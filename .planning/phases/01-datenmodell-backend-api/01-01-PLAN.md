---
phase: 01-datenmodell-backend-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/requirements.txt
  - api/pytest.ini
  - api/alembic.ini
  - api/alembic/env.py
  - api/alembic/script.py.mako
  - api/alembic/versions/0001_initial_schema.py
  - api/alembic/versions/0002_idempotency.py
  - api/app/__init__.py
  - api/app/main.py
  - api/app/core/config.py
  - api/app/core/database.py
  - api/app/core/security.py
  - api/app/core/errors.py
  - api/app/auth/jwt.py
  - api/app/models/__init__.py
  - api/app/models/category.py
  - api/app/models/note.py
  - api/app/models/link.py
  - api/app/models/task.py
  - api/app/models/event.py
  - api/app/models/board.py
  - api/app/routers/__init__.py
  - api/app/routers/auth.py
  - api/app/routers/capture.py
  - api/app/routers/health.py
  - api/tests/__init__.py
  - api/tests/conftest.py
  - api/tests/unit/test_auth.py
  - api/tests/unit/test_models.py
  - api/tests/integration/test_auth.py
  - api/tests/integration/test_tenancy.py
  - api/Dockerfile
  - api/.env.example
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, CAP-01]
user_setup: []

must_haves:
  truths:
    - "POST /auth/signup with email+password creates a user when user_count==0 and rejects with 409 SIGNUP_LOCKED when user_count>0 (AUTH-01, AUTH-03, D-24)"
    - "POST /auth/login returns a JWT; FastAPI verifies it via JWKS against Next.js Better Auth JWKS endpoint and extracts owner_id from sub claim (AUTH-02, D-21)"
    - "Every core table (notes, links, tasks, events, categories) carries indexed owner_id; Postgres RLS policies restrict rows so a tenant cannot read another tenant's rows (AUTH-04, D-01, D-25)"
    - "POST /drafts with {title, type, category_id, summary} persists a row with status=draft and returns 201 with the draft id (CAP-01, D-02, D-30)"
    - "GET /board-items returns rows from the board_items VIEW (UNION ALL over notes/links/tasks/events) filtered to the caller's owner_id (D-01)"
    - "First signup succeeds, second signup returns 409 (AUTH-03 boundary)"
    - "Cross-tenant reads return empty (not 403) for GET /board-items with a foreign owner_id JWT (AUTH-04 boundary)"
    - "All error responses follow {error:{code,message,details?}} (D-33)"
    - "All timestamps stored in Europe/Berlin (D-28); Accept header versioning enforced (D-27)"
  artifacts:
    - "api/app/main.py — FastAPI app with router registration, error handler, /docs disabled in prod (D-32)"
    - "api/app/auth/jwt.py — get_current_owner dependency using PyJWKClient (D-21)"
    - "api/app/models/{category,note,link,task,event,board}.py — SQLModel tables + board_items VIEW (D-01, D-03, D-04)"
    - "api/app/routers/{auth,capture,health}.py — signup/login/verify, drafts, /health+/ready (D-31)"
    - "api/alembic/versions/0001_initial_schema.py — tables, VIEW, RLS policies, seed categories (D-25, D-26)"
    - "api/tests/conftest.py — mock Postgres engine, transactional sessions, mock JWKS (Wave 0)"
  key_links:
    - "Next.js Better Auth JWKS endpoint → FastAPI PyJWKClient cache (D-21)"
    - "owner_id JWT sub claim → RLS policy + app-level WHERE filter (D-25)"
    - "categories.category_id ← notes/links/tasks/events.category_id FK (D-04)"
  prohibitions:
    - "No direct Hermes DB access from this phase (Hermes out of scope; MCP later)"
    - "No public Firecrawl/Camoufox endpoints (scraper work in Plan 03, internal only)"
    - "No signup allowed after first user (AUTH-03, D-24)"
---

<objective>
Foundation plan: stand up FastAPI app, Alembic schema with tenancy, Better Auth JWKS bridge, signup lock, draft creation, and board_items VIEW. Proves auth + schema + one CRUD path end-to-end before parallel expansion plans (02/03/04) build on it.

Purpose: Every other Phase 1 plan depends on this schema + auth dependency. Tracer proves owner_id isolation and JWKS verification work before expanding into timeout, scraper, and calendar.
Output: Runnable FastAPI app with Alembic-migrated Postgres schema, RLS, signup-locked auth, /drafts POST, /board-items GET, and pytest suite green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenmodell-backend-api/01-CONTEXT.md
@.planning/phases/01-datenmodell-backend-api/01-RESEARCH.md
@.planning/phases/01-datenmodell-backend-api/01-PATTERNS.md
@.planning/phases/01-datenmodell-backend-api/01-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — schema + JWKS auth + draft POST + board_items read end-to-end</name>
  <files>api/requirements.txt, api/app/__init__.py, api/app/main.py, api/app/core/config.py, api/app/core/database.py, api/app/core/security.py, api/app/core/errors.py, api/app/auth/jwt.py, api/app/models/__init__.py, api/app/models/category.py, api/app/models/note.py, api/app/models/link.py, api/app/models/task.py, api/app/models/event.py, api/app/models/board.py, api/app/routers/__init__.py, api/app/routers/capture.py, api/app/routers/health.py, api/alembic.ini, api/alembic/env.py, api/alembic/script.py.mako, api/alembic/versions/0001_initial_schema.py, api/.env.example</files>
  <read_first>
    - .planning/phases/01-datenmodell-backend-api/01-RESEARCH.md (lines 225-251 JWKS pattern)
    - .planning/phases/01-datenmodell-backend-api/01-PATTERNS.md (lines 42-75 jwt analog, lines 159-196 shared patterns)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-01..D-04, D-21..D-28, D-30..D-33)
  </read_first>
  <action>
Wire ONE end-to-end path: signup → JWKS-verified request → POST /drafts (note) → persist with owner_id → GET /board-items reads via VIEW. Per D-01: separate SQLModel tables notes/links/tasks/events with shared columns (id uuid pk, owner_id uuid not null indexed, category_id fk→categories, status enum draft|auto_saved|confirmed per D-02, deleted_at nullable per D-03, created_at/updated_at timestamptz defaulting to Europe/Berlin per D-28). Per D-04: categories table with seed defaults Inbox/Notizen/Links/Tasks/Termine in migration. Per D-01: board_items VIEW = UNION ALL over the four tables projecting common columns + type discriminator. Per D-25: enable Postgres RLS on all five core tables with policy `USING (owner_id = current_setting('app.owner_id')::uuid)`; set role + `SET app.owner_id` per request in database session dependency. Per D-21: PyJWKClient against BETTER_AUTH_JWKS_URL env (default http://webapp:3000/api/auth/jwks), cache_jwk_set=True, lifespan=300; get_current_owner returns payload sub as owner_id. Per D-22: accept JWT from Authorization Bearer OR puzzlessbox_session cookie. Per D-27: API versioning middleware rejects requests missing `Accept: application/vnd.puzzlessbox.v1+json` with 415. Per D-30: POST /drafts requires type in {note,link,task,event}. Per D-33: register HTTPException handler emitting {error:{code,message,details?}}. Per D-31: /health returns 200 liveness; /ready checks DB ping (503 on fail). Per D-32: /docs and /redoc disabled when ENV=prod. Alembic env.py uses SQLModel metadata. [BLOCKING] Run `alembic upgrade head` against dev Postgres before verify; migration 0001 creates all tables, VIEW, RLS policies, and seed categories in one revision. requirements.txt pins: fastapi==0.138.0, sqlmodel==0.0.22, pyjwt[crypto]==2.10.1, cryptography==42.0.8, httpx==0.28.1, uvicorn==0.34.0, alembic==1.13.3, psycopg2-binary, pytest, pytest-asyncio. Include a minimal /auth/signup + /auth/login (email/password only, no lock yet) to produce a JWT for the end-to-end test. Do NOT implement timeout (Plan 02), scraper (Plan 03), or calendar (Plan 04).
  </action>
  <verify>
    <automated>cd api && alembic upgrade head && pytest tests/unit/test_models.py::test_draft_validation tests/integration/test_tenancy.py::test_rls -q</automated>
  </verify>
  <acceptance_criteria>
    - `alembic upgrade head` succeeds on clean dev Postgres and creates notes, links, tasks, events, categories, board_items VIEW, RLS policies, and 5 seeded default categories
    - POST /drafts with valid JWT + {title, type:note, category_id, summary} returns 201 with draft id and status=draft
    - GET /board-items with valid JWT returns the caller's draft via the VIEW
    - Request with foreign owner_id JWT returns empty list (not 403) for GET /board-items
    - Request missing `Accept: application/vnd.puzzlessbox.v1+json` returns 415
    - /health returns 200; /ready returns 200 when DB up, 503 when DB down
  </acceptance_criteria>
  <done>One end-to-end path (signup → login → create draft → list board) works against migrated Postgres with RLS enforced; committed.</done>
  <reversibility rating="costly">Schema + RLS + VIEW shape all downstream CRUD/MCP; migrating after launch is expensive.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth router — signup lock + login + verify + service bearer prep</name>
  <files>api/app/routers/auth.py, api/app/auth/jwt.py, api/app/core/security.py, api/app/core/config.py, api/tests/unit/test_auth.py, api/tests/integration/test_auth.py, api/tests/conftest.py, api/pytest.ini</files>
  <read_first>
    - .planning/phases/01-datenmodell-backend-api/01-RESEARCH.md (lines 225-251 JWKS, lines 279-283 signup lock pitfall)
    - .planning/phases/01-datenmodell-backend-api/01-PATTERNS.md (lines 42-75)
    - .planning/phases/01-datenmodell-backend-api/01-VALIDATION.md (Wave 0 requirements, test map)
    - api/app/auth/jwt.py (from Task 1)
  </read_first>
  <behavior>
    - test_registration: POST /auth/signup with email+password when user_count==0 returns 201 and creates exactly one user
    - test_signup_lock: after first user exists, second POST /auth/signup returns 409 with code SIGNUP_LOCKED (AUTH-03 boundary)
    - test_jwt_decode: get_current_owner extracts owner_id from sub claim; expired/invalid token returns 401
    - test_login_persists_session: POST /auth/login returns 200 + sets puzzlessbox_session cookie on parent domain puzzlesstool.online
    - test_service_bearer: requests with X-Service-Bearer matching SERVICE_BEARER_TOKEN env resolve to a service owner_id without JWT
  </behavior>
  <action>
Per D-24: implement signup lock via Better Auth databaseHooks on user.create:before — hook queries `SELECT count(*) FROM users` and rejects with 409 SIGNUP_LOCKED when count>0. FastAPI auth router proxies signup/login to Better Auth (Next.js) over internal HTTP via httpx.AsyncClient; on success forwards the session cookie. Per D-21: /auth/verify endpoint returns {owner_id} after get_current_owner dependency. Per D-22: cookie domain = `.puzzlesstool.online`, httponly, samesite=lax, secure in prod. Per D-23: add optional X-Service-Bearer header auth path — when header matches SERVICE_BEARER_TOKEN env, resolve to a service principal with owner_id from an internal mapping (prep for MCP in Phase 2; mapping table service_principals(owner_id) created in Task 1 migration). Per D-33: all error responses use unified shape. pytest.ini: asyncio_mode=auto, testpaths=tests. conftest.py: mock Postgres engine via transactional fixture (rollback after each test), mock JWKS endpoint via httpx mock serving a generated RS256 keypair, fixtures for two distinct owner_ids for tenancy tests. Do NOT touch timeout/scraper/calendar.
  </action>
  <verify>
    <automated>cd api && pytest tests/unit/test_auth.py tests/integration/test_auth.py::test_registration tests/integration/test_auth.py::test_signup_lock -q</automated>
  </verify>
  <acceptance_criteria>
    - First signup succeeds (201); second signup returns 409 SIGNUP_LOCKED (AUTH-03 boundary exact)
    - Login returns 200 + cookie on parent domain; expired JWT returns 401
    - Service bearer header authenticates without JWT
    - pytest.ini + conftest.py exist and `pytest tests -q` collects all Wave 0 suites
  </acceptance_criteria>
  <done>Auth router complete with signup lock, login, verify, service bearer; all auth tests green; committed.</done>
</task>

<task type="auto">
  <name>Task 3: API shell — idempotency, error shape, /health+/ready, Dockerfile</name>
  <files>api/app/main.py, api/app/core/errors.py, api/app/core/config.py, api/app/routers/health.py, api/alembic/versions/0002_idempotency.py, api/Dockerfile, api/.env.example, api/tests/integration/test_tenancy.py</files>
  <read_first>
    - api/app/main.py (from Task 1)
    - api/app/core/errors.py (from Task 1)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-31, D-32, D-33, D-34)
  </read_first>
  <action>
Per D-34: add optional Idempotency-Key header on POST /drafts — store key+response in idempotency_keys(owner_id, key, response, created_at) table via new Alembic revision 0002_idempotency.py; replay returns stored response. Per D-33: register exception handlers for HTTPException, RequestValidationError, and generic Exception → unified {error:{code,message,details?}} shape; never leak stack traces in prod. Per D-31: /health returns {status:ok} 200 unconditionally; /ready checks DB SELECT 1 + optional scraper ping (skip scraper ping in this plan; 503 only if SCRAPER_ENABLED=true and unreachable). Per D-32: /docs and /redoc conditionally enabled — disabled when ENV=prod, or Basic-Auth behind Coolify when DOCS_BASIC_AUTH=true. Dockerfile: python:3.14-slim base, install requirements, COPY app, CMD uvicorn app.main:app —host 0.0.0.0 —port 8000. .env.example: BETTER_AUTH_JWKS_URL, DATABASE_URL, SERVICE_BEARER_TOKEN, ENCRYPTION_KEY, ENV, SCRAPER_ENABLED, DOCS_BASIC_AUTH. Add cross-tenant integration test: two owners, each creates a draft, assert neither sees the other's rows via /board-items (AUTH-04 boundary). Run `alembic upgrade head` [BLOCKING] before verify.
  </action>
  <verify>
    <automated>cd api && alembic upgrade head && pytest tests/integration/test_tenancy.py -q && python -c "import app.main; assert app.main.app.title == 'Puzzlessbox API'"</automated>
  </verify>
  <acceptance_criteria>
    - Idempotency-Key replay returns identical 201 response without creating a second draft
    - All error responses match {error:{code,message,details?}}; validation errors include field details
    - /health 200 unconditionally; /ready 200 when DB up, 503 when DB down
    - /docs returns 404 when ENV=prod
    - Dockerfile builds: `docker build -t puzzlessbox-api api/`
  </acceptance_criteria>
  <done>API shell complete with idempotency, unified errors, health/ready, Dockerfile; cross-tenant test green; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Traefik | Public HTTPS; untrusted |
| Traefik→FastAPI | Internal; Bearer/JWT verified |
| FastAPI→Postgres | Internal; RLS enforces tenant |
| FastAPI→Next.js JWKS | Internal; key material fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-auth | Spoofing | auth/jwt.py JWKS verification | high | mitigate | PyJWKClient cache + RS256 verification + exp check; reject missing sub (D-21) |
| T-01-tenant | Information Disclosure | core tables cross-tenant read | high | mitigate | Postgres RLS policy + app-level owner_id WHERE filter (D-25); cross-tenant integration test |
| T-01-signup | Tampering | auth router signup | high | mitigate | databaseHooks user_count>0 lock returns 409 SIGNUP_LOCKED (D-24, AUTH-03) |
| T-01-docs | Information Disclosure | /docs /redoc exposure | medium | mitigate | Disable in prod ENV=prod; Basic-Auth gate when DOCS_BASIC_AUTH=true (D-32) |
| T-01-versioning | Tampering | API version middleware | medium | mitigate | Reject missing Accept header with 415 (D-27) |
| T-01-SC | Tampering | pip installs (fastapi, sqlmodel, pyjwt, cryptography, httpx, uvicorn, alembic) | high | mitigate | All packages [OK] per RESEARCH Package Legitimacy Audit; pin versions in requirements.txt |
</threat_model>

<verification>
- `cd api && alembic upgrade head` clean on empty Postgres
- `cd api && pytest tests -q` all green
- `curl -H "Accept: application/vnd.puzzlessbox.v1+json" -H "Authorization: Bearer <jwt>" http://localhost:8000/board-items` returns caller's drafts
- Second signup returns 409 SIGNUP_LOCKED
</verification>

<success_criteria>
- AUTH-01: signup creates user (tested)
- AUTH-02: login + JWT verify via JWKS (tested)
- AUTH-03: signup lock after first user (tested boundary)
- AUTH-04: RLS + cross-tenant isolation (tested boundary)
- CAP-01: POST /drafts persists structured draft (tested)
- Schema migrated with owner_id, RLS, VIEW, seed categories
</success_criteria>

<output>
Create `.planning/phases/01-datenmodell-backend-api/01-01-SUMMARY.md` when done
</output>
