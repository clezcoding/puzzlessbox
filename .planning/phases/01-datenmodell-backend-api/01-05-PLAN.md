---
phase: 01-datenmodell-backend-api
plan: 05
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - api/alembic.ini
  - api/alembic/env.py
  - api/alembic/script.py.mako
  - api/alembic/versions/0001_initial_schema.py
  - api/app/models/__init__.py
  - api/app/models/category.py
  - api/app/models/note.py
  - api/app/models/link.py
  - api/app/models/task.py
  - api/app/models/event.py
  - api/app/models/board.py
  - api/tests/unit/test_models.py
  - api/tests/integration/test_tenancy.py
autonomous: true
requirements: [AUTH-04, CAP-01]
user_setup: []

must_haves:
  truths:
    - "Alembic migration 0001 creates notes, links, tasks, events, categories, users, service_principals tables with indexed owner_id on all core tables (AUTH-04, D-01, D-23, D-25)"
    - "Postgres RLS policies on notes/links/tasks/events/categories restrict rows to owner_id = current_setting('app.owner_id')::uuid (D-25)"
    - "board_items VIEW = UNION ALL over notes/links/tasks/events projecting common columns + type discriminator (D-01)"
    - "categories table seeded with 5 defaults: Inbox, Notizen, Links, Tasks, Termine (D-04)"
    - "service_principals(owner_id uuid pk, name text, bearer_hash text, created_at timestamptz) exists for D-23 service bearer owner_id mapping (prep for Phase 2 MCP)"
    - "All timestamps stored in Europe/Berlin (D-28)"
    - "SQLModel draft validates {title, type in {note,link,task,event}, category_id, summary} (CAP-01, D-02, D-30)"
  artifacts:
    - "api/app/models/{category,note,link,task,event,board}.py — SQLModel tables (D-01, D-03, D-04)"
    - "api/alembic/versions/0001_initial_schema.py — tables, VIEW, RLS policies, service_principals, seed categories (D-25, D-26)"
    - "api/tests/unit/test_models.py — test_draft_validation (CAP-01)"
    - "api/tests/integration/test_tenancy.py — test_rls (AUTH-04)"
  key_links:
    - "categories.category_id ← notes/links/tasks/events.category_id FK (D-04)"
    - "RLS policy USING (owner_id = current_setting('app.owner_id')::uuid) ↔ database session SET app.owner_id (D-25)"
  prohibitions:
    - "No auth router / signup / login / draft CRUD endpoints (Plan 06)"
    - "No timeout / scraper / calendar (Plans 02/03/04)"
---

<objective>
Schema + migration plan: SQLModel tables, Alembic 0001 with full tenancy (owner_id + RLS + VIEW + service_principals), seed categories, and schema tests (test_draft_validation, test_rls). Builds on Plan 01 shell + Wave 0 harness.

Purpose: Plan 06 auth + draft CRUD tracer needs the schema + RLS + models in place. Splitting schema out keeps each plan focused and under 15 files.
Output: Alembic-migrated Postgres schema with RLS + VIEW + service_principals + 5 seed categories; test_draft_validation + test_rls green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenmodell-backend-api/01-CONTEXT.md
@.planning/phases/01-datenmodell-backend-api/01-RESEARCH.md
@.planning/phases/01-datenmodell-backend-api/01-PATTERNS.md
@.planning/phases/01-datenmodell-backend-api/01-VALIDATION.md
@.planning/phases/01-datenmodell-backend-api/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLModel tables + Alembic 0001 migration (RLS + VIEW + service_principals + seeds)</name>
  <files>api/alembic.ini, api/alembic/env.py, api/alembic/script.py.mako, api/alembic/versions/0001_initial_schema.py, api/app/models/__init__.py, api/app/models/category.py, api/app/models/note.py, api/app/models/link.py, api/app/models/task.py, api/app/models/event.py, api/app/models/board.py</files>
  <read_first>
    - .planning/phases/01-datenmodell-backend-api/01-RESEARCH.md (lines 261-265 RLS don't hand-roll, lines 424-443 Security Domain)
    - .planning/phases/01-datenmodell-backend-api/01-PATTERNS.md (lines 159-196 shared model patterns)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-23, D-25, D-26, D-28)
    - api/app/core/database.py (from Plan 01)
  </read_first>
  <behavior>
    - test_seed_categories: after migration, categories contains Inbox, Notizen, Links, Tasks, Termine (D-04)
    - test_service_principals_table: service_principals(owner_id, name, bearer_hash) exists (D-23)
    - test_board_items_view: board_items VIEW returns UNION ALL of notes/links/tasks/events with type discriminator (D-01)
  </behavior>
  <action>
Per D-01: separate SQLModel tables notes/links/tasks/events with shared columns (id uuid pk, owner_id uuid not null indexed, category_id fk→categories, status enum draft|auto_saved|confirmed per D-02, deleted_at nullable per D-03, created_at/updated_at timestamptz defaulting Europe/Berlin per D-28). Per D-04: categories table. Per D-23: service_principals(owner_id uuid pk, name text not null, bearer_hash text not null, created_at timestamptz). board.py: define board_items VIEW as UNION ALL over the four tables projecting common columns + type discriminator (D-01). Alembic env.py uses SQLModel metadata; alembic.ini standard. 0001_initial_schema.py: create all tables (users, categories, notes, links, tasks, events, service_principals), CREATE VIEW board_items, enable RLS on notes/links/tasks/events/categories with policy `USING (owner_id = current_setting('app.owner_id')::uuid)` (D-25), seed 5 default categories (D-04). Wire database.py session dependency (from Plan 01) to SET LOCAL app.owner_id = owner_id and SET ROLE per request (RLS enforcement) — update database.py if needed, but it is owned by Plan 01 so only extend behavior, do not rewrite. Do NOT create auth/capture/links/events routers. Do NOT implement timeout/scraper/calendar. [BLOCKING] Run `alembic upgrade head` against dev Postgres before verify.
  </action>
  <verify>
    <automated>cd api && alembic upgrade head && python -c "from app.models import Note, Link, Task, Event, Category; assert all([Note, Link, Task, Event, Category])"</automated>
  </verify>
  <done>Migration 0001 creates all tables + VIEW + RLS + service_principals + 5 seed categories; models importable; committed.</done>
  <reversibility rating="costly">Schema + RLS + VIEW shape all downstream CRUD/MCP; migrating after launch is expensive.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema tests — test_draft_validation + test_rls</name>
  <files>api/tests/unit/test_models.py, api/tests/integration/test_tenancy.py</files>
  <read_first>
    - .planning/phases/01-datenmodell-backend-api/01-VALIDATION.md (test map: AUTH-04 test_rls, CAP-01 test_draft_validation)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-02, D-25, D-30)
    - api/tests/conftest.py (from Plan 01 — two-owner fixtures, mock Postgres)
    - api/app/models/note.py (from Task 1)
  </read_first>
  <behavior>
    - test_draft_validation: SQLModel draft with {title, type:note, category_id, summary} validates; type must be in {note,link,task,event} (CAP-01, D-02, D-30)
    - test_rls: with RLS enabled and app.owner_id=A, owner A sees only A's rows; owner B sees only B's rows; cross-tenant SELECT returns empty (AUTH-04 boundary, D-25)
  </behavior>
  <action>
test_models.py::test_draft_validation: instantiate Note/Link/Task/Event SQLModel rows with valid {title, type, category_id, summary}; assert Pydantic validation passes; assert type enum rejects values outside {note,link,task,event} (D-30). test_tenancy.py::test_rls: use conftest two-owner fixtures (owner_a, owner_b) + transactional Postgres fixture; insert a note for owner_a; SET app.owner_id=owner_a, SELECT notes → 1 row; SET app.owner_id=owner_b, SELECT notes → 0 rows (cross-tenant empty, not 403 — AUTH-04 boundary). Use the mock JWKS fixture only indirectly (RLS test does not need JWT). [BLOCKING] `alembic upgrade head` must run before this test.
  </action>
  <verify>
    <automated>cd api && alembic upgrade head && pytest tests/unit/test_models.py::test_draft_validation tests/integration/test_tenancy.py::test_rls -q</automated>
  </verify>
  <done>test_draft_validation + test_rls green; AUTH-04 + CAP-01 schema boundaries covered; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI→Postgres | Internal; RLS enforces tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-tenant | Information Disclosure | core tables cross-tenant read | high | mitigate | Postgres RLS policy + app-level owner_id WHERE filter (D-25); cross-tenant integration test_rls |
</threat_model>

<verification>
- `cd api && alembic upgrade head` clean on empty Postgres
- `cd api && pytest tests/unit/test_models.py tests/integration/test_tenancy.py -q` green
- `psql -c '\d service_principals'` shows table exists (D-23)
- `psql -c 'SELECT name FROM categories ORDER BY name'` returns 5 seeded rows (D-04)
</verification>

<success_criteria>
- AUTH-04: RLS + cross-tenant isolation (tested boundary)
- CAP-01: SQLModel draft validation (tested)
- Schema migrated with owner_id, RLS, VIEW, service_principals, seed categories
</success_criteria>

<output>
Create `.planning/phases/01-datenmodell-backend-api/01-05-SUMMARY.md` when done
</output>
