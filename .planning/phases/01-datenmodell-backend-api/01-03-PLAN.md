---
phase: 01-datenmodell-backend-api
plan: 03
type: execute
wave: 4
depends_on: [01-06]
files_modified:
  - api/app/services/__init__.py
  - api/app/services/scraper.py
  - api/app/routers/links.py
  - api/app/routers/health.py
  - api/app/core/config.py
  - docker-compose.scraper.yml
  - api/tests/integration/test_scraper.py
  - api/tests/unit/test_scraper.py
autonomous: false
requirements: [LINK-01, LINK-02]
user_setup:
  - service: firecrawl-self-host
    why: "Primary link metadata scraper (CF/WAF bypass)"
    env_vars:
      - name: FIRECRAWL_URL
        source: "Coolify internal service URL (e.g. http://firecrawl:3002)"
      - name: FIRECRAWL_BEARER
        source: "Coolify secret — shared internal bearer"
    dashboard_config:
      - task: "Deploy Firecrawl self-host on Coolify as internal-only app (no public route)"
        location: "Coolify → New Resource → Docker Compose → paste firecrawl service from docker-compose.scraper.yml"
  - service: camoufox-sidecar
    why: "Fallback scraper when Firecrawl fails (4s remainder of 12s budget)"
    env_vars:
      - name: CAMOUFOX_URL
        source: "Coolify internal service URL (e.g. http://camoufox:8080)"
      - name: CAMOUFOX_BEARER
        source: "Coolify secret — shared internal bearer"
    dashboard_config:
      - task: "Deploy Camoufox light sidecar on Coolify as internal-only app"
        location: "Coolify → New Resource → Docker Compose → paste camoufox service"

must_haves:
  truths:
    - "POST /links with {url} scrapes metadata (title, description, image) and persists a link row with JSONB metadata + owner_id (LINK-01, D-09, D-10)"
    - "Scrape is synchronous inside link POST with hard 12s budget: Firecrawl ≤8s, then Camoufox ≤4s remainder (D-09, D-13)"
    - "On scrape failure the link still persists with hostname/URL fallback title, scrape_status=failed, category=Links (D-11, LINK-02 boundary)"
    - "Link items always land in the Links category (seeded default) — never Inbox/Notes (LINK-02, D-04)"
    - "Firecrawl + Camoufox are internal-only (not publicly routed); shared bearer auth (D-14, D-15)"
    - "Camoufox sidecar returns raw HTML; API parses OpenGraph tags (D-16)"
    - "When SCRAPER_ENABLED=true, /ready pings Firecrawl (and Camoufox if reachable) and returns 503 if scraper unhealthy (D-31 optional scraper ping on /ready)"
  artifacts:
    - "api/app/services/scraper.py — ScrapeService with firecrawl_then_camoufox fallback, 12s budget, hostname fallback"
    - "api/app/routers/health.py — extended /ready to ping scraper services when SCRAPER_ENABLED=true (D-31)"
    - "api/app/routers/links.py — POST /links endpoint"
    - "docker-compose.scraper.yml — Firecrawl + Camoufox services, internal network, healthchecks"
    - "api/tests/integration/test_scraper.py — test_scrape (success), test_scrape_fail_fallback, test_default_cat, test_ready_scraper_ping"
  key_links:
    - "POST /links → ScrapeService.scrape(url) → Firecrawl (8s) → Camoufox (4s) → persist"
    - "links.category_id → categories row where name='Links' (seeded, D-04)"
    - "/ready (SCRAPER_ENABLED=true) → ping Firecrawl + Camoufox → 503 if unhealthy (D-31)"
  prohibitions:
    - "No public Firecrawl/Camoufox endpoints (D-14 — internal only)"
    - "No silent failure — scrape_status must be set to failed/ok explicitly"
    - "No paid proxies or Firecrawl core patch (D-12)"
    - "No link landing outside Links category (LINK-02)"
---

<objective>
Link scrape pipeline: POST /links synchronously scrapes metadata via Firecrawl (primary) → Camoufox (fallback) → hostname fallback, persists JSONB, always categorizes as Links.

Purpose: Zero-SaaS-cost link previews that survive Cloudflare/WAF. Tracer proves one link persists with metadata; expansion proves failure fallback + category invariant.
Output: ScrapeService, links router, Coolify scraper compose, tests covering LINK-01/02 boundaries.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-datenmodell-backend-api/01-CONTEXT.md
@.planning/phases/01-datenmodell-backend-api/01-RESEARCH.md
@.planning/phases/01-datenmodell-backend-api/01-PATTERNS.md
@.planning/phases/01-datenmodell-backend-api/01-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — POST /links → Firecrawl → persist JSONB end-to-end</name>
  <files>api/app/services/__init__.py, api/app/services/scraper.py, api/app/routers/links.py, api/app/core/config.py, api/tests/integration/test_scraper.py</files>
  <read_first>
    - .planning/phases/01-datenmodell-backend-api/01-RESEARCH.md (lines 354-366 state of the art on scrapers, lines 386-388 env availability)
    - .planning/phases/01-datenmodell-backend-api/01-PATTERNS.md (lines 224-234 scraper no-analog guidance)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-09, D-10, D-12, D-13, D-14, D-16)
    - api/app/models/link.py (from Plan 05)
  </read_first>
  <action>
Implement ScrapeService.scrape(url) -> ScrapeResult{title, description, image, scrape_status}. SSRF guard FIRST (T-01-ssrf mitigation): reject non-http(s) schemes (allow only `http`, `https`); resolve hostname via socket.getaddrinfo and reject if any resolved IP falls in private ranges (RFC1918 10/8, 172.16/12, 192.168/16, 169.254/16 link-local, 127.0.0.0/8 loopback, ::1, fc00::/7, fe80::/10); reject URLs longer than 2048 chars. Primary: httpx.AsyncClient POST to FIRECRAWL_URL/v1/scrape with bearer FIRECRAWL_BEARER, timeout=8.0s, body {url, formats:[\"html\"]} (D-12, D-13, D-14). Parse returned HTML for OpenGraph tags (og:title, og:description, og:image) — use a small regex/BeautifulSoup parser, not a heavy dep. If Firecrawl fails or returns empty title, fallback: GET CAMOUFOX_URL/?url=... with bearer CAMOUFOX_BEARER, timeout=4.0s (D-13, D-16 — Camoufox returns HTML, API parses OG). Required metadata: title + url (D-10); description + image optional, store as None if missing. Persist link row: owner_id from JWT, url, title (or hostname fallback if both scrapers failed — see Task 2), description, image as JSONB column `metadata`, scrape_status in {ok, failed}, category_id resolved to the seeded Links category (D-04, LINK-02). Wire POST /links router. Integration test test_scrape mocks Firecrawl HTTP via httpx mock, asserts 201 + JSONB metadata + scrape_status=ok + category=Links. Add test_ssrf_blocked: POST /links with url `http://127.0.0.1` or `http://169.254.169.254` returns 422 with code SSRF_BLOCKED (T-01-ssrf boundary). Do NOT implement Camoufox fallback or hostname-failed path in tracer (Task 2).
  </action>
  <verify>
    <automated>cd api && pytest tests/integration/test_scraper.py::test_scrape -q</automated>
  </verify>
  <acceptance_criteria>
    - POST /links with valid url returns 201; persisted row has metadata JSONB with title
    - scrape_status=ok when Firecrawl returns title
    - category_id resolves to the seeded Links category (LINK-02)
    - Firecrawl HTTP call uses bearer + 8s timeout (D-13, D-14)
  </acceptance_criteria>
  <done>One link persists with Firecrawl metadata + Links category; committed.</done>
  <reversibility rating="costly">Scraper service coupling + Coolify deploy; changing stack later means re-deploying services.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Camoufox fallback + hostname failure + Coolify scraper compose</name>
  <files>api/app/services/scraper.py, api/app/routers/links.py, api/app/routers/health.py, docker-compose.scraper.yml, api/tests/integration/test_scraper.py, api/tests/unit/test_scraper.py</files>
  <read_first>
    - api/app/services/scraper.py (from Task 1)
    - api/app/routers/health.py (from Plan 01 — extend /ready)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-11, D-12, D-13, D-14, D-15, D-16, D-31)
    - https://github.com/daijro/camoufox (sidecar API shape)
  </read_first>
  <behavior>
    - test_camoufox_fallback: Firecrawl fails (timeout or 5xx) → Camoufox succeeds → scrape_status=ok, title from Camoufox OG
    - test_scrape_fail_fallback: both Firecrawl and Camoufox fail → link still persists with title=hostname, scrape_status=failed (LINK-01 boundary)
    - test_default_cat: link always lands in Links category even when scrape fails (LINK-02 boundary)
    - test_12s_budget: total scrape time never exceeds 12s (Firecrawl 8s + Camoufox 4s)
    - test_ready_scraper_ping: when SCRAPER_ENABLED=true, /ready pings Firecrawl + Camoufox health endpoints and returns 503 if either is unreachable (D-31 optional scraper ping)
  </behavior>
  <action>
Extend ScrapeService: on Firecrawl failure (httpx.TimeoutException, HTTPStatusError, or empty title), call Camoufox with 4s timeout (D-13). On Camoufox failure too, set title=hostname (urllib.parse.urlparse(url).netloc), description=None, image=None, scrape_status=failed (D-11, LINK-01 boundary). Always resolve category_id to the seeded Links category regardless of scrape outcome (LINK-02). Total budget enforced by httpx timeouts (8s + 4s = 12s hard ceiling, D-13). Extend api/app/routers/health.py /ready endpoint (from Plan 01): when SCRAPER_ENABLED=true, after the DB SELECT 1 check, ping Firecrawl (GET FIRECRAWL_URL/health with bearer, 2s timeout) and Camoufox (GET CAMOUFOX_URL/health with bearer, 2s timeout); return 503 with {error:{code:SCRAPER_UNHEALTHY, details:{service}}} if either ping fails (D-31 optional scraper ping on /ready). When SCRAPER_ENABLED=false, /ready behavior is unchanged from Plan 01 (DB ping only). Write docker-compose.scraper.yml: firecrawl service (image from firecrawl self-host docs, internal network only, no published port, healthcheck), camoufox sidecar (light image, internal only, healthcheck). Both services behind shared bearer set via Coolify secrets (D-14). Add unit test test_default_cat asserting category resolution logic without HTTP. Add integration tests for the four failure paths + test_ready_scraper_ping (mock Firecrawl/Camoufox health endpoints via httpx mock; assert 503 when Firecrawl down, 200 when both up) using httpx mocks. Document in scraper.py that Firecrawl self-host lacks Fire-engine (D-12 note) — CF/WAF failures are best-effort.
  </action>
  <verify>
    <automated>cd api && pytest tests/integration/test_scraper.py tests/unit/test_scraper.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Firecrawl failure → Camoufox fallback succeeds → scrape_status=ok
    - Both fail → link persists with hostname title, scrape_status=failed (LINK-01 boundary)
    - Link always in Links category (LINK-02 boundary)
    - Total scrape time ≤ 12s
    - /ready with SCRAPER_ENABLED=true returns 503 SCRAPER_UNHEALTHY when Firecrawl or Camoufox unreachable (D-31)
    - /ready with SCRAPER_ENABLED=false behavior unchanged (DB ping only)
    - docker-compose.scraper.yml validates: `docker compose -f docker-compose.scraper.yml config`
  </acceptance_criteria>
  <done>All LINK-01/02 boundaries covered; /ready scraper ping wired (D-31); Coolify scraper compose ready; tests green; committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Firecrawl + Camoufox Coolify internal deploy</name>
  <what-built>Firecrawl self-host + Camoufox sidecar deployed on Coolify as internal-only apps; POST /links reaches them via internal network.</what-built>
  <how-to-verify>
    1. In Coolify, deploy docker-compose.scraper.yml as two internal-only apps (no public route, no Traefik label).
    2. Set FIRECRAWL_URL, FIRECRAWL_BEARER, CAMOUFOX_URL, CAMOUFOX_BEARER in the API app's env via Coolify secrets.
    3. From the API container: `curl -H "Authorization: Bearer $FIRECRAWL_BEARER" $FIRECRAWL_URL/v1/scrape -d '{"url":"https://example.com","formats":["html"]}'` returns HTML.
    4. `curl -H "Authorization: Bearer $CAMOUFOX_BEARER" "$CAMOUFOX_URL/?url=https://example.com"` returns HTML.
    5. From outside Coolify: `curl https://firecrawl.puzzlesstool.online` should NOT resolve (internal only — D-14).
    6. POST /links to a real URL → 201 with populated metadata.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. Firecrawl unreachable, Camoufox timeout)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→POST /links | JWT-verified; url is untrusted user input |
| FastAPI→Firecrawl/Camoufox | Internal HTTP; shared bearer (D-14) |
| Firecrawl/Camoufox→external site | Scraper fetches arbitrary user-supplied URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-ssrf | Tampering / Information Disclosure | scraper fetches user-supplied URL | high | mitigate | Block private IP ranges (RFC1918, 169.254.0.0/16, 127.0.0.0/8, ::1) before scrape; reject non-http(s) schemes; cap URL length |
| T-01-scraper-bearer | Spoofing | Firecrawl/Camoufox internal bearer | high | mitigate | Internal-only Coolify network (no public route); shared bearer from Coolify secret (D-14) |
| T-01-scrape-timeout | DoS | scraper hangs on slow site | medium | mitigate | Hard 12s budget (Firecrawl 8s + Camoufox 4s, D-13); httpx timeouts |
| T-01-SC | Tampering | pip installs (httpx already pinned in Plan 01; no new packages) | low | accept | No new packages in this plan; BeautifulSoup avoided — regex/stdlib OG parse |
</threat_model>

<verification>
- `cd api && pytest tests/integration/test_scraper.py tests/unit/test_scraper.py -q` all green
- `docker compose -f docker-compose.scraper.yml config` validates
- Human checkpoint confirms Coolify internal-only routing + reachability
</verification>

<success_criteria>
- LINK-01: link with metadata persisted in JSONB; failure still persists with hostname fallback (tested boundary)
- LINK-02: links always in Links category (tested boundary)
- D-09: sync scrape inside link POST
- D-12/13/14/15/16: Firecrawl+Camoufox stack, 12s budget, internal bearer, Phase 1 deploy, OG parse
</success_criteria>

<output>
Create `.planning/phases/01-datenmodell-backend-api/01-03-SUMMARY.md` when done
</output>
