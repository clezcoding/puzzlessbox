---
phase: 01-datenmodell-backend-api
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - api/app/services/__init__.py
  - api/app/services/timeout.py
  - api/app/routers/capture.py
  - api/tests/integration/test_capture.py
autonomous: true
requirements: [CAP-03]
user_setup: []

must_haves:
  truths:
    - "POST /drafts starts a 30s asyncio.Task timer; after 30s of inactivity the draft transitions draft→auto_saved and is queryable via /board-items (CAP-03, D-05, D-08)"
    - "PATCH /drafts/{id} resets the timer to now+30s (inactivity semantics, D-06)"
    - "POST /drafts/{id}/confirm cancels the timer and transitions draft→confirmed immediately (D-07)"
    - "30s inactivity is exact (not 60s Hermes cron); timer lives in FastAPI process memory (D-05 pitfall)"
    - "Parallel edits to the same draft are safe — timer reset is idempotent per draft (CAP-03 concurrency)"
    - "Timer cancellation on confirm is exact — no orphan auto_save fires after confirm (CAP-03 boundary)"
  artifacts:
    - "api/app/services/timeout.py — DraftTimeoutManager singleton with schedule_timeout/cancel_timeout"
    - "api/app/routers/capture.py — modified to call timeout_manager on POST/PATCH/confirm"
    - "api/tests/integration/test_capture.py — test_autosave, test_patch_resets, test_confirm_cancels, test_parallel_edits"
  key_links:
    - "POST /drafts → DraftTimeoutManager.schedule_timeout(draft_id, owner_id, 30.0)"
    - "PATCH /drafts/{id} → schedule_timeout (cancels old, starts new)"
    - "POST /drafts/{id}/confirm → cancel_timeout + status=confirmed"
  prohibitions:
    - "No Hermes-Cron-based timeout (60s tick insufficient; D-05 pitfall)"
    - "No silent overwrite of confirmed drafts by late auto_save"
---

<objective>
Capture timeout state machine: 30s inactivity auto-save with PATCH reset and confirm cancel, server-side in FastAPI.

Purpose: Hermes cron ticks at 60s — sub-minute precision impossible. State machine must live in API. Tracer proves one draft goes draft→auto_saved after 30s; expansion proves reset/cancel/parallel edges.
Output: DraftTimeoutManager + modified capture router + integration tests covering all CAP-03 boundaries.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-datenmodell-backend-api/01-CONTEXT.md
@.planning/phases/01-datenmodell-backend-api/01-RESEARCH.md
@.planning/phases/01-datenmodell-backend-api/01-PATTERNS.md
@.planning/phases/01-datenmodell-backend-api/01-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — 30s auto-save path end-to-end</name>
  <files>api/app/services/__init__.py, api/app/services/timeout.py, api/app/routers/capture.py, api/tests/integration/test_capture.py</files>
  <read_first>
    - .planning/phases/01-datenmodell-backend-api/01-RESEARCH.md (lines 287-322 DraftTimeoutManager pattern, lines 269-273 Hermes cron pitfall)
    - .planning/phases/01-datenmodell-backend-api/01-PATTERNS.md (lines 79-118 timeout analog)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-05, D-06, D-07, D-08)
    - api/app/routers/capture.py (from Plan 01)
  </read_first>
  <action>
Implement DraftTimeoutManager per RESEARCH lines 287-322: in-memory `Dict[draft_id, asyncio.Task]`, schedule_timeout cancels any existing timer for that draft_id then spawns `asyncio.create_task(_wait_and_autosave(draft_id, owner_id, 30.0))`. _wait_and_autosave sleeps 30s then in a DB transaction updates the row status draft→auto_saved (D-02, D-08) WHERE status='draft' (idempotent — if already confirmed, no-op). CancelledError swallowed (expected on PATCH/confirm). Wire POST /drafts to call timeout_manager.schedule_timeout after commit. Add integration test test_autosave: create draft, advance a mock clock or use asyncio.sleep(31) with a shortened delay override (inject delay via env DRAFT_TIMEOUT_SECONDS=1 for tests), assert status becomes auto_saved and GET /board-items returns it. Per D-05: timer is asyncio.Task in FastAPI process — no Celery/Redis. Per D-08: no Hermes push webhook in Phase 1. Do NOT implement PATCH reset or confirm in this tracer (Task 2).
  </action>
  <verify>
    <automated>cd api && DRAFT_TIMEOUT_SECONDS=1 pytest tests/integration/test_capture.py::test_autosave -q</automated>
  </verify>
  <acceptance_criteria>
    - Draft created with status=draft; after timeout elapses, status=auto_saved in DB
    - GET /board-items returns the auto_saved draft
    - Timer is asyncio.Task in FastAPI process (no external worker)
    - Cancellation via task.cancel() prevents auto_save from firing
  </acceptance_criteria>
  <done>One draft auto-saves after 30s (test uses 1s override); committed.</done>
  <reversibility rating="costly">State machine contract used by Hermes plugin later; changing semantics post-launch breaks Hermes.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH reset + confirm cancel + parallel edits</name>
  <files>api/app/services/timeout.py, api/app/routers/capture.py, api/tests/integration/test_capture.py</files>
  <read_first>
    - api/app/services/timeout.py (from Task 1)
    - api/app/routers/capture.py (from Task 1)
    - .planning/phases/01-datenmodell-backend-api/01-CONTEXT.md (D-06, D-07)
  </read_first>
  <behavior>
    - test_patch_resets: PATCH /drafts/{id} within 30s resets timer; auto_save fires 30s after the PATCH, not the original POST
    - test_confirm_cancels: POST /drafts/{id}/confirm sets status=confirmed and no auto_save fires afterward
    - test_parallel_edits: two concurrent PATCHes to the same draft both succeed; timer reset is idempotent; no race condition
    - test_no_orphan_autosave: after confirm, even if the original timer's sleep completes, status stays confirmed (WHERE status='draft' guard)
  </behavior>
  <action>
Per D-06: PATCH /drafts/{id} updates allowed fields (title, summary, category_id) and calls timeout_manager.schedule_timeout(draft_id, owner_id, 30.0) — schedule_timeout cancels prior task before spawning new one, so reset is implicit. Per D-07: POST /drafts/{id}/confirm sets status=confirmed in a transaction then calls cancel_timeout(draft_id). _execute_autosave must UPDATE ... WHERE status='draft' so a confirmed draft is never overwritten by a late auto_save (CAP-03 boundary, no silent overwrite). Parallel edit safety: schedule_timeout is synchronous w.r.t. the dict mutation (single-threaded asyncio loop) — no lock needed; document this in a comment. Add the four behavior tests. Do NOT change the 30s default — tests use DRAFT_TIMEOUT_SECONDS override.
  </action>
  <verify>
    <automated>cd api && DRAFT_TIMEOUT_SECONDS=1 pytest tests/integration/test_capture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - PATCH within timeout pushes auto_save to 30s after PATCH (test with 1s override)
    - Confirm sets status=confirmed; no auto_save fires after confirm
    - Two concurrent PATCHes both return 200; final timer fires once
    - Late auto_save does NOT overwrite a confirmed draft (WHERE status='draft' guard)
  </acceptance_criteria>
  <done>All CAP-03 boundaries covered: 30s exact, PATCH resets, confirm cancels, parallel safe; tests green; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→POST/PATCH/confirm | JWT-verified owner; draft must belong to caller |
| timer→DB | Internal; UPDATE guarded by owner_id + status='draft' |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-timer-race | Tampering | timeout manager parallel reset | medium | mitigate | Single-threaded asyncio loop; schedule_timeout cancels-then-spawns atomically |
| T-01-orphan-save | Tampering | late auto_save overwrites confirmed | high | mitigate | UPDATE WHERE status='draft' guard; cancel_timeout on confirm (D-07) |
| T-01-cross-owner | Information Disclosure | PATCH/confirm foreign draft | high | mitigate | RLS + app-level owner_id filter from Plan 01; 404 if draft not owned |
</threat_model>

<verification>
- `cd api && DRAFT_TIMEOUT_SECONDS=1 pytest tests/integration/test_capture.py -q` all green
- Manual: POST /drafts, wait 31s, GET /board-items shows auto_saved
- Manual: POST /drafts, PATCH at 10s, wait 31s from PATCH → auto_saved
- Manual: POST /drafts, confirm at 5s, wait 31s → status stays confirmed
</verification>

<success_criteria>
- CAP-03: 30s inactivity auto-save (tested, exact)
- D-05: timer in FastAPI, not Hermes
- D-06: PATCH resets timer
- D-07: confirm cancels timer
- D-08: auto_saved queryable via board_items
- Parallel edits safe; no silent overwrite of confirmed
</success_criteria>

<output>
Create `.planning/phases/01-datenmodell-backend-api/01-02-SUMMARY.md` when done
</output>
