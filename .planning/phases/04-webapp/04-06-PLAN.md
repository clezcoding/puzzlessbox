---
gap_closure: true
gap_ids: [G-04-bulk-move]
phase: 04-webapp
plan: 06
type: execute
wave: 1
depends_on: [04-03]
files_modified:
  - webapp/components/board/bulk-move-bar.tsx
  - webapp/tests/dnd.test.tsx
autonomous: true
requirements: [BOARD-03]
response_language: de

must_haves:
  truths:
    - "BulkMoveBar trigger Button exposes data-testid=\"bulk-move-trigger\" for stable automation targeting"
    - "BulkMoveBar destination DropdownMenuItem exposes data-testid=\"bulk-move-destination-{category.id}\" per category"
    - "Clicking a destination category in BulkMoveBar fires moveItem once per selectedId with correct (id, categoryId) args"
    - "moveItem call count equals the initial selection count"
    - "After all moveItem calls resolve, onMoved (board refresh) and onClear (selection reset) fire exactly once"
    - "After bulk destination commit, selection count resets to 0 and bulk bar unmounts (count===0 early return)"
    - "Vitest suite stays green with the new bulk destination click test asserting PATCH count + count delta"
  artifacts:
    - webapp/components/board/bulk-move-bar.tsx
    - webapp/tests/dnd.test.tsx
  key_links:
    - "DropdownMenuItem onClick -> handleBulkMove(categoryId) -> moveItem(id, categoryId) loop -> onMoved() + onClear() -> count=0 -> bar unmounts"
---

<objective>
Close the last open Phase 4 UAT gap (test 11, severity major): bulk multi-select move destination commit was never verified — UAT agents saw the bulk bar with "2 ausgewählt" but could not capture the destination PATCH or count delta. The feature is already wired in `bulk-move-bar.tsx`; the gap is automation-targeting + regression-proof, not missing functionality.

Purpose: Make the bulk destination commit reliably targetable and provable in CI so the UAT truth "Bulk multi-select moves >=2 items into chosen category" is backed by a green test that clicks the destination, asserts sequential `moveItem` calls, and asserts `onMoved`/`onClear` fire. No rebuild, no new behavior, no new dependency.

Output: Two tiny edits — `data-testid` on trigger + destination items in `bulk-move-bar.tsx`, and a strengthened `dnd.test.tsx` bulk test that opens the dropdown, clicks a destination category, and asserts sequential PATCH evidence. Phase 4 UAT #11 closes; BOARD-03 prod UAT pending list shrinks by one.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-webapp/04-UAT.md
@.planning/phases/04-webapp/04-VERIFICATION.md
@.planning/STATE.md
@webapp/components/board/bulk-move-bar.tsx
@webapp/components/board/board-card.tsx
@webapp/app/board/page.tsx
@webapp/tests/dnd.test.tsx
@webapp/lib/api/items.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add data-testid to bulk-move-bar trigger + destination items</name>
  <files>webapp/components/board/bulk-move-bar.tsx</files>
  <read_first>
    - webapp/components/board/bulk-move-bar.tsx (current implementation — already wires handleBulkMove sequentially)
    - webapp/components/board/board-card.tsx (sibling pattern: data-testid={`board-card-${item.id}`})
    - webapp/lib/api/items.ts (moveItem signature: (id, categoryId) => Promise<{id}>)
  </read_first>
  <behavior>
    - BulkMoveBar trigger button is queryable as [data-testid="bulk-move-trigger"]
    - Each destination DropdownMenuItem is queryable as [data-testid="bulk-move-destination-{category.id}"]
    - Existing data-testid="bulk-move-bar" on the container is preserved
    - No behavioral change to onClick wiring, toast sequence, or onMoved/onClear order
  </behavior>
  <action>
    Per D-22 (bulk multi-select + bulk move in v1). The bar already implements the correct sequential PATCH loop — do NOT rebuild it. Add only the two missing automation hooks:

    1. On the trigger `Button` inside `DropdownMenuTrigger asChild`, add `data-testid="bulk-move-trigger"`. Keep `size="sm"` and the existing label text "In Kategorie verschieben" unchanged (existing test dnd.test.tsx:221 asserts that text — do not break it).

    2. On each `DropdownMenuItem` rendered for `categories.map`, add `data-testid={`bulk-move-destination-${category.id}`}`. Keep `key={category.id}` and `onClick={() => void handleBulkMove(category.id)}` unchanged.

    Do NOT add a data-testid to `DropdownMenuContent` (Radix portals content; not needed). Do NOT change `handleBulkMove` logic, toast thresholds, or the `count === 0` early return. Do NOT add new props to `BulkMoveBarProps`. Do NOT touch `onClear` or the Abbrechen button.

    Why: UAT corroborators (sessions 310b1fc3 and 2dab9a70) both saw the bar but could not reliably target the destination item to capture the PATCH. Stable testids let automation (and the strengthened test in Task 2) click the destination deterministically.
  </action>
  <verify>
    <automated>cd webapp && pnpm vitest run tests/dnd.test.tsx -t "bulk" --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - trigger Button has data-testid="bulk-move-trigger"
    - each destination DropdownMenuItem has data-testid `bulk-move-destination-${category.id}`
    - container data-testid="bulk-move-bar" preserved
    - no new props, no behavioral change to handleBulkMove, toasts, onMoved, onClear
    - existing bulk render test still passes
  </acceptance_criteria>
  <done>Trigger and each destination item are queryable by stable data-testid; existing bulk render test green; no behavioral diff in bulk-move-bar.tsx beyond the two added testid attributes.</done>
  <reversibility rating="reversible">Two attribute additions on existing JSX; revertible by deleting the attributes with zero behavioral impact.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen dnd.test.tsx bulk test — destination click to moveItem evidence + count delta</name>
  <files>webapp/tests/dnd.test.tsx</files>
  <read_first>
    - webapp/tests/dnd.test.tsx (existing "shows bulk move bar when selection > 0" test at lines 209-222 — only asserts render, not destination commit)
    - webapp/components/board/bulk-move-bar.tsx (after Task 1 — has data-testid on trigger + destinations)
    - webapp/lib/api/items.ts (moveItem mock already set up at dnd.test.tsx:68-71, resolved to {id:"item-1"} at line 114)
  </read_first>
  <behavior>
    - Rendering BulkMoveBar with count=2, selectedIds=["item-1","item-2"], categories=[cat-a, cat-b], onMoved=fn, onClear=fn
    - Stateful wrapper tracks count and selectedIds: onClear sets count=0 and selectedIds=[]
    - Clicking the trigger opens the dropdown
    - Clicking destination with data-testid="bulk-move-destination-cat-b" calls moveItem once per selectedId with correct (id, categoryId) args
    - moveItem call count equals initial selection count (2)
    - nth-call args: 1st (item-1, cat-b), 2nd (item-2, cat-b)
    - onMoved fires exactly once after moveItem calls resolve
    - onClear fires exactly once after onMoved
    - After onClear, count resets to 0 and bulk-move-bar unmounts (BulkMoveBar returns null when count===0)
    - toast.success called with "Eintrag verschoben."
  </behavior>
  <action>
    Per D-22. Replace or extend the "shows bulk move bar when selection > 0" test (lines 209-222) with a strengthened test that proves the destination commit end-to-end AND the count delta. Use `userEvent.setup()` (already imported) and `await user.click(...)` for both trigger and destination to let Radix portal open synchronously.

    Test name: `bulk move: clicking destination fires moveItem per selected id, resets selection, and unmounts bulk bar`.

    Use a stateful wrapper component so onClear can actually zero the count (proves the count delta + bar unmount, which a plain vi.fn() cannot):

    1. Define a small wrapper inside the test:
       ```
       function Harness({ initialCount, selectedIds, categories }) {
         const [count, setCount] = useState(initialCount);
         const [ids, setIds] = useState(selectedIds);
         return (
           <BulkMoveBar
             count={count}
             categories={categories}
             selectedIds={ids}
             onMoved={() => {}}
             onClear={() => { setCount(0); setIds([]); }}
           />
         );
       }
       ```
       Import `useState` from "react" at the top of the test file (or inline via React import already available).
    2. const user = userEvent.setup();
    3. render(<Harness initialCount={2} selectedIds={["item-1","item-2"]} categories={categories} />);
    4. Confirm bar present: expect(screen.getByTestId("bulk-move-bar")).toBeInTheDocument();
    5. await user.click(screen.getByTestId("bulk-move-trigger"));
    6. await user.click(await screen.findByTestId("bulk-move-destination-cat-b"));
    7. await waitFor(() => { expect(vi.mocked(moveItem)).toHaveBeenCalledTimes(2); });
    8. expect(vi.mocked(moveItem)).toHaveBeenNthCalledWith(1, "item-1", "cat-b");
    9. expect(vi.mocked(moveItem)).toHaveBeenNthCalledWith(2, "item-2", "cat-b");
    10. expect(toast.success).toHaveBeenCalledWith("Eintrag verschoben.");
    11. Assert count delta + unmount:
        await waitFor(() => {
          expect(screen.queryByTestId("bulk-move-bar")).not.toBeInTheDocument();
        });

    Notes:
    - Do NOT assert "sequential vs parallel" — the await loop in handleBulkMove is sequential, but proving that requires an in-flight concurrency mock. Keep it simple: assert one moveItem call per selectedId with correct nth-call args. The nth-call args also serve as call-order evidence.
    - The `vi.mocked(moveItem).mockResolvedValue({ id: "item-1" })` in beforeEach already makes each call resolve.
    - The stateful Harness makes onClear actually zero count, which triggers BulkMoveBar's `count === 0` early return → bar unmounts. This is the count delta proof the checker required.
    - Do NOT delete the other DnD tests. Do NOT change the vi.mock("@hello-pangea/dnd", ...) or vi.mock("@/lib/api/items", ...) setup. Do NOT add new mocks beyond useState in the wrapper.

    If user.click on the Radix DropdownMenuItem is flaky in jsdom, fall back to fireEvent.click on the element returned by screen.findByTestId — but try userEvent first, it handles Radix pointer events correctly in modern RTL.

    Why: This test is the regression proof that closes UAT #11. Without it, the gap "no PATCH captured, counts not verified" recurs on every UAT run. The testids from Task 1 make the destination deterministic; this test makes the PATCH count + count delta + bar unmount provable in CI.
  </action>
  <verify>
    <automated>cd webapp && pnpm vitest run tests/dnd.test.tsx --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - new test "bulk move: clicking destination fires moveItem per selected id, resets selection, and unmounts bulk bar" passes
    - moveItem call count equals initial selection count (2)
    - moveItem nth-call args asserted: 1st (item-1, cat-b), 2nd (item-2, cat-b)
    - toast.success asserted with "Eintrag verschoben."
    - count delta asserted: after destination click, bulk-move-bar queryable returns null (unmounted via count===0 early return)
    - all other dnd.test.tsx tests still pass (no regression)
    - full webapp vitest suite green: cd webapp && pnpm test -- --run
  </acceptance_criteria>
  <done>Strengthened bulk destination-click test green; moveItem called once per selectedId with correct nth-call args; count resets to 0; bulk-move-bar unmounts; full vitest suite green; UAT #11 gap closed with CI regression proof including count delta.</done>
  <reversibility rating="reversible">Test additions only; revertible by deleting the new test block with zero production impact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> BulkMoveBar -> API | Existing boundary: session cookie crosses to FastAPI PATCH /items/{id}. No new boundary introduced by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06-01 | Tampering | data-testid attributes | low | accept | Testids are inert DOM attributes; no auth or data path reads them. Accept — no runtime impact. |
| T-04-06-02 | Repudiation | bulk moveItem PATCH audit | low | accept | Existing API already logs PATCH /items/{id} with owner_id RLS; this plan adds no new write path, only testids + test coverage. |
</threat_model>

<verification>
- `cd webapp && pnpm vitest run tests/dnd.test.tsx --reporter=verbose` — bulk destination-click test green, all dnd tests green
- `cd webapp && pnpm test -- --run` — full vitest suite green (no regression from testid additions)
- Manual: open webapp board, select 2 cards, click "In Kategorie verschieben", click a destination category, observe items move + toast "Eintrag verschoben." + selection clears (UAT #11 re-run on prod after deploy)
</verification>

<success_criteria>
- BulkMoveBar trigger + each destination item queryable by stable data-testid
- Strengthened dnd.test.tsx test asserts moveItem called once per selectedId with correct nth-call (id, categoryId) args
- Strengthened test asserts moveItem call count equals initial selection count (2)
- Strengthened test asserts count delta: bulk-move-bar unmounts after destination click (count===0)
- Full webapp vitest suite green
- Phase 4 UAT #11 closed at code level; prod re-run pending after next web deploy
</success_criteria>

<output>
Create `.planning/phases/04-webapp/04-06-SUMMARY.md` when done.
</output>

