---
gap_closure: true
gap_ids: [G-04-4]
phase: 04-webapp
plan: 07
type: execute
wave: 1
depends_on: [04-05]
files_modified:
  - webapp/app/login/login-form.tsx
  - webapp/tests/auth.test.tsx
  - .planning/phases/04-webapp/04-UAT.md
  - .planning/phases/04-webapp/04-VERIFICATION.md
autonomous: false
requirements: [AUTH-03]
response_language: de

must_haves:
  truths:
    - "Second registration attempt on prod shows sticky VOICE copy 'Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein.' on the Register tab (G-04-4 / UAT #6)"
    - "sessionStorage key 'pb.signup_locked' is set to '1' after the 409 response is detected from the better-auth client on prod"
    - "isSignupLockedError detects SIGNUP_LOCKED across all better-auth client error envelope shapes (flat message, code, nested body, Response-wrapped, string, circular)"
    - "VOICE copy survives remount via sessionStorage (sticky) — tab stays on Registrieren with copy visible after reload"
    - "Vitest suite stays green with new envelope-shape tests covering shapes that currently miss detection"
  artifacts:
    - webapp/app/login/login-form.tsx
    - webapp/tests/auth.test.tsx
  key_links:
    - "authClient.signUp.email 409 -> isSignupLockedError(envelope) -> lockSignupUi -> sessionStorage[pb.signup_locked]=1 -> setSignupLocked(true) -> VOICE copy rendered"
    - "mount -> readSignupLockedFlag() -> useEffect -> setSignupLocked(true) + setActiveTab(register) -> sticky VOICE copy"
---

<objective>
Close the last open Phase 4 UAT gap (G-04-4 / UAT #6, severity major): on second registration attempt the API correctly returns 409 `{"message":"SIGNUP_LOCKED"}` but the UI shows generic „Registrierung fehlgeschlagen." instead of the Apollo VOICE copy `Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein.`

04-05 shipped sessionStorage persistence (`pb.signup_locked`) + a unit test mock that passes — but prod UAT r3 still fails. Root cause hypothesis (from UAT notes): `isSignupLockedError` misses the better-auth client error envelope shape that arrives on prod; the unit-test mock shape `{ message: "SIGNUP_LOCKED", status: 409, statusText: "Conflict" }` does not match what `authClient.signUp.email` actually returns on a prod 409.

Purpose: Make SIGNUP_LOCKED detection robust to every plausible better-auth client error envelope so the VOICE copy shows on prod regardless of how the client wraps the 409 body. Prove it with envelope-shape tests that currently fail against the existing detector. Verify on prod with a real second registration.

Output: Hardened `isSignupLockedError` + diagnostic log in `webapp/app/login/login-form.tsx`; new envelope-shape tests in `webapp/tests/auth.test.tsx`; prod UAT #6 re-run checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-webapp/04-UAT.md
@.planning/phases/04-webapp/04-VERIFICATION.md
@.planning/phases/04-webapp/04-05-PLAN.md
@.planning/phases/04-webapp/04-05-SUMMARY.md
@.planning/STATE.md
@webapp/app/login/login-form.tsx
@webapp/tests/auth.test.tsx
@webapp/lib/auth-client.ts
@webapp/lib/auth.config.ts

# Gap context (do not reopen closed gaps)
# G-04-1, G-04-2, G-04-3, G-04-5, G-05-*, G-04-bulk-move (04-06) — all CLOSED. Do NOT touch.
# This plan closes G-04-4 ONLY.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Harden isSignupLockedError against better-auth client envelope shapes + envelope-shape tests</name>
  <files>webapp/app/login/login-form.tsx, webapp/tests/auth.test.tsx</files>
  <read_first>
    - webapp/app/login/login-form.tsx (current isSignupLockedError at lines 23-32; lockSignupUi at 34-41; handleRegister at 102-131; signupLocked render at 213-215; SIGNUP_LOCKED_COPY constant at 14-15; SIGNUP_LOCKED_STORAGE_KEY at 16)
    - webapp/tests/auth.test.tsx (existing SIGNUP_LOCKED test at lines 81-110 uses mock shape { message: "SIGNUP_LOCKED", status: 409, statusText: "Conflict" }; sticky-remount test at 112-127; beforeEach clears sessionStorage at line 61)
    - webapp/lib/auth-client.ts (createAuthClient from better-auth/react; jwtClient plugin; baseURL from NEXT_PUBLIC_APP_URL)
    - webapp/lib/auth.config.ts (server throws APIError("CONFLICT", { message: "SIGNUP_LOCKED" }) — 409 body is {"message":"SIGNUP_LOCKED"})
  </read_first>
  <behavior>
    - isSignupLockedError returns true for: flat { message: "SIGNUP_LOCKED" } (current mock)
    - isSignupLockedError returns true for: { code: "SIGNUP_LOCKED", status: 409 } (code field instead of message)
    - isSignupLockedError returns true for: { status: 409, body: { message: "SIGNUP_LOCKED" } } (nested body)
    - isSignupLockedError returns true for: { status: 409, response: { body: { message: "SIGNUP_LOCKED" } } } (Response-wrapped)
    - isSignupLockedError returns true for: { status: 409, json: { message: "SIGNUP_LOCKED" } } (json field variant)
    - isSignupLockedError returns true for a plain string "SIGNUP_LOCKED"
    - isSignupLockedError returns true for an object whose JSON.stringify contains "SIGNUP_LOCKED" (deep fallback)
    - isSignupLockedError returns false for: null, undefined, {}, { message: "INVALID_PASSWORD" }, { status: 500 }
    - When handleRegister receives an error that is detected as SIGNUP_LOCKED, lockSignupUi fires: sessionStorage[pb.signup_locked]="1", setSignupLocked(true), setActiveTab("register")
    - When handleRegister receives an error NOT detected as SIGNUP_LOCKED, the generic "Registrierung fehlgeschlagen." shows AND a console.warn logs the unrecognized error shape for prod diagnosis
    - VOICE copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein." renders when signupLocked is true
    - Existing sticky-remount test (lines 112-127) still passes
    - Existing login error tests (lines 129-211) still pass — no regression
  </behavior>
  <action>
    Per D-25 (Register tab always visible; when signup locked show friendly SIGNUP_LOCKED message, not hide route). Backend lock already wired in auth.config.ts. This task fixes the client-side detection gap.

    **RED first:** Add a new describe block "isSignupLockedError envelope shapes" in webapp/tests/auth.test.tsx. Write failing tests that call isSignupLockedError directly OR drive it via handleRegister by mocking authClient.signUp.email to resolve with each envelope shape. Export isSignupLockedError from login-form.tsx (named export) so tests can call it directly without going through the form. Run tests — they MUST fail for the shapes the current detector misses (nested body, Response-wrapped, json field, plain string).

    **GREEN:** Harden isSignupLockedError in webapp/app/login/login-form.tsx to pass every shape. Replace the current 4-or check with a robust detector that:
    1. Returns false for null/undefined/non-object/string-then-check-string.
    2. Checks flat fields: err.message === "SIGNUP_LOCKED" || err.code === "SIGNUP_LOCKED".
    3. Checks string inclusion on err.message (typeof string && includes "SIGNUP_LOCKED").
    4. Checks nested bodies: err.body?.message, err.response?.body?.message, err.json?.message — each === "SIGNUP_LOCKED" or includes "SIGNUP_LOCKED".
    5. Falls back to JSON.stringify(error).includes("SIGNUP_LOCKED") wrapped in try/catch (circular refs / Response objects throw on stringify — catch and return false, do not throw).
    6. If error is a string, return error === "SIGNUP_LOCKED" || error.includes("SIGNUP_LOCKED").

    Keep the function pure and synchronous. Do NOT import new dependencies. Do NOT change lockSignupUi, handleTabChange, or the render structure. Do NOT remove the existing SIGNUP_LOCKED_COPY constant or the SIGNUP_LOCKED_STORAGE_KEY.

    **Diagnostic log:** In handleRegister, in the else branch where setRegisterError("Registrierung fehlgeschlagen.") is called (both in the if (error) block and the catch block), add console.warn("[signup-locked] unrecognized error shape:", error) before setRegisterError. This makes prod diagnosis possible via browser console if the detector still misses a shape. The else branch is already the unrecognized path — no extra guard needed.

    **REFACTOR:** If the detector grows past ~15 lines, extract a helper getNestedMessage(error) that walks body/response.body/json. Keep it in the same file, do not create a new file.

    Do NOT change the unit test mock shape at line 82-85 — keep it as one of the shapes. Add the new shapes as separate tests in a new describe block so both the old and new shapes are covered.

    Why: The unit test mock shape does not match prod. Without envelope-shape tests, the detector passes CI but fails on prod. The JSON.stringify fallback in the current detector should catch most shapes, but Response objects and circular refs can break stringify — the try/catch + explicit nested-field checks make detection deterministic, not fallback-dependent.
  </action>
  <verify>
    <automated>cd webapp && pnpm vitest run tests/auth.test.tsx --reporter=verbose && pnpm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - isSignupLockedError exported from login-form.tsx and callable from tests
    - New describe block "isSignupLockedError envelope shapes" with tests for: flat message, code field, nested body, Response-wrapped, json field, plain string, deep stringify fallback, null/undefined/empty object negative cases
    - All envelope-shape tests pass (RED→GREEN confirmed in commits)
    - isSignupLockedError hardens against nested body, Response-wrapped, json field, plain string, and circular-ref-safe stringify fallback (try/catch)
    - Diagnostic console.warn("[signup-locked] unrecognized error shape:", error) added in both else branches of handleRegister
    - VOICE copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein." still renders when signupLocked is true
    - sessionStorage key "pb.signup_locked" set to "1" on detected 409
    - Existing SIGNUP_LOCKED test (lines 81-110) and sticky-remount test (lines 112-127) still pass
    - Full webapp vitest suite green: cd webapp && pnpm test -- --run
  </acceptance_criteria>
  <done>isSignupLockedError detects SIGNUP_LOCKED across all enumerated better-auth client envelope shapes; envelope-shape tests green; diagnostic log present for prod shape diagnosis; no regression in existing auth.test.tsx tests; full vitest suite green.</done>
  <reversibility rating="reversible">Pure logic hardening + test additions in two existing files; revertible by reverting the commits with zero architectural impact.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Prod UAT #6 re-run — second registration shows sticky VOICE copy</name>
  <files>(no file changes — verification only)</files>
  <read_first>
    - webapp/app/login/login-form.tsx (after Task 1 — hardened isSignupLockedError + diagnostic log)
    - .planning/phases/04-webapp/04-UAT.md (test 6 spec: SIGNUP_LOCKED shows sticky VOICE copy after second registration attempt)
    - .planning/STATE.md (prod UAT account: uat@puzzless.local / UatTestPass1! — first user already exists, signup is locked)
  </read_first>
  <what-built>
    Hardened isSignupLockedError in login-form.tsx to detect SIGNUP_LOCKED across all better-auth client error envelope shapes (flat message, code, nested body, Response-wrapped, json field, plain string, deep stringify fallback). Added envelope-shape unit tests. Added a diagnostic console.warn for unrecognized error shapes. Existing sessionStorage persistence (pb.signup_locked) and sticky-remount behavior from 04-05 unchanged.
  </what-built>
  <how-to-verify>
    Precondition: Task 1 merged and deployed to prod (web image rebuilt via deploy-web.yml + Coolify pull). Confirm the new bundle is live by checking that a normal login attempt does not log the old behavior.

    **Critical — controlled-state fill:** The UAT #6 alt root cause is that raw JS fill (e.g. `element.value = ...` or `browser_fill` without userEvent) bypasses React controlled inputs, so the form submits without the state the detector reads. To rule this out:
    - **Manual:** Click into each field, type the value with the keyboard (do NOT paste, do NOT use devtools `element.value=` assignment). Tab between fields so React `onChange` fires.
    - **gsd-browser:** Use `browser_fill_form` (which types via userEvent-style keyboard events that respect controlled state) — NOT `browser_evaluate` with `input.value = ...`. If `browser_fill_form` flakes on a field, fall back to manual keyboard typing for that field before submitting.

    Steps on prod (https://pbox.puzzlesstool.online):
    1. Open browser devtools (console + application/session storage tabs).
    2. Navigate to https://pbox.puzzlesstool.online/login.
    3. Click the "Registrieren" tab.
    4. Fill email: second@example.com (any email — signup is already locked after first user uat@puzzless.local). **Type with keyboard, do not paste.**
    5. Fill password: any 8+ char string. **Type with keyboard, do not paste.**
    6. Click "Konto anlegen".
    7. Expected within 2s: the Register tab stays active (does NOT switch to Anmelden, does NOT navigate away). Below the form, the VOICE copy appears: "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein."
    8. Expected: the generic "Registrierung fehlgeschlagen." does NOT appear.
    9. Expected: sessionStorage has key "pb.signup_locked" with value "1" (check via Application tab or sessionStorage.getItem("pb.signup_locked") in console).
    10. Hard refresh the page (Cmd+Shift+R / Ctrl+Shift+R).
    11. Expected: after reload, the "Registrieren" tab is active by default and the VOICE copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein." is visible immediately (sticky via sessionStorage).
    12. If the VOICE copy does NOT appear: open the browser console and look for "[signup-locked] unrecognized error shape:" — capture the logged error object and paste it into the SUMMARY so a follow-up can target the exact shape.

    **Escalation path if diagnostic shows a flat `SIGNUP_LOCKED` message (i.e. the detector's happy-path shape, not a nested/Response-wrapped envelope):** A flat `message: "SIGNUP_LOCKED"` is the shape the unit test already covers and the hardened detector matches it deterministically. If prod still shows the generic copy with a flat `SIGNUP_LOCKED` in the console.warn, the root cause is NOT an envelope-shape miss — it is the controlled-state fill bypass (React `onChange` never fired, so the form submitted with empty/stale state and the 409 path was never reached, or the wrong error object was caught). In that case:
    - Re-run steps 1–11 strictly with manual keyboard typing (no paste, no `browser_evaluate` fill).
    - If the VOICE copy then appears on the manual re-run, the gap is closed — record both runs in the SUMMARY and note the fill-bypass as the prior failure cause.
    - If the VOICE copy STILL does not appear after a clean manual re-run AND the console.warn shows a flat `SIGNUP_LOCKED`, that is a true detector regression — paste the full error object into the SUMMARY and schedule a follow-up plan targeting the exact shape.

    Alternative automation (if gsd-browser is available): use `browser_navigate` to /login, `browser_fill_form` on the register tab (userEvent-style typing — NOT `browser_evaluate` with `input.value=`), `browser_click` the submit button, `browser_snapshot` to assert the VOICE copy text is present, `browser_evaluate sessionStorage.getItem("pb.signup_locked")` to assert "1", `browser_reload`, `browser_snapshot` to assert sticky copy. If `browser_fill_form` flakes or the VOICE copy is missing, fall back to manual keyboard typing per the escalation path above before scheduling a follow-up.

    Login: uat@puzzless.local / UatTestPass1! (first user — do NOT wipe; signup is already locked, which is exactly the precondition needed for this test).
  </how-to-verify>
  <acceptance_criteria>
    - Second registration attempt on prod shows VOICE copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein." on the Register tab
    - Generic "Registrierung fehlgeschlagen." does NOT appear on second registration
    - sessionStorage key "pb.signup_locked" is "1" after the 409
    - After hard refresh, Register tab is active by default and VOICE copy is visible (sticky)
    - If VOICE copy still missing: the diagnostic console.warn "[signup-locked] unrecognized error shape:" is captured in the SUMMARY with the exact error object so a follow-up plan can target the shape
  </acceptance_criteria>
  <resume-signal>Type "approved" if VOICE copy shows and persists, or paste the console.warn error shape from step 12 to schedule a follow-up.</resume-signal>
</task>

<task type="auto" depends_on_checkpoint="Task 2 approval">
  <name>Task 3: Close G-04-4 in 04-UAT.md and 04-VERIFICATION.md</name>
  <files>.planning/phases/04-webapp/04-UAT.md, .planning/phases/04-webapp/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-webapp/04-UAT.md (test 6 currently `result: issue` / `severity: major`; Gaps section lists G-04-4 as `status: failed` with `missing: [VOICE sticky copy, sessionStorage pb.signup_locked=1]`)
    - .planning/phases/04-webapp/04-VERIFICATION.md (G-04-4 entry — locate via Grep "G-04-4"; current status reflects open gap)
  - .planning/phases/04-webapp/04-07-SUMMARY.md (created by executor after Task 2 — contains prod UAT #6 outcome, diagnostic shape if captured)
  </read_first>
  <action>
    Per the success_criteria requirement that G-04-4 be closed in both 04-UAT.md and 04-VERIFICATION.md. This task runs ONLY after Task 2 checkpoint returns "approved".

    **04-UAT.md updates (scoped Edit, NOT whole-file Write):**
    1. Test 6 (Signup Lock UI nach First User): change `result: issue` → `result: pass`; remove `severity: major`; update `reported:` line to record the r4 re-run pass (e.g. "r4 re-run: VOICE copy sticky on register tab; sessionStorage pb.signup_locked=1 confirmed"). Update `notes:` to reflect the hardened isSignupLockedError + envelope-shape tests + the actual fill method used (manual typing vs gsd-browser userEvent).
    2. Gaps section: change the G-04-4 entry `status: failed` → `status: closed`; add `closed_in: 04-07-PLAN.md` and `closed_at: <ISO date from SUMMARY>`. Move the gap entry out of the open-gaps list into a closed-gaps subsection (or mark in place per existing convention — match the format used for G-04-1..3, G-04-5 closures).
    3. Update `status:` frontmatter from `partial` → `complete` ONLY if no other open gaps remain in this UAT file (Grep for `status: failed` / `result: issue` — if zero hits, set `complete`; otherwise leave `partial` and note G-04-4 closed). Update `updated:` timestamp to the r4 re-run time.

    **04-VERIFICATION.md updates (scoped Edit):**
    1. Grep for `G-04-4` to locate the entry. Update its status to closed/pass per the file's existing convention (match how G-04-1, G-04-2, G-04-3, G-04-5 were recorded as closed — use the same field names and format).
    2. If the file has a summary/aggregate status line (e.g. "Phase 4 verification: partial"), update it to reflect G-04-4 closure. Do NOT mark the phase fully verified unless every other gap in the file is also closed — Grep `G-04-` and confirm before flipping any phase-level flag.

    Do NOT touch G-04-1, G-04-2, G-04-3, G-04-5, G-05-*, or the 04-06 bulk-move entries — those are CLOSED and must remain closed. Do NOT reopen or rephrase closed gaps.

    If Task 2 returned a diagnostic shape (not "approved" but a pasted console.warn error object), do NOT close G-04-4 — instead append a note to both files that 04-07 identified a remaining detector gap with the captured shape, leave G-04-4 status open, and flag for a follow-up plan. Only close when Task 2 resume-signal is the literal "approved".

    Why: success_criteria explicitly requires G-04-4 closed in both files; without this task the gap stays open in the record even after the prod UAT passes.
  </action>
  <verify>
    <automated>rg -n "G-04-4" .planning/phases/04-webapp/04-UAT.md .planning/phases/04-webapp/04-VERIFICATION.md && rg -A6 "### 6\. Signup Lock" .planning/phases/04-webapp/04-UAT.md | rg -q "result: pass" && ! rg -q "status: failed" .planning/phases/04-webapp/04-UAT.md</automated>
  </verify>
  <done>G-04-4 marked closed/pass in both 04-UAT.md (test 6 result: pass, gap status: closed) and 04-VERIFICATION.md; no previously-closed gaps (G-04-1..3, G-04-5, G-05-*) disturbed; phase-level status flipped to complete only if zero open gaps remain.</done>
  <reversibility rating="reversible">Doc-only edits in two planning markdown files; revertible by scoped Edit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (pbox) → Better Auth (pbox/api/auth) | Sign-up request crosses; 409 SIGNUP_LOCKED body returns through better-auth client envelope |
| better-auth client → React state | Error envelope must be detected by isSignupLockedError to set signupLocked + sessionStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07-01 | Tampering | isSignupLockedError detector | medium | mitigate | Harden detector to cover all envelope shapes; envelope-shape tests pin behavior; diagnostic log surfaces unrecognized shapes on prod for fast follow-up |
| T-04-07-02 | Info disclosure | console.warn diagnostic log | low | accept | Logs only the error object shape (no PII — better-auth error envelopes contain status/message/code, not user input); remove or gate behind verbose flag in future hardening if needed |
| T-04-07-03 | Denial of service | JSON.stringify fallback on circular error | low | mitigate | Wrap JSON.stringify in try/catch — circular refs / Response objects throw; catch returns false, detector falls through to negative path without crashing the form handler |
</threat_model>

<verification>
## Automated (Task 1)

```bash
# Envelope-shape tests (verbose) + full suite — both must exit 0
cd webapp && pnpm vitest run tests/auth.test.tsx --reporter=verbose
cd webapp && pnpm test -- --run
```

## Prod (Task 2 — human-verify checkpoint)

- Second register on https://pbox.puzzlesstool.online/login shows VOICE copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein."
- sessionStorage[pb.signup_locked]="1" after 409
- VOICE copy sticky across hard refresh
- If still failing: diagnostic console.warn "[signup-locked] unrecognized error shape:" captured for follow-up

## API regression guard (unchanged from 04-05)

```bash
# 409 still returned on second register
curl -s -o /dev/null -w "%{http_code}\n" -X POST "https://pbox.puzzlesstool.online/api/auth/sign-up/email" \
  -H "Content-Type: application/json" \
  -d '{"email":"blocked@example.com","password":"TestPass123!","name":"blocked"}'
# Expect: 409
```
</verification>

<success_criteria>
- isSignupLockedError detects SIGNUP_LOCKED across all enumerated better-auth client envelope shapes (flat message, code, nested body, Response-wrapped, json field, plain string, deep stringify fallback)
- Envelope-shape tests green (RED→GREEN confirmed in commits)
- Diagnostic console.warn present in both else branches of handleRegister for prod shape diagnosis
- VOICE copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein." renders on detected 409
- sessionStorage key "pb.signup_locked" set to "1" on detected 409
- VOICE copy sticky across remount via sessionStorage (Register tab active by default after reload)
- Existing auth.test.tsx tests pass (no regression)
- Full webapp vitest suite green
- Prod UAT #6 closed: second registration on https://pbox.puzzlesstool.online shows sticky VOICE copy (human-verify checkpoint passed)
- G-04-4 closed in 04-VERIFICATION.md and 04-UAT.md
</success_criteria>

## Artifacts this phase produces

- **`webapp/app/login/login-form.tsx`** (modified) — Hardened `isSignupLockedError` covering flat message, code, nested body, Response-wrapped, json field, plain string, and circular-ref-safe stringify fallback; exported as named export for direct testing; diagnostic `console.warn("[signup-locked] unrecognized error shape:", error)` in both else branches of `handleRegister`. Existing `SIGNUP_LOCKED_COPY` constant, `SIGNUP_LOCKED_STORAGE_KEY`, `lockSignupUi`, `readSignupLockedFlag`, sticky-remount `useEffect`, and `handleTabChange` unchanged.
- **`webapp/tests/auth.test.tsx`** (modified) — New `describe("isSignupLockedError envelope shapes")` block with tests for each envelope shape (flat message, code, nested body, Response-wrapped, json field, plain string, deep stringify fallback) and negative cases (null, undefined, empty object, INVALID_PASSWORD, status 500). Existing SIGNUP_LOCKED test (lines 81-110) and sticky-remount test (lines 112-127) preserved.
- **`.planning/phases/04-webapp/04-07-SUMMARY.md`** (created at plan completion) — Records envelope-shape test results, prod UAT #6 outcome (pass or diagnostic shape captured), commits, and G-04-4 closure status.

<output>
Create `.planning/phases/04-webapp/04-07-SUMMARY.md` when done.
</output>
