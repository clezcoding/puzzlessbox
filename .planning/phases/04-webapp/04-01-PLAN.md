---
phase: 04-webapp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/package.json
  - webapp/vitest.config.ts
  - webapp/tests/setup.ts
  - webapp/tests/board.test.tsx
  - webapp/tests/auth.test.tsx
  - webapp/components.json
  - webapp/app/globals.css
  - webapp/app/layout.tsx
  - webapp/app/page.tsx
  - webapp/middleware.ts
  - webapp/lib/api-client.ts
  - webapp/lib/auth-client.ts
  - webapp/components/ui/button.tsx
  - webapp/components/ui/input.tsx
  - webapp/components/ui/label.tsx
  - webapp/components/ui/tabs.tsx
  - webapp/components/ui/avatar.tsx
  - webapp/components/ui/dropdown-menu.tsx
  - webapp/components/ui/sonner.tsx
  - webapp/app/login/page.tsx
  - webapp/app/board/page.tsx
  - webapp/components/board/board-column.tsx
  - webapp/components/board/board-card.tsx
  - webapp/components/board/board-header.tsx
  - webapp/next.config.ts
autonomous: true
requirements: [BOARD-01, CAP-05]
user_setup: []

must_haves:
  truths:
    - "Unangemeldeter Aufruf von /board leitet auf /login um (middleware-geschützt)"
    - "Nach Email/Passwort-Anmeldung bleibt Session über Browser-Refresh erhalten (D-24, AUTH-02)"
    - "Board rendert 5 Default-Kategorien (Inbox, Notizen, Links, Tasks, Termine) aus GET /categories (BOARD-01)"
    - "Board zeigt nur auto_saved und confirmed Items; draft bleibt chat-only (D-04)"
    - "Register-Tab immer sichtbar; bei gesperrter Registrierung SIGNUP_LOCKED Copy (D-25)"
    - "Logout im Avatar-Menu leitet auf /login (D-28)"
    - "Brand-Tokens aus brand/tokens.css via Tailwind preset konsumierbar (kein hex-Hardcode)"
    - "shadcn primitives auf brand CSS-Variablen gemappt (keine parallele Palette, UI-SPEC Init Gate)"
    - "Vitest + RTL läuft in <30s; BOARD-01 und Auth Stub-Tests grün"
  artifacts:
    - webapp/vitest.config.ts
    - webapp/tests/setup.ts
    - webapp/components.json
    - webapp/lib/api-client.ts
    - webapp/lib/auth-client.ts
    - webapp/middleware.ts
    - webapp/app/login/page.tsx
    - webapp/app/board/page.tsx
  key_links:
    - "webapp/app/globals.css → @import brand/tokens.css"
    - "webapp/lib/api-client.ts → NEXT_PUBLIC_API_URL mit credentials: include"
    - "webapp/lib/auth-client.ts → better-auth/react createAuthClient"
    - "webapp/middleware.ts → Better Auth session cookie check für /board und /settings"
  backstops:
    - statement: "Category/tab labels truncate with ellipsis; full name in tooltip"
      verification: backstop
  assumptions:
    - "BOARD-03 adjacency: Karten mergen nie; jede Karte bleibt eigenständiges Item (Kanban-Semantik)"
    - "CAP-05 ordering: Poll merged Items nach id; gleiche sort_order tie-break via created_at DESC"
  prohibitions:
    - "Kein Drawer-Item-Detail und keine /items/[id]-Route als Primary (Modal wins, UI-SPEC Rejected)"
    - "Kein Verstecken des Register-Tab bei gesperrter Registrierung (D-25)"
    - "Keine parallele Farbpalette neben brand/tokens.css (UI-SPEC Init Gate)"
    - "Kein Confirm-Dialog bei Soft-Delete (nur Undo-Toast, D-12 — hier noch nicht implementiert)"
    - "Keine emoji als UI-Icons; lucide-react only"
---

<objective>
TRACER: Authentifizierter Nutzer loggt sich via Brand-Hero-Login ein und sieht das Kanban-Board mit den 5 Default-Kategorien aus der API. Beweist End-to-End: Better Auth Client + Middleware + API-Client + Brand-Token-Wiring + shadcn init + Vitest Wave 0.

Purpose: Architektur end-to-end auf frühen Kontext-Tokens beweisen, bevor Plan 02–04 Features aufbauen. Production-quality Skeleton, keine Prototyp-Stubs.
Output: Lauffähige WebApp mit Login → Board-Pfad, Brand-Tokens wired, Wave 0 Test-Infra grün.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-webapp/04-CONTEXT.md
@.planning/phases/04-webapp/04-RESEARCH.md
@.planning/phases/04-webapp/04-PATTERNS.md
@.planning/phases/04-webapp/04-UI-SPEC.md
@brand/tokens.css
@brand/tailwind.preset.ts
@brand/VOICE.md
@webapp/lib/auth.config.ts
@webapp/next.config.ts
@api/app/routers/categories.py
@api/app/routers/items.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wave 0 Test-Infra + shadcn init + Brand-Wiring + Login→Board Tracer</name>
  <files>
    webapp/vitest.config.ts,
    webapp/tests/setup.ts,
    webapp/tests/board.test.tsx,
    webapp/tests/auth.test.tsx,
    webapp/components.json,
    webapp/app/globals.css,
    webapp/app/layout.tsx,
    webapp/app/page.tsx,
    webapp/middleware.ts,
    webapp/lib/api-client.ts,
    webapp/lib/auth-client.ts,
    webapp/components/ui/button.tsx,
    webapp/components/ui/input.tsx,
    webapp/components/ui/label.tsx,
    webapp/components/ui/tabs.tsx,
    webapp/components/ui/sonner.tsx,
    webapp/app/login/page.tsx,
    webapp/app/board/page.tsx,
    webapp/components/board/board-column.tsx,
    webapp/components/board/board-card.tsx,
    webapp/components/board/board-header.tsx,
    webapp/next.config.ts,
    webapp/package.json
  </files>
  <read_first>
    brand/tokens.css,
    brand/tailwind.preset.ts,
    brand/VOICE.md,
    webapp/lib/auth.config.ts,
    webapp/next.config.ts,
    api/app/routers/categories.py,
    api/app/routers/capture.py,
    .planning/phases/04-webapp/04-RESEARCH.md,
    .planning/phases/04-webapp/04-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1 (board.test.tsx): Board rendert 5 Default-Kategorien aus GET /categories Mock (Inbox, Notizen, Links, Tasks, Termine)
    - Test 2 (board.test.tsx): Board rendert nur Items mit status auto_saved oder confirmed; draft-Items nicht
    - Test 3 (auth.test.tsx): Login-Page zeigt Anmelden- und Registrieren-Tab; Registrieren-Tab immer sichtbar
    - Test 4 (auth.test.tsx): Bei SIGNUP_LOCKED-Response zeigt Register-Form die VOICE-Copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein."
    - Test 5 (auth.test.tsx): Erfolgreiches Login setzt Session und leitet nach /board weiter
  </behavior>
  <action>
    Wave 0 zuerst: lege webapp/vitest.config.ts (jsdom env, setup.ts glob path) und webapp/tests/setup.ts (@testing-library/jest-dom import) an. Schreibe die 5 Stub-Tests oben (RED). Installiere dev-deps: vitest, @testing-library/react, @testing-library/jest-dom, @vitejs/plugin-react, jsdom. Füge "test": "vitest run" und "test:watch": "vitest" scripts zu webapp/package.json hinzu.

    shadcn init: `npx shadcn@latest init` mit components.json, der brand/tokens.css CSS-Variablen mappt (keine parallele Palette, UI-SPEC Init Gate). Tailwind v4 @theme in webapp/app/globals.css via @import "../brand/tokens.css" und tailwind.preset.ts wiring. Installiere shadcn primitives: button, input, label, tabs, avatar, dropdown-menu, sonner via `npx shadcn@latest add`. Installiere runtime-deps: @hello-pangea/dnd, lucide-react, zod, react-hook-form, @hookform/resolvers, sonner.

    webapp/lib/auth-client.ts: createAuthClient({ baseURL: process.env.NEXT_PUBLIC_APP_URL }) aus better-auth/react; export authClient + useSession hook.

    webapp/lib/api-client.ts: apiFetch<T>(endpoint, options) mit baseURL aus NEXT_PUBLIC_API_URL, credentials: "include", JSON-Header, Fehler-Parsing für { error: { code, message, details? } }-Shape (PATTERNS api/app/auth/jwt.py). Export typed wrappers: getCategories, getBoardItems.

    webapp/middleware.ts: Next.js middleware prüft Better-Auth-Session-Cookie; leitet /board und /settings auf /login?next= um, wenn keine Session. Nutze better-auth Next middleware helper.

    webapp/app/layout.tsx: root layout mit Toaster (sonner), Apollo favicon, brand font (Instrument Serif + DM Sans via next/font), html lang="de".

    webapp/app/page.tsx: redirect / → /board (oder /login wenn unangemeldet).

    webapp/app/login/page.tsx (D-24): Brand-Hero-Page. Top: apollo-splash.png + Instrument Serif Wordmark. Form darunter auf surface card (hairline, radius-lg). Tabs (shadcn Tabs): Anmelden | Registrieren — Registrieren immer sichtbar (D-25). Anmelden: email + passwort, charcoal primary button "Anmelden". Registrieren: email + passwort + "Konto anlegen"; fängt SIGNUP_LOCKED APIError ab und zeigt VOICE-Copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein." (UI-SPEC Copywriting Contract). Nach Erfolg: redirect ?next= oder /board. Loading-State: button disabled + "Einen Moment…".

    webapp/app/board/page.tsx (D-01, D-04): client component, useSession() guard, lädt getCategories + getBoardItems parallel, rendert BoardHeader + Spalten-Grid (CSS grid, ~5 Default-Spalten ohne H-Scroll, scrollbar-gutter: stable). Filtert items auf status in [auto_saved, confirmed] (defense-in-depth, API filtert bereits). Pro Spalte: board-column mit Header (category name, count badge mono/muted) + Karten-Stack.

    webapp/components/board/board-card.tsx (D-03): rich card — title (2-line clamp), meta line (type · relative time), 2px category accent hairline top. Link-thumbnail: next/image mit allowlist remote patterns (next.config.ts), fallback bone block + link icon wenn OG image missing.

    webapp/components/board/board-header.tsx (D-28, D-37): wordmark, manual refresh button (aria-label "Board aktualisieren"), avatar dropdown menu mit Logout.

    webapp/next.config.ts: remotePatterns für OG image domains (discretionary allowlist, UI-SPEC OG thumbnails).

    GREEN: implementiere minimal bis Tests grün. Kein DnD, kein Modal, kein Poll in diesem Task — das kommt in Plan 03/04. Tracer-Pfad ist nur: login → board rendert default categories + items.
  </action>
  <verify>
    <automated>cd webapp && pnpm install && pnpm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test --run` in webapp/ läuft <30s und ist grün (5 Tests)
    - `pnpm build` in webapp/ erfolgreich ohne Type-Errors
    - `npx shadcn@latest init` ausgeführt; components.json existiert und referenziert brand/tokens.css CSS-Variablen (keine parallele Palette)
    - webapp/app/globals.css importiert brand/tokens.css
    - webapp/middleware.ts leitet unangemeldeten /board-Aufruf auf /login?next=/board um
    - webapp/app/login/page.tsx zeigt Tabs Anmelden | Registrieren (Registrieren immer sichtbar)
    - webapp/app/board/page.tsx rendert 5 Default-Kategorien aus GET /categories (Mock im Test)
    - webapp/app/board/page.tsx filtert draft-Items heraus (Test bestätigt)
    - Keine hex-Farben hardcoded; alle via Tailwind brand tokens (var(--color-*))
  </acceptance_criteria>
  <done>
    Wave 0 Test-Infra steht und 5 Stub-Tests grün. shadcn init mit Brand-Token-Wiring complete. Login → Board Tracer-Pfad end-to-end lauffähig. Build grün.
  </done>
  <reversibility rating="reversible">shadcn init und Brand-Wiring sind rücksetzbar via git revert; keine Schema- oder API-Änderung.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session-Refresh-Survival, ?next=-Redirect, Logout aus Avatar-Menu</name>
  <files>
    webapp/tests/auth.test.tsx,
    webapp/app/login/page.tsx,
    webapp/app/board/page.tsx,
    webapp/components/board/board-header.tsx,
    webapp/lib/auth-client.ts,
    webapp/middleware.ts
  </files>
  <read_first>
    webapp/app/login/page.tsx,
    webapp/components/board/board-header.tsx,
    webapp/lib/auth-client.ts,
    webapp/middleware.ts,
    brand/VOICE.md,
    .planning/phases/04-webapp/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 6: Nach erfolgreichem Login bleibt Session nach `router.refresh()` / Page-Reload erhalten (Cookie persistent)
    - Test 7: Aufruf /board?next=/settings als unangemeldeter User leitet nach Login + Return zu /settings
    - Test 8: Logout via Avatar-Menu ruft authClient.signOut auf und leitet auf /login
    - Test 9: Avatar-Menu zeigt User-Email (truncate with ellipsis bei langen Emails)
  </behavior>
  <action>
    Erweitere auth.test.tsx um die 4 Tests (RED). Implementiere:

    Session-Survival: Better Auth Cookie ist httpOnly + Secure + SameSite=Lax (schon in auth.config.ts via betterAuth defaults). useSession() aus better-auth/react liefert persistenten State über Refresh. Kein extra Code nötig — Test verifiziert das Verhalten.

    ?next=-Redirect: middleware.ts liest ?next= query, leitet nach /login?next=<next> weiter. login/page.tsx liest searchParams.next, validiert dass next relativ/same-origin (kein Open-Redirect — reject absolute URLs oder fremde Hosts), redirect nach next bei Erfolg. Nutze Next.js searchParams (server component) oder useSearchParams (client).

    Logout (D-28): board-header.tsx Avatar-Dropdown-Menü (shadcn DropdownMenu) zeigt User-Email (truncate, title-Attribut mit voller Email als Tooltip — UI-SPEC long-text E1) + "Abmelden" Item. onClick: authClient.signOut(), dann router.push("/login").

    Avatar-Menu: shadcn Avatar mit Apollo-Icon (apollo-avatar.png) als Fallback, Email-Label, Dropdown trigger.
  </action>
  <verify>
    <automated>cd webapp && pnpm test -- --run auth</automated>
  </verify>
  <acceptance_criteria>
    - 4 neue Tests grün (gesamt 9)
    - Session überlebt `router.refresh()` (Test 6)
    - ?next= wird respektiert; absolute URLs werden abgewiesen (Open-Redirect-Schutz)
    - Logout leitet auf /login
    - Avatar-Menu zeigt Email mit truncate + title-Tooltip
  </acceptance_criteria>
  <done>
    Session-Survival, ?next=-Redirect und Logout aus Avatar-Menu implementiert und getestet.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js (App Router) | Unangemeldeter Aufruf; middleware prüft Session-Cookie |
| Next.js Client → FastAPI | API-Aufrufe mit Session-Cookie (credentials: include); API validiert owner_id |
| Next.js → npm registry | shadcn + npm installs; nur offizielle shadcn-Registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | login endpoint | high | mitigate | Better Auth email/password mit secure defaults; httpOnly+Secure+SameSite=Lax Cookie; keine custom Token-Logik |
| T-04-02 | Information Disclosure | session cookie | high | mitigate | httpOnly verhindert JS-Zugriff; Secure nur über HTTPS; SameSite=Lax verhindert CSRF-Default |
| T-04-03 | Tampering | JWT in cookie | medium | mitigate | API validiert JWT via JWKS (Phase 1 D-21); Client vertraut nie ungeprüften Token-State |
| T-04-04 | Information Disclosure | ?next= redirect | medium | mitigate | Nur relative/same-origin Pfade erlaubt; absolute URLs oder fremde Hosts abgewiesen (Open-Redirect-Schutz) |
| T-04-05 | Tampering | npm/pnpm installs | high | mitigate | Nur offizielle shadcn-Registry; Package Legitimacy Audit aus 04-RESEARCH.md berücksichtigt (lucide-react/vitest SUS = false positive, OK); keine drittanbieter-Block-Registries |
| T-04-06 | Tampering | OG image proxy | medium | mitigate | next/image mit remotePatterns allowlist; keine beliebigen externen Domains; broken-image fallback bone block |
</threat_model>

<verification>
- `pnpm test --run` in webapp/ grün (9 Tests, <30s)
- `pnpm build` in webapp/ erfolgreich
- Manuell: `pnpm dev` → /board leitet auf /login um → Login mit erstem User → Board rendert 5 Default-Kategorien → Browser-Refresh hält Session → Avatar-Menu Logout leitet auf /login
</verification>

<success_criteria>
- Login → Board Tracer-Pfad end-to-end lauffähig
- Brand-Tokens wired (kein hex-Hardcode)
- shadcn init complete
- Wave 0 Test-Infra grün
- Session-Survival, ?next=, Logout aus Avatar-Menu
</success_criteria>

<output>
Create `.planning/phases/04-webapp/04-01-SUMMARY.md` when done
</output>

