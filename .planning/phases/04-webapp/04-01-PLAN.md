---
phase: 04-webapp
plan: 01
type: execute
wave: 2
depends_on: [04-00]
files_modified:
  - webapp/tests/board.test.tsx
  - webapp/tests/auth.test.tsx
  - webapp/components/ui/tabs.tsx
  - webapp/components/ui/avatar.tsx
  - webapp/components/ui/dropdown-menu.tsx
  - webapp/app/login/page.tsx
  - webapp/app/board/page.tsx
  - webapp/components/board/board-column.tsx
  - webapp/components/board/board-card.tsx
  - webapp/components/board/board-header.tsx
  - webapp/next.config.ts
autonomous: true
requirements: [BOARD-01, CAP-05]
user_setup: []

must_haves:
  truths:
    - "Unangemeldeter Aufruf von /board leitet auf /login um (middleware aus 04-00)"
    - "Nach Email/Passwort-Anmeldung bleibt Session über Browser-Refresh erhalten (D-24, AUTH-02)"
    - "Board rendert 5 Default-Kategorien (Inbox, Notizen, Links, Tasks, Termine) aus GET /categories (BOARD-01)"
    - "Board zeigt nur auto_saved und confirmed Items; draft bleibt chat-only (D-04)"
    - "Register-Tab immer sichtbar; bei gesperrter Registrierung SIGNUP_LOCKED Copy (D-25)"
    - "Logout im Avatar-Menu leitet auf /login (D-28)"
    - "Leere Board-Spalte zeigt Apollo-empty PNG + VOICE-Copy (D-05)"
    - "?next=-Redirect respektiert; absolute URLs abgewiesen (Open-Redirect-Schutz)"
  artifacts:
    - webapp/app/login/page.tsx
    - webapp/app/board/page.tsx
    - webapp/components/board/board-column.tsx
    - webapp/components/board/board-card.tsx
    - webapp/components/board/board-header.tsx
  key_links:
    - "webapp/app/login/page.tsx → authClient.signIn + redirect ?next= oder /board"
    - "webapp/app/board/page.tsx → getCategories + getBoardItems (aus 04-00 api-client)"
    - "webapp/components/board/board-column.tsx → apollo-empty-{category}.png + VOICE empty copy bei 0 Items (D-05)"
    - "webapp/components/board/board-header.tsx → authClient.signOut + avatar dropdown (D-28)"
  backstops:
    - statement: "Category/tab labels truncate with ellipsis; full name in tooltip"
      verification: backstop
  assumptions:
    - "BOARD-03 adjacency: Karten mergen nie; jede Karte bleibt eigenständiges Item (Kanban-Semantik)"
    - "CAP-05 ordering: Poll merged Items nach id; gleiche sort_order tie-break via created_at DESC"
    - "shadcn primitives button, input, label, sonner aus 04-00 verfügbar; tabs, avatar, dropdown-menu werden hier installiert"
  prohibitions:
    - "Kein Drawer-Item-Detail und keine /items/[id]-Route als Primary (Modal wins, UI-SPEC Rejected)"
    - "Kein Verstecken des Register-Tab bei gesperrter Registrierung (D-25)"
    - "Keine parallele Farbpalette neben brand/tokens.css (UI-SPEC Init Gate)"
    - "Keine emoji als UI-Icons; lucide-react only"
    - "Kein hex-Hardcode; alle via brand tokens"
---

<objective>
TRACER (Plan 01, slim): Authentifizierter Nutzer loggt sich via Brand-Hero-Login ein und sieht das Kanban-Board mit den 5 Default-Kategorien aus der API. Beweist End-to-End: Better Auth Client + Middleware + API-Client + Brand-Token-Wiring (aus 04-00) + Login-UI + Board-UI + Empty-States (D-05).

Purpose: Architektur end-to-end auf frühen Kontext-Tokens beweisen, bevor Plan 03–05 Features aufbauen. Production-quality Skeleton, keine Prototyp-Stubs. Baut auf 04-00 Infra auf.
Output: Lauffähige WebApp mit Login → Board-Pfad, Empty-States mit Apollo PNGs, Wave 0 Stub-Tests grün.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-webapp/04-CONTEXT.md
@.planning/phases/04-webapp/04-RESEARCH.md
@.planning/phases/04-webapp/04-PATTERNS.md
@.planning/phases/04-webapp/04-UI-SPEC.md
@brand/tokens.css
@brand/tailwind.preset.ts
@brand/VOICE.md
@brand/assets
@webapp/lib/auth.config.ts
@webapp/next.config.ts
@api/app/routers/categories.py
@api/app/routers/items.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Login→Board Tracer mit Empty-States (D-05) — slim, baut auf 04-00 Infra auf</name>
  <files>
    webapp/tests/board.test.tsx,
    webapp/tests/auth.test.tsx,
    webapp/components/ui/tabs.tsx,
    webapp/components/ui/avatar.tsx,
    webapp/components/ui/dropdown-menu.tsx,
    webapp/app/login/page.tsx,
    webapp/app/board/page.tsx,
    webapp/components/board/board-column.tsx,
    webapp/components/board/board-card.tsx,
    webapp/components/board/board-header.tsx,
    webapp/next.config.ts
  </files>
  <read_first>
    brand/tokens.css,
    brand/VOICE.md,
    brand/assets,
    webapp/lib/auth.config.ts,
    webapp/lib/api-client.ts,
    webapp/lib/auth-client.ts,
    webapp/middleware.ts,
    webapp/next.config.ts,
    api/app/routers/categories.py,
    api/app/routers/capture.py,
    .planning/phases/04-webapp/04-RESEARCH.md,
    .planning/phases/04-webapp/04-UI-SPEC.md,
    .planning/phases/04-webapp/04-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1 (board.test.tsx): Board rendert 5 Default-Kategorien aus GET /categories Mock (Inbox, Notizen, Links, Tasks, Termine)
    - Test 2 (board.test.tsx): Board rendert nur Items mit status auto_saved oder confirmed; draft-Items nicht
    - Test 3 (board.test.tsx): Leere Spalte zeigt Apollo-empty PNG (z.B. apollo-empty-inbox.png für Inbox) + VOICE-Copy "Hier ist gähnende Leere." / "Apollo hat noch nichts gefangen. Sende eine Nachricht, um den ersten Eintrag zu stashen." (D-05)
    - Test 4 (auth.test.tsx): Login-Page zeigt Anmelden- und Registrieren-Tab; Registrieren-Tab immer sichtbar
    - Test 5 (auth.test.tsx): Bei SIGNUP_LOCKED-Response zeigt Register-Form die VOICE-Copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein."
    - Test 6 (auth.test.tsx): Erfolgreiches Login setzt Session und leitet nach /board weiter
  </behavior>
  <action>
    RED: Schreibe webapp/tests/board.test.tsx (3 Tests) und webapp/tests/auth.test.tsx (3 Tests) gegen die unten beschriebene UI (RED). Nutze vitest config + setup aus 04-00.

    Installiere shadcn primitives die 04-00 nicht abgedeckt hat: tabs, avatar, dropdown-menu via `npx shadcn@latest add`. button, input, label, sonner kommen aus 04-00.

    webapp/app/login/page.tsx (D-24, D-25): Brand-Hero-Page. Top: apollo-splash.png + Instrument Serif wordmark (PATTERNS: brand/tokens.css @theme pattern; UI-SPEC Asset Map). Form darunter auf surface card (hairline, radius-lg). Tabs (shadcn Tabs): Anmelden | Registrieren — Registrieren immer sichtbar (D-25). Anmelden: email + passwort, charcoal primary button "Anmelden". Registrieren: email + passwort + "Konto anlegen"; fängt SIGNUP_LOCKED APIError ab und zeigt VOICE-Copy "Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein." (UI-SPEC Copywriting Contract). Nach Erfolg: redirect ?next= oder /board. Loading-State: button disabled + "Einen Moment…". Nutze authClient.signIn / authClient.signUp aus 04-00.

    webapp/app/board/page.tsx (D-01, D-04): client component, useSession() guard (aus 04-00), lädt getCategories + getBoardItems parallel via apiFetch (04-00), rendert BoardHeader + Spalten-Grid (CSS grid, ~5 Default-Spalten ohne H-Scroll, scrollbar-gutter: stable — UI-SPEC Spacing). Filtert items auf status in [auto_saved, confirmed] (defense-in-depth, API filtert bereits). Pro Spalte: board-column mit Header (category name, count badge mono/muted) + Karten-Stack oder Empty-State.

    webapp/components/board/board-column.tsx (D-05): wenn items.length === 0 → rendere Empty-State mit Apollo-empty PNG passend zur Kategorie (Inbox→apollo-empty-inbox.png, Notizen→apollo-empty-notes.png, Links→apollo-empty-links.png, Tasks→apollo-empty-tasks.png, Termine→apollo-empty-cal.png — UI-SPEC Asset Map) + VOICE empty copy (UI-SPEC Copywriting Contract: "Hier ist gähnende Leere." / "Apollo hat noch nichts gefangen. Sende eine Nachricht, um den ersten Eintrag zu stashen." für Inbox; "Keine Notizen stasht sich von selbst. Lass Apollo etwas aufschreiben." für Notizen; etc.). Sonst: Karten-Stack.

    webapp/components/board/board-card.tsx (D-03): rich card — title (2-line clamp), meta line (type · relative time), 2px category accent hairline top. Link-thumbnail: next/image mit allowlist remote patterns (next.config.ts), fallback bone block + link icon wenn OG image missing.

    webapp/components/board/board-header.tsx (D-28, D-37): wordmark, manual refresh button (aria-label "Board aktualisieren"), avatar dropdown menu (shadcn Avatar mit apollo-avatar.png Fallback + DropdownMenu) mit Logout "Abmelden" → authClient.signOut + router.push("/login").

    webapp/next.config.ts: remotePatterns für OG image domains (discretionary allowlist, UI-SPEC OG thumbnails).

    GREEN: implementiere minimal bis Tests grün. Kein DnD, kein Modal, kein Poll in diesem Task — das kommt in Plan 04/05. Tracer-Pfad ist nur: login → board rendert default categories + items + empty states.
  </action>
  <verify>
    <automated>cd webapp && pnpm test -- --run board auth</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test --run board auth` in webapp/ läuft <30s und ist grün (6 Tests)
    - `pnpm build` in webapp/ erfolgreich ohne Type-Errors
    - webapp/app/login/page.tsx zeigt Tabs Anmelden | Registrieren (Registrieren immer sichtbar)
    - webapp/app/board/page.tsx rendert 5 Default-Kategorien aus GET /categories (Mock im Test)
    - webapp/app/board/page.tsx filtert draft-Items heraus (Test bestätigt)
    - webapp/components/board/board-column.tsx rendert Apollo-empty PNG + VOICE-Copy bei 0 Items (D-05, Test 3)
    - Keine hex-Farben hardcoded; alle via Tailwind brand tokens (var(--color-*))
  </acceptance_criteria>
  <done>
    Login → Board Tracer-Pfad end-to-end lauffähig. Empty-States mit Apollo PNGs (D-05) implementiert. 6 Stub-Tests grün. Build grün.
  </done>
  <reversibility rating="reversible">UI-Routes sind rücksetzbar via git revert; keine Schema- oder API-Änderung.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Session-Refresh-Survival, ?next=-Redirect, Logout aus Avatar-Menu</name>
  <files>
    webapp/tests/auth.test.tsx,
    webapp/app/login/page.tsx,
    webapp/components/board/board-header.tsx,
    webapp/lib/auth-client.ts,
    webapp/middleware.ts
  </files>
  <read_first>
    webapp/app/login/page.tsx,
    webapp/components/board/board-header.tsx,
    webapp/lib/auth-client.ts,
    webapp/middleware.ts,
    brand/VOICE.md,
    .planning/phases/04-webapp/04-CONTEXT.md,
    .planning/phases/04-webapp/04-PATTERNS.md
  </read_first>
  <behavior>
    - Test 7: Nach erfolgreichem Login bleibt Session nach `router.refresh()` / Page-Reload erhalten (Cookie persistent)
    - Test 8: Aufruf /board?next=/settings als unangemeldeter User leitet nach Login + Return zu /settings
    - Test 9: Logout via Avatar-Menu ruft authClient.signOut auf und leitet auf /login
    - Test 10: Avatar-Menu zeigt User-Email (truncate with ellipsis bei langen Emails)
  </behavior>
  <action>
    Erweitere auth.test.tsx um die 4 Tests (RED). Implementiere:

    Session-Survival: Better Auth Cookie ist httpOnly + Secure + SameSite=Lax (schon in auth.config.ts via betterAuth defaults — PATTERNS webapp/lib/auth.config.ts). useSession() aus better-auth/react liefert persistenten State über Refresh. Kein extra Code nötig — Test verifiziert das Verhalten.

    ?next=-Redirect: middleware.ts (aus 04-00) liest ?next= query, leitet nach /login?next=<next> weiter. login/page.tsx liest searchParams.next, validiert dass next relativ/same-origin (kein Open-Redirect — reject absolute URLs oder fremde Hosts), redirect nach next bei Erfolg. Nutze Next.js searchParams (server component) oder useSearchParams (client).

    Logout (D-28): board-header.tsx Avatar-Dropdown-Menü (shadcn DropdownMenu) zeigt User-Email (truncate, title-Attribut mit voller Email als Tooltip — UI-SPEC long-text E1) + "Abmelden" Item. onClick: authClient.signOut(), dann router.push("/login").
  </action>
  <verify>
    <automated>cd webapp && pnpm test -- --run auth</automated>
  </verify>
  <acceptance_criteria>
    - 4 neue Tests grün (gesamt 10)
    - Session überlebt `router.refresh()` (Test 7)
    - ?next= wird respektiert; absolute URLs werden abgewiesen (Open-Redirect-Schutz)
    - Logout leitet auf /login
    - Avatar-Menu zeigt Email mit truncate + title-Tooltip
  </acceptance_criteria>
  <done>
    Session-Survival, ?next=-Redirect und Logout aus Avatar-Menu implementiert und getestet.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js (App Router) | Unangemeldeter Aufruf; middleware prüft Session-Cookie (aus 04-00) |
| Next.js Client → FastAPI | API-Aufrufe mit Session-Cookie (credentials: include); API validiert owner_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | ?next= redirect | medium | mitigate | Nur relative/same-origin Pfade erlaubt; absolute URLs oder fremde Hosts abgewiesen (Open-Redirect-Schutz) |
| T-04-06 | Tampering | OG image proxy | medium | mitigate | next/image mit remotePatterns allowlist; keine beliebigen externen Domains; broken-image fallback bone block |
</threat_model>

<verification>
- `pnpm test --run board auth` in webapp/ grün (10 Tests, <30s)
- `pnpm build` in webapp/ erfolgreich
- Manuell: `pnpm dev` → /board leitet auf /login um → Login mit erstem User → Board rendert 5 Default-Kategorien → leere Spalte zeigt Apollo-empty PNG + VOICE-Copy → Browser-Refresh hält Session → Avatar-Menu Logout leitet auf /login
</verification>

<success_criteria>
- Login → Board Tracer-Pfad end-to-end lauffähig
- Empty-States mit Apollo PNGs (D-05)
- Session-Survival, ?next=, Logout aus Avatar-Menu
- 10 Tests grün
</success_criteria>

<output>
Create `.planning/phases/04-webapp/04-01-SUMMARY.md` when done
</output>
