---
phase: 4
slug: webapp
status: draft
shadcn_initialized: false
preset: pending-init — map to brand/tokens.css (Util Bone + Apollo terracotta)
created: 2026-08-02
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for WebApp (BOARD-01..04, CAP-05, CAL-01).
> Sources: `04-CONTEXT.md` D-01..D-37 · `brand/tokens.css` · `brand/BRAND.md` · `brand/VOICE.md` · sketch-findings Util Bone · Hallmark anti-slop.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui (initialize in plan/execute — `components.json` absent today) |
| Preset | Custom — CSS vars from `brand/tokens.css`; Tailwind via `brand/tailwind.preset.ts` |
| Component library | Radix (shadcn default) |
| Icon library | lucide-react (shadcn default; charcoal strokes, no emoji as icons) |
| Font | Instrument Serif (display) · DM Sans (UI/body) · JetBrains Mono (rare: timestamps/IDs) |
| Styling | Tailwind CSS v4 + `@import` brand tokens |
| DnD | `@hello-pangea/dnd` (stack research; Claude discretion locked here) |
| Theme | Light/dark from tokens + `prefers-color-scheme` + user toggle (D-07) |

**Init gate:** First plan task runs `npx shadcn@latest init` with preset mapped to existing brand CSS variables (do **not** invent a parallel palette). Wire `components.json` → consume `--color-*` / `--font-*` / `--spacing-*` from `brand/tokens.css`.

**Tone:** Clean warm Hallmark + Util Bone workbench. Premium via craft/hairlines — **reject** purple neon, dark-void default, scanlines, CRT, WebGL heroes.

**Signature moments:** Apollo on brand-facing surfaces (login, welcome, empty) + terracotta pulse on new capture items (~2s, D-08).

---

## Spacing Scale

Declared values (multiples of 4) — source: `brand/tokens.css` + BRAND.md:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, card accent bar |
| sm | 8px | Compact element spacing, card internal gaps |
| md | 16px | Default element spacing, column padding |
| lg | 24px | Section padding, modal body pad |
| xl | 32px | Layout gaps, masthead vertical rhythm |
| 2xl | 48px | Major section breaks (auth hero → form) |
| 3xl | 64px | Page-level spacing (auth outer) |

**Also allowed (sketch foundation):** 12px for masthead control gaps / compact meta rows (between sm and md).

**Board density:** Tighter **internal** card spacing (sm/xs) than **external** column/board gaps (md). `scrollbar-gutter: stable` on board scroll containers.

**Exceptions:**
- Icon-only / drag-handle touch targets ≥ **44×44px** hit area (visual icon may be 16–20px).
- Mobile category tabs: min height **44px**.
- Modal max-width: **560px** content; overlay full viewport.

---

## Typography

| Role | Size | Weight | Line Height | Family |
|------|------|--------|-------------|--------|
| Body | 16px | 400 | 1.5 | DM Sans |
| Label | 14px | 600 | 1.4 | DM Sans |
| Heading | 20px | 600 | 1.2 | DM Sans |
| Display | 28px | 400 | 1.2 | Instrument Serif |

**Rules:**
- Exactly **2 weights** for UI: 400 + 600. Display stays 400 (serif).
- No italic headings (Hallmark). Emphasis via weight / terracotta / space.
- Mono only for timestamps, IDs, poll/debug meta — never body.
- Hierarchy via weight/color/space — not size sprawl beyond the 4 roles.
- Masthead product name: Display / Instrument Serif; board column titles: Label or Heading (DM Sans).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#f7f6f3` (`--color-bg`) | Page bone paper; dark: `#1a1a1a` |
| Secondary (30%) | `#ffffff` (`--color-surface`) | Cards, columns, panels, modal; dark: `#242424` |
| Accent (10%) | `#c45c3e` (`--color-brand` / `--color-accent`) | See reserved list below |
| Destructive | `#d9383a` (`--color-destructive`) | Soft-delete / disconnect confirm only |
| Primary CTA fill | `#1a1a1a` (`--color-primary`) | Charcoal buttons (not terracotta fill for primary board CTAs) |
| Muted text | `#787774` | Meta lines, placeholders |
| Hairline | `#eaeaea` / dark `#2d2d2d` | Borders, column rules |
| Info (demoted) | `#1f6c9f` | Informational links only — never brand accent |

**Accent (`#c45c3e`) reserved for:**
1. New-item highlight pulse (~2s) and brand-soft wash (`#fce8e0`) behind pulse
2. Focus rings on brand-primary moments (login CTA secondary, welcome)
3. Active category tab indicator / Termine category accent token
4. Capture/live status dots and Apollo bandana continuity in illustrations
5. Selected checkbox accent when multi-select active

**Not accent:** default charcoal primary buttons, hairlines, body text, category pastel fills (wayfinding only).

**Category pastels** — column header tint / card accent hairline only (must not compete with terracotta):

| Category | Soft bg | Accent |
|----------|---------|--------|
| Inbox | `#f0eeea` | `#787774` |
| Notizen | `#f5f0e6` | `#b8956a` |
| Links | `#e8f0f5` | `#5a7a8f` |
| Tasks | `#eaf3ec` | `#5f7d64` |
| Termine | `#f5ebe8` | `#c45c3e` |

**Shape:** radius-sm **4px**, radius-md **8px**, radius-lg **10px**. Hairlines over shadows. Shadows only: card hover `0 2px 8px rgba(0,0,0,0.04)`; drag ghost may use slightly stronger elevation.

---

## Copywriting Contract

Product UI language: **German** · Apollo voice (`brand/VOICE.md`). Verbs: gefangen / gestasht / sortiert / stibitzt. No OK/Submit/Persistiert.

| Element | Copy |
|---------|------|
| Primary CTA (auth) | **Anmelden** (login) · **Konto anlegen** (register) |
| Primary CTA (board bulk) | **In Kategorie verschieben** |
| Primary CTA (calendar wizard) | **Mit Google verbinden** → **Kalender wählen** → **Fertig** |
| Empty state heading (board/inbox) | Hier ist gähnende Leere. |
| Empty state body (board/inbox) | Apollo hat noch nichts gefangen. Sende eine Nachricht, um den ersten Eintrag zu stashen. |
| Empty Notizen | Keine Notizen stasht sich von selbst. Lass Apollo etwas aufschreiben. |
| Error state | Da ist wohl ein Zahnrad blockiert. Versuche es gleich noch einmal. |
| Offline banner | Keine Verbindung. Apollo sucht nach dem Signal… |
| New-item toast (CAP-05) | Eintrag gesichert. Apollo hat es stibitzt und sortiert. |
| Welcome (first login) | Hallo, ich bin Apollo. Lass uns das Chaos ordnen. |
| Signup locked (D-25) | Registrierung ist geschlossen. Apollo lässt nur den ersten Nutzer rein. |
| Soft-delete undo toast | Eintrag verstaut. **Rückgängig** (≈5s) |
| Autosave failure toast | Speichern hakte. Versuch’s gleich nochmal. |
| Move failure toast | Verschieben fehlgeschlagen. Eintrag ist zurück. |
| Type-change confirm | Typ ändern? Manche Felder gehen verloren. **Ändern** / **Abbrechen** |
| Google disconnect confirm | Trennen: Google-Kalender trennen? Lokale Termine bleiben; Sync stoppt. |
| Destructive hard-delete (if ever) | Löschen: Eintrag unwiderruflich löschen? Apollo kann ihn nicht wiederbeschaffen. |
| Manual refresh aria | Board aktualisieren |
| A11y move menu | In Kategorie verschieben |

**Empty-state typing:**
- **First-use / empty category:** Apollo PNG + VOICE empty copy (above).
- **Filtered / no matches in tab:** „Hier liegt nichts in dieser Kategorie.“ + no blame.
- **Error empty:** Error copy + **Erneut versuchen** button — never internal error codes.

---

## Motion & Interaction Contract

| Concern | Spec |
|---------|------|
| Easing | UI enter: **ease-out**. Never ease-in for chrome. |
| Press | 100–160ms; `active: scale(0.97)` on buttons |
| Tooltip | 125–200ms |
| Dropdown / Sheet | 150–250ms |
| Modal | 200–300ms; center origin; enter `opacity + scale(0.95)` — **never** scale(0) |
| Cap | UI motion ≤ **300ms** |
| New-item pulse (D-08) | Terracotta / brand-soft wash ≈ **2s**, then settle — status, not celebration |
| Drag ghost (D-21) | Classic floating card (opacity ~0.95 + soft shadow); interruptible CSS/transform |
| High-frequency keys | **No** animation on arrow-reorder / typing / poll ticks |
| `prefers-reduced-motion` | Drop transform/translate; keep opacity/color/pulse via color only |
| Modal close (D-15) | X + Escape only; overlay click does **not** close; flush pending autosave first |
| Optimistic DnD (D-19) | Instant UI move; API fail → revert + toast |

---

## Surface Contracts

### 1. Auth — Login / Register (D-24, D-25, D-31)

- **Layout:** Brand-hero page. Top: Apollo (`apollo-splash` / `apollo-onboard` / wordmark) + Instrument Serif product name. Form below on surface card (hairline, radius-lg).
- **Tabs:** Anmelden | Registrieren — Register **always visible**; locked signup shows SIGNUP_LOCKED copy inline (do not hide tab).
- **Fields:** Email, Passwort; labels 14/600; inputs hairline border, radius-md.
- **Primary button:** Charcoal fill, label Anmelden / Konto anlegen.
- **Post-auth:** First login → short Apollo welcome (`apollo-onboard`) then Board. Later → Board. Honor `?next=` / OAuth return over welcome when present (discretion: welcome flag in `localStorage` key `pb.welcome.seen`).

### 2. Board — Desktop Kanban (D-01, D-04..D-08)

- Compact columns so **~5 defaults fit without H-scroll**; extra categories may H-scroll.
- Grid: hairline outer border, column rules, category pastel header wash, count in mono/muted.
- Items: only `auto_saved` + `confirmed` (never `draft`).
- Header: wordmark/title, manual refresh, theme toggle (also Settings), avatar menu (Logout), multi-select bulk bar when selection > 0.
- “Kategorien verwalten” panel: create + reorder; rename/color on column header (D-06).
- Live rail optional quiet status — poll-driven, not marketing hero.

### 3. Board — Mobile (D-02, D-17)

- `<768px`: single column + category **tabs/chips** (not stacked Kanban, not swipe-board).
- Category change: **long-press → Sheet** picker (shadcn Sheet). No drag-to-tab.
- Tabs scroll horizontally if many categories; active = terracotta underline/dot.

### 4. Cards (D-03, D-16, D-21..D-23)

- Collapsed: **title** + **meta line** (type · relative time) + **link thumbnail** (OG image; fallback muted bone block + link icon).
- 2px category accent hairline at top of card.
- Drag **handle only** (desktop); body click → modal.
- Checkbox multi-select + bulk move (D-22).
- Cross-category = drop on column; in-column = vertical reorder only (D-23).
- Persist `sort_order` (D-18).

### 5. Item Modal (D-09..D-15)

- Centered **Dialog** (shadcn); board dimmed; max-width 560px.
- Autosave on blur/debounce — **no** Save button; error → toast.
- Soft-delete → Undo toast ~5s (no confirm dialog) (D-12).
- Link type: full OG preview block above fields (D-13).
- Calendar 412: inline conflict UI — remote preview + **Übernehmen** / **Behalten** / **Abbrechen** (D-14).
- Type change: warning confirm + field mapping (D-10).

### 6. Settings Hub `/settings` (D-26..D-30)

| Section | Content |
|---------|---------|
| Account | Email display, password change, Logout |
| Google Calendar | 3-step wizard: Connect → Pick calendar → Done; Disconnect + confirm (D-30) |
| Appearance | Theme: System / Light / Dark; optional **Klang bei neuem Eintrag** default **off** (D-36) |

OAuth starts on `app.`; callback on `api.` then return to wizard Done (Phase 1).

### 7. Live update / Offline (D-32..D-37)

- Poll ≈ **10s** while session live (not visibility-paused).
- Merge new items + remote edits/moves.
- Offline/API-down: persistent banner (VOICE offline) + **Erneut versuchen**; keep last data.
- New item: toast + pulse **always** (D-35).
- Manual refresh control in header (D-37).
- Discretion: exponential backoff on errors — 10s → 20s → 40s cap 60s + ± ±20%; reset on success.

---

## Component Inventory (shadcn)

Install/wire to brand CSS variables:

| Primitive | Use |
|-----------|-----|
| Button | CTAs, ghost icon buttons |
| Input / Textarea / Label | Auth + modal fields |
| Checkbox | Multi-select |
| Dialog | Item detail modal |
| Sheet | Mobile category picker |
| DropdownMenu | Avatar, column overflow, a11y move menu |
| Tabs | Auth Anmelden/Registrieren; Settings sections; mobile categories |
| Toast / Sonner | Undo, errors, new-item, move failure |
| Switch | Appearance toggles |
| Separator | Settings / masthead rules |
| Avatar | Header menu |
| ScrollArea | Column card lists (optional) |

**Third-party registries:** none.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Input, Textarea, Label, Checkbox, Dialog, Sheet, DropdownMenu, Tabs, Toast/Sonner, Switch, Separator, Avatar, ScrollArea | not required |
| third-party | none | n/a |

---

## UI Considerations

Applicable state considerations resolved: **14 covered, 2 backstop, 0 unresolved**

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | Board columns, category tabs | ✅ covered | First-use: Apollo asset + VOICE empty; per-category empty variants (inbox/notes/etc.); filtered empty uses short category-empty line |
| empty | Auth form | ✅ covered | Empty fields = placeholders only; no empty-data illustration on auth |
| empty | Calendar wizard pre-connect | ✅ covered | Step 1 Connect with Apollo/calendar empty (`apollo-empty-cal`) + Connect CTA |
| loading | Board initial fetch | ✅ covered | Column skeletons (hairline blocks) or `apollo-loading` centered once — no spinner spam |
| loading | Modal open / autosave | ✅ covered | Open: brief content skeleton; autosave: quiet muted “Speichert…” near header, no blocking overlay |
| loading | Auth submit | ✅ covered | Button loading state (disabled + label „Einen Moment…“) |
| error | Board fetch / poll | ✅ covered | Banner or toast with Error copy + Erneut versuchen; last-good data stays |
| error | Autosave / move / bulk | ✅ covered | Toast with actionable German line; optimistic revert on move fail |
| error | Auth | ✅ covered | Inline field/form error; signup locked = friendly locked copy (not generic 403) |
| populated | Board cards | ✅ covered | Rich collapsed cards at typical density (~5 cols); meta + optional thumb |
| partial | Link card missing OG image | ✅ covered | Thumbnail fallback bone block + link icon (D-03) |
| partial | Calendar 412 conflict | ✅ covered | Inline remote vs local in modal (D-14) |
| overflow | Board columns / many categories | ✅ covered | Desktop: H-scroll for extra cols; column lists V-scroll; mobile tabs H-scroll |
| overflow | Card title / long URL | ✅ covered | Title 2-line clamp ellipsis; URL truncate mid with ellipsis |
| zero-one-many | Column item counts | ✅ covered | Count badge always; 0 shows empty state inside column; singular/plural not required in DE count |
| long-text | Modal body / notes | 🧪 backstop | Visual: wrap in scrollable modal body; title Input single-line truncate on card |
| long-text | Category names | 🧪 backstop | Header truncate ellipsis; full name in tooltip |

---

## Claude's Discretion — Locked Recommendations

| Topic | Recommendation |
|-------|----------------|
| Theme toggle placement | Header **and** Settings → Appearance (D-07) |
| Welcome flag | `localStorage` `pb.welcome.seen`; `?next=` wins |
| Bulk move API | Sequential PATCHes with progress toast if >5; batch endpoint later if needed |
| Poll backoff | 10s base; on error 20→40→60s + ±20% jitter; reset on OK |
| DnD library | `@hello-pangea/dnd` |
| OG thumbnails | Next.js `<Image>` + allowlist remote patterns; broken → fallback |
| Sound | WebAudio soft tick; default off; respect reduced-motion as off |

---

## Rejected Patterns (do not ship)

- Purple/indigo neon, glassmorphism glow, dark-void cyberpunk, scanlines
- Pill-heavy chip clusters, multi-layer shadows, emoji as UI icons
- Cards-as-marketing tiles in first viewport of board
- Teal as brand accent
- Confirm dialog for soft-delete (Undo toast only)
- Drawer item detail or `/items/[id]` as primary (modal wins)
- Hiding Register when signup locked

---

## Asset Map (Apollo)

| Surface | Asset |
|---------|-------|
| Login hero | `apollo-splash.png` + `apollo-wordmark.png` |
| Welcome | `apollo-onboard.png` |
| Empty board / inbox | `apollo-empty-inbox.png` / `apollo-empty-board.png` |
| Empty notes/links/tasks/cal | `apollo-empty-notes.png` etc. |
| Error | `apollo-error.png` |
| Offline | `apollo-offline.png` |
| Loading | `apollo-loading.png` |
| Favicon / app icon | `apollo-icon-favicon.png` / `apollo-icon-app.png` |
| Dark icon | `apollo-icon-dark.png` — scenes stay light PNGs on dark surfaces (BRAND D-08) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
