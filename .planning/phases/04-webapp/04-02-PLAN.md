---
phase: 04-webapp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - api/alembic/versions/0006_board_color_sortorder.py
  - api/app/routers/categories.py
  - api/app/routers/items.py
  - api/app/routers/capture.py
  - api/app/models/category.py
  - api/tests/test_categories_color_sort.py
  - api/tests/test_items_edit_softdelete.py
autonomous: true
requirements: [BOARD-02, BOARD-03, BOARD-04]
user_setup: []

must_haves:
  truths:
    - "categories-Tabelle hat color (text, hex) und sort_order (int) Spalten via Alembic 0006"
    - "notes/links/tasks/events haben sort_order (int) Spalte für in-column reorder (D-18)"
    - "PATCH /categories/{id} ändert name, color, sort_order; owner_id-gescoped (BOARD-02)"
    - "DELETE /categories/{id} soft-deleted nur wenn nicht letzte verbleibende Kategorie; Items werden nicht gelöscht"
    - "POST /categories/reorder setzt sort_order für mehrere IDs atomar (BOARD-02 sort)"
    - "GET /categories liefert color und sort_order; sortiert nach sort_order, dann name"
    - "PATCH /items/{id} akzeptiert title, body/notes, type-spezifische Felder, type-change mit Feldmapping (D-10, BOARD-04)"
    - "DELETE /items/{id} setzt deleted_at (soft-delete); POST /items/{id}/restore löscht deleted_at (Undo, D-12)"
    - "Alle Mutationen owner_id-gescoped via RLS + expliziter WHERE (AUTH-04)"
    - "color-Werte validiert als hex-regex #?[0-9a-fA-F]{6}; kein raw HTML (XSS-Schutz)"
  artifacts:
    - api/alembic/versions/0006_board_color_sortorder.py
    - api/tests/test_categories_color_sort.py
    - api/tests/test_items_edit_softdelete.py
  key_links:
    - "PATCH /categories/{id} → categories.owner_id WHERE + RLS"
    - "PATCH /items/{id} → type-spezifische Tabelle (notes/links/tasks/events) via table_for_item_type"
    - "DELETE /items/{id} → deleted_at setzen; POST /items/{id}/restore → deleted_at IS NULL"
  assumptions:
    - "BOARD-03 ordering: gleiche sort_order → tie-break via created_at DESC (stabil)"
    - "BOARD-04 adjacency: PATCH eines Items beeinflusst keine anderen Items; keine Merge-Logik"
    - "BOARD-04 empty: PATCH/DELETE auf nicht-existierender ID → 404 NOT_FOUND"
    - "color default: NULL erlaubt (UI greift auf category-pastel fallback aus UI-SPEC zurück)"
  prohibitions:
    - "Kein Hard-Delete von Items (D-12: nur soft-delete via deleted_at + Undo)"
    - "Kein Löschen der letzten Kategorie (UI-SPEC zero-one-many E8: cannot delete last category in v1)"
    - "Kein color-Wert außerhalb hex-Formats (XSS via style-Attribut verhindern)"
    - "Kein Bulk-Move-Endpoint in v1 (Claude discretion: sequential PATCHes, UI-SPEC Locked Recommendations)"
---

<objective>
Erweitere Backend-API um Board-Features: Category-Farben + Sortierung, Item-Feld-Edit + Type-Change, Soft-Delete mit Undo. Schafft die API-Oberfläche, die Plan 03 (Board-UI) konsumiert.

Purpose: BOARD-02 (CRUD mit color/sort), BOARD-03 (persistierte in-column sort_order), BOARD-04 (Item-Edit + Type-Change), D-12 (Soft-Delete + Undo) brauchen API-Support, der in Phase 1 nicht gebaut wurde.
Output: Alembic-Migration 0006, erweiterte /categories und /items Endpunkte, neue Tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-webapp/04-CONTEXT.md
@.planning/phases/04-webapp/04-RESEARCH.md
@.planning/phases/04-webapp/04-PATTERNS.md
@api/alembic/versions/0001_initial_schema.py
@api/app/routers/categories.py
@api/app/routers/items.py
@api/app/routers/capture.py
@api/app/auth/jwt.py
@api/app/services/timeout.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alembic 0006 — categories.color + sort_order, items.sort_order; Category PATCH/DELETE/reorder</name>
  <files>
    api/alembic/versions/0006_board_color_sortorder.py,
    api/app/routers/categories.py,
    api/app/models/category.py,
    api/tests/test_categories_color_sort.py
  </files>
  <read_first>
    api/alembic/versions/0001_initial_schema.py,
    api/app/routers/categories.py,
    api/app/models/category.py,
    api/app/auth/jwt.py,
    api/app/core/database.py,
    api/tests/test_categories.py
  </read_first>
  <behavior>
    - Test 1: GET /categories liefert color und sort_order Felder; sortiert nach sort_order ASC, dann name ASC
    - Test 2: PATCH /categories/{id} mit {name, color} aktualisiert beide Felder; 404 wenn fremde Kategorie
    - Test 3: PATCH /categories/{id} mit invalid color (z.B. "red" oder "<script>") → 422 VALIDATION_ERROR
    - Test 4: POST /categories/reorder mit [{id, sort_order}, ...] setzt sort_order atomar für alle IDs
    - Test 5: DELETE /categories/{id} soft-deleted (deleted_at) nur wenn >1 Kategorie existiert; 409 wenn letzte
    - Test 6: DELETE /categories/{id} mit Items → Items bleiben erhalten (Kategorie wird auf Inbox oder NULL gemappt — discretion: reassign zu Inbox default)
    - Test 7: Alle Mutationen owner_id-gescoped; fremde Tenant-Kategorie → 404
  </behavior>
  <action>
    RED: Schreibe api/tests/test_categories_color_sort.py mit den 7 Tests gegen bestehende API (alle failen).

    Alembic 0006_board_color_sortorder.py:
    - add_column categories.color TYPE TEXT NULLABLE (default NULL)
    - add_column categories.sort_order TYPE INTEGER NOT NULL DEFAULT 0
    - add_column categories.deleted_at TYPE TIMESTAMPTZ NULLABLE
    - add_column notes.sort_order, links.sort_order, tasks.sort_order, events.sort_order TYPE INTEGER NOT NULL DEFAULT 0
    - data migration: bestehende 5 Default-Kategorien bekommen sort_order 0..4 nach Reihenfolge Inbox, Notizen, Links, Tasks, Termine
    - downgrade: drop columns

    api/app/models/category.py: erweitere Category SQLModel um color: Optional[str], sort_order: int, deleted_at: Optional[datetime].

    api/app/routers/categories.py:
    - GET /categories: SELECT id, owner_id, name, color, sort_order, created_at FROM categories WHERE (owner_id = :owner_id OR owner_id IS NULL) AND deleted_at IS NULL ORDER BY sort_order ASC, name ASC
    - CategoryUpdate SQLModel: name: Optional[str] (min_length 1, max_length 40 — UI-SPEC backstop E8), color: Optional[str] (regex `^#[0-9a-fA-F]{6}$` oder NULL), sort_order: Optional[int]
    - PATCH /categories/{id}: UPDATE categories SET name=COALESCE(:name, name), color=COALESCE(:color, color), sort_order=COALESCE(:sort_order, sort_order), updated_at=NOW() WHERE id=:id AND owner_id=:owner_id AND deleted_at IS NULL RETURNING *; 404 wenn kein Row; validiere color-format im SQLModel (zod-äquivalent: Field regex pattern)
    - DELETE /categories/{id}: prüfe Count der aktiven Kategorien für owner; wenn <=1 → 409 CONFLICT "Letzte Kategorie kann nicht gelöscht werden."; sonst SET deleted_at=NOW() WHERE id=:id AND owner_id=:owner_id; reassign items der gelöschten Kategorie auf Inbox (discretion: SELECT Inbox id WHERE owner_id IS NULL) — siehe assumptions
    - POST /categories/reorder: ReorderPayload { items: [{id, sort_order}] }; validiere dass alle IDs dem owner gehören; UPDATE in Transaction für jedes Paar; 404 wenn fremde ID; 200 OK
    - CategoryCreate erweitern: name + optional color + optional sort_order (default 0)

    GREEN: implementiere bis Tests grün. Nutze bestehende get_db_for_owner dependency + current_owner_id context var (PATTERNS).
  </action>
  <verify>
    <automated>cd api && pytest tests/test_categories_color_sort.py -x</automated>
  </verify>
  <acceptance_criteria>
    - 7 Tests grün
    - Alembic 0006 migration läuft forward + backward clean
    - GET /categories liefert color + sort_order; sortiert nach sort_order, name
    - PATCH /categories/{id} validiert color hex-format; 404 bei fremder Kategorie
    - DELETE /categories/{id} blockiert bei letzter Kategorie (409); reassign Items auf Inbox
    - POST /categories/reorder atomar
    - RLS + owner_id WHERE auf jeder Query
  </acceptance_criteria>
  <done>
    Alembic 0006 + Category PATCH/DELETE/reorder implementiert und getestet.
  </done>
  <reversibility rating="costly">Schema-Migration mit data-backfill; downgrade existiert, aber sort_order/color Werte gehen verloren.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PATCH /items/{id} Feld-Edit + Type-Change; DELETE soft-delete + POST restore (Undo)</name>
  <files>
    api/app/routers/items.py,
    api/app/routers/capture.py,
    api/tests/test_items_edit_softdelete.py
  </files>
  <read_first>
    api/app/routers/items.py,
    api/app/routers/capture.py,
    api/app/services/timeout.py,
    api/app/models/note.py,
    api/app/models/link.py,
    api/app/models/task.py,
    api/app/models/event.py,
    api/app/auth/jwt.py,
    api/tests/test_items.py
  </read_first>
  <behavior>
    - Test 1: PATCH /items/{id} mit {title} aktualisiert title in der type-spezifischen Tabelle; 404 wenn fremd
    - Test 2: PATCH /items/{id} mit {body} für note-type aktualisiert body
    - Test 3: PATCH /items/{id} mit {url} für link-type aktualisiert url
    - Test 4: PATCH /items/{id} mit {due} für task-type aktualisiert due
    - Test 5: PATCH /items/{id} mit {event_start, event_end} für event-type aktualisiert beide
    - Test 6: PATCH /items/{id} mit {type: "task"} ändert Item-Typ; bestehende Felder werden gemappt (title bleibt, body→description, url→NULL); altes type-spezifisches Row wird gelöscht, neues erstellt
    - Test 7: DELETE /items/{id} setzt deleted_at; GET /board-items filtert deleted Items heraus
    - Test 8: POST /items/{id}/restore löscht deleted_at; Item erscheint wieder in /board-items
    - Test 9: PATCH/DELETE/restore auf fremdem Item → 404 (owner_id-gescoped)
  </behavior>
  <action>
    RED: Schreibe api/tests/test_items_edit_softdelete.py mit den 9 Tests.

    api/app/routers/items.py erweitern:
    - ItemUpdate SQLModel: title: Optional[str], body: Optional[str], url: Optional[str], due: Optional[datetime], event_start: Optional[datetime], event_end: Optional[datetime], category_id: Optional[UUID], type: Optional[ItemType]
    - PATCH /items/{id} erweitern: lese aktuellen type via _lookup_item_type; wenn payload.type gesetzt und != current type → Type-Change-Flow (discretion: in einer Transaction: INSERT neue type-Tabelle Row mit gemappten Feldern, DELETE alte Row, UPDATE board_items status falls nötig); sonst UPDATE aktuelle type-Tabelle mit COALESCE für alle Felder
    - Type-Change-Feldmapping (D-10): title bleibt erhalten; body/notes → description (task) oder NULL (link/event); url → nur für link; due → nur für task; event_start/end → nur für event; nicht-zutreffende Felder werden NULL
    - DELETE /items/{id}: UPDATE der type-spezifischen Tabelle SET deleted_at=NOW() WHERE id=:id AND owner_id=:owner_id; 404 wenn nicht gefunden
    - POST /items/{id}/restore: UPDATE type-Tabelle SET deleted_at=NULL WHERE id=:id AND owner_id=:owner_id; 404 wenn nicht gefunden

    api/app/routers/capture.py: GET /board-items erweitern: WHERE deleted_at IS NULL AND status IN ('auto_saved', 'confirmed') (defense-in-depth, falls noch nicht vorhanden).

    GREEN: implementiere bis Tests grün. Nutze table_for_item_type helper aus services/timeout.py.
  </action>
  <verify>
    <automated>cd api && pytest tests/test_items_edit_softdelete.py -x</automated>
  </verify>
  <acceptance_criteria>
    - 9 Tests grün
    - PATCH /items/{id} ändert type-spezifische Felder; 404 bei fremdem Item
    - Type-Change mit Feldmapping (title bleibt, type-spezifische Felder gemappt)
    - DELETE setzt deleted_at; restore löscht deleted_at
    - GET /board-items filtert deleted Items heraus
    - Alle Mutationen owner_id-gescoped
  </acceptance_criteria>
  <done>
    Item-Feld-Edit, Type-Change, Soft-Delete + Restore implementiert und getestet.
  </done>
  <reversibility rating="costly">Type-Change löscht alte type-Tabelle-Row; revert nur über manuelle DB-Wiederherstellung.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API-Client → FastAPI | Authentifizierter Request mit JWT; API validiert owner_id |
| FastAPI → Postgres | RLS pro Tenant; explizite WHERE owner_id zusätzlich |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering / IDOR | PATCH /categories/{id} | high | mitigate | owner_id WHERE + RLS; 404 bei fremder Kategorie (kein Info-Leak) |
| T-04-08 | Tampering / XSS | categories.color | high | mitigate | SQLModel Field regex `^#[0-9a-fA-F]{6}$`; 422 bei invalid; nie raw HTML / style-Attribut ohne Escaping |
| T-04-09 | Information Disclosure | GET /board-items | high | mitigate | WHERE owner_id + deleted_at IS NULL + status IN (auto_saved, confirmed); RLS als second layer |
| T-04-10 | Tampering / IDOR | PATCH /items/{id} | high | mitigate | _lookup_item_type prüft owner_id; 404 bei fremdem; Type-Change in Transaction |
| T-04-11 | Repudiation | DELETE /items/{id} | medium | mitigate | deleted_at timestamp + owner_id in DB; restore nur durch owner |
| T-04-12 | Denial of Service | POST /categories/reorder | medium | mitigate | Payload-Größenlimit; max 100 Items pro Reorder-Request; owner_id-Validierung aller IDs |
</threat_model>

<verification>
- `pytest tests/test_categories_color_sort.py tests/test_items_edit_softdelete.py -x` grün
- `alembic upgrade head` clean
- `alembic downgrade -1 && alembic upgrade head` roundtrip clean
</verification>

<success_criteria>
- Alembic 0006 migriert color + sort_order + deleted_at
- Category PATCH/DELETE/reorder owner-scoped
- Item Feld-Edit + Type-Change + Soft-Delete + Restore
- 16 neue Tests grün
</success_criteria>

<output>
Create `.planning/phases/04-webapp/04-02-SUMMARY.md` when done
</output>

