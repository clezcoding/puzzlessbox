---
phase: 05.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/auth/jwt.py
  - api/app/core/bootstrap.py
  - api/tests/unit/test_auth.py
  - api/tests/integration/test_bootstrap.py
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
user_setup: []

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Malformed X-Service-Bearer on any auth call site (jwt.py + internal.py) returns 401, never 500"
    - "Empty mcp_clients with user count > 1 refuses bootstrap unless SERVICE_OWNER_ID is set and valid"
    - "Empty mcp_clients with user count == 1 still auto-binds first user (preserved happy path)"
    - "Prod refuse (empty MCP_BOOTSTRAP_TOKEN) still zero DB writes — no partial service_principals row"
    - "Dev auto-generated bootstrap token still written to 0600 .local/mcp-bootstrap-token, never to stdout plaintext or logger.*"
  artifacts:
    - api/app/auth/jwt.py
    - api/app/core/bootstrap.py
    - api/tests/unit/test_auth.py
    - api/tests/integration/test_bootstrap.py
  key_links:
    - "get_current_owner → _resolve_service_owner (jwt.py) length guard before compare_digest"
    - "check_and_bootstrap_first_user (bootstrap.py) user_count branch → SERVICE_OWNER_ID validation"
---

<objective>
Close code-review warnings WR-01..04 in MCP bootstrap and service-bearer HMAC paths with regression tests. WR-01 (D-05) and WR-02 (D-06) are already fixed in code and covered by existing tests — this plan verifies them and adds the missing code+tests for WR-03 second call site (jwt.py) and WR-04 (multi-user bootstrap owner binding).

Per D-04: all four warnings fixed in code with regression tests — not docs-only.
Per D-07: every SERVICE_BEARER / hmac.compare_digest auth call site rejects empty or length-mismatched bearer with 401 before compare_digest.
Per D-08: when mcp_clients empty and user count > 1, require explicit SERVICE_OWNER_ID or refuse; count == 1 keeps first-user auto-bind.

Purpose: Eliminate TypeError→500 on malformed bearer (WR-03) and silent earliest-user binding in multi-user ops edge case (WR-04); confirm WR-01/02 stay fixed.
Output: Patched jwt.py + bootstrap.py with new/extended tests in test_auth.py and test_bootstrap.py.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04/05.3-CONTEXT.md
@.planning/phases/05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04/05.3-RESEARCH.md
@.planning/phases/05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04/05.3-PATTERNS.md
@.planning/phases/05.1-address-tech-debt-g-05-7-security-harden-advisories/05.1-REVIEW.md
@api/app/auth/jwt.py
@api/app/core/bootstrap.py
@api/app/routers/internal.py
@api/tests/integration/test_bootstrap.py
@api/tests/unit/test_auth.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: WR-03 length-guarded service bearer in jwt.py + 401 regression test</name>
  <files>api/app/auth/jwt.py, api/tests/unit/test_auth.py</files>
  <read_first>
    - api/app/auth/jwt.py (current `_resolve_service_owner` — bare `hmac.compare_digest` at line 72)
    - api/app/routers/internal.py (lines 23-36 — reference length-guard pattern to mirror)
    - api/tests/unit/test_auth.py (existing `test_service_bearer` happy path — preserve)
    - .planning/phases/05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04/05.3-CONTEXT.md (D-07)
    - .planning/phases/05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04/05.3-PATTERNS.md (shared pattern "Timing-Attack & Malformed Input Protection on Bearer Tokens")
    - .planning/phases/05.1-address-tech-debt-g-05-7-security-harden-advisories/05.1-REVIEW.md (WR-03 finding text)
  </read_first>
  <behavior>
    - Test 1: X-Service-Bearer with length != len(SERVICE_BEARER_TOKEN) returns 401 (not 500)
    - Test 2: X-Service-Bearer empty string returns 401
    - Test 3: X-Service-Bearer correct token still resolves owner (existing test_service_bearer stays green)
  </behavior>
  <action>
    In `api/app/auth/jwt.py` `_resolve_service_owner`, replace the bare `hmac.compare_digest(service_bearer, settings.SERVICE_BEARER_TOKEN)` with the same length-guard pattern already used in `api/app/routers/internal.py:28-32` (per D-07 and PATTERNS.md shared pattern "Timing-Attack & Malformed Input Protection on Bearer Tokens"). Reject when `service_bearer` is empty or `len(service_bearer) != len(expected)` with HTTP 401 before calling `hmac.compare_digest`. Do NOT touch the calendar webhook HMAC path (out of scope per CONTEXT). Do NOT change `_decode_owner_from_token` or `_validate_owner_header`. Mirror the exact detail dict shape `{"code": "UNAUTHORIZED", "message": "Invalid service bearer token."}` already used by internal.py so error responses are uniform.

    Then in `api/tests/unit/test_auth.py` add `test_service_bearer_length_mismatch_returns_401` and `test_service_bearer_empty_returns_401` (asyncio.mark, `_FakeRequest` with short / empty `X-Service-Bearer` header, no service_principal row needed because the guard fires before DB lookup). Assert `HTTPException.status_code == 401`. Do NOT regress `test_service_bearer` (happy path) — keep it green. Per D-07: this completes the second auth call site; internal.py already has the guard.
  </action>
  <verify>
    <automated>cd api && .venv/bin/pytest tests/unit/test_auth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "len\(service_bearer\) != len\(expected\)" api/app/auth/jwt.py` returns at least 1 match
    - `rg -n "test_service_bearer_length_mismatch_returns_401|test_service_bearer_empty_returns_401" api/tests/unit/test_auth.py` returns 2 matches
    - `cd api && .venv/bin/pytest tests/unit/test_auth.py -x -q` exit 0
    - `cd api && .venv/bin/pytest tests/unit/test_auth.py::test_service_bearer -x -q` exit 0 (happy path preserved)
  </acceptance_criteria>
  <done>
    - `_resolve_service_owner` rejects empty or length-mismatched bearer with 401 before `hmac.compare_digest`
    - New tests `test_service_bearer_length_mismatch_returns_401` and `test_service_bearer_empty_returns_401` pass
    - Existing `test_service_bearer` still passes (happy path preserved)
    - No 500 path reachable via malformed X-Service-Bearer on jwt.py
  </done>
  <reversibility rating="reversible">Single-function guard added; trivially revertible. Aligns with existing internal.py pattern — no architectural change.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-04 multi-user SERVICE_OWNER_ID check in bootstrap.py + regression tests + WR-01/02 re-verify</name>
  <files>api/app/core/bootstrap.py, api/tests/integration/test_bootstrap.py</files>
  <read_first>
    - api/app/core/bootstrap.py (current `check_and_bootstrap_first_user` lines 100-155 — earliest-user bind at line 112-114)
    - api/app/core/config.py (Settings.SERVICE_OWNER_ID field — confirm env var name)
    - api/tests/integration/test_bootstrap.py (existing tests + `_insert_user` + `_clear_bootstrap_tables` helpers)
    - .planning/phases/05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04/05.3-CONTEXT.md (D-08, D-05, D-06)
    - .planning/phases/05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04/05.3-PATTERNS.md (shared pattern "Multi-User Isolation on Auto-Bootstrap Setup")
    - .planning/phases/05.1-address-tech-debt-g-05-7-security-harden-advisories/05.1-REVIEW.md (WR-04 finding text)
  </read_first>
  <behavior>
    - Test 1: mcp_clients empty + 2 users + SERVICE_OWNER_ID unset → bootstrap refuses, zero mcp_clients + zero service_principals writes, error log emitted
    - Test 2: mcp_clients empty + 2 users + SERVICE_OWNER_ID set to existing user → bootstrap binds that user (not earliest createdAt ASC)
    - Test 3 (regression): mcp_clients empty + 1 user + SERVICE_OWNER_ID unset → still auto-binds first user (happy path preserved)
    - Test 4 (regression, already green): prod + empty MCP_BOOTSTRAP_TOKEN → zero DB writes (WR-01 D-05)
    - Test 5 (regression, already green): dev + empty MCP_BOOTSTRAP_TOKEN → 0600 file, no plaintext in stdout/log (WR-02 D-06)
  </behavior>
  <action>
    In `api/app/core/bootstrap.py` `check_and_bootstrap_first_user`, after the `mcp_count > 0` early return and the `first_user_id` SELECT, replace the silent earliest-user bind with a user-count branch per D-08:

    1. Compute `user_count = session.execute(text('SELECT count(*) FROM "user"')).scalar_one()`.
    2. If `user_count == 1`: keep `bootstrap_owner_id = first_user_id` (preserved happy path).
    3. If `user_count > 1`: read `owner_id_env = settings.SERVICE_OWNER_ID.strip()`. If empty → `logger.error("Multi-user setup detected but SERVICE_OWNER_ID is empty. Aborting bootstrap.")` and return with zero writes. If non-empty → validate it exists in `user` table via `SELECT 1 FROM "user" WHERE id = CAST(:oid AS uuid) LIMIT 1`; if missing → `logger.error` with the offending id and return; else `bootstrap_owner_id = owner_id_env`.
    4. If `user_count == 0`: existing `first_user_id is None` branch already returns — keep it.
    5. Use `bootstrap_owner_id` (not `first_user_id`) for the subsequent `service_principals` and `mcp_clients` INSERTs.

    Do NOT change `_resolve_bootstrap_token` (WR-01/02 already correct: token resolved before any INSERT; 0600 file + stdout path-only hint, never logger.* plaintext). Do NOT change `ensure_service_principal` or `ensure_mcp_client` lifespan paths.

    Then in `api/tests/integration/test_bootstrap.py` add:
    - `test_multi_user_bootstrap_requires_owner_id`: insert 2 users (owner_id_a + owner_id_b with different createdAt if needed), clear mcp_clients + service_principals, unset SERVICE_OWNER_ID, POST /internal/mcp-auth with service headers + matching bearer_hash, assert response 401, `mcp_count == 0`, `sp_count == 0`, and "SERVICE_OWNER_ID is empty" in caplog.text.
    - `test_multi_user_bootstrap_uses_owner_id`: same setup, set `SERVICE_OWNER_ID` to owner_id_b (not the earliest), POST with bearer_hash matching the bootstrap token, assert response 200, `mcp_count == 1`, the bound `mcp_clients.owner_id == owner_id_b` (not earliest user).
    - Keep `test_happy_path_first_user_bootstraps` (single-user) green — it covers the `user_count == 1` branch.

    Re-verify WR-01/02 by re-running the existing `test_prod_refuse_when_env_token_empty` (asserts `sp_count == 0`) and `test_dev_generate_when_env_token_empty` (asserts token not in stdout/caplog) — no code change needed there, but they must stay green after the bootstrap.py edit.
  </action>
  <verify>
    <automated>cd api && .venv/bin/pytest tests/integration/test_bootstrap.py tests/unit/test_auth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Multi-user setup detected but SERVICE_OWNER_ID is empty" api/app/core/bootstrap.py` returns 1 match
    - `rg -n "test_multi_user_bootstrap_requires_owner_id|test_multi_user_bootstrap_uses_owner_id" api/tests/integration/test_bootstrap.py` returns 2 matches
    - `cd api && .venv/bin/pytest tests/integration/test_bootstrap.py -x -q` exit 0 (all 8+ bootstrap tests green incl. existing happy path, prod refuse, dev generate, race, malformed bearer, lifespan ensure)
    - `cd api && .venv/bin/pytest tests/integration/test_bootstrap.py::test_prod_refuse_when_env_token_empty tests/integration/test_bootstrap.py::test_dev_generate_when_env_token_empty -x -q` exit 0 (WR-01/02 re-verify)
  </acceptance_criteria>
  <done>
    - `check_and_bootstrap_first_user` branches on `user_count`: 1 → auto-bind, >1 → require valid SERVICE_OWNER_ID or refuse with zero writes, 0 → no-op
    - `test_multi_user_bootstrap_requires_owner_id` and `test_multi_user_bootstrap_uses_owner_id` pass
    - `test_happy_path_first_user_bootstraps` (single-user auto-bind) still passes
    - `test_prod_refuse_when_env_token_empty` (WR-01 zero-write) still passes
    - `test_dev_generate_when_env_token_empty` (WR-02 0600 file + no plaintext) still passes
    - `test_malformed_service_bearer_returns_401_not_500` (WR-03 internal.py) still passes
  </done>
  <reversibility rating="reversible">Branch added inside one function; existing single-user behavior preserved. Rollback = restore the single SELECT-then-bind line.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes → API `/internal/mcp-auth` | Service bearer crosses trust boundary; malformed header must not crash (DoS) or auth-bypass |
| API → Postgres `mcp_clients`/`service_principals` | Bootstrap writes must be atomic and owner-correct; partial writes break invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.3-01 | Denial of Service | `api/app/auth/jwt.py:_resolve_service_owner` `hmac.compare_digest` | high | mitigate | Length pre-check before `compare_digest` (Task 1) — malformed bearer → 401, no TypeError/500 |
| T-05.3-02 | Elevation of Privilege | `api/app/core/bootstrap.py:check_and_bootstrap_first_user` earliest-user bind | medium | mitigate | user_count > 1 branch requires explicit SERVICE_OWNER_ID validated against user table (Task 2) |
| T-05.3-03 | Tampering | `api/app/core/bootstrap.py` partial `service_principals` write on prod refuse | high | accept | Already mitigated in 05.1: `_resolve_bootstrap_token` runs before any INSERT; existing test `test_prod_refuse_when_env_token_empty` asserts `sp_count == 0`. Task 2 re-verifies. |
| T-05.3-04 | Information Disclosure | `api/app/core/bootstrap.py` dev stdout token | high | accept | Already mitigated in 05.1: 0600 `.local/mcp-bootstrap-token` + stdout path-only hint, never `logger.*` plaintext. Existing test asserts token absent from stdout/log. |
| T-05.3-SC | Tampering | (no package installs in this plan) | n/a | n/a | No npm/pip/cargo installs — package-legitimacy gate not applicable |
</threat_model>

<verification>
- `cd api && .venv/bin/pytest tests/unit/test_auth.py tests/integration/test_bootstrap.py -x -q` green
- `cd api && .venv/bin/pytest -q` full suite green (no regressions in calendar/scrape/health suites)
- Grep gate: `rg -n "hmac.compare_digest" api/app/auth/jwt.py api/app/routers/internal.py` — both call sites preceded by length guard
- Grep gate: `rg -n "createdAt.*ASC.*LIMIT 1" api/app/core/bootstrap.py` no longer the sole bind path (replaced by user_count branch)
</verification>

<success_criteria>
- All four WR findings closed in code: WR-01 verified, WR-02 verified, WR-03 fixed at both call sites (internal.py + jwt.py), WR-04 fixed
- New regression tests for WR-03 jwt.py (2 tests) and WR-04 (2 tests) pass
- Existing bootstrap + auth tests stay green
- No new external dependencies
- Threat model dispositions: T-01/02 mitigated by code, T-03/04 accepted (already mitigated in 05.1, re-verified here)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

### New tests
- `api/tests/unit/test_auth.py::test_service_bearer_length_mismatch_returns_401` — WR-03 jwt.py regression
- `api/tests/unit/test_auth.py::test_service_bearer_empty_returns_401` — WR-03 jwt.py regression
- `api/tests/integration/test_bootstrap.py::test_multi_user_bootstrap_requires_owner_id` — WR-04 refuse path
- `api/tests/integration/test_bootstrap.py::test_multi_user_bootstrap_uses_owner_id` — WR-04 valid owner bind path

### Modified functions
- `api/app/auth/jwt.py::_resolve_service_owner` — length guard added before `hmac.compare_digest` (WR-03 / D-07)
- `api/app/core/bootstrap.py::check_and_bootstrap_first_user` — `user_count` branch with `SERVICE_OWNER_ID` validation (WR-04 / D-08); WR-01/02 paths (`_resolve_bootstrap_token`, INSERT ordering) unchanged and re-verified

### Doc flips
- (none in this plan — doc flips happen in Plan 02 Wave 2)
</artifacts_produced>

<output>
Create `.planning/phases/05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04/05.3-01-SUMMARY.md` when done
</output>
