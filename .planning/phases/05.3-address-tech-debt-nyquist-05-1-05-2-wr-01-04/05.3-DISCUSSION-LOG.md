# Phase 05.3: address-tech-debt-nyquist-05-1-05-2-wr-01-04 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-09
**Phase:** 05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04
**Mode:** `--auto` (recommended defaults; no AskUserQuestion)
**Areas discussed:** Nyquist closure, WR-01..04 fix approach, 05.1-UAT refresh, Scope boundary

---

## Nyquist closure

| Option | Description | Selected |
|--------|-------------|----------|
| Close in-phase | Update 05.1/05.2 VALIDATION maps to existing tests; run green; set validated + nyquist_compliant | ✓ |
| Operator checklist only | Leave draft; document "run validate-phase later" | |
| Regenerate Wave 0 stubs | Rebuild stubs from scratch regardless of existing suites | |

**User's choice:** [auto] Close in-phase (recommended default)
**Notes:** No new product features; smallest missing test only if map row lacks automation for shipped behavior. Manual HSTS rows stay manual.

---

## WR-01..04 scope

| Option | Description | Selected |
|--------|-------------|----------|
| All four in code + tests | WR-01..04 code fixes with regression tests | ✓ |
| Security-critical only | WR-01 + WR-03 only | |
| Docs-only | Document warnings; no code change | |

**User's choice:** [auto] All four in code + tests (recommended default)

---

## WR-01 approach

| Option | Description | Selected |
|--------|-------------|----------|
| Token before writes | Resolve/validate token before any SP insert; prod refuse = zero writes | ✓ |
| Transaction rollback | Wrap both inserts; rollback on refuse | |
| Cleanup orphan SP | Allow insert then delete on abort | |

**User's choice:** [auto] Token before writes (recommended default)

---

## WR-02 approach

| Option | Description | Selected |
|--------|-------------|----------|
| 0600 file + isatty gate | Write `.local/mcp-bootstrap-token` (0600); stdout path hint; plaintext stdout only if isatty | ✓ |
| isatty stdout only | No file; print token only on TTY | |
| Keep stdout | Document Coolify log risk only | |

**User's choice:** [auto] 0600 file + isatty gate (recommended default)

---

## WR-03 approach

| Option | Description | Selected |
|--------|-------------|----------|
| Guard all SERVICE_BEARER sites | Empty/length mismatch → 401 before compare_digest (jwt.py + internal.py) | ✓ |
| jwt.py only | Fix primary auth path only | |
| Custom helper | New constant-time wrapper module | |

**User's choice:** [auto] Guard all SERVICE_BEARER sites (recommended default)

---

## WR-04 approach

| Option | Description | Selected |
|--------|-------------|----------|
| Multi-user requires SERVICE_OWNER_ID | users>1 + empty mcp_clients → require env owner or refuse; users==1 keep first-user | ✓ |
| Document ASC only | Keep earliest createdAt; docstring only | |
| Latest user DESC | Bind newest signup | |

**User's choice:** [auto] Multi-user requires SERVICE_OWNER_ID (recommended default)

---

## 05.1-UAT refresh

| Option | Description | Selected |
|--------|-------------|----------|
| Narrow stale-row refresh | Update well-known / known-stale rows vs live curl | ✓ |
| Skip UAT docs | Leave stale UAT | |
| Full 05.1 re-UAT | Re-run all HSTS/banner checks as UAT | |

**User's choice:** [auto] Narrow stale-row refresh (recommended default)

---

## Scope boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Strict phase title | Nyquist + WR-01..04 + stale UAT only; defer title-only / dbhub / RuntimeWarning | ✓ |
| Include title-only | Also fix Google Missing end time | |
| All audit tech_debt | Fold every audit residual row | |

**User's choice:** [auto] Strict phase title (recommended default)
**Notes:** Deferred ideas recorded in CONTEXT.md

---

## Claude's Discretion

- Exact `.local/` filename / copy-to-`.env` reminder wording
- Calendar webhook HMAC length guard same plan vs follow-up
- Wave ordering (WR code first vs Nyquist sign-off first)

## Deferred Ideas

- Title-only Google "Missing end time"
- dbhub default DSN = prod
- Phase 3 schedule_autosave_poll RuntimeWarning
