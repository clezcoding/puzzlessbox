---
phase: 05.3
slug: address-tech-debt-nyquist-05-1-05-2-wr-01-04
status: verified
threats_open: 0
asvs_level: 1
created: 2026-08-09
---

# Phase 05.3 — Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> Sourced from plan `<threat_model>` blocks + VERIFICATION evidence (WR-01..04 closed).

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Client → API service bearer | `X-Service-Bearer` on JWT/service paths; malformed length must not 500 | SERVICE_BEARER_TOKEN |
| First-user / MCP auth → bootstrap | Empty `mcp_clients` triggers auto-bootstrap; multi-user must not silent-bind wrong owner | MCP_BOOTSTRAP_TOKEN, SERVICE_OWNER_ID, user ids |
| Dev stdout / Coolify logs | Generated bootstrap token must not enter container log streams | MCP_BOOTSTRAP_TOKEN plaintext |
| Docs / VALIDATION maps | Sign-off docs must not invent coverage that doesn't exist | Test command references |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-05.3-01 | Denial of Service | `api/app/auth/jwt.py` `_resolve_service_owner` `hmac.compare_digest` | high | mitigate | Length/empty guard before `compare_digest` → 401 (mirrors `internal.py`); unit tests length-mismatch + empty | closed |
| T-05.3-02 | Elevation of Privilege | `api/app/core/bootstrap.py` `check_and_bootstrap_first_user` | medium | mitigate | `user_count > 1` requires validated `SERVICE_OWNER_ID`; refuse with zero writes if missing/invalid; integration tests | closed |
| T-05.3-03 | Tampering | Partial `service_principals` on prod refuse | high | accept | Already mitigated 05.1: token resolve before INSERT; `test_prod_refuse_when_env_token_empty` re-verified | closed |
| T-05.3-04 | Information Disclosure | Dev bootstrap token on stdout | high | accept | Already mitigated 05.1: 0600 `.local/mcp-bootstrap-token` + isatty gate; re-verified | closed |
| T-05.3-05 | Tampering | VALIDATION.md map rows | medium | mitigate | Flip only after mapped pytest/vitest green (D-02); 05.1/05.2 `nyquist_compliant: true` | closed |
| T-05.3-06 | Information Disclosure | Stale 05.1-UAT well-known row | low | accept | Refresh from live curl 200; no secret surface | closed |

*Severity: critical > high > medium > low — only open threats at or above workflow.security_block_on (`high`) count toward threats_open*

---

## Accepted Risks

| Risk | Reason | Review trigger |
|------|--------|----------------|
| Calendar webhook `compare_digest` length guard | Deferred per CONTEXT Claude's Discretion / D-07 scope (SERVICE_BEARER sites only) | Next calendar auth touch |

---

## Verification Evidence

| Check | Result |
|-------|--------|
| `test_service_bearer_length_mismatch_returns_401` | pass |
| `test_service_bearer_empty_returns_401` | pass |
| `test_multi_user_bootstrap_requires_owner_id` | pass |
| `test_multi_user_bootstrap_uses_owner_id` | pass |
| WR-01/02 regression tests | pass |
| Phase VERIFICATION.md | passed (11/11) |

---

## Sign-off

- [x] `threats_open: 0` confirmed
- [x] High+ threats mitigated or accepted with evidence
- [x] No new package installs (T-SC n/a)
