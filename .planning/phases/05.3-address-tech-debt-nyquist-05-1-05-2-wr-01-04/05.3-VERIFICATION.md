---
phase: 05.3-address-tech-debt-nyquist-05-1-05-2-wr-01-04
verified: 2026-08-09T10:42:00Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 05.3: Address Tech Debt — Nyquist 05.1/05.2 + WR-01..04 Verification Report

**Phase Goal:** Close residual v1.0 milestone tech debt: (1) flip Phase 05.1 and 05.2 Nyquist `VALIDATION.md` from `status: draft` / `nyquist_compliant: false` to validated + compliant using existing tests, (2) fix code-review warnings WR-01..04 in MCP bootstrap / service-bearer HMAC paths with regression tests, (3) refresh stale `05.1-UAT.md` well-known row that live curl already proves 200.
**Verified:** 2026-08-09T10:42:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Malformed X-Service-Bearer on any auth call site (jwt.py + internal.py) returns 401, never 500 | ✓ VERIFIED | `api/app/auth/jwt.py:75` `len(service_bearer) != len(expected)` guard before `hmac.compare_digest`; `api/app/routers/internal.py:30` same guard. `pytest tests/unit/test_auth.py::test_service_bearer_length_mismatch_returns_401 tests/unit/test_auth.py::test_service_bearer_empty_returns_401` → 2 passed. Happy path `test_service_bearer` → 1 passed. |
| 2 | Empty mcp_clients with user count > 1 refuses bootstrap unless SERVICE_OWNER_ID is set and valid | ✓ VERIFIED | `api/app/core/bootstrap.py:120-139` user_count > 1 branch requires `SERVICE_OWNER_ID`, validates against `user` table, returns with zero writes on empty/missing. `pytest tests/integration/test_bootstrap.py::test_multi_user_bootstrap_requires_owner_id` → 10 passed (full suite green). |
| 3 | Empty mcp_clients with user count == 1 still auto-binds first user (preserved happy path) | ✓ VERIFIED | `bootstrap.py:118-119` `user_count == 1` branch keeps `bootstrap_owner_id = first_user_id`. `test_happy_path_first_user_bootstraps` passes (part of 10/10 green). |
| 4 | Prod refuse (empty MCP_BOOTSTRAP_TOKEN) still zero DB writes — no partial service_principals row | ✓ VERIFIED | `bootstrap.py:34-36` prod refuse in `_resolve_bootstrap_token` before any INSERT. `test_prod_refuse_when_env_token_empty` asserts `mcp_count == 0`, `sp_count == 0` — passes. |
| 5 | Dev auto-generated bootstrap token written to 0600 `.local/mcp-bootstrap-token`, never to stdout plaintext or `logger.*` | ✓ VERIFIED | `bootstrap.py:22-44` `_write_dev_bootstrap_token` chmods 0600, stdout writes path-only hint. `test_dev_generate_when_env_token_empty` asserts token absent from stdout + caplog — passes. |
| 6 | 05.1-VALIDATION.md frontmatter `status: validated` and `nyquist_compliant: true` | ✓ VERIFIED | `05.1-VALIDATION.md:6-9` `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `validated: 2026-08-09`. |
| 7 | 05.2-VALIDATION.md frontmatter `status: validated` and `nyquist_compliant: true` | ✓ VERIFIED | `05.2-VALIDATION.md:6-9` `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`, `validated: 2026-08-09`. |
| 8 | 05.1-VALIDATION.md Per-Task Verification Map references concrete Task IDs from 05.1 plans and 05.3-01 plans (for WR rows) | ✓ VERIFIED | `05.1-VALIDATION.md:47-48` rows `05.3-01-T1` (WR-03) and `05.3-01-T2` (WR-04) added with green status; original rows `05.1-01-01`, `05.1-02-01`, `05.1-03-01` preserved. |
| 9 | 05.2-VALIDATION.md Per-Task Verification Map references concrete Task IDs from 05.2 plans | ✓ VERIFIED | 13 per-task rows reference concrete IDs (05.2-00-01..05.2-04-03); all green; `Approval: validated 2026-08-09`. |
| 10 | 05.1-UAT.md well-known row reflects live 200 (not blocked) and `status: complete` | ✓ VERIFIED | Live curl `https://mcp.puzzlesstool.online/.well-known/oauth-protected-resource/mcp` → `200` (re-run by verifier 2026-08-09). `05.1-UAT.md:2` `status: complete`; Test #15 `result: pass` with notes `live curl 2026-08-09 -> 200`; Summary 15/15 pass, 0 blocked; Gaps [none]. |
| 11 | 05.3-VALIDATION.md Per-Task Verification Map updated with concrete Task IDs from 05.3-01 | ✓ VERIFIED | `05.3-VALIDATION.md:43-47` rows `05.3-01-T1`, `05.3-01-T2`, `05.3-02-T1`, `05.3-02-T2`, `05.3-02-T3` present; Wave 0 boxes ticked. Frontmatter left `draft` for validate-phase tool (per plan — not a phase-goal miss). |

**Score:** 11/11 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|---------|--------|---------|
| `api/app/auth/jwt.py` | Length guard before `hmac.compare_digest` in `_resolve_service_owner` | ✓ VERIFIED | Line 73-77 guard; mirrors `internal.py:30-32`. |
| `api/app/core/bootstrap.py` | `user_count` branch in `check_and_bootstrap_first_user`; WR-01/02 paths unchanged | ✓ VERIFIED | Lines 117-139 branch; `_resolve_bootstrap_token` unchanged. |
| `api/tests/unit/test_auth.py` | `test_service_bearer_length_mismatch_returns_401` + `test_service_bearer_empty_returns_401` | ✓ VERIFIED | Lines 99, 112; both pass. |
| `api/tests/integration/test_bootstrap.py` | `test_multi_user_bootstrap_requires_owner_id` + `test_multi_user_bootstrap_uses_owner_id` | ✓ VERIFIED | Lines 328, 373; both pass; 10/10 suite green. |
| `05.1-VALIDATION.md` | `status: validated`, `nyquist_compliant: true` | ✓ VERIFIED | Frontmatter flipped; sign-off approved. |
| `05.2-VALIDATION.md` | `status: validated`, `nyquist_compliant: true` | ✓ VERIFIED | Frontmatter flipped; all rows green. |
| `05.1-UAT.md` | `status: complete`, Test #15 pass | ✓ VERIFIED | 15/15 pass; 0 blocked. |
| `05.3-VALIDATION.md` | Concrete Task IDs in map | ✓ VERIFIED | 5 concrete IDs (05.3-01-T1/T2, 05.3-02-T1/T2/T3). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `get_current_owner` | `_resolve_service_owner` (jwt.py) | Length guard before `compare_digest` | ✓ WIRED | `jwt.py:73-77` guard fires before DB lookup; 401 returned. |
| `check_and_bootstrap_first_user` | `SERVICE_OWNER_ID` validation | `user_count` branch | ✓ WIRED | `bootstrap.py:117-139` branch; validates against `user` table. |
| 05.1 VALIDATION | 05.1-01/02/03 SUMMARYs + 05.3-01 PLAN (WR rows) | Per-task map rows | ✓ WIRED | Rows 05.1-01-01..05.1-03-01 + 05.3-01-T1/T2 reference existing tests. |
| 05.2 VALIDATION | 05.2-00..04 SUMMARYs | Per-task map rows | ✓ WIRED | 13 rows reference concrete Task IDs from 05.2 plans. |
| 05.1-UAT row #15 | Live curl 200 evidence | `curl https://mcp.puzzlesstool.online/...` | ✓ WIRED | Verifier re-ran curl → 200; UAT row notes 2026-08-09. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| WR-03 jwt.py length-mismatch bearer → 401 | `pytest tests/unit/test_auth.py::test_service_bearer_length_mismatch_returns_401 tests/unit/test_auth.py::test_service_bearer_empty_returns_401 -x -q` | 2 passed in 0.01s | ✓ PASS |
| WR-03 happy path preserved | `pytest tests/unit/test_auth.py::test_service_bearer -x -q` (with postgres) | 1 passed | ✓ PASS |
| WR-04 multi-user + happy path + WR-01/02 regression | `pytest tests/integration/test_bootstrap.py -x -q` (docker postgres :5433) | 10 passed in 0.80s | ✓ PASS |
| Live well-known endpoint | `curl -s -o /dev/null -w "%{http_code}" https://mcp.puzzlesstool.online/.well-known/oauth-protected-resource/mcp` | `200` | ✓ PASS |

### Probe Execution

Phase 05.3 PLANs declare pytest commands as the automated verification (no `scripts/*/tests/probe-*.sh` declared). Probes ran as behavioral spot-checks above — all PASS.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-----------|-------------|--------|----------|
| D-01 | 05.3-02 | Nyquist VALIDATION flip 05.1 + 05.2 | ✓ SATISFIED | Both files `status: validated`, `nyquist_compliant: true`. |
| D-02 | 05.3-02 | No new product features / Wave-0 stubs | ✓ SATISFIED | No new product code; only WR fixes + test additions + doc flips. |
| D-03 | 05.3-02 | Manual-only rows preserved (HSTS / banner / apex) | ✓ SATISFIED | 05.1-VALIDATION manual rows retained with last-verified-live column. |
| D-04 | 05.3-01 | All four WR warnings fixed in code with regression tests | ✓ SATISFIED | WR-01/02 re-verified; WR-03 jwt.py guard + tests; WR-04 branch + tests. |
| D-05 | 05.3-01 | Resolve/validate bootstrap token before any `service_principals` insert | ✓ SATISFIED | `bootstrap.py:141-143` token resolved before INSERTs; prod refuse test asserts zero writes. |
| D-06 | 05.3-01 | 0600 file under `.local/` + stdout path-only hint, never `logger.*` plaintext | ✓ SATISFIED | `bootstrap.py:22-44`; `test_dev_generate_when_env_token_empty` asserts token absent from stdout/log. |
| D-07 | 05.3-01 | Every `SERVICE_BEARER` / `hmac.compare_digest` auth call site rejects empty/length-mismatched bearer with 401 | ✓ SATISFIED | `jwt.py:73-77` + `internal.py:30-32` both guarded; tests pass. |
| D-08 | 05.3-01 | Multi-user empty `mcp_clients` requires explicit valid `SERVICE_OWNER_ID`; single-user keeps auto-bind | ✓ SATISFIED | `bootstrap.py:117-139`; refuse + uses_owner_id tests pass; happy path preserved. |
| D-09 | 05.3-02 | Refresh stale 05.1-UAT well-known row to live 200 | ✓ SATISFIED | UAT status: complete; Test #15 pass; live curl 200 confirmed. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | No TBD/FIXME/XXX in modified files; no stub returns; no orphaned artifacts. |

### Human Verification Required

None. All truths resolved with code + test + live evidence. Live curl re-run by verifier; automated suites green; doc flips match frontmatter contract.

### Gaps Summary

No gaps. All 11 must-have truths verified. Phase goal (Nyquist 05.1/05.2 validated + WR-01..04 fixed + UAT refresh) achieved:

1. **Nyquist 05.1/05.2 validated** — both `VALIDATION.md` files flipped to `status: validated`, `nyquist_compliant: true`; per-task maps green; sign-off approved 2026-08-09.
2. **WR-01..04 fixed** — WR-01/02 re-verified (existing tests green); WR-03 length guard added at second call site (`jwt.py`) with 2 regression tests; WR-04 multi-user `SERVICE_OWNER_ID` branch added with 2 regression tests. All 10 bootstrap tests + 5 auth tests green.
3. **UAT refresh** — `05.1-UAT.md` `status: complete`; Test #15 well-known row `result: pass` with live curl 200 notes; 15/15 pass, 0 blocked; Gaps [none].

`05.3-VALIDATION.md` frontmatter remains `status: draft` — this is intentional per plan (validate-phase tool sets it, not the executor). Not a phase-goal miss.

---

_Verified: 2026-08-09T10:42:00Z_
_Verifier: Claude (gsd-verifier)_
