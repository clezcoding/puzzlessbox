---
context: spike
phase: 03-hermes-plugin-timeout-spike
task: 0
total_tasks: 0
status: ready_for_planning
last_updated: 2026-07-31T04:03:57.608Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: main branch protected — direct push rejected; all main changes via PR
- [ ] CONSTRAINT: Hermes cron is NOT the 30s timer — API `DraftTimeoutManager` only (spike 001 INVALIDATED Hermes-as-timer)
- [ ] CONSTRAINT: mcp-test CI needs `pythonpath = .` in `mcp-server/pytest.ini` — without it collection fails on main

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Hermes cron as autosave timer | 60s tick cannot hit 30s deadline; chaotic early/late saves | blocking | Read `references/draft-timeout.md`; never add dispatch_tool/cron for deadline |
| Direct DB from Hermes VPS | Breaks tenancy + Phase 3 architecture | blocking | MCP tools only per `references/mcp-remote-integration.md` |
| Dismissing Dependabot alerts | User required proper fixes, not ignore/dismiss | advisory | Bump deps + overrides; verify `npm audit` / GitHub API count = 0 |
| owner_id in MCP tool args | Tenancy leak — owner from bearer | advisory | Bearer → `/internal/mcp-auth` only |

<current_state>
Phase 3 prep complete. Spikes 001–004 done + wrap-up skill committed (`fd4e26d`). On branch `docs/phase-3-spikes`. PR #19 open (not merged). Phase 3 plan directory does not exist yet — next step is `/gsd-plan-phase 3`.
</current_state>

<completed_work>

Completed Tasks:
- Phase 2 ship: PR #15 merged to main (`65dfe71`)
- CI extended: `api-test`, `mcp-test`, `webapp-build` jobs + CodeQL path filters
- Security: 42 Dependabot alerts → 0 (next 16.2.12, pyjwt 2.13.0, cryptography 48.0.1)
- mcp-test fix on main: `mcp-server/pytest.ini` `pythonpath = .` (`ec40c7a`)
- Spikes 001–004: MCP-04 VALIDATED, mock confirm flow VALIDATED, remote MCP VALIDATED, CAP-02 VALIDATED
- Spike wrap-up: `.claude/skills/spike-findings-puzzlessbox/` + `WRAP-UP-SUMMARY.md` + CLAUDE.md routing
</completed_work>

<remaining_work>

- Merge PR #19 — **done** (`1d0efb1` on main)
- `/gsd-plan-phase 3` — create phase plans for Hermes plugin (CAP-02, CAP-04, MCP-03, MCP-04)
- `/gsd-execute-phase 3` — after planning
- Optional: live E2E confirm on Hermes VPS (spike 002 PARTIAL — needs `MCP_BEARER` + `MCP_CATEGORY_ID`)
</remaining_work>

<decisions_made>

- API owns 30s `DraftTimeoutManager`; Hermes cron may notify post-autosave only
- Hermes plugin: `create_item` → optional `update_item` → `confirm_item` (no DB on Hermes)
- Production MCP: `https://mcp.puzzlesstool.online/mcp` — 401 on bad bearer
- CAP-02 confirmation: German plain-text template in `sources/004-confirmation-payload/format_confirmation.py`
</decisions_made>

<blockers>
(none — PR #19 merge is optional before planning but recommended so skill lands on main)
</blockers>

## Required Reading (in order)

1. `.claude/skills/spike-findings-puzzlessbox/SKILL.md` — spike requirements index
2. `.planning/spikes/WRAP-UP-SUMMARY.md` — verdict table
3. `.planning/STATE.md` — phase position + TODOs
4. `.planning/ROADMAP.md` — Phase 3 scope (CAP-02, CAP-04, MCP-03, MCP-04)

## Infrastructure State

- **main:** PR #19 merged (`1d0efb1`) — spikes + wrap-up skill on main
- **Local branch:** `docs/phase-3-spikes` — consider `git checkout main && git pull`
- **MCP prod:** `mcp.puzzlesstool.online` — health OK
- **Untracked:** `graphify-out/` (local graph cache — do not commit)
- **Background processes:** none

<context>
Session shipped Phase 2, hardened CI/security, ran frontier spikes for Phase 3 risk, wrapped findings into persistent skill. MCP-04 gate satisfied. Ready to plan Phase 3 Hermes plugin implementation using spike-findings skill as blueprint.
</context>

<next_action>
Start with: `git checkout main && git pull`, then `/gsd-discuss-phase 3` or `/gsd-plan-phase 3` with `Skill("spike-findings-puzzlessbox")` loaded.
</next_action>
