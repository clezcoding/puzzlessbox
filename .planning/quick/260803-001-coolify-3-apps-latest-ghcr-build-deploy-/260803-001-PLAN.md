---
phase: quick
plan: 260803-001
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-AUDIT.json
  - .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-PR-RESOLVE.json
autonomous: true
requirements: [OPS-01, OPS-02, OPS-04]
user_setup: []

must_haves:
  truths:
    - "All 3 Coolify apps run LATEST GHCR digest matching recent successful deploy-* GH Actions run"
    - "All env vars on all 3 apps match locked spec (JWKS, BETTER_AUTH_URL, NEXT_PUBLIC_API_BASE_URL, health paths, D-14 timings 10s/5s/5/15s)"
    - "Old API app dxoflgio67786lc4yilhce43 stopped, no domain"
    - "DB backup schedule jl0skzwpd3ot7hgfmohlny9s enabled with successful baseline"
    - "Every open Renovate/Dependabot chore PR merged (CI green) or escalated with evidence"
    - "No stable update PR closed as duplicate without git/gh verification against main"
  artifacts:
    - .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-AUDIT.json
    - .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-PR-RESOLVE.json
  key_links:
    - "GH Actions deploy-* run → GHCR :latest digest → Coolify app running digest"
    - "Coolify webhook URL pattern https://puzzlesstool.online/api/v1/deploy?uuid=<app-uuid>"
    - "Kodiak automerge label → CI green → PR merge"
---

<objective>
Two parallel workstreams: (1) Coolify audit+fix — verify all 3 apps on latest GHCR digest, env vars correct, health/D-14 timings, backups, old app stopped; trigger GH Actions re-deploy + Coolify redeploy where behind. (2) GitHub chore PRs — enumerate all open Renovate/Dependabot PRs on clezcoding/puzzlessbox, verify CI green, rebase if needed, merge or enable automerge; never skip stable bumps.

Purpose: Keep production on latest GHCR images; resolve all pending dependency chore PRs so backlog stays at zero.
Output: Two audit-trace JSON files (Coolify + PRs) recording state before/after, decisions, verifications.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-VERIFICATION.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/COVERAGE.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-03-SUMMARY.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-04-SUMMARY.md
@.github/workflows/deploy-api.yml
@.github/workflows/deploy-web.yml
@.github/workflows/deploy-mcp.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coolify 3-app GHCR latest digest audit + env/health/backup fix</name>
  <files>.planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-AUDIT.json</files>
  <precondition>COOLIFY_TOKEN env var set for Coolify CLI (--context hostunlimited) and Coolify MCP reachable; gh CLI authenticated to clezcoding/puzzlessbox.</precondition>
  <action>
Run Coolify audit + GH Actions digest comparison + fix in order, recording every step in 260803-001-AUDIT.json:

1. Inventory (Coolify MCP get_application x3): pull API pasmduuzitoh21qipyq3ay1l, MCP n5frtiupale5c2zjm9fyk1qc, Web qxpgv6p1rp3vupue9al8hbzz. Capture status, fqdn, image (docker_registry_image_name + :latest), running digest if exposed, health_check_enabled, health_check_path, health timings (interval/timeout/retries/start_period), custom_docker_run_options (network rmj3pan623pikht2yqq2efsd).

2. Old app check: get_application for dxoflgio67786lc4yilhce43 — assert status exited/stopped and fqdn null. If running or has domain, stop_application + REST PATCH {fqdn: null}.

3. GH Actions latest successful runs: gh run list --workflow=deploy-api.yml --status=success --limit 3, same for deploy-web.yml and deploy-mcp.yml. Capture run ID, head SHA, conclusion, docker/metadata-action digest output (gh run view ID --log | grep -i digest).

4. GHCR latest digest: for each image ghcr.io/clezcoding/puzzlessbox-{api,web,mcp}:latest, fetch via gh api /orgs/clezcoding/packages/container/PKG/versions (or docker buildx imagetools inspect). Fall back to user UI confirmation if 403 (D-17 visibility carve-out).

5. Drift detection per app: compare Coolify running digest with GHCR :latest digest and latest successful deploy-* run SHA. Mark in_sync or behind.

6. If behind — trigger rebuild+deploy: gh workflow run deploy-{api,web,mcp}.yml --ref main (workflow_dispatch). Poll gh run watch ID until completion. Workflow webhook step calls Coolify deploy. Verify webhook returned 200/202 (D-06 GET pattern for api/web; POST for mcp per COVERAGE OPT-OUT). If webhook secret missing, gh secret set COOLIFY_{API,WEB,MCP}_WEBHOOK.

7. Coolify redeploy if webhook skipped/failed: MCP deploy_application UUID or restart_application UUID. Wait for status=running:healthy.

8. Env audit (MCP get_application_envs x3):
   - API: BETTER_AUTH_JWKS_URL=https://pbox.puzzlesstool.online/.well-known/jwks.json, BETTER_AUTH_URL (align to https://pbox.puzzlesstool.online per 05-VERIFICATION anti-pattern note if still app.puzzlesstool.online/api/auth), DATABASE_URL present, SERVICE_BEARER_TOKEN + SERVICE_OWNER_ID present.
   - MCP: MCP_CATEGORY_ID, DATABASE_URL, bearer token, BETTER_AUTH_JWKS_URL if used.
   - Web: BETTER_AUTH_URL, NEXT_PUBLIC_APP_URL=https://pbox.puzzlesstool.online, NEXT_PUBLIC_API_BASE_URL=https://api.puzzlesstool.online (or NEXT_PUBLIC_API_URL — match existing key), DATABASE_URL, BETTER_AUTH_SECRET.
   Fix missing/wrong via MCP update_application_env or update_application_envs_bulk. Record old→new in audit JSON. Never log secret values — record only key presence + length.

9. Health probe audit: assert each app health_check_enabled=true, path = /health (API/MCP) or /api/health (Web) — NOT /ready. Timings 10s/5s/5/15s (D-14). If drifted, REST PATCH /api/v1/applications/UUID with {health_check_interval:10, health_check_timeout:5, health_check_retries:5, health_check_start_period:15} (CLI/MCP schema lacks fields per COVERAGE).

10. Live health curl: curl -sS -o /dev/null -w http_code for https://api.puzzlesstool.online/health, https://mcp.puzzlesstool.online/health, https://pbox.puzzlesstool.online/api/health — all 200.

11. DB backup audit: MCP get_database_backups on pfqgb5pcvgi9oh64bpe3shtn — schedule jl0skzwpd3ot7hgfmohlny9s enabled=true, cron 0 3 * * *, retention 14/14, save_s3=false. If disabled or no successful baseline, create_database_backup and assert status=success.

12. Deploy webhooks pattern audit: confirm each app webhook URL follows https://puzzlesstool.online/api/v1/deploy?uuid=APP-UUID (D-19). Verify GitHub secrets COOLIFY_API_WEBHOOK, COOLIFY_WEB_WEBHOOK, COOLIFY_MCP_WEBHOOK set (gh secret list).

Write 260803-001-AUDIT.json with: per-app state before/after, GH Actions run IDs, GHCR digests, drift verdict, redeploy actions, env fixes (old→new, secrets as presence-only), health timings, backup state, live curl results, user-escalation items (e.g., GHCR 403 per D-17).

Use Sequential Thinking MCP for decision branching: drift remediation order, env-fix prioritization, webhook secret re-set vs reuse.

Use Coolify CLI with --context hostunlimited for any CLI ops; prefer Coolify MCP for state reads and env updates.
  </action>
  <verify>
    <automated>jq '.apps[] | select(.in_sync_after != true)' .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-AUDIT.json && exit 1 || true; jq '.apps[].live_health_http_code' .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-AUDIT.json | grep -v '^200$' && exit 1 || true; jq '.old_app.stopped == true and .old_app.fqdn == null' .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-AUDIT.json | grep -q true; jq '.backup.schedule_enabled == true and .backup.baseline_success == true' .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-AUDIT.json | grep -q true</automated>
  </verify>
  <done>All 3 apps in_sync_after=true, live health 200, old app stopped+no domain, backup schedule enabled with successful baseline, env vars match locked spec, D-14 timings correct, audit JSON committed.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve all open Renovate/Dependabot chore PRs on clezcoding/puzzlessbox (latest stable, no skips)</name>
  <files>.planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-PR-RESOLVE.json</files>
  <precondition>gh CLI authenticated to clezcoding/puzzlessbox with merge rights; Kodiak automerge label already applied by user to candidate PRs.</precondition>
  <action>
Run in parallel with Task 1 if executor supports Task subagents; otherwise sequentially after Task 1. Steps:

1. Enumerate open chore PRs: gh pr list --repo clezcoding/puzzlessbox --state open --json number,title,author,labels,headRefName,mergeable,mergeStateStatus,reviewDecision,isDraft,createdAt — filter author renovate[bot] / dependabot[bot] OR labels containing dependencies/renovate/automerge. Also include open chore PRs titled chore(deps): or build(deps):.

2. For each PR, build a record: number, title, package+version bump (parse from title), base SHA, head SHA, CI status (gh pr checks N --json name,state,conclusion), mergeable, mergeStateStatus, labels (esp. Kodiak automerge label).

3. Anti-duplicate verification (FORBIDDEN to skip): before closing ANY PR as duplicate or dismissing with already-have-this, run git log --oneline main --grep PACKAGE | head -5 and gh pr list --state merged --search PACKAGE in:title to verify bump truly on main. If not verifiably merged, do NOT close — proceed to merge.

4. CI green + automerge path: for each PR with Kodiak automerge label and CI green (mergeStateStatus == CLEAN or BEHIND):
   - If BEHIND: gh pr update-branch N (or gh pr edit N --rebase) to rebase onto main; wait for CI rerun.
   - If CI fails after rebase: attempt fix only if trivial (e.g., lockfile conflict). Otherwise escalate with evidence (CI log URL, failing job, package) in PR-RESOLVE.json — do NOT merge red CI.
   - If CLEAN and automerge enabled: wait for Kodiak to merge (poll gh pr view N --json state up to 5 min). If Kodiak does not merge, gh pr merge N --squash --auto or gh pr merge N --squash.

5. PRs without automerge label: apply via gh pr edit N --add-label automerge (if label missing, gh label create automerge --color 0E8A16 once). Then follow step 4 path.

6. Major breaking bumps: if CI red on a major bump (e.g., v4→v5), do NOT merge. Attempt fix only if failure is in this repo code and obvious; otherwise record under escalated with: PR URL, failing job name, log excerpt, package, suggested next step (e.g., pin to previous minor + open follow-up issue).

7. Policy enforcement: latest STABLE for everything — no skipping stable bumps. If a PR targets a pre-release/beta, close with reason pre-release not in policy; record in JSON.

8. Final sweep: re-run gh pr list --state open --search author:renovate[bot] and --search author:dependabot[bot] — assert zero open chore PRs (or only escalated with evidence).

Write 260803-001-PR-RESOLVE.json with: total_open_before, per-PR records (number, package, version, ci_state, action_taken, result), merged_count, escalated_count, duplicates_verified_and_closed (only if truly on main), total_open_after.

Use Sequential Thinking MCP for triage ordering when many PRs interlock (shared lockfile, base-package bumps that invalidate dependents).

Use GitHub MCP (pull_request_read, pull_request_review_write, merge_pull_request) and gh CLI both as available; prefer gh CLI for batch operations.
  </action>
  <verify>
    <automated>jq '.total_open_after' .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-PR-RESOLVE.json | grep -qE '^(0|[1-9][0-9]*)$'; jq -e '.merged_count + .escalated_count == (.prs | length)' .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-PR-RESOLVE.json >/dev/null; gh pr list --repo clezcoding/puzzlessbox --state open --search 'is:pr author:renovate[bot]' --json number | jq 'length' | grep -qE '^[0-9]+$'</automated>
  </verify>
  <done>Every open Renovate/Dependabot chore PR on clezcoding/puzzlessbox is either merged (CI green, latest stable) or escalated with concrete evidence (CI log URL, failing job, package). No stable bump skipped or closed as duplicate without git/gh verification against main. PR-RESOLVE.json committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coolify CLI/MCP → Coolify API | Auth via COOLIFY_TOKEN; production deploy control plane |
| gh CLI → GitHub API | Auth via gh; merge rights on clezcoding/puzzlessbox |
| GH Actions → GHCR → Coolify webhook | Build pipeline triggers production deploys |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q01-01 | Tampering | Coolify env var updates | high | mitigate | Use MCP update_application_envs_bulk with exact key/value; record old→new in audit JSON; never log secret values (BETTER_AUTH_SECRET, SERVICE_BEARER_TOKEN, DATABASE_URL) — record only key presence + length. |
| T-q01-02 | Spoofing | GHCR image digest | high | mitigate | Compare digest from gh api packages endpoint AND latest successful deploy-* run SHA; do not trust :latest tag alone — verify by digest. Escalate to user if GHCR API 403 (D-17 visibility carve-out). |
| T-q01-03 | Tampering | GitHub PR merge | high | mitigate | Verify mergeStateStatus CLEAN before merge; never merge red CI; rebase BEHIND PRs before merge; record CI conclusion per PR. |
| T-q01-04 | Information Disclosure | Audit JSON secret leakage | high | mitigate | Audit JSON stores only env key presence + value length for secrets; never plaintext secrets. Review before commit. |
| T-q01-05 | Repudiation | Coolify deploy actions | medium | mitigate | Record every deploy/restart/env-update action with timestamp, actor, before/after state in audit JSON. |
| T-q01-06 | Denial of Service | Production redeploy storm | medium | mitigate | Trigger at most one workflow_dispatch per app; wait for completion before next; do not batch-redeploy all 3 simultaneously unless all behind. |
| T-q01-SC | Tampering | npm/pip/cargo installs via Renovate PRs | high | mitigate | Renovate PRs come from trusted bot but lockfile changes can introduce slop — CI must be green (includes slopcheck if configured) before merge; blocking human checkpoint for any [ASSUMED]/[SUS] package per policy. |
</threat_model>

<verification>
- All 3 Coolify apps report in_sync_after=true in audit JSON
- Live curl returns 200 for api/mcp/pbox health endpoints
- Old API app dxoflgio67786lc4yilhce43 stopped with fqdn null
- DB backup schedule enabled with successful baseline
- All env vars match locked spec (JWKS, BETTER_AUTH_URL, NEXT_PUBLIC_API_BASE_URL, D-14 timings)
- gh pr list open chore PRs returns zero (or only escalated with evidence)
- PR-RESOLVE.json merged_count + escalated_count == total PRs processed
</verification>

<success_criteria>
- Production on latest GHCR digests for all 3 apps, verified against successful deploy-* runs
- Env vars correct and D-14 health timings enforced
- Old API app stopped; backups healthy
- All open Renovate/Dependabot PRs merged or escalated with evidence; no stable bumps skipped
- Two audit JSON files committed to .planning/quick/260803-001-.../
</success_criteria>

<output>
Create .planning/quick/260803-001-coolify-3-apps-latest-ghcr-build-deploy-/260803-001-SUMMARY.md when done.
</output>

