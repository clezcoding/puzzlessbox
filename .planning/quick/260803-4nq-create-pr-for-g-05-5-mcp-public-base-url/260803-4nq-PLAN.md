---
phase: quick-260803-4nq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-UAT.md
autonomous: false
requirements: [OPS-02, MCP-02]
tags: [mcp, pr, ci, coolify, uat, www-authenticate]

must_haves:
  truths:
    - PR exists from quick/260803-uat-gap-fixes → main and reaches merge-ready (CI green)
    - main branch receives the G-05-5 fix commits (merge or Kodiak auto-merge)
    - deploy-mcp.yml GH Actions run on main succeeds (mcp-server/** path trigger)
    - Coolify MCP app n5frtiupale5c2zjm9fyk1qc healthy after redeploy with MCP_PUBLIC_BASE_URL set
    - Live curl POST https://mcp.puzzlesstool.online/mcp returns 401 with WWW-Authenticate containing mcp.puzzlesstool.online and NOT localhost
    - 05-UAT.md gap G-05-5 marked resolved with live evidence
  artifacts:
    - .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-UAT.md (G-05-5 → resolved)
  key_links:
    - PR head quick/260803-uat-gap-fixes → base main
    - main → deploy-mcp.yml trigger → Coolify MCP webhook → mcp.puzzlesstool.online
---

<objective>
Ship G-05-5 MCP_PUBLIC_BASE_URL fix to production via PR + CI + Coolify deploy, then re-run live UAT curl and close gap G-05-5 in 05-UAT.md.

Purpose: UAT gap G-05-5 (MCP 401 WWW-Authenticate leaks localhost:8000) is fixed in code on `quick/260803-uat-gap-fixes` (commit 754c102 / 4ji). Coolify MCP env already set. To make prod serve the fix, commits must land on `main` so `deploy-mcp.yml` rebuilds the MCP image and redeploys. Then live curl verifies RFC 9728 resource_metadata uses prod FQDN.

Output: Merged PR, green deploy-mcp.yml run, healthy Coolify MCP, resolved G-05-5 in 05-UAT.md.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260803-4ji-fix-g-05-5-mcp-401-www-authenticate-reso/260803-4ji-SUMMARY.md
@.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-UAT.md
@.github/workflows/deploy-mcp.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push branch + create PR quick/260803-uat-gap-fixes → main</name>
  <files>(no file edits — git + gh CLI only)</files>
  <action>
Push the 6 ahead commits to origin and open a PR. Do NOT force-push, do NOT skip hooks.

Commands:
- `git push origin quick/260803-uat-gap-fixes` (push the 6 ahead commits)
- Confirm head SHA matches local: `git rev-parse HEAD` == `git rev-parse origin/quick/260803-uat-gap-fixes`
- Create PR via gh CLI with HEREDOC body:
  `gh pr create --base main --head quick/260803-uat-gap-fixes --title "fix(UAT): G-05-5 MCP_PUBLIC_BASE_URL + gap fixes (signup sticky, openapi off, bulk click)" --body "$(cat <<'EOF'
## Summary
- Fix G-05-5: MCP 401 WWW-Authenticate resource_metadata now uses production FQDN (mcp.puzzlesstool.online) via MCP_PUBLIC_BASE_URL env → OwnerResolvingVerifier.base_url (RFC 9728). localhost:8000 leak closed.
- Quick 260803-3gu UAT gap fixes: signup sticky on 409, openapi off in prod, bulk click handler.
- Tests: mcp-server/.venv/bin/python -m pytest tests/test_auth.py -x -q → 5/5 pass (new test_www_authenticate_uses_public_base_url_in_prod_like_config).

## Coolify operator action (already done this session)
MCP app `n5frtiupale5c2zjm9fyk1qc` env: `MCP_PUBLIC_BASE_URL=https://mcp.puzzlesstool.online` set. Redeploy triggered after merge.

## Test plan
- [ ] CI green on PR (web/api/mcp workflows as applicable)
- [ ] After merge: deploy-mcp.yml succeeds on main (mcp-server/** path trigger)
- [ ] Coolify MCP app healthy
- [ ] `curl -si -X POST https://mcp.puzzlesstool.online/mcp` → 401, WWW-Authenticate contains `mcp.puzzlesstool.online`, NOT `localhost`
- [ ] 05-UAT.md G-05-5 → resolved
EOF
)"`

If `gh pr create` fails because a PR already exists for the head, fetch the existing URL via `gh pr list --head quick/260803-uat-gap-fixes --json url,number` and proceed to Task 2 with that PR number.

Do NOT enable auto-merge in this task — Task 2 handles merge after CI green. If Kodiak is installed and auto-enables on the PR, that is fine; just do not duplicate.
  </action>
  <verify>
    <automated>gh pr view --json url,state,mergeable,mergeStateStatus | jq -r '.url, .state, .mergeable, .mergeStateStatus'</automated>
  </verify>
  <done>PR exists for quick/260803-uat-gap-fixes → main, URL captured, branch pushed to origin (0 ahead).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>PR created from quick/260803-uat-gap-fixes to main carrying the G-05-5 MCP_PUBLIC_BASE_URL fix plus UAT gap fixes (260803-3gu).</what-built>
  <how-to-verify>
Open the PR URL printed by Task 1. Confirm:
- Title and body render correctly.
- Files changed include mcp-server/app/config.py, mcp-server/app/auth.py, mcp-server/app/factory.py, mcp-server/tests/test_auth.py (4ji) plus the 3gu gap-fix files.
- No force-push, no skipped hooks (commit signatures intact, no `[skip ci]` anomalies).

If anything looks wrong, describe the issue and resume. Otherwise type `approved`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Monitor CI green + merge PR (Kodiak or gh pr merge)</name>
  <files>(no file edits — gh CLI only)</files>
  <action>
Wait for PR CI to finish, then merge once green. Do NOT force-push, do NOT skip hooks.

Steps:
1. Poll PR checks (max ~15 min, back off 60s):
   `gh pr checks <PR_NUMBER> --watch --interval 60 --fail-fast`
   Stop when all required checks pass or any fails. If a required check fails, stop and surface the failing job name + log URL; do NOT merge.
2. Determine merge path:
   - Check Kodiak: `gh pr view <PR_NUMBER> --json comments | jq '.comments[] | select(.author.login == "kodiakhq") | .body'` — if Kodiak bot is enabled and auto-merge configured, Kodiak will merge when green. Wait for merge state `MERGED` (poll `gh pr view <PR_NUMBER> --json state`).
   - If Kodiak not auto-merging, merge manually: `gh pr merge <PR_NUMBER> --squash --delete-branch` (squash keeps main history clean; delete-branch removes the head). Use `--merge` instead only if repo convention prefers merge commits (check recent `git log main --oneline -5` for squash vs merge convention; default squash).
3. Verify merge landed on main:
   `git fetch origin main && git log origin/main --oneline -3`
   Confirm the G-05-5 fix commit (or its squash) is on origin/main.
4. Delete local branch only after origin confirms merge:
   `git branch -d quick/260803-uat-gap-fixes` (skip if Kodiak already deleted remote and local is gone; do not error on missing).

If merge blocked by `mergeStateStatus != CLEAN`, surface the blocker (e.g., stale branch, review required) — do NOT use `--admin` to force.
  </action>
  <verify>
    <automated>gh pr view <PR_NUMBER> --json state,mergedAt | jq -r '.state, .mergedAt' && git fetch origin main && git log origin/main --oneline -3</automated>
  </verify>
  <done>PR state == MERGED, origin/main contains the G-05-5 fix, head branch deleted.</done>
</task>

<task type="auto">
  <name>Task 3: Verify deploy-mcp.yml success + Coolify MCP healthy + live UAT curl + update 05-UAT.md G-05-5</name>
  <files>.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-UAT.md</files>
  <action>
After merge, the `deploy-mcp.yml` workflow triggers on main because mcp-server/** changed. Verify end-to-end prod health, then close G-05-5.

Steps:
1. Find the deploy-mcp.yml run for the merge commit on main:
   `gh run list --workflow=deploy-mcp.yml --branch=main --limit 3 --json databaseId,headSha,conclusion,createdAt`
   Match the run whose headSha == origin/main HEAD from Task 2. Poll until conclusion is `success` (max ~20 min, 60s backoff):
   `gh run watch <RUN_ID> --interval 60 --exit-status`
   If conclusion is `failure`, surface the failed step + log URL and stop.

2. Coolify MCP app health (app UUID `n5frtiupale5c2zjm9fyk1qc`):
   Use the user-coolify MCP `get_application` tool with the application UUID to confirm `status == running` and `health_check_status` healthy. If Coolify redeploy is not yet triggered by the webhook, trigger it manually via Coolify `deploy_application` for the UUID, then re-check health.

3. Live UAT curl (the G-05-5 acceptance check):
   `curl -si -X POST https://mcp.puzzlesstool.online/mcp`
   Capture full headers + status. Acceptance:
   - HTTP status == 401
   - `www-authenticate` header (case-insensitive) contains the substring `mcp.puzzlesstool.online`
   - `www-authenticate` header does NOT contain `localhost`
   If any condition fails, stop and surface the actual header value — do NOT mark G-05-5 resolved.

4. Update `.planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-UAT.md`:
   - Test 16 `result: issue` → `result: pass`
   - Test 16 `notes:` append: `RESOLVED 2026-08-03 via MCP_PUBLIC_BASE_URL (quick 260803-4ji, PR <PR_NUMBER>). Live curl 401 WWW-Authenticate contains mcp.puzzlesstool.online, no localhost. deploy-mcp.yml run <RUN_ID> success.`
   - Gaps section G-05-5 `status: failed` → `status: resolved`
   - Gaps section G-05-5 add `resolved_at: 2026-08-03` and `evidence: live curl https://mcp.puzzlesstool.online/mcp 401 www-authenticate mcp.puzzlesstool.online (no localhost); deploy-mcp.yml run <RUN_ID> success; Coolify MCP n5frtiupale5c2zjm9fyk1qc healthy`
   - Prior Gaps Reconciliation table row G-05-5 `now` column: `open` → `resolved (live curl 401 mcp.puzzlesstool.online, no localhost)`
   - Summary: `issues: 1` → `issues: 0`, `passed: 15` → `passed: 16`
   - Frontmatter `updated:` bump to current ISO timestamp.

Use the Edit tool (scoped replacements) for each edit — never Write the whole file.
  </action>
  <verify>
    <automated>curl -si -X POST https://mcp.puzzlesstool.online/mcp | grep -iE '^(HTTP|www-authenticate)' && rg -n "G-05-5" .planning/phases/05-coolify-deployment-ci-cd-h-rtung/05-UAT.md | rg -v "resolved"</automated>
  </verify>
  <done>deploy-mcp.yml run for the merge commit succeeded; Coolify MCP app healthy; live curl returns 401 with www-authenticate containing mcp.puzzlesstool.online and no localhost; 05-UAT.md G-05-5 marked resolved with evidence, summary issues == 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR branch → main | Untrusted branch commits cross into protected main via PR + CI gate |
| main → GHCR → Coolify | Built image deployed to prod; image integrity + env correctness boundary |
| Public internet → mcp.puzzlesstool.online | Untrusted clients hit MCP 401 path; WWW-Authenticate must advertise prod FQDN only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4nq-01 | Tampering | PR merge bypass | high | mitigate | Require CI green before merge; no --admin force; no force-push; no --skip-hooks |
| T-4nq-02 | Information Disclosure | WWW-Authenticate header | medium | mitigate | Live curl asserts no localhost leak in header; G-05-5 only resolved on positive evidence |
| T-4nq-03 | Denial of Service | deploy-mcp.yml failure | medium | mitigate | Watch run to success; surface failure logs and stop if not green |
| T-4nq-04 | Spoofing | Coolify redeploy not triggered | low | accept | Webhook usually fires; manual deploy_application fallback documented in Task 3 step 2 |
</threat_model>

<verification>
- `gh pr view <PR_NUMBER> --json state` → state == MERGED
- `git log origin/main --oneline -3` contains G-05-5 fix
- `gh run list --workflow=deploy-mcp.yml --branch=main --limit 1 --json conclusion` == success
- Coolify MCP app `n5frtiupale5c2zjm9fyk1qc` status == running, healthy
- `curl -si -X POST https://mcp.puzzlesstool.online/mcp` → 401, www-authenticate contains `mcp.puzzlesstool.online`, NOT `localhost`
- `rg "G-05-5" 05-UAT.md` shows `status: resolved`
</verification>

<success_criteria>
- PR merged to main with G-05-5 fix
- deploy-mcp.yml green on main
- Coolify MCP healthy with MCP_PUBLIC_BASE_URL env
- Live UAT curl passes (401, prod FQDN in WWW-Authenticate, no localhost)
- 05-UAT.md G-05-5 → resolved, summary issues == 0
</success_criteria>

<output>
Create `.planning/quick/260803-4nq-create-pr-for-g-05-5-mcp-public-base-url/260803-4nq-SUMMARY.md` when done.
</output>
