---
quick_id: 260728-8an
slug: bevor-ich-phase-0-angehe-erstelle-via-gi
status: ready
---

# Quick Task 260728-8an: GitHub-Repo puzzlessbox einrichten

## Task 1: Lokales Repo vorbereiten

**files:** `.gitignore`, `PUZZLESSBOX_PROJECT_BRIEF.md`, `skills-lock.json`
**action:** `.DS_Store` in `.gitignore` aufnehmen; ungetrackte Projektdateien committen, damit der erste Push vollständig ist.
**verify:** `git status` zeigt clean working tree (bis auf ignorierte Dateien).
**done:** Commit mit Projekt-Brief und Lockfile existiert auf `main`.

## Task 2: Private GitHub-Repo erstellen und verbinden

**files:** (git remote only)
**action:** `gh repo create puzzlessbox --private --source=. --remote=origin --description "..." --push`; Repo-Metadaten setzen (Topics, Issues aktiv).
**verify:** `gh repo view clezcoding/puzzlessbox`; `git remote -v`; `git ls-remote origin main`.
**done:** Remote `origin` zeigt auf `https://github.com/clezcoding/puzzlessbox.git`, `main` ist gepusht.

## Task 3: Einrichtung verifizieren

**files:** none
**action:** Prüfen: private visibility, default branch `main`, upstream tracking, alle Commits auf Remote.
**verify:** `gh repo view --json visibility,defaultBranchRef,url`; `git status -sb`.
**done:** Repo ist privat, tracking aktiv, lokaler und Remote-Stand stimmen überein.
